## 2026-08-24 compression

Original bullet beginning “Verify an artifact written FOR an audience”:

- **Verify an artifact written FOR an audience from that audience's environment, before review** (endorsed 2026-08-16). The README's 1-minute check passed only in a shell holding `CC_PROMPTS_PIN`; `npm run check` exited 3 for every visitor. Re-running in the author's shell cannot find an environment defect. Strip author state first: `env -u <VAR>`, fresh clone, no repo-local config. This generalizes the capability/environment rule written after the 2026-07-25 CI incident; it is not merely a CI lesson. **Degenerate case, 2026-08-18: verify a documented command by RUNNING it.** The primer used `git rev-parse --short HEAD origin/main`; `--short` accepts one revision, exited 128 (`fatal: Needed a single revision`), and its leading position in an `&&` chain skipped the cadence for anyone pasting it. The working form existed in prose but was never run. Treat each line as its own artifact: a KP-78 "both answers demonstrated" note certifies only the command it ran, not adjacent lines. Do not try `--short=7`; it also exits 128 because the flag's arity is fixed. Finding: `docs/findings/2026-08-18-the-guard-against-a-wrong-commit-read-never-ran.md`.

Original bullet beginning “Prove the instrument can fail”:

- **Prove the instrument can fail (KP-78).** Ship no detector—test, guard, lint, probe, `--check`, alarm—until it fires with the condition present and stays silent when absent; record both outputs in the finding/commit. An always-firing detector satisfies the first half while proving nothing. First prove the mutation landed with `git diff --numstat`: `\n` against CRLF silently no-opped and falsely condemned the pipefail guard on 2026-07-25. **Test recent commits, not only the test directory.** The 2026-07-31 audit found A-8's hand-added CI step had no detector preserving it: `docs/findings/2026-07-31-kp78-prove-the-instrument-can-fail.md`, watch W-4. **Extension, 2026-08-04: one proven probe does not validate its enumeration.** `gh secret list` was positively controlled on other repos, but "0 secrets, 0 deploy keys, 1 collaborator" still missed four GitHub Apps (`vercel`, `render`, `cursor`, `claude`, visible only in `/check-suites`; scope needs an app-authorized token). A probe licenses only its claim. For enumeration ask what it can never show (A-13 F-4). **Extension, 2026-08-17: prove non-firing means the CONDITION is absent, not the INSTRUMENT.** The alarm-title guard resolved `bash` from `PATH` on PowerShell, spawn failed, returned `""`, and `assert.doesNotMatch(title, /^Drift:/)` passed on empty output. Before asserting output lacks X, assert output exists; otherwise a broken instrument certifies absence. **Extension, 2026-08-23: an inlined page script is EXECUTED, never pattern-matched.** `index.html` carries its filter and ordering logic as an inlined `<script>`, and nothing in an offline battery runs a browser — so the tempting test is `assert.match(html, /appendChild/)`, which proves the code SHIPPED and says nothing about whether it works. That is the detector-never-shown-to-fire shape, one level up. The working form costs about fifteen lines: extract the script from the page the renderer just generated, and run it under `new Function` against a hand-built stub DOM. **The extraction must come from the generated artifact, never a retyped copy** — a retyped copy tests the retyping, which is the 2026-08-18 and 2026-08-21 lesson arriving for the third time. Put the real decision logic in an exported function so it can be tested directly, and keep the in-page half thin enough that a stub can cover it. First applied to the ordering control; four negative controls fired, each proven to land first.

Original bullet beginning “The hosted brief drifts from its source”:

- **The hosted brief drifts from its source, and hand-checking it under-reports.** `docs/lane-brief.md` is ported to `/t/lanes/bounds-ledger` by the orchestrator, so the page can silently fall behind. It did — and two consecutive hand-checks (7/30, 7/31) each reported ONE missing block because both searched for the block the primer named, instead of deriving the expected set from the source. `check-brief.mjs` (2026-07-31) derives it and found **two** missing, stale since 26 July. Recognition-over-recall applies to our own checks: a hand-check that inherits its search term from a doc can only ever confirm that doc. **Deliberately NOT in the CI drift job** — it would go red and stay red until an orchestrator re-port lands, and a permanently-red alarm carries as much information as a permanently-green one. It runs in `npm run check` and **stays out of CI permanently** — ~~joins CI the run after the page first reads green~~, a pre-commitment written 7/31 that was **retired 2026-08-03, the very day it first fired**. The page read green for the first time that morning, and the clause would have sent the next session to wire a check into CI that CI cannot run: the 8/02 sign-in wall means a runner with no pin exits 3 UNVERIFIABLE, so the only way to satisfy the clause is putting `CC_PROMPTS_PIN` in repo secrets — forbidden with a public flip pending. A threshold derived from the old regime kept reading as authoritative after the regime changed (the A-11 class, 2026-07-31). the lane's goal signal measures the LEDGER; a stale brief must not consume it.

Original bullet beginning “`UNREACHABLE` is not `BROKEN`”:

- **`UNREACHABLE` is not `BROKEN`, and the alarm must never title it as one.** `BROKEN` = the cited source was read and the pinned string is gone (a record moved). `UNREACHABLE` = it was never read. Grouping them titled a pure HTTP 429 `Drift: claims C-1,pin:12a:U …` — six records asserted moved, zero actually — which is A-5's defect surviving where A-5's own fix could not reach it, because `UNREACHABLE` matched before the `elif` testing for `^error:`. **The red itself is correct and must stay red**: unreachable means unverified, and a green there would launder an unverifiable into a pass. Only the title was ever wrong. Do not "fix" a 429 by making it non-fatal. Finding: `docs/findings/2026-08-17-the-alarm-called-a-rate-limit-a-record-movement.md`.

Original bullet beginning “`check-brief.mjs` needs `CC_PROMPTS_PIN`”:

- **`check-brief.mjs` needs `CC_PROMPTS_PIN` in env, or it can see nothing.** `/t/lanes/*` went behind a Supabase sign-in wall on 2026-08-02 (skylark-site `53c87f83`, intentional, site-wide). The route 307s to `/t/signin` and **the sign-in page returns HTTP 200**, so an anonymous fetch looks successful and every dated block reads "missing" — which is exactly how the checker spent a morning reporting `4 of 4 missing … the port is the orchestrator's` about a page it had never received. The script now proves the response IS the brief (tests for the source's own H1) before assessing staleness, and sends `x-cc-pin` from `process.env.CC_PROMPTS_PIN` when present. **Consequences: CI can never run this check** (no pin on a runner → exit 3 `BRIEF UNVERIFIABLE`, the brief job logs it and deliberately files nothing — a permanently-open issue no action can close is the founding defect again), and **an anonymous local run proves nothing either way**. Only a run with the pin set is evidence. Same shape as C-7. Do NOT "fix" this by putting the PIN in repo secrets — this repo is heading for a public flip. Finding: `docs/findings/2026-08-02-the-brief-check-blamed-the-porter-for-a-login-wall.md`.

Original bullet beginning “An HTTP 200 is not evidence”:

- **An HTTP 200 is not evidence you fetched the artifact, and a NEGATIVE result from the wrong document is how you publish a false accusation.** Two cases in one day (2026-08-12): preprints.org returns 200 for a 2.6 KB bot-protection page (title `&nbsp;`), and **ar5iv silently falls back to the arXiv abstract page** — real, relevant, correctly-titled HTML about the right paper. On the strength of "Jin" being absent from that 38 KB fallback I was one step from writing that a mathematician's paper omits an acknowledgement upstream says it makes. **A positive match is self-validating; a negative match proves nothing until you have separately proven you had the right document.** Before concluding a cited source lacks something, name the token from the SAME fetch that proves it is the source — here `numerical range` and `spectral set` were also missing, which for a paper about the numerical range being a spectral set was the tell. The route that worked when the server is walled: **Crossref content negotiation** (`api.crossref.org/works/<doi>`) never touches it and returns title/author/date, enough to verify a citation including a priority claim. Same family as the sign-in wall below and the 403 below that. Finding: `docs/findings/2026-08-12-two-record-movements-in-one-cycle.md`.

Original bullet beginning “erdosproblems.com 403s automated fetch”:

- **erdosproblems.com 403s automated fetch — from DATACENTER IPs. The block is IP-dependent.** A plain Node fetch from a residential IP returns **HTTP 200** with the bound readable; the identical fetch from a **GitHub Actions runner returns 403**. So: **CI can never verify C-7** (it stays `manual: true` / UNVERIFIED by design), but **an agent running `npm run check` locally genuinely re-verifies it mechanically** — better than the screenshot round-trip, and available to any local session. **A local 200 proves nothing about CI.** This was established the hard way on 2026-07-25: I tested locally, concluded the constraint was stale, automated C-7, declared "zero UNVERIFIED claims", and CI went red four minutes later. Finding: `docs/findings/2026-07-25-the-403-that-wasnt.md`. **Do not re-automate C-7 on the strength of a local 200 — it has been tried; the only evidence that counts is a green CI run.**

Original text changed only to restore exact keep-list casing:

never snapshot to silence red

listing position

NUMERIC or BYTE-ONLY

Text replaced while restoring the final keep-list string:

Re-automate only on a green CI run.

## Cut 2026-08-28 (A-35 compression, 49202 -> under 45000)

Every rule below still stands in CLAUDE.md; only the incident narrative was trimmed. Full prior
text is in git history at `de8eb84:CLAUDE.md` and earlier.

### node-kill fleet rule

- **NEVER kill a node process by NAME, by AGE, or because it "looks orphaned" — the process table on this machine is shared by all 24 panes** (fleet rule, effective 2026-08-27). `Get-Process node` lists every lane's bus listener, loop-death waker, dev server and vitest worker, not yours. **Measured, not cautionary:** three lane-side sweeps at ~13:11, ~14:03 and ~14:17 MT killed node processes machine-wide; one "reaped 43 orphans" that were the other lanes' live listeners and wakers. Every listener but one died at 14:17 in the same second. The same class killed three verify runs on one lane and wrote a `KILLED (-1)` receipt on another — vitest workers are node too. **Only two permitted kill shapes:** (1) a PID you own, read from YOUR pidfile (`tmp/.bus-listen.pid`) or a job record you wrote; (2) a process whose COMMAND LINE contains this lane's repo path — `Get-CimInstance Win32_Process -Filter "name='node.exe'" | Where-Object { $_.CommandLine -match 'bounds-ledger' }` — never a bare `Get-Process node`. Suspect a stray? Post its PID and command line on the bus and LEAVE IT ALONE; the orchestrator holds the fleet-wide census. **Why this is in THIS lane's gotchas even though we caused none of it** (`grep -rniE "stop-process|taskkill|process\.kill|pkill"` over `scripts/`, `.githooks/`, `package.json` and `docs/` returns nothing here): we were a VICTIM, and the failure mode is diagnostic. Our waker exited `127` twice and our listener died, and the honest first reading — "my relaunch is broken" — was wrong; the cause was another pane. The hooks re-armed the fleet within five minutes and every replay reported 0 missed, which is exactly why it read as local flakiness rather than as the machine-wide outage it was. Same shape as the 2026-08-17 rate-limit retraction: a cause flattering to admit is still an unverified attribution, and this one had an external cause the whole time.

### disclosure-review bullet

- **Reviewing what we WROTE is not reviewing what the repo DISCLOSES.** Four artifact-level reviews plus a package-level review all cleared the flip package, and a whole-project pre-publication review (2026-08-05, David's instruction) then returned **eight blockers** and "I would not flip public with this tree and history". None of the earlier five could have caught them, because every one asked "is this artifact accurate?" and none asked "what does this repository reveal about us, our machine, and other people?" The eight were: no `LICENSE` while redistributing 111 mirrored files; an absolute local path in `continuity/health.json`; a doc publishing our own unprotected security posture (which platform-side controls are off); `items.json` publicly describing a portfolio-wide personal-data question belonging to other repos (details held privately — not ours to disclose); a personal author email on every commit; 15 commit bodies carrying private `claude.ai/code/session_…` URLs; and commit `293cec9`, which added a Playwright snapshot of erdosproblems.com containing **third-party forum user handles**. Before any publication, run the disclosure question as its own pass — and note that a decision to accept one disclosure (David's 8/05 accept the history, given about a maintainer's *published* email address) licenses that item and nothing wider.

### redaction bullet

- **A redaction is done when the STRING is absent from the repository, including from the record of the redaction — and a redaction scoped to one SURFACE leaves the same string on the others.** Four recurrences now, all the same shape: the fix cleans the flagged location and the note explaining the fix becomes a fresh copy. 8/06 the eight-blocker list spelled out the third-party handles and an address *while cataloguing them as things not to publish*; 8/07 the commit written straight after the rewrite re-contaminated from a stale git config; 8/08 the write-up of that put the address back into four files; **8/09 the session-URL sweep was scoped to COMMIT BODIES — which worked, zero — and nobody swept TRACKED FILES, where a complete artifact UUID was sitting in A-13's own note** (`2b751f5`). An identifier has at least three surfaces — author fields, tracked files, commit bodies — and a doc is a surface too. **The check that works is ENUMERATE what the repo reveals from a FRESH CLONE OF THE REMOTE; the check that keeps failing is confirm-the-named-defect-is-gone** (`git log --format=%ae` returning one noreply is TRUE and licenses only *no author field carries it*). Enumerate by shape — every email-shaped token, every URL-with-an-id — not by searching for the string you already know about. Never verify a redaction from the working tree.

### scripts inventory

- `scripts/` — `reverify.mjs` (mirror diff), `check-claims.mjs` (cross-surface claim check), `extract-pins.mjs` (regenerates generated pins), `render-state-block.mjs` (**generates README's committed "Current state"; never hand-edit it**; `--check` is in `npm test` and `npm run check`; it records file count, upstream sha and claim inventory, never a stale live verdict), `check-brief.mjs` (hosted-brief drift; needs the pin), `history-sweep.mjs` (no-secrets gate), `gap-table.mjs` (G-2 ranker; writes `docs/gap-table.json`, asserts nothing mathematical), `traffic-snapshot.mjs` (W-6 14-day arrival sampler; renders no verdict, uses `gh` credentials, and only its merge is CI-guarded).

### KP-78 bullet

- **Prove the instrument can fail (KP-78).** Ship no detector—test, guard, lint, probe, `--check`, alarm—until it fires with the condition present and stays silent when absent; record both outputs. First prove the mutation landed with `git diff --numstat`: `\n` against CRLF silently no-opped on 2026-07-25. **Test recent commits, not only the test directory:** the 2026-07-31 audit found A-8's CI step had no preserving detector (`docs/findings/2026-07-31-kp78-prove-the-instrument-can-fail.md`, W-4). **2026-08-04: one proven probe does not validate its enumeration.** `gh secret list` missed four GitHub Apps (`vercel`, `render`, `cursor`, `claude`, visible only in `/check-suites`; needs an app-authorized token); ask what a probe can never show (A-13 F-4). **2026-08-17: prove non-firing means the CONDITION is absent, not the INSTRUMENT.** On PowerShell the alarm-title guard resolved `bash` from `PATH`, spawn failed, returned `""`, and an absence assertion passed; assert output exists first. **2026-08-23: an inlined page script is EXECUTED, never pattern-matched.** For `index.html`, `assert.match(html, /appendChild/)` proves only that code shipped. Extract the generated script and run it under `new Function` against a stub DOM; never test a retyped copy (the 2026-08-18 and 2026-08-21 lesson). Export decision logic and keep the in-page half thin. First applied to ordering; four negative controls fired after each mutation was proven to land.

### voice-lint bullet

- **David-facing prose is linted; which surface decides how.** `node ../skylark-site/scripts/check-david-voice.mjs <file>` (standard: `skylark-site/docs/skylark-voice-standard.md`, adopted 2026-08-03 after David said again that he cannot understand what we write). **`docs/lane-brief.md` is wholly David-facing, so lint the WHOLE file** — the fleet's lint-the-summary-line-never-the-file rule exists because daily reports are repo-facing, and it does not apply here. Daily reports: lint the BLUF line only. **No other document in this repo is voice-linted — that list is exhaustive, not illustrative.** Findings, flow docs, cold-start primers, reconciliations and decision files are all repo-facing, and running the check on one returns exit 3 on whatever em dashes and capitals its previous authors left, which teaches you nothing and costs a round-trip. Written down 2026-08-23 after a run on a flow doc returned 52 pre-existing em dashes; the bullet had simply never said where the scope stopped, so widening it was the reasonable inference. Two carve-outs found on 2026-08-03 in this repo, both reported upstream: `supersedes` is on the jargon list glossed as the bus flag ("replaces an earlier message") and mis-fires on ordinary English about a number; and the check does not exempt blockquotes, so **the verbatim quote of the email David sent on 24 July keeps its em dashes** — that block is a record of an outward artifact, and rewriting it to satisfy a style rule would make the brief misquote what went out. Same instinct as the mirror: byte-fidelity to what was actually sent.

- (script paragraphs) `check-deferrals.mjs` (A-25, 2026-08-25) — a deferral must carry `deferral: true`, an `expiresOn` and a runnable `releaseTest`; items leg is in `npm run check`, PR leg is `npm run deferrals`. See AGENTS.md for its adoption-not-inventory counter and why it must never fire on existence.
- (script paragraphs) `check-self-attribution.mjs` (2026-08-27) — a self-attributed CAUSE must carry `control:` on the same line, naming what would show the cause is NOT ours. In `npm test` and in `.githooks/pre-commit` for staged `docs/daily/` and `docs/findings/`. Its trigger list is MEASURED against this repo's corpus (see the script header): `exhausted` was dropped for producing the only false positives, and the surviving terms fire on exactly the three real self-attributions in the corpus. Fires on blame pointed at US and stays silent on blame pointed at upstream — deliberately asymmetric, because nobody challenges you for blaming yourself.
- (script paragraphs) `scripts/catch-rate.mjs`: a catch is a GENERATED pin whose `expect` changed; count DISTINCT records per week, never movements. Positive counts are an UPPER BOUND/activity ceiling; zero is exact.
- (script paragraphs) Quote PER-WEEK, never the running total. A month of zeros means adopt a second surface, but that is David's call, never self-approved; see the script header for the full 2026-08-14 derivation.
### flattering-cause bullet

- **A cause you find flattering to admit still needs a positive control.** This bullet first read "two dispatches inside ten minutes tripped `raw.githubusercontent.com` secondary rate limiting on 2026-08-17". **That was wrong and is retracted.** GitHub was in a *critical outage* from 2026-08-17T13:40:03Z (githubstatus.com incident API, read directly); the first dispatch was 14:32Z, fifty-two minutes later, with Actions already degraded. Three pieces of disconfirming evidence were in hand and unassembled: a local ~450-fetch run was **fully green at 14:28Z**, 48 minutes into the outage; a later *local* check failed although the dispatches were runner-side and share no egress with this machine; and the errors included **502 and 503**, which request volume does not produce. The same self-blame went into a finding, a commit message, `A-5 note4` and two issue comments before any of it was checked. **Self-blame feels like rigour and is just another unverified attribution — and it is the more dangerous kind, because nobody challenges you for blaming yourself.** The repo's existing rule already covered it (positive control before any absence or cause claim); it did not fire because the claim was about our own infrastructure rather than someone else's record. `A-5 note5`.

### as-of rule

- **A hand-written statement of a live instrument's value carries an AS-OF and its read command — never a bare present tense** (approved 2026-08-26; orchestrator flagged it a portfolio candidate). Today's primer opened with `CI IS RED AT 4f4d249 … issue #30 is open`. It was **true when committed** and false **seventeen seconds later**, when the push of that same commit produced a green run; `#30` closed two minutes after that, and a further commit 40 minutes later did not touch the primer. Same day, same shape, twice more: the report called `A-18` "the only declared deferral" while `A-30` was declared 82 minutes later, and the primer stated "it reads 1" in one section while naming both rows in another. **Why the primer's own convention cannot save it:** later sections supersede earlier ones, but a banner sits ABOVE the append point, so the block the document promotes hardest is the one block that rule cannot reach. Write "as of 22:25Z CI was red at `4f4d249`; release: `node ../skylark-site/scripts/check-ci-status.mjs --workflow reverify.yml`". Do NOT retro-edit historical reports — they were true as of their write time, and the as-of is exactly what makes that legible instead of looking like a lie.


### daily-report layout bullet

- `docs/daily/<date>-prelaunch.md` — daily reports (needs §3a YAML frontmatter — the r8 linter FAILs without it). **The `-prelaunch` suffix is a HISTORICAL FILENAME, not a lifecycle claim, and it is deliberately NOT being renamed** (2026-08-27). David ruled this lane LAUNCHED that day ([[A-13]] note23) and `lifecycle_stage: launched` has been in the frontmatter for weeks, but the path stays: 35+ committed files plus every cross-reference in findings, primers and reconciliations point at it, `check-doc-references.mjs` would flag each broken link, and the fleet's `REPORT_CONFIGS` already resolves to exactly this path — so renaming buys a tidier name and costs a day of dead references. Read the suffix as "where the reports have always lived", never as the lane's current stage; the frontmatter is the authority on that. This note exists because a stale-looking label on a changed state is the shape this repo keeps getting bitten by, and the fix here is to LABEL it rather than chase it.
### erdosproblems 403 + environment

- **erdosproblems.com 403s automated fetch — from DATACENTER IPs. The block is IP-dependent.** Residential Node gets HTTP 200; GitHub Actions gets 403. C-7 stays `manual: true` / UNVERIFIED in CI, while local `npm run check` genuinely re-verifies it. **A local 200 proves nothing about CI:** automating C-7 on one made CI red four minutes later on 2026-07-25. Finding: `docs/findings/2026-07-25-the-403-that-wasnt.md`. Do not re-automate from local 200; the only evidence that counts is a green CI run.

### erdosproblems 403 + environment

- **Test a capability from the environment that will exercise it.** Generalising a laptop result to a runner is how the above happened, and it failed optimistically — the ledger briefly asserted "zero unverified claims" while the claim gating the lane's north-star watch was silently unverifiable in CI.

### pipe exit-code pair

- **Never read an exit code through a pipe — locally, not just in CI.** `node scripts/check-claims.mjs --selftest | tail -3` reports *`tail`'s* exit 0 even as the selftest fails. The CI form of this bug is the lane's founding defect (below); the local form bit again during the 2026-07-25 sweep. Redirect to a file and check `$?`, or use `${PIPESTATUS[0]}`.

### pipe exit-code pair

- **Never pipe a check step in CI without `shell: bash`.** GitHub Actions' default runner shell is `bash -e {0}` — no `pipefail` — so `node scripts/reverify.mjs --check | tee ...` reports `tee`'s exit 0 and a real drift passes green. This made the alarm fake for the lane's first two days (`docs/findings/2026-07-24-drift-alarm-was-never-armed.md`). `reverify.test.mjs` now guards it; don't defeat the guard.

### mirror layout bullet

- `ledger/teorth-optimizationproblems/` — machine **mirror** of the adopted surface: every `constants/*.md` file **plus the repo-root `README.md`** (W-5, added 2026-08-10) + `manifest.json` pinning the upstream sha. The count grows when upstream adds a constant; `--check` prints the live figure, so don't write one here. **Why the README is in scope:** the constants files carry the bound tables, but the README is where upstream declares which of those rows it will STAND BEHIND — a demotion to asterisked-unverified leaves every table byte-identical, so mirroring `constants/**` alone was blind to it (upstream `dee1660`, 2026-08-02: our alarm named three constants files and was silent on the semantically largest change of the day). File maps are keyed by **path as upstream sees it**, so a report names the README at the root and each constants file under its own `constants/` prefix — never the README filed under the constants prefix, which is where the old string-concatenation would have put it. **Do not hand-edit**; refresh only via `--snapshot`.

### classification clause (2)

  Three clauses the sentence must carry. (1) **Whether a counted catch was numeric or BYTE-ONLY** (2026-08-24): `npm run catches` counts a generated pin whose `expect` changed, and that day changed two of them on one backslash per row while no number moved anywhere — so it reported 2 for a week in which nothing was recorded as moving. The counter is honest by construction (its stdout leads with "a CEILING on catches, not a count of them") and must NOT be replaced by a smarter one; numeric parsing of these cells is known-defeated here. The seam it cannot close is a report or bus line quoting the bare figure. (2) **The RUNNING COUNT of consecutive instrument-facing days** (2026-08-25) — still a number a human writes into the sentence, NOT a counter over `docs/findings/`, and the distinction is the point. The dry-week rule fires only on "no movements AND no findings", so a stretch with both reports "not dry" while every finding in it is about our own machinery; this number says when to ASK the second-surface question and never answers it. **Do not quote a day-count from THIS file — it is stale by construction; today's report carries the live one.** (3) **The standing prediction, because a prediction never checked is decoration:** the next record-facing catch is claimed to be a witness-value mismatch on a constant upstream added within ~30 days, flagged by a human recomputing a cited certificate and by no instrument we run. If one arrives via an alarm instead, say so and correct the claim.


### snapshot-on-drift bullet

- **Snapshot-on-drift discipline.** Treat red `reverify` as the alarm, not a bug: records are supposed to move. Verify the change against **primary sources** before `--snapshot`, then commit the refreshed mirror deliberately; Never snapshot to silence red. **Red is not automatically a record event, and editorial drift is real drift** (2026-07-28: dead cross-link and missing `C` in a LaTeX inequality; no bound moved and pins were byte-identical). The mirror promises byte-fidelity, not only numeric fidelity, so never wave editorial changes through as cosmetic: verify against repo content—the link exists, the corrected inequality is right—and use the same verify → snapshot → re-pin cycle. **The ratchet is THREE steps** (corrected 2026-08-14): snapshot; `node scripts/extract-pins.mjs`, then commit regenerated pins (they never auto-follow, so moved rows remain BROKEN in `check-claims`); and `node scripts/render-state-block.mjs` (README file/claim counts otherwise go STALE). Omitting the third step made `npm run check` exit 1 on 2026-08-14. Use `npm run resnap` (added that day) to run all three in order.

### sha-class bullet

- **Gloss the sha CLASS on first use in anything that leaves the repo.** Upstream `teorth/optimizationproblems` shas do not resolve locally: `git cat-file -t 603052d` returns *not a valid object*, and an unglossed arrow chain of them reads to an outside reader as local commits that do not exist — indistinguishable from fabrication until they go looking. Write "(an upstream sha; it does not exist in this repo)" the first time. This bit a retro on 2026-08-12 whose figures were all correct. **Same rule for upstream PATHS, found 2026-08-15 by `check-doc-references.mjs`:** a bare `constants/<name>.md` in our prose is upstream's path, not ours, and resolves to nothing at our HEAD — write the mirrored `ledger/teorth-optimizationproblems/…` form in prose, and keep the bare upstream form only inside file maps and drift reports, where "path as upstream sees it" is the documented key. Note that the first draft of this very rule quoted the bare path as its example and tripped the checker — the note explaining a fix becoming a fresh instance of it is a shape this repo has now hit five times.
