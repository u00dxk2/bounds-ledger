<!-- GENERATED — regenerating REPLACES everything outside the hand: markers, so put prose inside them. Provenance stamp at the foot of this block. -->

<!-- hand:begin banner -->
2026-09-19 close. **No dated gate is due tomorrow; the nearest are three rows dated 2026-09-21, and one of them governs what you do TOMORROW rather than on its own date.** `A-54`'s `signalDateNote` says, verbatim:

> "A slip is said on this row BEFORE 2026-09-21, not on it — A-34 slipped four times and the fourth is the reason for that sentence."

So if phase 2 of the second watched area (a detection counter spanning both areas, or an explicit re-scoping of the printed catches figure) will not land on 09-21, **2026-09-20 is the day to say so on the row** — not the 21st, and not by re-dating. This paragraph is quoted rather than paraphrased on purpose: this is the new banner convention, approved by the orchestrator on 2026-09-19 after a 38-field row read was needed to recover a ruling a paraphrase had softened. **When a row's own disposition FORECLOSES an option, the banner quotes that clause word for word.** Clearing condition: once `A-54` phase 2 has landed or its slip is recorded on the row, delete this paragraph.

> **Do NOT promote R(5, 13) ≤ 1138 or R(8, 14) ≤ 41525 to SOUND on the arithmetic, and the browser question is STILL OPEN — it was not settled on 2026-09-19.** The retest that day went through playwright-cli's DEFAULT browser, which identifies itself as HeadlessChrome and which the publisher refused exactly as it refused every earlier automated fetch; the block page echoed that user agent back and the agent did not read that far before writing it up as an ordinary browser session. So what would still settle these rows is a genuinely non-automated session against *Discrete Mathematics* 307 (2007) 760-763 in Elsevier's open archive. What that session could establish is also narrower than it looks: the survey's note 2.1.o credits the HW+ computations to Boza's 2013 personal communication, so the paper may hold the method rather than these two numbers. Clearing condition: a human reads that paper, or the rows are re-verdicted with a reason.

> **Waker ladder — relaunch ranks 1, 2 and 3 at session start.** The commands are listed in `/listen` step 3b, one per rank; run them as separate background calls, never joined. A rung whose predecessor is alive refuses with `already live (pid N)` rather than arming a duplicate, so relaunching ranks 1, 2 and 3 is always safe. A context compact does not re-run `/listen`, which is how this pane lost its ladder twice on 2026-09-17.
<!-- hand:end banner -->

UNVERIFIED FIRST ACTION — matching execution evidence and current governing dependencies have not been established by these release commands.
Recovery read (PowerShell or bash, cwd: lane root): `git status --short` followed by `git log -5 --oneline`; reconcile the authored action in docs/daily-config.md with the current ruling and ledger before running it.

First command (also below, in place):

```bash
git pull --ff-only
# commits after this primer was written are invisible to it — read them before acting:
git log --oneline 53c3abd50ca2212769374af319e0277b2b4879a6..HEAD
```

### Primer — bounds-ledger, 2026-09-20

What ONLY this primer holds comes first; the FULL runnable **First action** block
sits at the foot, below the reads. The single command above is the same block's
first line, repeated for reference — it replaces nothing. Establish the governing action and its preconditions before running release commands.

_**Composed against HEAD commit `53c3abd50ca2212769374af319e0277b2b4879a6` at 2026-09-19T16:30:12.340Z.** Nothing that landed after that instant is in this file. David's board answers especially: an answer he gives later cannot be here, and this primer will go on reading as though the work were not owed — that is exactly how two lanes concluded an OTA was not owed on 2026-09-04. Read them live, never off this page: `node scripts/sky.mjs answered-cards.mjs --project bounds-ledger --full`._

_Shared skylark-site scripts below are spelled `node scripts/sky.mjs …` — this lane vendors `scripts/sky.mjs` (S-20260906-75), so a shared script is named LANE-RELATIVE and resolves from a worktree too._

#### Owed gates — 0 due on/before 2026-09-20

_Ledger read at 2026-09-19T16:30:12.340Z by `cc-due-gates.collectDue` — the same resolver `check-due-gates-dispositioned` uses, so any difference is TIMING, not two answers. Rows filed or re-dated after that are NOT READ at generation — the close-out commit lands after this, so the COUNT above is stale by construction and this list cannot claim completeness. The list below, not the count, is the artifact; re-derive both before acting: `node scripts/sky.mjs check-due-gates-dispositioned.mjs --print --today=2026-09-20`._

_None due. This is a real zero: 1 clock store(s) were read and returned no row due on/before 2026-09-20._

#### What shipped since 24 hours ago

_Read at 2026-09-19T16:30:12.340Z; commits landing after that — the close-out especially — are not here. Re-derive: `git log --oneline --since='24 hours ago'`._

- commit `53c3abd` — A-33 leg 2 ships and the row CLOSES: a drifted file we reported against says go look, to us only
- commit `645457a` — A-54: the two HW+ readings say what was attempted, and a guard that the saying reaches a reader
- commit `15e14f8` — Report 2026-09-18: the Gates line names the receipt taken after the report's own commit
- commit `5603abd` — Hygiene 2026-09-18: the day's report, tomorrow's primer, the phase-4 record, and the Gotchas move
- commit `fc0bbba` — A-54 phase 4: the first five Small Ramsey Numbers bounds read against their sources, on the page

**Yesterday's report:** `docs/daily/2026-09-19-prelaunch.md` (coverage 2026-09-19, the prior day). No findings fence declared in it.
Prior report: historical; re-read its findings and later changes before treating its opener as current.

**Today's report (owed):** write `docs/daily/2026-09-20-prelaunch.md` — OUTPUT obligation due at close, the artifact of record for service day 2026-09-20. Check it exists at close: `node scripts/sky.mjs check-daily-report-exists.mjs --date 2026-09-20` (it sweeps the fleet and applies each lane's coverageOffset; it takes no --project). <!-- owed-output path="docs/daily/2026-09-20-prelaunch.md" service-day="2026-09-20" due="close" -->

#### State — as of 2026-09-19T16:30:12.340Z (re-run the release command; never trust the value)

- **ledger**: 68 item(s): 48 closed · 20 open · as of 2026-09-19T16:30:12.340Z — release: `node scripts/sky.mjs gen-primer-first-action.mjs --repo . --target-date 2026-09-20 --json`

#### Id glossary — 22 id(s) this primer's day already has words for

_Each gloss is the ledger row's OWN title, not a re-derivation — paste it. Rows = every open ledger row plus every id named above. `(no ledger row)` means the id is named here and the ledger carries nothing under it: that is a finding, not a formatting gap._

- `A-2` — STANDING: drift-resolution log for the adopted surfaces…
- `A-9` — engineering-health P2 backlog (fix-on-touch, not scheduled…
- `A-33` — A change to a file we have filed an upstream report…
- `A-45` — TIER-1 LEADING INDICATOR (orchestrator-approved…
- `A-46` — A close-out post announcing a FIX is textually identical…
- `A-47` — THE DEPTH AUDIT, David-ruled 2026-09-09: read our own…
- `A-49` — npm run close:primer passes when tomorrow's banner is…
- `A-50` — Importing one of our CLI scripts can run it — one selftest…
- `A-51` — Upstream's [YKLBMWKCZGS2026] reference in the…
- `A-52` — Write down what an outside party acting on a record could…
- `A-53` — The audit store and the public page can disagree about how…
- `A-54` — Adopt Small Ramsey Numbers (Electronic Journal of…
- `G-2` — GOAL: contribute a verified bound improvement to the…
- `G-4` — GOAL (Tier-0 from G-3 close): an outside party acts on a…
- `S-20260906-75` — (no ledger row)
- `W-3` — Watch for acknowledgement of the erdosproblems.com/36…
- `W-4` — KP-78 applies to every new detector: demonstrate BOTH…
- `W-6` — Read window for the README report-an-error channel — the…
- `W-8` — Does a materially amended outward artifact go back to…
- `W-9` — Does this lane's detection method find what the lane…
- `W-10` — reverify.test.mjs alarm-title leg failed once and would…
- `W-11` — Before asserting a claim is decision-ready, search this…

### First action — bounds-ledger, 2026-09-20

Release commands — subordinate to the governing action.
HANDOFF INCOMPLETE — governing action not authored.

UNVERIFIED FIRST ACTION — these commands have no matching execution evidence here. Command text alone establishes neither safety nor the promised evidence, shell compatibility, or tomorrow's priority.

```bash
git pull --ff-only
node scripts/sky.mjs update-david-board.mjs --list --json --full-ids --project bounds-ledger
git rev-parse HEAD origin/main
git rev-list --count origin/main...HEAD
npm run verify > tmp/verify-out.txt 2>&1
node scripts/sky.mjs check-ci-status.mjs --workflow reverify.yml
```

Morning release commands — deliberately NOT READ at generation (a value written
last night would already be stale); run each, never trust a remembered one:
- `git -C . log -1 --format=%h%x20%s` — HEAD
- `git -C . status --porcelain` — working tree
- `node scripts/sky.mjs check-ci-status.mjs --workflow reverify.yml` — CI
- `node scripts/sky.mjs check-deployed-sha-drift.mjs` — deploy
  - WHERE IT LOOKED: no read-only deploy-state script in this lane's package.json (looked for ops:deploy-state, deploy-state, check-deploy, render-deploys, deploy:status, daily-snapshot). The fleet's check-deploy-state.mjs is not a substitute — it requires --fix <sha> --deploy <sha> and exits 1 without them — so this is the FLEET read, which takes no required flags. service: NOT DECLARED — no row for "bounds-ledger" in skylark-site/src/data/cc-deploy-gates.json (generatedAt 2026-09-17T06:11:22.338Z, 17 repo row(s)) — nothing in this repo declares that lane's Render service. No `--service` flag is emitted: the lane SLUG is not the service name (antebellum-twitter's is disunion-feed), and a guessed flag exits 1.

<!-- hand:begin narrative -->
_(orchestrator narrative — this region survives regeneration)_
<!-- hand:end narrative -->

<!-- generated by gen-primer-first-action.mjs (skylark-site) @ commit 68c12f611 · lane bounds-ledger · for 2026-09-20 · at 2026-09-19T16:30:12.340Z -->
<!-- writtenAtSha: `53c3abd50ca2212769374af319e0277b2b4879a6` — the lane's HEAD commit when this primer was written. Provenance anchor for check-primer-freshness leg ii; NOT a claim about HEAD now (the close-out commit that carries this file lands after generation). -->
<!-- reading this block: a generated line that is wrong in a way you can see FIXED in skylark-site's git log after the commit above is STALE, not a finding — regenerate before filing it. `git -C C:/dev/skylark/skylark-site log --oneline 68c12f611^{commit}..HEAD -- scripts/gen-primer-first-action.mjs src/lib/cc-primer-commands.mjs src/lib/cc-due-gates.mjs src/lib/cc-report-artifact.mjs src/lib/cc-hand-edit-merge.mjs src/lib/cc-cold-readability.mjs src/lib/cc-skeleton-gloss.mjs` -->
