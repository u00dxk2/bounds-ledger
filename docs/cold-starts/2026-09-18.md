<!-- GENERATED — regenerating REPLACES everything outside the hand: markers, so put prose inside them. Provenance stamp at the foot of this block. -->

<!-- hand:begin banner -->
2026-09-17 close. **`A-7` CAN CLOSE TODAY and nothing else here can.** Its closing condition is a
*scheduled* history-sweep run whose log shows the **full commit count** — two numbers the script
prints on every run — not the workflow existing, which is the inspection-not-execution failure the
row was opened about. The run fires at 10:43 UTC. Clearing condition for this paragraph: once `A-7`
is closed or the run is read and found short, delete it.

> **Waker ladder — relaunch ranks 1, 2 and 3 at session start** (the three commands are listed in
> `/listen` step 3b; run them as separate background calls, never joined). This pane lost
> its ladder twice on 2026-09-17: once to a context compact (a compact does not re-run `/listen`)
> and once to the classifier rate-limiting the launch call itself. A rung whose predecessor is alive
> refuses with `already live (pid N)` instead of arming a duplicate, so relaunching ranks 1, 2 and 3
> is always safe and never the wrong move.

> **`claude-sonnet-5[1m]`, the permission classifier, rate-limited this pane twice on 2026-09-17**
> (~16:01Z and ~20:0xZ), refusing Bash and PowerShell calls while Read/Grep/Glob stayed available.
> It is partial and transient — identical calls succeeded on retry within the same minute. Report it
> to the bus naming the exact model id BEFORE retrying, then retry.
<!-- hand:end banner -->

First command (also below, in place):

```bash
git pull --ff-only
# commits after this primer was written are invisible to it — read them before acting:
git log --oneline 76554fdd97bf6745fde3a652f90703cdf5483b2c..HEAD
```

### Primer — bounds-ledger, 2026-09-18

What ONLY this primer holds comes first; the FULL runnable **First action** block
sits at the foot, below the reads. The single command above is the same block's
first line, repeated so it can be running while you read — it replaces nothing.

_**Composed against HEAD commit `76554fdd97bf6745fde3a652f90703cdf5483b2c` at 2026-09-17T20:14:31.933Z.** Nothing that landed after that instant is in this file. David's board answers especially: an answer he gives later cannot be here, and this primer will go on reading as though the work were not owed — that is exactly how two lanes concluded an OTA was not owed on 2026-09-04. Read them live, never off this page: `node scripts/sky.mjs answered-cards.mjs --project bounds-ledger --full`._

_Shared skylark-site scripts below are spelled `node scripts/sky.mjs …` — this lane vendors `scripts/sky.mjs` (S-20260906-75), so a shared script is named LANE-RELATIVE and resolves from a worktree too._

#### Owed gates — 3 due on/before 2026-09-18

_Ledger read at 2026-09-17T20:14:31.933Z by `cc-due-gates.collectDue` — the same resolver `check-due-gates-dispositioned` uses, so any difference is TIMING, not two answers. Rows filed or re-dated after that are NOT READ at generation — the close-out commit lands after this, so the COUNT above is stale by construction and this list cannot claim completeness. The list below, not the count, is the artifact; re-derive both before acting: `node scripts/sky.mjs check-due-gates-dispositioned.mjs --print --today=2026-09-18`._

Unfiltered as of the read time above — nothing was curated out of what the read could
see; a row absent here may simply have been filed after it ran.

- `A-47` (nextCheckDate=2026-09-18, open) — THE DEPTH AUDIT, David-ruled 2026-09-09: read our own cited records against their sources 
  - read: `node scripts/depth-audit.mjs — prints the verdict counts with the sampling rule and the denominator beside them, refuses a bare zero (exit 2) and a count that does not sum to the rows (exit 3). node scripts/depth-audit.mjs --corpus answers the population half. Literal outputs, the red-arm record and the retired placeholder are on this row in readCommandHistory2026_09_13.` · last run: 2026-09-16T17:00:51.949Z
- `A-51` (expectedSignalBy=2026-09-18, open) — Upstream's [YKLBMWKCZGS2026] reference in the minimum-overlap table cites a dead page — it
  - read: `curl -sIL https://test-time-training.github.io/discover.pdf — prints the response headers. The status line proves nothing here, because on 2026-09-05 the dead page answered HTTP 200. Read the Content-Type: text/html means still the Site-not-found page; application/pdf means restored and this row closes as moot.` · last run: NEVER
- `A-7` (expectedSignalBy=2026-09-18, open) — engineering-health P1s — four of five shipped 2026-08-09; OPEN on R7 alone (scheduled veri
  - read: `grep -rn 'history-sweep' .github/workflows/ # R7 (a SCHEDULED verified full-history sweep) is the only open leg. Literal output 2026-08-26: THREE hits, and all three are the --selftest step plus a comment ABOUT R7; no scheduled sweep exists, so R7 is unshipped. READING THE HIT COUNT IS THE TRAP here — the pattern matches the self-test that already ships, so judge the hits, never the number. POSITIVE CONTROL that the glob reads workflow files at all: grep -c 'schedule' .github/workflows/reverify.yml returns 3.` · last run: 2026-09-17T15:24:34.999Z

#### What shipped since 24 hours ago

_Read at 2026-09-17T20:14:31.933Z; commits landing after that — the close-out especially — are not here. Re-derive: `git log --oneline --since='24 hours ago'`._

- commit `e9ad288` — A-7 R7: the scheduled full-history secret sweep, with its known hits dispositioned rather than allowlisted
- commit `aa1aeca` — A-54: the second watched area gets a page a reader can use today
- commit `0c61006` — A-54 phase 1 done on a GitHub runner; A-9 gets a decision instead of a re-date
- commit `9360084` — A-54: adopt Small Ramsey Numbers as the second watched area, scoped, with its revision watch

**Yesterday's report:** `docs/daily/2026-09-17-prelaunch.md` (coverage 2026-09-17, the prior day). No findings fence declared in it.

**Today's report (owed):** write `docs/daily/2026-09-18-prelaunch.md` — the artifact of record for service day 2026-09-18. Check it exists at close: `node scripts/sky.mjs check-daily-report-exists.mjs --date 2026-09-18` (it sweeps the fleet and applies each lane's coverageOffset; it takes no --project).

#### State — as of 2026-09-17T20:14:31.933Z (re-run the release command; never trust the value)

- **ledger**: 68 item(s): 46 closed · 22 open · as of 2026-09-17T20:14:31.933Z — release: `node scripts/sky.mjs gen-primer-first-action.mjs --repo . --target-date 2026-09-18 --json`

#### Id glossary — 23 id(s) this primer's day already has words for

_Each gloss is the ledger row's OWN title, not a re-derivation — paste it. Rows = every open ledger row plus every id named above. `(no ledger row)` means the id is named here and the ledger carries nothing under it: that is a finding, not a formatting gap._

- `A-2` — STANDING: drift-resolution log for the adopted surfaces…
- `A-7` — engineering-health P1s — four of five shipped 2026-08-09;…
- `A-9` — engineering-health P2 backlog (fix-on-touch, not scheduled…
- `A-33` — A change to a file we have filed an upstream report…
- `A-45` — TIER-1 LEADING INDICATOR (orchestrator-approved…
- `A-46` — A close-out post announcing a FIX is textually identical…
- `A-47` — THE DEPTH AUDIT, David-ruled 2026-09-09: read our own…
- `A-49` — npm run close:primer passes when tomorrow's banner is…
- `A-50` — Importing one of our CLI scripts can run it — one selftest…
- `A-51` — Upstream's [YKLBMWKCZGS2026] reference in the…
- `A-52` — Write down what an outside party acting on a record could…
- `A-53` — The audit store and the public page can disagree about how…
- `A-54` — Adopt Small Ramsey Numbers (Electronic Journal of…
- `G-2` — GOAL: contribute a verified bound improvement to the…
- `G-4` — GOAL (Tier-0 from G-3 close): an outside party acts on a…
- `S-20260906-75` — (no ledger row)
- `W-3` — Watch for acknowledgement of the erdosproblems.com/36…
- `W-4` — KP-78 applies to every new detector: demonstrate BOTH…
- `W-6` — Read window for the README report-an-error channel — the…
- `W-8` — Does a materially amended outward artifact go back to…
- `W-9` — Does this lane's detection method find what the lane…
- `W-10` — reverify.test.mjs alarm-title leg failed once and would…
- `W-11` — Before asserting a claim is decision-ready, search this…

### First action — bounds-ledger, 2026-09-18

Every line in the block below is a COMMAND, not a claim. A command re-reads at the
moment you run it and cannot go stale; a state line written last night can.

```bash
git pull --ff-only
node scripts/sky.mjs update-david-board.mjs --list --json --full-ids --project bounds-ledger
git rev-parse HEAD origin/main
git rev-list --count origin/main...HEAD
npm run verify > tmp/verify-out.txt 2>&1
node scripts/sky.mjs check-ci-status.mjs --workflow reverify.yml
```

Morning release commands — deliberately NOT READ at generation (a value written
last night would already be stale); run each, never trust a remembered one:
- `git -C . log -1 --format=%h%x20%s` — HEAD
- `git -C . status --porcelain` — working tree
- `node scripts/sky.mjs check-ci-status.mjs --workflow reverify.yml` — CI
- `node scripts/sky.mjs check-deployed-sha-drift.mjs` — deploy
  - WHERE IT LOOKED: no read-only deploy-state script in this lane's package.json (looked for ops:deploy-state, deploy-state, check-deploy, render-deploys, deploy:status, daily-snapshot). The fleet's check-deploy-state.mjs is not a substitute — it requires --fix <sha> --deploy <sha> and exits 1 without them — so this is the FLEET read, which takes no required flags. service: NOT DECLARED — no row for "bounds-ledger" in skylark-site/src/data/cc-deploy-gates.json (generatedAt 2026-09-17T06:11:22.338Z, 17 repo row(s)) — nothing in this repo declares that lane's Render service. No `--service` flag is emitted: the lane SLUG is not the service name (antebellum-twitter's is disunion-feed), and a guessed flag exits 1.

<!-- hand:begin narrative -->
## Session close-out — 2026-09-17

**What yesterday's Recommendation asked for, quoted, beside the gates that are due today.** The
generated gate list above says *what* is owed; this says what the last person to look at these rows
thought should happen to them, in their words, so today's read is a continuation rather than a
cold re-derivation:

- **`A-7`** — 2026-09-16 recommended: *"`A-7` and `A-9`, the two engineering-health rows, come due
  tomorrow and each needs a decision or a written close rather than a re-date."* Both got one.
  `A-9` was re-pointed to 2026-10-05 with the reason written down; `A-7`'s last open leg shipped
  (`76554fd`). **It is now one read away from closing.**
- **`A-51`** — 2026-09-16 recommended: *"decide `A-51` (the dead upstream citation) … after
  re-reading whether the link is still dead."* Unchanged and still owed. **Read the
  `Content-Type`, not the status line:** that page answered HTTP 200 on 2026-09-05 while serving
  GitHub Pages' "Site not found". `text/html` means still dead; `application/pdf` means restored
  and the row closes as moot. Its `readCommand` has never been run — the row was created 09-16.
- **`A-47`** — 2026-09-16 recommended: *"take slice 6 of the depth audit … the slice's next
  positions are already fixed (125, 175, 225, 275, 325)."* Unchanged. `node scripts/depth-audit.mjs
  --draw 125 175 225 275 325` prints the rows; **print the draw before reading any row**, because
  the stored hashes prove which row a verdict belongs to and not that the draw came first.

### The one number that will lie to you today

`npm run catches` reads **0** and will keep reading 0 however much the Small Ramsey Numbers table
moves. It counts generated pins on the `teorth/optimizationproblems` mirror whose `expect` changed;
the area adopted yesterday is pinned by three **hand** claims (`C-12`, `C-13`, `C-14`) against a
different store, and the counter is silent on hand claims by construction — its own self-test pins
that silence. The lane doubled its watched surface and its catch figure cannot see half of it.
`A-54` phase 2 (2026-09-21) owns the choice: give the second area its own counter, or re-scope the
figure explicitly where it is printed. The wrong outcome is nobody choosing.

### Load-bearing commits from 2026-09-17

- `9360084` — the scope decision for the second area (`docs/decisions/2026-09-17-a54-adopt-small-ramsey-numbers.md`), with what is **not** covered written down.
- `2661e95` (PR #35) — phase 1, the revision watch: `scripts/extract-ds1.mjs` plus `ledger/ejc-ds1/section-2-1.json`.
- `0ad3615` (PR #36) — `ramsey.html`, 72 entries, live and fetch-verified.
- `76554fd` (PR #37) — the scheduled full-history secret sweep.

### What NOT to touch

- **`gridsOf` and `readTable` in `scripts/extract-ds1.mjs`.** They place every glyph in the cell the
  PDF itself draws rules for rather than counting numbers left to right. That is the only reason a
  blank cell in Table Ib cannot silently shift every value after it into the wrong row. Five review
  rounds landed on this code.
- **The disposition mechanism in `scripts/history-sweep.mjs`.** Known hits are keyed to the sha256
  of the **matched line**, never the path, so a new secret in a file that already has a dispositioned
  fixture still fires. An allowlist by path would look equivalent and would not be.

### Four defects found yesterday, all instrument-facing

1. The secret scanner attributed a hit inside a **commit message** to whichever file the previous
   commit's diff ended on — `git log -p` prints messages between the commit header and the first
   diff. The hit was real; the location was fiction. Fixed with a self-test.
2. Stale dispositions were printed **before** undispositioned hits, so a rotted record could have
   hidden a genuinely new secret found in the same run. Reordered.
3. The manual-claim advisory ignored `nothingAfter`. Fixed; both answers proven.
4. About a dozen geometry defects in the PDF extractor across five adversarial rounds — saved text
   state across `q`/`Q`, text rise, ink width conflated with advance width, mirrored matrices,
   digits reordered by a backwards `TJ`, zero-width glyphs, text spanning two ruled rows.

**Nothing record-facing arrived.** 122 cells were transcribed and every one agreed with an
independent sub-agent's transcription taken from a different file format, with a working negative
control. Consecutive instrument-facing days: **2**.
<!-- hand:end narrative -->

<!-- generated by gen-primer-first-action.mjs (skylark-site) @ commit accf3f567 · lane bounds-ledger · for 2026-09-18 · at 2026-09-17T20:14:31.933Z -->
<!-- writtenAtSha: `76554fdd97bf6745fde3a652f90703cdf5483b2c` — the lane's HEAD commit when this primer was written. Provenance anchor for check-primer-freshness leg ii; NOT a claim about HEAD now (the close-out commit that carries this file lands after generation). -->
<!-- reading this block: a generated line that is wrong in a way you can see FIXED in skylark-site's git log after the commit above is STALE, not a finding — regenerate before filing it. `git -C C:/dev/skylark/skylark-site log --oneline accf3f567^{commit}..HEAD -- scripts/gen-primer-first-action.mjs src/lib/cc-primer-commands.mjs src/lib/cc-due-gates.mjs src/lib/cc-report-artifact.mjs src/lib/cc-hand-edit-merge.mjs src/lib/cc-cold-readability.mjs src/lib/cc-skeleton-gloss.mjs` -->
