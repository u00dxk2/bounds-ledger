# Cold-start primer for 2026-08-02 (MT)

You are the bounds-ledger agent. Read this, then `CLAUDE.md`, then `continuity/items.json`. This lane is a
**steward** of drifting mathematical records — a ledger that alarms when a cited record moves. Pre-launch,
private repo, **no self-rating**.

**THE RAIL CHANGED YESTERDAY.** David-ruled 2026-08-01, effective today: **PACED rail** (recorded in
CLAUDE.md, replaces lite). Each session: steward cadence FIRST, then **≥1 concrete launch increment from
A-13's backlog**, weekly forced-decision pass. This primer's job is to hand you both halves.

## Session close-out — 2026-08-01 (one session, the fullest day the lane has had)

Full detail: `docs/daily/2026-08-01-prelaunch.md` (4/4 linters). Tree clean on `main` at `53c685e`, pushed,
CI green (log-read). Findings/receipts: `docs/findings/2026-08-01-third-drift-certified-records-land.md`.

### State

**229 claims / 227 hold / 0 broken / 2 UNVERIFIED (by design)** over a **111-file** mirror, clean at
upstream `621429c`. **G-1 green-streak: day 9 of 30** as of 8/01 (day 1 = 7/24; a green 8/02 scheduled run
makes it day 10; ends ~8/22). North star (externally-acknowledged corrections): **0** — page advisory-read
8/01 from residential IP: unchanged, both pins present. Issue #7 (the 8/01 drift) closed with the
verification record; **issue #6 open** (A-12, hosted brief — see below).

### What happened 8/01, compressed

1. **Third real drift, the largest yet (12 files), verified file-by-file BEFORE snapshot** — every
   recomputable value in the rewritten 3a reproduces; 3d ADDED (single-set sum-difference exponent,
   **solved**: C_3d = 2, LiLi26 arXiv:2607.27199, AI-assisted). Mirror 110→111, pins 218→220. `pin:3a:L`
   is now the K2026 row. Resolved same-day at `60a044e`.
2. **The strategic event was inside the drift**: the stewarded table now carries record rows accepted from
   apparently-automated contributors (Numaro, Mosaic Intelligence) with archived certificates + replayable
   checkers, via ordinary merged PRs. The acceptance channel for G-1 exists in public.
3. **Contribution-channels deep-research ran and returned SAME DAY** (David ran it externally; 2-source).
   Ingested: `skylark-site/docs/research-library/2026-08-01-bounds-ledger-contribution-channels.md` (+ raws
   alongside). Verdict: lead with a repository-changing CORRECTION, never a bare replay; scope/literature
   fit outranks certificates; **any verified-by/attestation system is KILLED until one manual acceptance**.
   Note: the two engines disagreed on whether the GHR rows had merged — **our mirror settled it** (they had).
4. **Rail assessment answered → David ruled** (paced rail, above). Exit criterion finally written: **A-13**
   (public flip + stranger-readable surface). SECURITY.md landed (A-9's R3).
5. **DAVID DECIDED BOTH OPEN QUESTIONS IN-PANE (supersedes his 7/26 holds — his own words in A-13 note3 /
   W-3 note7):** (1) **flip = YES in principle** — gate is the package + adversarial review, NOT a date;
   (2) **the 7/24 correction converts to the PUBLIC route** (comment on erdosproblems.com/36) — GO to
   prepare; W-3 is a monitor only and no longer paces anything. **NEW STANDING RULE (CLAUDE.md): at least
   one adversarial refute-it review on EVERYTHING this lane produces that might touch the public.**

### Today's first-action (steward half)

Read the overnight run's **LOG, not the badge** (`gh run list --limit 3`, `gh run view <id> --log`). Expect:
`No drift. 111 files match upstream 621429c…` · `229 claim(s): 227 hold, 0 broken/unreachable, 2 unverified
(manual)` · TWO `advisory fetch failed (HTTP 403)` lines (C-7/C-9 — expected in CI). Green → note streak
day 10, move on. Then `npm run check` locally (residential IP does the real advisory fetch; redirect to a
file, check the exit code — never through a pipe). If red: verify → `--snapshot` → `extract-pins` → commit,
in that order; a red run is the alarm working (three real drifts so far, all editorial-or-additive).

### Today's launch half (the paced-rail quota) — in this order

1. **Draft the public correction wording** for erdosproblems.com/36 (source material:
   `docs/decisions/2026-07-23-A3-erdosproblems-report.md` — the corrected emailed text; adapt for the public
   venue). **Re-verify the record state first** (is 0.380868 still the curated best upper bound in 1b?).
   Then run the **adversarial refute-it review** (fresh angles; include the METHOD-sentence angle — the
   7/24 lesson). The reviewed wording goes to David for his pre-send read. **Do not post anything yourself.**
2. **Flip package**: README-as-product rewrite (a stranger's first read), no-secrets history sweep, then the
   package-level adversarial review. When it passes, the flip click goes to David.
3. **First-PR target selection** (if time): replay the open unreviewed certificate PRs on
   teorth/optimizationproblems (#129 / #130 / #135 per the research) — a discrepancy found upgrades a
   comment to a correction, which is the accepted contribution shape. Local replay only; any outward step is
   review + David-gated.

### Watch items

- **W-3** — monitor only now: `npm run check` reads both page pins; forward any maintainer reply if David
  supplies one. Do NOT treat it as a blocker; do NOT close it without his word.
- **A-12** — hosted brief **3 blocks stale** (26 Jul, 28 Jul, 1 Aug) — verified against production at close
  8/01 (`check-brief.mjs` exit 1). Issue #6 is the standing alarm; orchestrator owns the port; close-sweep
  ask filed 8/01 evening. **If still red after 2026-08-07, escalate to David** naming the blocks by date.
- **W-4** — before shipping ANY new detector, demonstrate both sides (fire + silent) and prove mutations
  landed with `git diff --numstat` (CRLF `\n` no-op trap).

### What NOT to touch

- **Do not re-automate C-7/C-9** — the 403 is IP-dependent; a local 200 proves nothing about CI.
- **Do not build a verified-by column / attestation schema / badge system** — killed until one manual
  contribution is accepted (both research engines, same kill criterion; A-13 note2).
- **Generated pins stay listing-position pins** — do not upgrade the new 3a record rows to "record" claims
  (two are asterisked limit values upstream itself declines to certify).
- **Curated vs live: never blend.** Outward sends: adversarial review + David's gate, always — plus the new
  standing rule above for anything public-touching.
- UNVERIFIED stays at 2 (David 7/26, untouched by the 8/01 decisions).
