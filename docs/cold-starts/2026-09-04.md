<!-- GENERATED — regenerating REPLACES everything outside the hand: markers, so put prose inside them. Provenance stamp at the foot of this block. -->

<!-- hand:begin banner -->
> **ORDERED MOST-GOVERNING-FIRST ON PURPOSE.** On 2026-09-03 the kickoff quoted this banner
> truncated at 2000 chars and the cut fell in the TAIL, taking the two dated-gate pointers and
> `A-34`'s no-slip clause with it. Until that composer fix lands, what matters most is at the top.
>
> **START LINE 4 OF THE FIRST ACTION BEFORE YOU READ THIS DOCUMENT.** `npm run verify` needs no
> context, takes about three minutes and writes its whole answer to a receipt keyed to a sha.
> Background it, then read on:
> ```
> npm run verify > tmp/verify-out.txt 2>&1
> ```
>
> **`A-34` MUST NOT SLIP A THIRD TIME — due 2026-09-05, one day out.** David asked where else this
> ledger can have the kind of impact `G-3` produced. Its own trigger says give a date rather than
> send a thinner answer. **New on 09-03:** `G-4`'s denominator cannot fill by its own 09-26 date —
> the threshold is 30 unique viewers and the read was ≤6 distinct viewers in 26 days, 2 in the
> trailing 14 (`npm run traffic`, read 2026-09-03) — so that arithmetic was escalated INTO this
> row rather than re-dated. Breadth-versus-depth now carries it.
>
> **THIS LANE IS LAUNCHED** — David ruled it 2026-08-27. Instruments a launched app has and this
> one does not — retention, active users, Sentry, revenue — are answered **"not an instrument on
> this lane, by design"**, never with a zero. The real reads are `npm run reports` and
> `npm run traffic`.
>
> **A `npm run catches` ZERO CAN CONTAIN A REAL RECORD MOVEMENT.** A catch is a generated pin whose
> `expect` CHANGED; when upstream ADDS a constant there is no prior `expect`, so the week reads 0
> while the mirror gained a record. That happened on 09-02 with `88a`/`88b`. Never quote the figure
> without reading the same window's drift log.
>
> **`CLAUDE.md` + `AGENTS.md` SIT AT EXACTLY THE LOADED BUDGET.** 44812 alone, 60000 loaded, of
> 60000 — as of 2026-09-03, from
> `node ../skylark-site/scripts/check-claude-md-sizes.mjs --project bounds-ledger`. **Any addition
> must be paired with a removal**, or land in a doc that is not context-loaded. A convention added
> to CLAUDE.md on 09-03 pushed it to 61363 and was moved back out the same day.
>
> **READER REACH IS UNMEASURED, AND THAT ABSENCE IS CONSIDERED, NOT A GAP.** Client-side analytics
> are refused by `render-site.mjs`'s selftest; a server-side read is NOT forbidden by that
> invariant — what actually kills it is that GitHub Pages gives no request log. So `[reached: …]`
> on a page ship reads `unknown` with that reason, and `npm run traffic` / `npm run reports` count
> github.com, **never page readers**. Full reasoning: `docs/evangelism-bar.md` § Reader reach.
> Standing an instrument up here is David's call via `A-34`, never self-approved.
>
> **The report path stays `docs/daily/<date>-prelaunch.md`.** Historical filename, not a lifecycle
> claim. Do not helpfully fix it.
>
> **THE MATHSTODON POST IS CLOSED, UNSENT, AND MUST NOT BE RE-PROPOSED.** Nothing is owed there.
>
> **`gen-primer-first-action.mjs` REPORTS `RESULT: PASS` AT EXIT 0 WHILE WRITING NOTHING** unless
> you pass `--out`. It also does NOT carry the previous day's banner forward — this block is
> authored fresh at every close. The form that actually writes:
> ```
> node ../skylark-site/scripts/gen-primer-first-action.mjs --repo . --target-date <date> --out docs/cold-starts/<date>.md
> ```
>
> **CODEX CANNOT WRITE ANYTHING UNDER `.git/` ON THIS MACHINE — measured three times on 09-02.**
> `git add`, `git commit`, `git checkout` all fail `Permission denied`; a write probe to `tmp/`
> succeeds. Brief a dispatch to run **no git command at all** and to prove a mutation landed with
> `grep -c`, never `git diff`. You stage and commit. And **diff its output against the artifact the
> REVIEW read**, not the tree it was written on.
>
> **`git rev-parse --short HEAD origin/main` exits 128.** Two revisions plus `--short`. Drop
> `--short`.
<!-- hand:end banner -->

First command (also below, in place):

```bash
node ../skylark-site/scripts/update-david-board.mjs --list --json --full-ids --project bounds-ledger
```

### Primer — bounds-ledger, 2026-09-04

What ONLY this primer holds comes first; the FULL runnable **First action** block
sits at the foot, below the reads. The single command above is the same block's
first line, repeated so it can be running while you read — it replaces nothing.

#### Owed gates — 0 due on/before 2026-09-04

_Ledger read at 2026-09-03T16:02:00.945Z by `cc-due-gates.collectDue` — the same resolver `check-due-gates-dispositioned` uses, so any difference is TIMING, not two answers. Rows filed or re-dated after that are NOT READ at generation — the close-out commit lands after this, so the COUNT above is stale by construction and this list cannot claim completeness. The list below, not the count, is the artifact; re-derive both before acting: `node ../skylark-site/scripts/check-due-gates-dispositioned.mjs --print --today=2026-09-04`._

_None due. This is a real zero: 1 clock store(s) were read and returned no row due on/before 2026-09-04._

#### What shipped since 24 hours ago

_Read at 2026-09-03T16:02:00.945Z; commits landing after that — the close-out especially — are not here. Re-derive: `git log --oneline --since='24 hours ago'`._

- commit `5d46d05` — Hygiene: three ledger instructions pointed at dates their own rows had left
- commit `8142057` — The from-to pair I published was the shape this repo nearly published on 09-01
- commit `3d7358f` — The day's report, and the week's zero that contains a bound improvement
- commit `28eebf4` — Both dated gates dispositioned, and R7 was nearly shipped against two standing prohibitions
- commit `938854b` — The 114 pages we shipped yesterday become reachable from the table
- commit `0a8594f` — The separator debt reaches zero, and the checker that measured it grows the guard it was missing
- commit `1885cb6` — Bonus cycle: every constant becomes a page you can cite, instead of a row inside a table of 114
- commit `c65f3aa` — The dating render for 88a and 88b
- commit `3eb95d7` — Second drift in fifteen minutes: the bounded prime gap constant, and a burst recorded rather than chased
- commit `dfa210d` — The second render, which is the only one that can date the row that just arrived
- commit `c28ffd7` — Drift resolved: upstream added C_3e two minutes before our check, and the alarm named exactly the two files it touched
- commit `c8d257d` — The separator predicate becomes a committed script, because tmp/ does not survive to tomorrow
- commit `6c79ad3` — P5 follow-through: two directed rules into CLAUDE.md, paid for, and the separator debt measured properly
- commit `45c5111` — Hygiene 2026-09-02: the day's report, tomorrow's primer, and two gates pre-staged with their reads already run
- commit `096e126` — P4: both gates dispositioned, and the delegated fix was a regression wearing a fix's name

**Yesterday's report:** `docs/daily/2026-09-03-prelaunch.md` (coverage 2026-09-03, the prior day). No findings fence declared in it.

**Today's report (owed):** write `docs/daily/2026-09-04-prelaunch.md` — the artifact of record for service day 2026-09-04. Check it exists at close: `node ../skylark-site/scripts/check-daily-report-exists.mjs --date 2026-09-04` (it sweeps the fleet and applies each lane's coverageOffset; it takes no --project).

#### State — as of 2026-09-03T16:02:00.945Z (re-run the release command; never trust the value)

- **ledger**: 52 item(s): 35 closed · 17 open · as of 2026-09-03T16:02:00.945Z — release: `node ../skylark-site/scripts/gen-primer-first-action.mjs --repo . --target-date 2026-09-04 --json`

#### Id glossary — 17 id(s) this primer's day already has words for

_Each gloss is the ledger row's OWN title, not a re-derivation — paste it. Rows = every open ledger row plus every id named above. `(no ledger row)` means the id is named here and the ledger carries nothing under it: that is a finding, not a formatting gap._

- `A-2` — STANDING: drift-resolution log for the adopted surfaces…
- `A-7` — engineering-health P1s — four of five shipped 2026-08-09;…
- `A-9` — engineering-health P2 backlog (fix-on-touch, not scheduled…
- `A-20` — Should the fetch layer retry with backoff on 429/502? —…
- `A-29` — secret_scanning_validity_checks will not enable via API —…
- `A-31` — Should the public page publish be CI-gated? It is not, and…
- `A-33` — A change to a file we have filed an upstream report…
- `A-34` — David asked where else this ledger can have the kind of…
- `A-39` — A mid-run 429 becomes up to 339 requests in two seconds at…
- `G-2` — GOAL: contribute a verified bound improvement to the…
- `G-4` — GOAL (Tier-0 from G-3 close): an outside party acts on a…
- `W-3` — Watch for acknowledgement of the erdosproblems.com/36…
- `W-4` — KP-78 applies to every new detector: demonstrate BOTH…
- `W-6` — Read window for the README report-an-error channel — the…
- `W-8` — Does a materially amended outward artifact go back to…
- `W-9` — Does this lane's detection method find what the lane…
- `W-10` — reverify.test.mjs alarm-title leg failed once and would…

### First action — bounds-ledger, 2026-09-04

Every line in the block below is a COMMAND, not a claim. A command re-reads at the
moment you run it and cannot go stale; a state line written last night can.

```bash
node ../skylark-site/scripts/update-david-board.mjs --list --json --full-ids --project bounds-ledger
git rev-parse HEAD origin/main
git rev-list --count origin/main...HEAD
npm run verify > tmp/verify-out.txt 2>&1
node ../skylark-site/scripts/check-ci-status.mjs --workflow reverify.yml
```

Morning release commands — deliberately NOT READ at generation (a value written
last night would already be stale); run each, never trust a remembered one:
- `git -C . log -1 --format=%h%x20%s` — HEAD
- `git -C . status --porcelain` — working tree
- `node ../skylark-site/scripts/check-ci-status.mjs --workflow reverify.yml` — CI
- **deploy read: NOT AVAILABLE** — no read-only deploy-state script in this lane's package.json (looked for ops:deploy-state, deploy-state, check-deploy, render-deploys, deploy:status, daily-snapshot). The fleet's check-deploy-state.mjs is not a substitute — it requires --fix <sha> --deploy <sha> and exits 1 without them

<!-- hand:begin narrative -->
_(orchestrator narrative — this region survives regeneration)_
<!-- hand:end narrative -->

<!-- generated by gen-primer-first-action.mjs (skylark-site) @ commit 8de1d73a · lane bounds-ledger · for 2026-09-04 · at 2026-09-03T16:02:00.945Z -->
<!-- writtenAtSha: `5d46d05dd110a1883e0efdf2813b20b5bdbbc334` — the lane's HEAD commit when this primer was written. Provenance anchor for check-primer-freshness leg ii; NOT a claim about HEAD now (the close-out commit that carries this file lands after generation). -->
<!-- reading this block: a generated line that is wrong in a way you can see FIXED in skylark-site's git log after the commit above is STALE, not a finding — regenerate before filing it. `git -C ../skylark-site log --oneline 8de1d73a..HEAD -- scripts/gen-primer-first-action.mjs src/lib/cc-primer-commands.mjs src/lib/cc-due-gates.mjs src/lib/cc-report-artifact.mjs src/lib/cc-hand-edit-merge.mjs src/lib/cc-cold-readability.mjs src/lib/cc-skeleton-gloss.mjs` -->
