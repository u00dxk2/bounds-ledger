# Cold-start primer for 2026-08-13 (MT)

Multi-session primer — each /close-session run appends a new section below. Later sections
supersede earlier ones explicitly.

## Session close-out — 21:05Z (2026-08-12, ~15:05 MT)

You are the bounds-ledger agent. Read this, then `AGENTS.md` + `CLAUDE.md`, then
`continuity/items.json`. The repo is PUBLIC.

### THE HEADLINE — the north star finally moved, and it is not 0 any more

**Terence Tao merged PR #141 on 2026-08-11T17:18:12Z.** Externally-acknowledged corrections:
**0 → 1**. `mergedBy teorth`, merge commit `25cb9304` (an **upstream** sha — like every `teorth/…` sha in
this file, it does not resolve in our repo), 1 file, +6/−6 on `constants/15a.md`. Verified from
the GitHub API, not from our own tooling.

**G-1's acknowledgement condition is MET. G-1 stays open on the 30-day green streak alone.**
**W-3 — the watch on the erdosproblems.com email — is a DIFFERENT channel, still unanswered.
Do not close it on this.**

Read the two failures attached to that win before you celebrate it, because they are the
reusable part and they are both *yours* to avoid repeating:

1. **Our own alarm detected the merge and we misread it.** The 8/11 evening "upstream editorial
   drift" on `15a.md` — five citation keys normalised, verified carefully, attributed to
   upstream housekeeping — **was our own PR landing** (`d30d4e4`, same file, 6+/6−). The
   instrument worked perfectly; the interpretation was wrong. **When a drift lands on a file we
   have an open PR against, run `gh pr view` before writing the attribution.**
2. **The morning report said "PR #141 open and untouched; checked, not nudged" — nobody had
   checked.** The phrase was inherited from the primer. The same defect landed twice more the
   same day: an *answered* board card was described as riding David's board. **"Check, do not
   poll" is not licence not to read.** Verify by executing, never by inheriting.

### Tomorrow's first action — NOTE THE ORDER CHANGED TODAY

**`npm run check` FIRST, with the pin. Then read the overnight CI log.** This is a new rule
(`CLAUDE.md`), approved today: the two reads answer different questions and only the first is
actionable at 07:45. On 8/12 the overnight green log was four and a half hours stale and
described a world that no longer existed.

```powershell
npm run check
```

Run it with `CC_PROMPTS_PIN` present in the environment, or the hosted-brief leg can see nothing
and an anonymous run proves nothing either way. The retrieval idiom is the user-scope one in the
global `CLAUDE.md`; it is deliberately **not** written out here, because the pre-commit secret
sweep refuses a staged diff carrying that assignment shape — it blocked this very file on the
first attempt to commit it, which is the detector working on its author.

Expect: `No drift. 112 files match upstream 603052de…` (an **upstream** sha — it does not exist
in this repo, do not `git show` it), `231 claim(s): 229 hold, 0 broken/unreachable, 2 unverified
(manual)`, `state block: in sync`, `brief: in sync`. Green → **streak day 21 of 30** (day 1 =
2026-07-24; 8/12 was day 20).

Then the log, for the **final pushed commit**:
`gh run view (gh run list --limit 1 --json databaseId --jq '.[0].databaseId') --log`

Then `npm run traffic` — GitHub keeps only 14 days.

**Expect churn, and do not chase it.** Upstream pushed **five times** on 8/12 and is actively
reworking one attribution paragraph. New rule in `CLAUDE.md`: after a second consecutive red
caused by an upstream push inside an hour, resolve to the latest sha, **record the burst, and
let the scheduled run be the next check**. A burst inflates the drift-cycle count in a way that
looks like productivity and is really a measure of upstream's typing speed.

### Then the increment — pick one; nothing is owed

Every orchestrator carry-forward from today is closed. Candidates, no ranking imposed:

- **G-2 round four** — the standing technical increment. Needs a family that buys colour more
  cheaply per edge than the joins of rounds one through three.
- **A root-level `DRIFTS.md`** — Tier 1c of the findability menu, agent-doable, still unbuilt.
  Only worth it if it goes deeper than the README table it would duplicate.
- **Tier 2 findability** — all David-send. The menu names Mathstodon first, *after* a merged PR
  to point at. We now have one. Propose; do not send.

### Today's load-bearing ships (8 commits, all pushed)

- **`ec01082`** — two named constants in one morning. The real **Grothendieck constant**, both
  ends improved by one paper ([arXiv:2608.11158]), settling its tenths digit as 7; and
  **Crouzeix's conjecture**, upper bound `1+√2 → 2`, claimed proved ([arXiv:2608.03841]). Both
  verified against the papers before the mirror moved.
- **`4d06395`** — a **priority correction**, a drift class we had not seen: no bound moved,
  upstream credited a second independent claimed proof (Jin) posted eight days earlier.
- **`3f0abce`** — **`scripts/render-state-block.mjs`**, ruled in by the orchestrator. See the
  don't-touch list below; this changes how you edit the README.
- **`369cedc`** — the findability menu, plus the discovery that our repo had an **empty
  description and no topics**. Both now set.
- **`651322c`** — A-14 dropped after adversarial review (below).

### A-14 is CLOSED — dropped, not sent, and it should not be revived

David opened the drafting gate: *"Draft it and show me the PR, codex adversarial review it,
then after I approve you can send it."* The review killed it before it reached him.

`git log` on the exact line settled it: an outside contributor (PR #142) added the unmarked 3b
improvement line on 8/10, then **teorth committed "Mark C_3b unverified record with asterisk in
README"** on 8/11 — a commit whose entire stated purpose was that marking — and put the asterisk
on the table row, **not** on the changelog line three above. His legend scopes the asterisk to
"the table below". We would have told the maintainer his own deliberate edit of the previous day
was incomplete, as our second contact, one day after he merged our first.

**The lesson that outlives it:** we enumerated the *state* of 111 rows and 19 lines across two
sessions and never asked **who made it that way and when**. One `git log` answered it. An
enumeration tells you what is inconsistent, never whether the inconsistency was intentional —
and only the second earns the right to write to a person.

### What changed about how you work here

- **`README.md`'s "Current state" block is GENERATED.** Do not hand-edit between the
  `state-block` markers. `node scripts/render-state-block.mjs` rewrites it; `--check` fails if it
  disagrees with committed state and runs in `npm test`, `npm run check`, and CI. It states only
  what is true of the repo *as committed* and deliberately **never** a live verdict like
  "229 hold" — baking a fetch-time result into a static file is the staleness it exists to kill.
- **Positive control before any absence claim.** Before concluding a cited artifact lacks
  something, **name the token from the same fetch that proves you had the right document**.
  Twice on 8/12 an HTTP 200 was not the artifact — preprints.org's bot wall, then **ar5iv
  silently falling back to the arXiv abstract page**. The second nearly became a public claim
  that a named mathematician's paper omits an acknowledgement. Crossref content negotiation
  (`api.crossref.org/works/<doi>`) is the route that works on a walled server.
- **Gloss the sha class on first use in anything leaving the repo.** Upstream shas do not resolve
  locally; an unglossed arrow chain reads as fabrication to an outsider. The bus verifier now
  refuses posts that carry them bare — pass `--known-ids` *and* gloss the prose.

### CI

**`reverify`: success @ `3f0abce`**, read by log, not by badge. Local `main` and `origin/main`
agree; tree clean. 12 self-tests across 18 workflow steps.

### Codex — environment note, read before you conclude it is broken

**Codex was FIXED this afternoon** (PowerShell 7 MSI installed; sandbox probe clean). Any pane
carrying a stale PATH will still fail the probe — that is **environment-stale, not
Codex-broken**. A fresh session gets working Codex with no action. Today's A-14 review ran with a
dead shell (`CreateProcessAsUserW failed: -1073283067`) and completed only via GitHub API
tooling, so it never read the PR body and **failed closed saying so** — which is why its facts
were trustworthy and its clearance was not.

### Tier 0 / binding constraint

**North star: 1** (was 0 for 21 days). **W-6 — arrival: 2 unique viewers, 0 referrers, day 5 of
90.** Nothing on the internet links here. Arrival is still the binding constraint for the
*second* acknowledgement.

**Measurement change approved today:** add **verified catches on named records, per week** as a
Tier-1 leading indicator (8/12: 2). Not yet wired into any file — that is a real, small,
unclaimed piece of work. It exists because G-1 has no gradient, and it answers a question nothing
else can: **whether the adopted surface is still alive enough to be worth stewarding.** A month
of zeros means adopt a second surface, not try harder.

### Evangelism read, honestly

The ledger is the product and it still has no users. The would-tell-a-friend moment is a stranger
finding a record we caught moving before their own source did — nobody has arrived to have it.
**But the adjacent moment did happen today: the maintainer of the surface we watch accepted a
correction we found.** That is the strongest evidence this repo has ever had that the machine
works, and it is now the first thing the README says. Retention/word-of-mouth instrument: the
traffic sampler, day 5 of 90. Advocacy signal: one merged PR — the first non-zero reading of
anything in this lane.

### Nothing is waiting on David

Awaiting rows for this project: **0** (HTTP 200, real probe). Board card `ed3347cc` (the A-14
send question) is **answered** and acted on. Do not re-open it.

### What NOT to touch

- **Do not hand-edit README's `state-block` region** — regenerate it.
- **Do not revive A-14.** It was refuted on the maintainer's own git history, not deprioritised.
- Do not re-automate C-7/C-9 — the erdosproblems 403 is IP-dependent; only a green CI run is
  evidence about CI.
- Do not put `CC_PROMPTS_PIN` in repo secrets. Public repo.
- Do not re-propose the public comment on /36 — decided NO on 2026-08-02.
- Do not send anything outward off your own bat; every Tier-2 findability move is David-gated.
- Do not hand-edit the mirror or the generated pins; do not weaken the 27b verifier.
- Do not allowlist `history-sweep.mjs`'s four hits.
- **Do not "upgrade" the generated-pin rule.** A pin asserts listing position, never "the
  record". Today it carried a claimed proof of a conjecture open since 2004 *and* a priority
  dispute, and our ledger asserted neither. Every pressure today ran toward making it more
  interesting. **If a future session proposes that, the proposal is the alarm.**

### State at close

`main` at the primer commit, tree clean, local and origin agree, PUBLIC. 8 commits today.
`npm test` exit 0 (12 self-tests). `npm run check` exit 0. Daily report
`docs/daily/2026-08-12-prelaunch.md`, 7/7 linters PASS. Open continuity items: **8** (A-14 closed
today). Dead references: 0. Zero cards waiting on David. **Codex calls: 1.**
