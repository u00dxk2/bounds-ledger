# Cold-start primer for 2026-08-25

Multi-session primer — each close-out appends a section below. Later sections supersede earlier ones.

## Session close-out — 2026-08-24

Paced rail, day 23. Six commits: one drift cycle, one user-visible ship, one forced decision closed, one new item filed, and two of my own claims retracted after the orchestrator checked them.

### Tomorrow's first-action — VERIFIED, exit 0 in the turn that wrote this

```bash
git rev-parse HEAD origin/main && npm run verify > tmp/verify-out.txt 2>&1; echo "EXIT=$?"
```

Run from the repo root. **No leading `cd`** — that changed today. A `cd X && …` compound cannot be statically resolved by the permission classifier, so it parks at a silent approval prompt that reads exactly like a hang. Yesterday's primer carried the `cd` form and it worked, but it was one classifier mood away from stalling your first ninety seconds. The report linter flagged it; the fix is free.

Executed at close: printed `bcb9b52…` twice, `npm run verify` **exit 0**.

Still `git rev-parse HEAD origin/main`, **not** `--short` — `--short` takes one revision and dies with `fatal: Needed a single revision`, and in a leading `&&` position that silently skips the entire cadence. Reproduced by three people who read the warning the same morning. And still redirect rather than pipe: the guard refuses a piped gate, correctly.

### ⚠ YOUR DAY HAS A SPACE CONSTRAINT, NOT A TIME CONSTRAINT — READ THIS BEFORE YOU START

**Four gates come due today and `CLAUDE.md` is 340 bytes under its budget.**

`44,660` bytes against a `45,000` warn threshold, read from `node ../skylark-site/scripts/check-claude-md-sizes.mjs` (the canonical measurer — UTF-8 bytes, LF-normalized, per the unit ruling in skylark-site's `size-budgets.md`; never self-count). Four gates each wanting a line **will** breach it.

**Do the compression FIRST, as scoped work.** Not as a squeeze at the end of a gate write-up — done badly it cuts the load-bearing sentences, and this file's whole value is that its sentences were each paid for. The P5 rail defaults doc-compression to Codex with Sonnet supplying only the keep-list; that split exists for exactly this.

### The four gates, each with a read command that answers its own question

Added yesterday (`892c25b`) because they had a paragraph of intent between them and no way to establish current state. **Two of the four commands lied on their first run and were rewritten — do not "simplify" them back.**

- **A-18** (generated STATE block for the primer) — `ls -1 docs/cold-starts/ ; grep -L "exit 0" docs/cold-starts/2026-08-*.md`. Empty grep means the hand-written property held all week and this **closes**. Read the listing too: the grep cannot see a day with NO primer, and 2026-08-20 was one.
- **A-19** (two review nits) — `sed -n '185,195p' scripts/reverify.test.mjs`. N-2 is the alarm-title guard's SKIPPED branch at line 191, which reports success for a guard that never ran when `bash` is absent from PATH. Evidenced, not hypothetical.
- **A-25** (deferral expiry field) — `grep -c '"expiresOn"' continuity/items.json`, **quoted key form**. The bare word form returns 1 and that hit is A-25's own note describing the field it is asking for.
- **A-26** (the evangelism-bar doc this repo lacks) — `ls docs/ | grep evangelism-bar` plus the quoted `"loopArchetype"` key. Same trap; also **do not** name that doc by its literal `docs/…md` path in a tracker field, because the dead-reference checker reads it as a reference and goes red on the probe.

  **This paragraph tripped that checker while writing it.** The first draft spelled the path out inside the sentence warning you not to spell it out, and `check-doc-references.mjs` flagged the primer. Seventh time this repo has produced a fix note that is a fresh instance of its own defect; the count is kept because the document is inside the class it describes.

**A-25's single re-date is SPENT.** Today it ships or closes as declined. It does not roll again.

### THE THING THAT WILL MISLEAD YOU FIRST

**`npm run catches` says 2 for the current week and no number moved in either.** `pin:53a:L` and `pin:56a:U` both changed because upstream escaped a backslash inside inline math. A pin is a byte comparison of a whole table row, so a backslash moves it.

The counter is honest — its own output leads with "a CEILING on catches, not a count of them" — and the seam is any report or bus line quoting the bare figure. The A-23 classification sentence now carries a **numeric-vs-byte-only** clause for exactly this; keep using it.

### What a user can now see that they could not

**The page said `last changed <date>` for three different real events.** It now says which one happened (`a7bf5ff`, sha named in the flow doc at `118c88c`):

- `value changed 2026-08-23` — the numerals in the bound cell differ from the previous version.
- `text edited 2026-08-24 — bound unchanged` — the row changed byte-wise and the bound did not.
- `first pinned 2026-07-24 — unchanged since` — never changed since tracking began.

Live over 222 pins: **208 first-pinned, 8 value changed, 6 text edited.**

**The third label is the bigger win.** 208 rows carried the bootstrap date and read as though something had happened that day — the misreading the 08-23 primer named as the first thing that would mislead a reader. It cost one extra branch, because the comparison that separates value from text also detects "there was no previous version".

**Do not widen the comparison to the whole row.** It is confined to the bound cell on purpose: whole-row comparison calls `87a`'s degree-8-to-degree-12 citation correction a moved bound. An assertion pins that case, so a future simplification fails a test rather than a reader. And `value changed` claims only that the numerals differ — never that a bound improved, never which row is the record.

### Two claims of mine were WRONG and are retracted — do not repeat either

Both were in the P1 retro; the orchestrator checked them and was right.

1. **There is no auto-close in `reverify.yml`.** Issue #29 was closed by the `Closes #29` keyword in my own commit, and GitHub's close event carries `commit_id`, so the audit trail I claimed was missing is the strongest field on the event. AGENTS.md now says not to use that trailer on a drift commit — it fires before anyone writes down what was verified — and names the timeline read for auditing past closes.
2. **`index.html` does not embed the README drift count.** `render-site.mjs` opens exactly `ledger/claims.json` and the mirror manifest. The real mechanism: row dates come from a pickaxe search over **committed** history, so `resnap` renders before the new `claims.json` is committed and the page publishes `date unknown` for the rows that just moved — while their `data-changed` sort key still holds the OLD date. Label and sort key disagree, and the sort key is what the Order control reads. **So a pin-moving cycle needs a second `render-site.mjs` after the commit.** Both corrections live in the AGENTS.md `resnap` entry.

The shape worth keeping: I inferred both mechanisms from observed behaviour instead of reading the source, and both turned out to be different files than I named. **A step that audits instruments owes the same evidence bar it demands — name the file and line.**

### W-7 was DECIDED and closed: read one instrument against its own claim, every session

Not a script. A detector that audits detectors is itself an instrument owing the same question, and none of the instances so far could have been caught by an assertion. Pick one script, alarm, counter or `readCommand`; run it; ask whether its output could ever have said otherwise; name it in the day's report; rotate.

**First day, three catches** — the two lying probes above, and `A-2.nextCheckDateNote` calling 2026-08-24 "the next Sunday" when it is a Monday, sitting in the field whose only job is dating the cadence.

### A-28 — the arrival move has been waiting eleven days and its figures went stale

Found by the P4 waits gate. The Mathstodon announcement (drafted 2026-08-13, adversarially reviewed the same day, nine angles, SURVIVES with six amendments) **has no card on David's board**. Board read live: 95 cards across 18 projects, bounds-ledger among none of them, with a positive control (8 cards match "skylark"). W-6's note says it was carded; which diverged cannot be established from here.

**The gate says "card it now" and that is the wrong action, because the artifact decayed while it waited.** Three stale figures: "231 claims" (now 233), "~230 pinned claims" (222 generated + 11 hand), "eight upstream changes" (eleven cycles). A ledger whose product is noticing stale cited numbers must not announce itself with three stale numbers.

Dated **2026-08-31**, two outcomes only: refresh-and-card, or strike. A third re-date without refreshing is explicitly unavailable — the decay is asymmetric, so re-dating alone makes it worse rather than later.

**Why it went unseen:** it lived as a note field on W-6, a watch about a different question. That is the exact shape W-7 was opened to catch and that shipped this morning — first independent confirmation, same day.

### State at close

**HEAD `bcb9b52` at the time this was written**, in sync with origin, CI GREEN at that commit; the report and this primer land after it. `npm run verify` exit 0. `continuity-check` exit 0. Report linters **8 of 8 clean**. Dead-reference check **0 across 66 paths in 5 docs**.

Mirror **113 files at upstream `5c4aeee`** (an upstream sha; it does not resolve in this repo — `git cat-file -t` returns *not a valid object*). **233 claims: 231 hold, 2 UNVERIFIED** (`C-7`/`C-9`, the `manual: true` erdosproblems pins, correct by design).

**Items: 40 total, 18 open.** W-7 closed as SHIPPED; A-28 opened.

Zero open issues, zero open PRs, zero Dependabot alerts. No Sentry project — nothing deploys, which is **structural here, not a sweep anyone performed**; say it that way when the fleet gate asks. `check-instrument-liveness` exits 2 with "0 feeds examined": that is swept-nothing, not clean.

**Traffic: 3 unique viewers against 180 unique cloners** in the trailing 14 days; 5 unique viewer-days since the flip, an upper bound. The clones are largely our own CI, which checks out daily and on every push and PR — six pushes today.

### The honest label on today's catches

**Instrument-facing, and both of the week's counted catches were byte-only rather than numeric.** No defect found in the mathematics. Last record-facing catch remains **2026-08-14**.

**The standing prediction carries forward unchanged and is still uncontradicted:** the next record-facing catch will be a witness-value mismatch on a constant upstream added within about a month, found by a human recomputing a cited certificate and flagged by no instrument we run. If one arrives through an alarm instead, the prediction was wrong — say so.

### Nothing is owed to David

Board read fresh at close, with a positive control. No waiting, answered, or pending-verification card. No David-blocker. A-28 is dated, not carded, and deliberately so.
