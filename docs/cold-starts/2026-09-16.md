<!-- GENERATED — regenerating REPLACES everything outside the hand: markers, so put prose inside them. Provenance stamp at the foot of this block. -->

<!-- hand:begin banner -->
> **PULL, THEN START THE GATE, BEFORE READING THIS** — three minutes, all network:
> ```
> git pull --ff-only
> npm run verify > tmp/verify-out.txt 2>&1
> ```
> Expect **exit 0**. Any non-zero code is yours; read `tmp/.verify-receipt.json`'s `failedGates`,
> never infer the leg.
>
> **TODAY'S DELIVERY IS `A-48` AND IT IS PROMISED TO DAVID FOR TODAY, 2026-09-16.** Do the
> criterion that can DISQUALIFY surface 1 before any comparison prose: does a correction path exist
> that is NOT a cold email? The landing page carries only journal navigation and **the survey PDF
> body has never been read** — reach it from `https://www.combinatorics.org/ojs/index.php/eljc/article/view/DS1`
> and write the finding into `continuity/items.json` → row `A-48` → field `surface1Scored`. A
> deliverable that arrives polished with its disqualifier unchecked is worse than one that arrives late.
>
> **THE FOOTPRINT FIGURES ARE OVER 24 HOURS OLD BY THE TIME YOU READ THIS — re-read both, do not
> quote these.** As of 2026-09-15 19:29Z: surface 1 (`10.37236/21`) crossref 36 · semantic-scholar 612 ·
> openalex 432; surface 2 (`10.37236/27`) crossref 156 · 143 · 188. Release:
> `node scripts/citation-footprint.mjs 10.37236/21` and `… 10.37236/27`. **The finding that matters
> survived a same-day re-read: two indices rank surface 1 higher and crossref alone inverts it**, so a
> crossref-only comparison would rank the surfaces backwards — which `A-48`'s own `onTrigger` forbids.
>
> **`A-43` CARRIES TWO CORRECTIONS AT THE TOP OF ITS `onTrigger`, WRITTEN SIX HOURS APART, POINTING
> OPPOSITE WAYS. READ THE SECOND ONE FIRST — it is deliberately above the first.** Yesterday this lane
> built a one-cell upstream correction and then refuted it: Haugland's own abstract states the bound as
> "0.380926...", so upstream's cell is his abstract figure with the ellipsis dropped. The row is due
> today and the decision is binary — either a draft can explain why this nuance is worth upstream
> contact when `docs/findings/2026-07-23-haugland-final-digit.md` declined the identical one on
> Wikipedia as "a cosmetic nuance — low value, skipped", or the row CLOSES on that precedent. Do not
> re-open the research leg; `closeWhen` forbids it.
>
> **`W-11` IS NEW AND IT IS ABOUT YESTERDAY'S MISS.** Both halves of that refutation were already in
> this repo and neither reached the analysis. See for yourself — this is the row's own read command and
> it takes seconds:
> ```
> grep -rn "0.380926" ledger/claims.json docs/findings/
> ```
> It returns the hand claims that pin Haugland's abstract figure and the July finding that declined the
> same nuance. Before you assert any claim is decision-ready, run that search against the claim's own
> terms and say in the post what it returned, including nothing.
>
> **DO NOT "FIX" THE H2016 ROW ON `c/1b.html` TO SOUND.** It is recorded UNRESOLVED on purpose: the
> source settles the VALUE (0.3809268534330870 in its final section) and cannot settle the CONVENTION,
> because its own abstract uses the truncated form. Neither DEFECTIVE nor SOUND is available. The entry
> is `A-47-0019` in `continuity/depth-audit.json`; its `sourceRead` renders verbatim on the public page.
>
> **NOTHING WAS WAITING ON DAVID at 2026-09-15 19:20Z** — release:
> `node scripts/sky.mjs answered-cards.mjs --project bounds-ledger --full`. **CI was GREEN at `2af6082`
> at 19:32Z** — release: `node scripts/sky.mjs check-ci-status.mjs --workflow reverify.yml`. Both are
> as-of readings: re-run them, do not quote them.
<!-- hand:end banner -->

First command (also below, in place):

```bash
git pull --ff-only
# commits after this primer was written are invisible to it — read them before acting:
git log --oneline 2af6082ae706b4821593386ebf6283aad41fd81b..HEAD
```

### Primer — bounds-ledger, 2026-09-16

What ONLY this primer holds comes first; the FULL runnable **First action** block
sits at the foot, below the reads. The single command above is the same block's
first line, repeated so it can be running while you read — it replaces nothing.

_**Composed against HEAD commit `2af6082ae706b4821593386ebf6283aad41fd81b` at 2026-09-15T19:39:11.108Z.** Nothing that landed after that instant is in this file. David's board answers especially: an answer he gives later cannot be here, and this primer will go on reading as though the work were not owed — that is exactly how two lanes concluded an OTA was not owed on 2026-09-04. Read them live, never off this page: `node scripts/sky.mjs answered-cards.mjs --project bounds-ledger --full`._

_Shared skylark-site scripts below are spelled `node scripts/sky.mjs …` — this lane vendors `scripts/sky.mjs` (S-20260906-75), so a shared script is named LANE-RELATIVE and resolves from a worktree too._

#### Owed gates — 3 due on/before 2026-09-16

_Ledger read at 2026-09-15T19:39:11.108Z by `cc-due-gates.collectDue` — the same resolver `check-due-gates-dispositioned` uses, so any difference is TIMING, not two answers. Rows filed or re-dated after that are NOT READ at generation — the close-out commit lands after this, so the COUNT above is stale by construction and this list cannot claim completeness. The list below, not the count, is the artifact; re-derive both before acting: `node scripts/sky.mjs check-due-gates-dispositioned.mjs --print --today=2026-09-16`._

Unfiltered as of the read time above — nothing was curated out of what the read could
see; a row absent here may simply have been filed after it ran.

- `A-48` (expectedSignalBy=2026-09-15, open) — The second-area citation comparison David asked for, promised to him for 2026-09-16
  - read: `node scripts/citation-footprint.mjs 10.37236/21 — surface 1 ("Small Ramsey Numbers", EJC dynamic survey). Reads THREE independent indices (Crossref is-referenced-by-count, Semantic Scholar citationCount, OpenAlex cited_by_count) and prints figures with NO verdict. A non-200, or a 200 carrying no count field, prints BLOCKED and exits 2 — a fact about the INDEX, never a low or zero footprint for the surface. Exit 0 means at least the two independent indices this row requires answered. Indices disagree by an order of magnitude on the same work, so surfaces are compared by ORDER ACROSS INDICES, never by one index’s magnitude (note3).` · last run: 2026-09-14T19:23:34.768Z
- `A-43` (expectedSignalBy=2026-09-16, open) — Upstream moved Haugland's minimum-overlap bound to its truncation — is that one wrong cell
  - read: `curl -s https://raw.githubusercontent.com/teorth/optimizationproblems/main/constants/1b.md — prints the live file. The H2016 row currently reads 0.380926; if it reads 0.380927 again upstream reverted and this row closes as moot. POSITIVE CONTROL that the fetch is the right document and not an error page: the response must also contain the string 'Erdos minimum overlap constant' heading text and the neighbouring value 0.380924, so an empty or redirected body cannot be misread as 'the row is gone'.` · last run: 2026-09-12T19:05:20.396Z
- `A-47` (nextCheckDate=2026-09-16, open) — THE DEPTH AUDIT, David-ruled 2026-09-09: read our own cited records against their sources 
  - read: `node scripts/depth-audit.mjs — prints the verdict counts with the sampling rule and the denominator beside them, refuses a bare zero (exit 2) and a count that does not sum to the rows (exit 3). node scripts/depth-audit.mjs --corpus answers the population half. Literal outputs, the red-arm record and the retired placeholder are on this row in readCommandHistory2026_09_13.` · last run: NEVER

#### What shipped since 24 hours ago

_Read at 2026-09-15T19:39:11.108Z; commits landing after that — the close-out especially — are not here. Re-derive: `git log --oneline --since='24 hours ago'`._

- commit `2af6082` — W-11: the record held the refutation and the analysis did not read it
- commit `9c23621` — The row this ledger opened on is on the page, and the case against upstream is weaker than we wrote
- commit `6461898` — A-43: the row stops ordering work that was finished on 09-05
- commit `aea4f69` — finding: A-43 asserted two incompatible states about the same work
- commit `3c07434` — chore: gitignore the continuity disposition-events shadow log
- commit `b0b5bb8` — docs(close-session): close-out 2026-09-14 — primer narrative and today's commits on their rows
- commit `f805190` — Hygiene pass 2026-09-14 — today's report, tomorrow's primer, and a banner count nobody could check
- commit `c9791f9` — W-6 traffic sample 2026-09-14: 1 unique viewer in the trailing 14 days
- commit `433282c` — P3: the index says which rows we actually opened the paper for

**Yesterday's report:** `docs/daily/2026-09-15-prelaunch.md` (coverage 2026-09-15, the prior day). No findings fence declared in it.

**Today's report (owed):** write `docs/daily/2026-09-16-prelaunch.md` — the artifact of record for service day 2026-09-16. Check it exists at close: `node scripts/sky.mjs check-daily-report-exists.mjs --date 2026-09-16` (it sweeps the fleet and applies each lane's coverageOffset; it takes no --project).

#### State — as of 2026-09-15T19:39:11.108Z (re-run the release command; never trust the value)

- **ledger**: 62 item(s): 44 closed · 18 open · as of 2026-09-15T19:39:11.108Z — release: `node scripts/sky.mjs gen-primer-first-action.mjs --repo . --target-date 2026-09-16 --json`

#### Id glossary — 19 id(s) this primer's day already has words for

_Each gloss is the ledger row's OWN title, not a re-derivation — paste it. Rows = every open ledger row plus every id named above. `(no ledger row)` means the id is named here and the ledger carries nothing under it: that is a finding, not a formatting gap._

- `A-2` — STANDING: drift-resolution log for the adopted surfaces…
- `A-7` — engineering-health P1s — four of five shipped 2026-08-09;…
- `A-9` — engineering-health P2 backlog (fix-on-touch, not scheduled…
- `A-33` — A change to a file we have filed an upstream report…
- `A-43` — Upstream moved Haugland's minimum-overlap bound to its…
- `A-45` — TIER-1 LEADING INDICATOR (orchestrator-approved…
- `A-46` — A close-out post announcing a FIX is textually identical…
- `A-47` — THE DEPTH AUDIT, David-ruled 2026-09-09: read our own…
- `A-48` — The second-area citation comparison David asked for,…
- `G-2` — GOAL: contribute a verified bound improvement to the…
- `G-4` — GOAL (Tier-0 from G-3 close): an outside party acts on a…
- `S-20260906-75` — (no ledger row)
- `W-3` — Watch for acknowledgement of the erdosproblems.com/36…
- `W-4` — KP-78 applies to every new detector: demonstrate BOTH…
- `W-6` — Read window for the README report-an-error channel — the…
- `W-8` — Does a materially amended outward artifact go back to…
- `W-9` — Does this lane's detection method find what the lane…
- `W-10` — reverify.test.mjs alarm-title leg failed once and would…
- `W-11` — Before asserting a claim is decision-ready, search this…

### First action — bounds-ledger, 2026-09-16

Every line in the block below is a COMMAND, not a claim. A command re-reads at the
moment you run it and cannot go stale; a state line written last night can.

```bash
git pull --ff-only
node scripts/sky.mjs update-david-board.mjs --list --json --full-ids --project bounds-ledger
git rev-parse HEAD origin/main
git rev-list --count origin/main...HEAD
npm run verify > tmp/verify-out.txt 2>&1
node scripts/sky.mjs check-ci-status.mjs --workflow reverify.yml
```

Morning release commands — deliberately NOT READ at generation (a value written
last night would already be stale); run each, never trust a remembered one:
- `git -C . log -1 --format=%h%x20%s` — HEAD
- `git -C . status --porcelain` — working tree
- `node scripts/sky.mjs check-ci-status.mjs --workflow reverify.yml` — CI
- `node scripts/sky.mjs check-deployed-sha-drift.mjs` — deploy
  - WHERE IT LOOKED: no read-only deploy-state script in this lane's package.json (looked for ops:deploy-state, deploy-state, check-deploy, render-deploys, deploy:status, daily-snapshot). The fleet's check-deploy-state.mjs is not a substitute — it requires --fix <sha> --deploy <sha> and exits 1 without them — so this is the FLEET read, which takes no required flags. service: NOT DECLARED — no row for "bounds-ledger" in skylark-site/src/data/cc-deploy-gates.json (generatedAt 2026-09-11T05:03:44.019Z, 17 repo row(s)) — nothing in this repo declares that lane's Render service. No `--service` flag is emitted: the lane SLUG is not the service name (antebellum-twitter's is disunion-feed), and a guessed flag exits 1.

<!-- hand:begin narrative -->
## Session close-out — 2026-09-15

**What yesterday was.** A day that built an upstream correction and then killed it with its own
reading. The account is in `docs/daily/2026-09-15-prelaunch.md`; the one-line version is that this
lane refuted its own case, and the evidence was already in the repository when the case was written.

### Yesterday's load-bearing commits
- `6461898` — `A-43`'s three corrected field texts applied, closing a mid-phase park. Each text was
  extracted from the committed finding by script and asserted byte-exact after the write.
- `9c23621` — the H2016 row is on `c/1b.html` as read against its cited source, verdict UNRESOLVED,
  plus the second correction on `A-43`. Verified on the live published page: HTTP 200, 8,426 bytes
  against 7,631 before, `H2016` present, the block's count 4 → 5 suspicion-drawn rows.
- `2af6082` — `W-11` filed.

### What NOT to touch
- **The `sourceRead` text on `A-47-0019`.** It renders verbatim on the public page and it is the one
  sentence there explaining how a row can be read against its source and be neither supported nor
  refuted. It was written to survive a review that asked whether it claims more than its two quoted
  source sentences support.
- **The `usableAudit` / `identityVerified` / `isStale` chain in `scripts/render-constant-pages.mjs`**,
  and the single shared `isBoundRead` — unchanged from 09-14's close and still the reason the index and
  the constant pages cannot disagree about what counts as read.

### Two environment events worth knowing, neither a lane defect
- **A classifier outage** refused one Bash call mid-P4 (`claude-sonnet-5[1m] … temporarily
  unavailable`); four commands in the same batch ran normally and the refused one re-ran clean.
  Reported before retrying as bus `7925bd52`.
- **The harness killed all three waker rungs at once for low system memory** (~19:23Z). That is not a
  rung exiting on purpose. Evidence the ladder was genuinely empty: all three relaunched printed a
  fresh `armed` line, where a rung whose predecessor is alive refuses with `already live (pid N)`.
  Reported as bus `df0ffbe1`. If a notification says a rung was *stopped*, relaunch all three and check
  for `armed` rather than a refusal.

### Tomorrow's first action — names the FILE it acts on
Pull and gate (banner, top). Then read the front matter of the *Small Ramsey Numbers* survey PDF for a
correction path that is not a cold email, and write the finding into **`continuity/items.json` → row
`A-48` → field `surface1Scored`**, criterion (4). Then `node scripts/citation-footprint.mjs` on both
DOIs, then the comparison to David. `A-43`'s decision — draft or close on the precedent — is the second
item, not the first.

### Gate 2
`gate 2: NOT APPLICABLE — this lane has no doc-only CI fast path.` Every push to `main` runs the full
`reverify.yml`; there is no path filter and no fast-path workflow to prove a green on.
<!-- hand:end narrative -->

<!-- generated by gen-primer-first-action.mjs (skylark-site) @ commit accf3f567 · lane bounds-ledger · for 2026-09-16 · at 2026-09-15T19:39:11.108Z -->
<!-- writtenAtSha: `2af6082ae706b4821593386ebf6283aad41fd81b` — the lane's HEAD commit when this primer was written. Provenance anchor for check-primer-freshness leg ii; NOT a claim about HEAD now (the close-out commit that carries this file lands after generation). -->
<!-- reading this block: a generated line that is wrong in a way you can see FIXED in skylark-site's git log after the commit above is STALE, not a finding — regenerate before filing it. `git -C C:/dev/skylark/skylark-site log --oneline accf3f567^{commit}..HEAD -- scripts/gen-primer-first-action.mjs src/lib/cc-primer-commands.mjs src/lib/cc-due-gates.mjs src/lib/cc-report-artifact.mjs src/lib/cc-hand-edit-merge.mjs src/lib/cc-cold-readability.mjs src/lib/cc-skeleton-gloss.mjs` -->
