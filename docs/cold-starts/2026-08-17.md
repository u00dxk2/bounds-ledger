# Cold-start primer for 2026-08-17

Multi-session primer — each /close-session run appends a new section below.

## Session close-out — 19:10Z

Paced rail, day 15 closed. Eight commits, all pushed, CI green at HEAD.

### Today's load-bearing ships

- **`1eead7b`** — README's catch section: the count was **stale at eight; it is nine** (`433091b`, upstream's Martinet constant on 8/14, was never recorded), and the prose now hands the reader the command that derives it instead of a number. Added a three-step self-verification block so a visitor can watch the alarm fire *and* go quiet in about a minute.
- **`17e1392` + `c7a081f`** — adopted the fleet's CI-truth instruments and then corrected their stated rationale for this repo. **The `--workflow` flag is NOT what protects this lane** (we have one workflow, so it disambiguates nothing); the guard is the unconditional event filter at `check-ci-status.mjs:80`. I pushed the opposite claim first and had to fix it.
- **`58345b8`** — `render-state-block --check` compared bytes, so a *correct* tree read STALE whenever git re-materialised `README.md` as CRLF. Switching branches was enough to trip it. Compares content now.

### Tomorrow's first-action

The steward cadence, plus the new visitor-path step (see "what changed in the ritual"):

```bash
cd C:/dev/skylark/bounds-ledger && npm run check && node scripts/reverify.mjs --check && node scripts/check-claims.mjs
```

The first command is the full cadence; the second pair is the **credential-free** path a stranger actually runs — it is now part of the cadence, because every indicator we keep was green this morning while the front door was broken for every visitor who is not us.

### Active in-flight work — one branch, deliberately unmerged

**`guard-catch-count`** (`dd336d8`, pushed, NOT merged). Extends `render-state-block.mjs` so `--check` derives the drift-cycle count from mirror history and asserts it against the README's own sentence — the thing that would have caught today's stale count on 8/14 instead of two days later.

It went through the cross-family review gate, came back **FINDINGS (2)**, both fixed:
- shallow history (`actions/checkout` is depth-1) made it read 1 cycle against a README saying 9 — a red on a *correct* checkout. Now reports UNKNOWN and passes.
- the regex scanned the whole README, so a deleted sentence could pass on unrelated prose. Now anchored to the catch section.

**Do not merge it just because the findings are fixed.** It has been wrong twice in the trust-eroding polarity, and all three of its defects were found by *executing in an environment*, never by reasoning about one. The one environment it has never run in is a Linux CI runner. Run it there first; merging now means learning from a red `main`. Nothing depends on it landing — `main` is green and the README already shipped.

### CI

**CI:** reverify: completed/success @ `58345b8` — verified with `check-ci-status.mjs --workflow reverify.yml`, exit 0, on a completed **non-scheduled** run matching HEAD. Not a branch-page read.

### Pending decision-blocked items — both are David's, neither is to be raised

- **A-16** (upstream's Martinet-constant row cites a witness whose arithmetic yields the *previous* record). Gate 1 cleared 8/15; gate 2 is David's approval. **Do not raise it unprompted, do not PR, do not contact upstream.** Its precondition still holds — the mirror is in sync at `e70b4a4` (an **upstream** `teorth/optimizationproblems` sha; it does not resolve in this repo, `git cat-file -t` will say so), the upstream commit that added the page. Deliberately kept OUT of the README's public catch table: that table is our scoreboard, and routing gate-pending criticism into it is the same content through a louder channel.
- **A-15** (Mathstodon). Dormant *by instruction*; card dismissed. Do not ask, do not card, do not send.

### What NOT to touch tomorrow

- **The cross-model adversarial review on everything public-touching.** It returned DOES NOT SURVIVE on 8 defects today for a diff I believed finished and had gate-checked twice. It works because it runs against the *working tree* (defects die as edits, not commits) and because it is a *different model* — it questioned the environment my testing assumed instead of re-running commands I had already passed. Do not soften it to "the risky ones"; I thought this one was a doc edit.
- **The two `manual: true` claims staying UNVERIFIED.** They never touch counts or the exit code. That is the design, not a gap.
- **`guard-catch-count`** — see above.

### Watch items maturing tomorrow

None with a 24h trigger. **W-3** (the erdosproblems.com correction emailed 24 July) is 23 days unanswered; the un-stick lives in someone else's inbox, so it is not ours to resolve — but both pins on that page were checked today and read unchanged (`0.380876`, "last edited 23 January 2026"). That is evidence about the page and none about the reply.

### Tier 0 / binding-constraint status

**G-1 — externally-acknowledged corrections: 1**, threshold met since the 8/11 merge of `teorth/optimizationproblems#141`. Open now on the 30-day green-streak leg alone. Posture: hold. The catches indicator's current partial week shows 4 distinct pins moved; 0 completed dry weeks, so the adopt-a-second-surface rule is nowhere near firing.

### Evangelism read

- **The core problem, in the user's words:** "I cited a number and I have no idea if it's still the best one." Widely-cited bounds go stale silently and nothing tells you.
- **The would-tell-a-friend moment:** a maintainer accepts a correction we found. That is not a proxy here — the maintainers *are* the users, and accepting a correction IS the tell-a-friend act. Distance from it: we have done it once (8/11). A second is not scheduled, and A-16 is the nearest candidate but it is gated on David.
- **Retention / word-of-mouth trend:** the instrument exists and is accumulating — `npm run traffic` sampled today, 21 days stored, **3 unique viewer-days since the 8/08 public flip** against a threshold of 90 days AND 100 unique visitors. That 3 is an upper bound (viewer-days do not dedupe people) and the 126 cloner-days are unattributed. Nobody has been told the repo exists, so this is arithmetic, not a verdict. Do not read a signal into it yet.
- **Advocacy signal this session:** none. No issues, no external contact, no reply on W-3.

### What changed in the ritual today

Two rules landed in `CLAUDE.md` (`9d719eb`), both endorsed:
1. **An artifact written FOR an audience is verified from that audience's environment, before review.** Today's worst defect was a "verify this yourself" block tested in a shell holding `CC_PROMPTS_PIN` — so `npm run check` printed green for me and exits 3 for every actual reader. Every command passed when I ran it; the defect was the environment. Strip it first (`env -u <VAR>`, fresh clone).
2. **The visitor path is part of the steward cadence** — see tomorrow's first-action.

Also corrected: **A-7's `blockedOn`** claimed this repo is private and that push protection needs a paid add-on plus a David spend decision. `gh api` says `private=false, secret_scanning=enabled, push_protection=enabled`, at zero spend. A live-looking blocker whose premise dissolved on 8/08, sitting in the ledger whose job is catching stale records (`a896dfb`). A-7 stays open on R7 alone — and note both settings are **state reads nobody has shown to fire**, so they are not citable as working controls.
