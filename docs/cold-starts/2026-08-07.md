# Cold-start primer for 2026-08-07 (MT)

Multi-session primer — later sections supersede earlier ones explicitly.

## Session close-out — 22:55Z (2026-08-06, ~16:55 MT)

You are the bounds-ledger agent. Read this, then `AGENTS.md` + `CLAUDE.md`, then `continuity/items.json`.
Steward of drifting mathematical records; pre-launch, private repo, **PACED rail**, **no self-rating**.
**As of today the lane has a second thesis: G-2, steward → steward + contributor. David ruled it in-pane.**

### Tomorrow's first-action

**Steward cadence first, as always — but read it through the outage lens.** GitHub Actions went into a
**major outage 15:22 UTC today** ("Incident with Actions") and was still draining at close. Consequences
you must not misread:

- The re-queued run for `cbed1a6` (id 31118370959) was still `queued` at close. It may have completed
  overnight — check it.
- **The `421c9c3` push created NO run at all** (Actions wasn't accepting work when it landed). A missing
  run is the outage's artifact, not a repo defect and not drift.
- Tonight's scheduled drift run may be missing, late, or red for outage reasons. **A run that never got a
  runner says nothing about drift** — the annotation "job was not acquired by Runner" is the tell.

**Verify recipe once Actions is healthy:** `gh run list --limit 5` → you want a green run at HEAD
`421c9c3` (it exercises all **7** self-tests, including the two new ones). If no run exists at HEAD, run
`gh workflow run reverify` and read the **LOG** (`gh run view <id> --log`): expect `No drift. 111 files
match upstream dee1660…`, `229 claim(s): 227 hold, 0 broken/unreachable, 2 unverified (manual)`, and the
7 selftest PASS lines. Green → **streak day 15**. Do NOT dock G-1's streak for outage-shaped failures;
the 8/06 local run with the pin was green on every leg. Then `npm run check` locally **with
`CC_PROMPTS_PIN` set**.

**Then the day's real work: the G-2 campaign, round one.** The 27b verifier stands and has reproduced
the known record — the next increment is **proposal rounds**: propose construction families for a
**10-chromatic biplanar graph** (beats Sulanke 1980's 9), realize candidates as explicit graphs, and let
`search/27b/verifier.mjs` be the only judge. David ruled **Fable drives proposals**, telling us not to economise on model selection — your session model IS Fable now (he switched defaults 8/06), so propose
in-session on subscription. Log every family tried and why it died — dead candidates are the campaign's
data. Budget: campaign hard-capped per G-2 note1; API-billed overnight volume needs an API-key decision
from David first (not asked, not needed yet).

### What shipped today (all pushed; 4 commits `9c47167` → `421c9c3`)

1. **Flip blockers 1–4 cleared** (`9c47167`): mirror LICENSE (Apache-2.0 verbatim) + root NOTICE;
   `continuity/health.json` untracked+gitignored; security-posture doc redacted to "held privately";
   portfolio PII generalised. **Finding: blockers 5+7 had a working-tree leg the review misfiled as
   history-only** — items.json and the 8/06 primer spelled out three third-party forum handles and a
   personal email verbatim; both redacted. History legs untouched, still David's.
2. **G-2 ruled + gap table + targets** (`cbed1a6`): `scripts/gap-table.mjs` (111 constants: 65 numeric
   gaps, 6 parse-suspect, 40 symbolic → `docs/gap-table.json`). Targets read-then-picked: **27b primary**,
   69a secondary, 39a stretch, 73a rejected (its lower bound is the conjectured optimum).
3. **27b verifier + known-record reproduction** (`421c9c3`): exact DMP planarity + exact chromatic
   number + certificate check, stdlib only; selftest (17 cases, both answers) wired into npm test + CI;
   Sulanke's graph split 27/23 (PRNG seed 1923, restart 924), full certificate verified, committed as
   `search/27b/sulanke-certificate.json`.
4. Daily report `docs/daily/2026-08-06-prelaunch.md` (6/6 linters PASS).

### Numbers that changed meaning today (old values are DEAD)

- **Self-tests: 7, not 5 and not 6.** Two were added in one day (gap-table, 27b verifier). The
  reverify.test guard now counts 7 in CI. Any doc saying 5 or 6 is stale.
- **Open continuity items: 11** (G-2 added; was 10).
- **Flip blockers: 4 of 8 cleared + the working-tree halves of 5 and 7.** The "eight open blockers"
  framing is dead; what remains is three David decisions (below) + history legs.
- `npm test` now runs **7** chained commands; `search/27b/selftest.mjs` is the seventh and lives outside
  `scripts/` — the first non-`scripts/` self-test.

### Decision-blocked (David's, not yours — surface, don't nag)

- **Own-work licence**: public repo with no root LICENSE = all-rights-reserved by default; his choice to
  make, flagged in the 8/06 daily + bus. (The mirror's Apache-2.0 is handled and is NOT this question.)
- **His quoted private words in decision docs** (blocker 8's real half) — F-1 class.
- **History legs of blockers 5, 6, 7** (author email, session URLs, forum handles in `228bd46`).
- **The flip stays ON HOLD** until he clears the above.
- **#141** — OPEN, 0 comments/reviews at ~26h. Check, don't poll. **W-3** — wait (his standing word).

### Don't-touch markers (unchanged + new)

- **Do NOT pre-stage a second citation-key PR** while #141 has no outcome (10a/22b/46a/50a/52a wait).
- **Do NOT weaken or "tune" the 27b verifier to make a candidate pass** — the verifier is the product's
  authority; a candidate that needs a softer judge is not a candidate.
- **Do NOT claim any G-2 mathematical result without: exact re-verify + literature/priority check +
  adversarial review + David's gate.** A verified harness is not a verified discovery.
- Do not re-automate C-7/C-9 (IP-dependent 403). Do not put `CC_PROMPTS_PIN` in repo secrets. Do not
  re-propose the /36 public comment. Do not edit the mirror by hand — including its new `LICENSE` file
  (refresh-safe: `--snapshot` only rm's `constants/`).

### CI / state at close

Tree clean at `421c9c3`, everything pushed. CI green through `5fd3689`; `cbed1a6` re-run queued behind
the outage; `421c9c3` unrun (see first-action recipe). Ledger: 111 files at `dee1660`, 229 claims / 227
hold / 2 UNVERIFIED by design. **G-1 streak day 14/30** (8/06 green read from the log + local pin run).
**North star: 0** — a sent PR and a standing harness are not acknowledgements. Open items: 11.

### Evangelism read (required carry-forward)

- The would-tell-a-friend moment is unchanged (a stranger sees the alarm catch a record move) — **plus a
  new candidate moment**: a stranger watches a machine-found graph beat a 45-year-old record with a
  certificate they can run themselves. Distance: repo private, reach still zero; G-2 could produce the
  first artifact strangers would come TO.
- Retention/WoM instrument: none, correctly — no audience until the flip.
