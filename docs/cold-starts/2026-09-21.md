<!-- GENERATED — regenerating REPLACES everything outside the hand: markers, so put prose inside them. Provenance stamp at the foot of this block. -->

<!-- hand:begin banner -->
2026-09-20 close. **Three gates are due today and one of them is the day's work; the other two are reads.** `A-47` (the depth audit David ruled on) is the governing action: take the next slice's readings on the constants mirror, at the positions drawn before any was opened. `A-2` (the standing drift-resolution log) and `A-51` (the dead-link correction, whose read has NEVER run) are reads to disposition, not builds.

**The governing first command, RUN at 18:19Z on 2026-09-20 so you start from a known reading rather than a promise:** `node scripts/depth-audit.mjs` printed `depth audit (A-47): 24 row(s) audited — 13 sound, 0 defective, 5 unresolved, 6 unreachable` and `RESULT: PASS … (exit 0)`. **That count is UNCHANGED from 2026-09-16, and `A-47`'s own note says so explicitly: "if the audited count has not moved since today's 24, that is the finding and it gets reported as one."** No slice landed on 09-17 through 09-20. So tomorrow's read is not a fresh discovery — it is the fourth day of a flat count, and moving it is the work.

> **`A-54`'s phase 2 slipped yesterday and the slip is already said — do NOT say it again, and do NOT re-date it on the 21st.** `expectedSignalBy` now reads **2026-09-22** for the Wikipedia comparator, and `phase2Slip2026_09_20` on that row carries the firing, the reasoning and what it does not blame. Its own words on a second slip, which apply to you today: *"a second slip here is the A-34 shape and must be said before 2026-09-22, not on it."* So if the comparator will not land tomorrow, **today is the day to say so** — not the 22nd. Clearing condition: delete this block once phase 2's last two deliverables land or a second slip is recorded on the row.

> **David ruled on 2026-09-20 at 16:59Z, and the operative half is a HOLD.** His words, verbatim: *"Rebuild the table from the underlying papers as our own arrangement, say so on the page, and keep the credit to the survey. Until that is done, leave the page as written."* So `ramsey.html` and `copying.html` are **not to be edited** — not softened, not partially migrated, not "improved" — until the rebuild lands. A half-migrated page is neither the disclosure he read nor the arrangement he asked for. Full reading and its five challengeable points: `A-54.davidRuling2026_09_20`.

> **ANSWERED 2026-09-20T17:44:52Z — card `5910378d` is closed and the rebuild is UNBLOCKED.** His words, verbatim: *"Yes — ship the partial table. Independently source what you can, and mark anything you cannot as credited to the survey and not independently checked."* That is the card's drafted reply sent byte-for-byte, so there is no divergence between his wording and ours to reconcile. Full reading: `A-54.davidRuling2026_09_20_partialTable`.
>
> **Three things that ruling does NOT do, and each is a way to get it wrong tomorrow.** (1) It gives no date. (2) **It does not lift the page freeze** — "leave the page as written" stands until the rebuild LANDS, so `ramsey.html` and `copying.html` stay untouched; publishing a partly-rebuilt table early is the half-migrated state the freeze forbids. (3) It does not say what fraction counts as rebuilt, so it is not a licence to source two values and declare it done.
>
> **Tomorrow's step on this is SCOPING, not building, and it is deliberately undated.** Take the per-bound rate from the readings that already exist, decide whether the rebuild extends `A-47`'s slice method or becomes its own phase with its own store, and only then put a date on the row. A completion date guessed before anyone has measured how long one bound takes is what `signalDateNote` exists to stop, and this row already slipped once on 2026-09-20. Phase 2's 2026-09-22 clock is untouched by this and still owns the comparator and the catch-counter statement.

> **The primer's generated First-action block below still reads `HANDOFF INCOMPLETE — governing action not authored`, and that is NOT a bounds-ledger omission.** Measured 2026-09-20: `grep -rl "primer:handoff" C:/dev/skylark --include=daily-config.md` returns **nothing**, while the same grep for `primer:first-action` returns **22** lane configs — so the field exists, the grep can find it, and **no lane in the fleet has authored one**. It is a strict JSON contract validated by `validateHandoff` in skylark-site's `src/lib/cc-primer-first-action-blocks.mjs` (a committed aggregate-only reader, a narrow invocation grammar, and nine required fields), not the "one sentence a night" the 2026-09-20 P1 feedback described. **Until it is authored, read THIS banner as the governing action and treat the block below as release commands only.** The orchestrator is carrying whether that block earns its place at all.

> **Waker ladder — relaunch ranks 1, 2 and 3 at session start.** The commands are in `/listen` step 3b, one per rank, as separate background calls, never joined. A rung whose predecessor is alive refuses with `already live (pid N)` rather than arming a duplicate, so relaunching ranks 1, 2 and 3 is always safe. **Read each rung's OUTPUT file, not its launch acknowledgement** — on 2026-09-20 a rank-1 exit read `actionable loop-death signal (unconsumed=1) — exiting to wake the pane`, which is a real fire, while ranks 2 and 3 read `already live (pid N)`, which is not. A context compact does not re-run `/listen`.
<!-- hand:end banner -->

UNVERIFIED FIRST ACTION — matching execution evidence and current governing dependencies have not been established by these release commands.
Recovery read (PowerShell or bash, cwd: lane root): `git status --short` followed by `git log -5 --oneline`; reconcile the authored action in docs/daily-config.md with the current ruling and ledger before running it.

First command (also below, in place):

```bash
git pull --ff-only
# commits after this primer was written are invisible to it — read them before acting:
git log --oneline 30d5e02ecb78ffa50827661d3e1a38dc5fde4085..HEAD
```

### Primer — bounds-ledger, 2026-09-21

What ONLY this primer holds comes first; the FULL runnable **First action** block
sits at the foot, below the reads. The single command above is the same block's
first line, repeated for reference — it replaces nothing. Establish the governing action and its preconditions before running release commands.

_**Composed against HEAD commit `30d5e02ecb78ffa50827661d3e1a38dc5fde4085` at 2026-09-20T17:51:39.320Z.** Nothing that landed after that instant is in this file. David's board answers especially: an answer he gives later cannot be here, and this primer will go on reading as though the work were not owed — that is exactly how two lanes concluded an OTA was not owed on 2026-09-04. Read them live, never off this page: `node scripts/sky.mjs answered-cards.mjs --project bounds-ledger --full`._

_Shared skylark-site scripts below are spelled `node scripts/sky.mjs …` — this lane vendors `scripts/sky.mjs` (S-20260906-75), so a shared script is named LANE-RELATIVE and resolves from a worktree too._

#### Owed gates — 3 due on/before 2026-09-21

_Ledger read at 2026-09-20T17:51:39.320Z by `cc-due-gates.collectDue` — the same resolver `check-due-gates-dispositioned` uses, so any difference is TIMING, not two answers. Rows filed or re-dated after that are NOT READ at generation — the close-out commit lands after this, so the COUNT above is stale by construction and this list cannot claim completeness. The list below, not the count, is the artifact; re-derive both before acting: `node scripts/sky.mjs check-due-gates-dispositioned.mjs --print --today=2026-09-21`._

Unfiltered as of the read time above — nothing was curated out of what the read could
see; a row absent here may simply have been filed after it ran.

- `A-2` (nextCheckDate=2026-09-21, open) — STANDING: drift-resolution log for the adopted surfaces (re-verification CI shipped 2026-0
  - read: `node scripts/reverify.mjs --check # the drift alarm itself. Literal output 2026-08-25: "No drift. 113 files match upstream <upstream sha elided 2026-09-07 — reprint with `node scripts/reverify.mjs --check`, or read it from ledger/teorth-optimizationproblems/manifest.json>." (that 40-hex token is an UPSTREAM teorth/optimizationproblems commit; it does not resolve in this repo), exit 0. Any non-zero exit means a cycle is owed and this row is the log for it. Positive control that the command can say otherwise: reverify.test.mjs plants synthetic drift in 10a.md every run and asserts it is detected, so the detecting path is exercised on every npm test. INVARIANT: the words "No drift." ALONE. The FILE COUNT IS INFORMATIONAL, never part of it — it moves when our own resolution cycles mirror further files (113 on 2026-08-31, 116 on 2026-09-07). A count that MOVED is not a catch; a count that moved with no resolution cycle of ours behind it is worth one look, and `git log ledger/teorth-optimizationproblems/` settles which you are holding. The upstream sha in that literal also moves on its own and is NOT a catch. SECOND LEG: `node scripts/render-state-block.mjs --check` — leg one compares the mirror and is BLIND to whether the README catch table, the public rendering of THIS log, has fallen behind the cycles it lists; on 2026-09-05 everything else was in sync and it still failed. Literal 2026-09-05: "catch table: 16 cycles, and README.md says 16." exit 0. How this wording was arrived at, including four dated corrections to it: field readCommandProvenance.` · last run: 2026-09-14T19:22:58.826Z
- `A-47` (nextCheckDate=2026-09-21, open) — THE DEPTH AUDIT, David-ruled 2026-09-09: read our own cited records against their sources 
  - read: `node scripts/depth-audit.mjs — prints the verdict counts with the sampling rule and the denominator beside them, refuses a bare zero (exit 2) and a count that does not sum to the rows (exit 3). node scripts/depth-audit.mjs --corpus answers the population half. Literal outputs, the red-arm record and the retired placeholder are on this row in readCommandHistory2026_09_13.` · last run: 2026-09-16T17:00:51.949Z
- `A-51` (expectedSignalBy=2026-09-21, open) — Upstream's [YKLBMWKCZGS2026] reference in the minimum-overlap table cites a dead page — it
  - read: `curl -sIL https://test-time-training.github.io/discover.pdf — prints the response headers. The status line proves nothing here, because on 2026-09-05 the dead page answered HTTP 200. Read the Content-Type: text/html means still the Site-not-found page; application/pdf means restored and this row closes as moot.` · last run: NEVER

#### What shipped since 24 hours ago

_Read at 2026-09-20T17:51:39.320Z; commits landing after that — the close-out especially — are not here. Re-derive: `git log --oneline --since='24 hours ago'`._

- commit `30d5e02` — A-54 P4: the slip fired, and it is said today with a date rather than on the 21st
- commit `988ca44` — A-54: David rules the table gets rebuilt from the underlying papers, and the page stays as written until it is
- commit `334e49a` — A-54 phase 2: the disclosure ships as its own page, and the table shows numbers before prose
- commit `638834c` — A-54 P2: phase 2's three remaining deliverables get a field of their own, and the slip's condition is said today

**Yesterday's report:** `docs/daily/2026-09-20-prelaunch.md` (coverage 2026-09-20, the prior day). No findings fence declared in it.
Prior report: historical; re-read its findings and later changes before treating its opener as current.

**Today's report (owed):** write `docs/daily/2026-09-21-prelaunch.md` — OUTPUT obligation due at close, the artifact of record for service day 2026-09-21. Check it exists at close: `node scripts/sky.mjs check-daily-report-exists.mjs --date 2026-09-21` (it sweeps the fleet and applies each lane's coverageOffset; it takes no --project). <!-- owed-output path="docs/daily/2026-09-21-prelaunch.md" service-day="2026-09-21" due="close" -->

#### State — as of 2026-09-20T17:51:39.320Z (re-run the release command; never trust the value)

- **ledger**: 68 item(s): 48 closed · 20 open · as of 2026-09-20T17:51:39.320Z — release: `node scripts/sky.mjs gen-primer-first-action.mjs --repo . --target-date 2026-09-21 --json`

#### Id glossary — 21 id(s) this primer's day already has words for

_Each gloss is the ledger row's OWN title, not a re-derivation — paste it. Rows = every open ledger row plus every id named above. `(no ledger row)` means the id is named here and the ledger carries nothing under it: that is a finding, not a formatting gap._

- `A-2` — STANDING: drift-resolution log for the adopted surfaces…
- `A-9` — engineering-health P2 backlog (fix-on-touch, not scheduled…
- `A-45` — TIER-1 LEADING INDICATOR (orchestrator-approved…
- `A-46` — A close-out post announcing a FIX is textually identical…
- `A-47` — THE DEPTH AUDIT, David-ruled 2026-09-09: read our own…
- `A-49` — npm run close:primer passes when tomorrow's banner is…
- `A-50` — Importing one of our CLI scripts can run it — one selftest…
- `A-51` — Upstream's [YKLBMWKCZGS2026] reference in the…
- `A-52` — Write down what an outside party acting on a record could…
- `A-53` — The audit store and the public page can disagree about how…
- `A-54` — Adopt Small Ramsey Numbers (Electronic Journal of…
- `G-2` — GOAL: contribute a verified bound improvement to the…
- `G-4` — GOAL (Tier-0 from G-3 close): an outside party acts on a…
- `S-20260906-75` — (no ledger row)
- `W-3` — Watch for acknowledgement of the erdosproblems.com/36…
- `W-4` — KP-78 applies to every new detector: demonstrate BOTH…
- `W-6` — Read window for the README report-an-error channel — the…
- `W-8` — Does a materially amended outward artifact go back to…
- `W-9` — Does this lane's detection method find what the lane…
- `W-10` — reverify.test.mjs alarm-title leg failed once and would…
- `W-11` — Before asserting a claim is decision-ready, search this…

### First action — bounds-ledger, 2026-09-21

Release commands — subordinate to the governing action.
HANDOFF INCOMPLETE — governing action not authored.

UNVERIFIED FIRST ACTION — these commands have no matching execution evidence here. Command text alone establishes neither safety nor the promised evidence, shell compatibility, or tomorrow's priority.

```bash
git pull --ff-only
node scripts/sky.mjs update-david-board.mjs --list --json --full-ids --project bounds-ledger
git rev-parse HEAD origin/main
git rev-list --count origin/main...HEAD
npm run verify > tmp/verify-out.txt 2>&1
node scripts/sky.mjs check-ci-status.mjs --workflow reverify.yml
```

Morning release commands — deliberately NOT READ at generation (a value written
last night would already be stale); run each, never trust a remembered one:
- `git -C . log -1 --format=%h%x20%s` — HEAD
- `git -C . status --porcelain` — working tree
- `node scripts/sky.mjs check-ci-status.mjs --workflow reverify.yml` — CI
- `node scripts/sky.mjs check-deployed-sha-drift.mjs` — deploy
  - WHERE IT LOOKED: no read-only deploy-state script in this lane's package.json (looked for ops:deploy-state, deploy-state, check-deploy, render-deploys, deploy:status, daily-snapshot). The fleet's check-deploy-state.mjs is not a substitute — it requires --fix <sha> --deploy <sha> and exits 1 without them — so this is the FLEET read, which takes no required flags. service: NOT DECLARED — no row for "bounds-ledger" in skylark-site/src/data/cc-deploy-gates.json (generatedAt 2026-09-19T22:29:42.268Z, 17 repo row(s)) — nothing in this repo declares that lane's Render service. No `--service` flag is emitted: the lane SLUG is not the service name (antebellum-twitter's is disunion-feed), and a guessed flag exits 1.

<!-- hand:begin narrative -->
_(orchestrator narrative — this region survives regeneration)_
<!-- hand:end narrative -->

<!-- generated by gen-primer-first-action.mjs (skylark-site) @ commit 68c12f611 · lane bounds-ledger · for 2026-09-21 · at 2026-09-20T17:51:39.320Z -->
<!-- writtenAtSha: `30d5e02ecb78ffa50827661d3e1a38dc5fde4085` — the lane's HEAD commit when this primer was written. Provenance anchor for check-primer-freshness leg ii; NOT a claim about HEAD now (the close-out commit that carries this file lands after generation). -->
<!-- reading this block: a generated line that is wrong in a way you can see FIXED in skylark-site's git log after the commit above is STALE, not a finding — regenerate before filing it. `git -C C:/dev/skylark/skylark-site log --oneline 68c12f611^{commit}..HEAD -- scripts/gen-primer-first-action.mjs src/lib/cc-primer-commands.mjs src/lib/cc-due-gates.mjs src/lib/cc-report-artifact.mjs src/lib/cc-hand-edit-merge.mjs src/lib/cc-cold-readability.mjs src/lib/cc-skeleton-gloss.mjs` -->
