# Cold-start primer for 2026-08-16

Multi-session primer — each /close-session run appends a new section below.

## Session close-out — 19:55Z

### Today's load-bearing ships

- **`8ca908a`** — the Tier-1 indicator's dry-week counter could not count a drought. `catch-rate.mjs`
  built its week buckets from commits that touched `ledger/claims.json`, so a week where nothing
  happened was **absent**, not zero — and in a real drought *nothing* touches that file, so the
  trailing counter could never exceed 1 against a rule that fires at 4. Found by reading the
  script's own printed table (three rows for a four-week repo). Fixed, reviewed cross-family,
  merged. `docs/findings/2026-08-15-the-dry-week-counter-could-not-count-a-drought.md`.
- **`8b1c018`** — A-16 (the C_87 witness defect) cleared **gate 1**. Independent cross-model
  refute-it pass: verdict SURVIVES on every angle, all figures reproduced to the digit, and it beat
  our own work by sweeping 275 one-edit candidates against integer degrees 1–64.
  `docs/decisions/2026-08-15-A16-adversarial-review.md`.
- **`66f3dd4`** — A-15 parked at David's discretion; W-6 traffic sampled.

### CI

**CI:** reverify: completed/**success** @ `8e9a0c1` (HEAD). Green on every commit pushed today.

### Tomorrow's first action

Sunday — the **weekly forced-decision pass** is due (last two were 2026-08-03 and 2026-08-09).
Steward cadence first, then the pass over all open items.

```bash
cd C:/dev/skylark/bounds-ledger && npm run check
```

Then read the overnight scheduled run's **log** (not the badge) for the **final pushed commit** —
resolve that sha with `git log --oneline -1`, do **not** take it from the dispatch. Today's kickoff
named `60b1820` as the final commit and it was one behind; adopted as standing discipline, and the
orchestrator agreed.

### Pending decision-blocked items

- **A-16 — the watch on C_87's self-contradicting record row.** Gate 1 cleared today. Gate 2 is
  David's explicit approval and **has not been asked for**. Do **not** raise it unprompted; it is
  not urgent. If he raises it, the artifacts are the finding plus the review doc, and the ask is one
  sentence. **Before any send, re-run `npm run check`** — the claim is only reportable while
  upstream's page still carries the defect, and that is the one thing about it that expires.
- **A-15 — the watch on the parked Mathstodon send.** Dormant by instruction as of today. No card,
  no ask, nothing sent. It is **not** a no. Note the continuity checker flags A-15 as modified in
  the last 24h — that is this change, deliberate, per the orchestrator's ruling relayed in today's
  kickoff.

### Active in-flight work

None of ours. Tree clean, `main` at `8e9a0c1`, local and origin agree, no held branches, no queued jobs.

**One loose end, deliberately not killed:** `codex-health.mjs --reap-dead` flags an ORPHAN `codex`
process (pid 66068, started 09:05 MT, ~6 CPU-seconds) with no claiming job, and the reaper declined
to reap it (`0 reaped / 9 no-status of 9 candidates`). Today's session made exactly one Codex
dispatch, at 09:14, so this is plausibly its leftover but not provably so. Left running rather than
killed on a guess — it costs nothing idle. If the reaper still will not take it tomorrow, that is a
reaper question for the fleet, not a bounds-ledger one.

### What NOT to touch tomorrow

- **`movedPins()` in `scripts/catch-rate.mjs`.** Today's fix rebuilt the week scaffolding around it
  and deliberately changed nothing about what counts as a catch — which is why every historical
  figure is identical before and after. It compares only *generated* pins present on both sides of a
  commit, so it cannot be moved by our own editing. The tempting "improvement" is to make it
  understand values; that would put an unverified mathematical judgment in our own ledger.
- **The two `manual: true` claims (C-7, C-9).** Do not re-automate on the strength of a local 200 —
  it has been tried, and CI went red in four minutes.
- **`check-brief.mjs` staying out of CI.** A runner with no pin exits 3 UNVERIFIABLE; a permanently
  red alarm carries as much information as a permanently green one.

### Watch items maturing tomorrow

Nothing with a 24h trigger. **W-6 — the watch on arrivals** wants `npm run traffic` run regularly
(the API forgets days older than fourteen; today's sample is committed). **G-1's green streak** is
day 24 of 30 tomorrow.

### Tier 0 binding-constraint status

**G-1 — externally-acknowledged corrections: 1** (PR #141, merged by the upstream maintainer on
2026-08-11). The acknowledgement condition is **met**; the item stays open on the 30-day green-CI
streak alone, day 23 today. Posture: green.

**Read this before quoting the streak.** Now that the acknowledgement half is satisfied, the half
still gating G-1 measures **our uptime**, not anyone's regard for the work, and it advances by our
doing nothing. Proposed in today's retro: when it closes around 22 August, do **not** open a second
30-day streak — the successor should be a **rate** (acknowledgements per quarter, or accepted
upstream PRs per month), for the same reason the catches indicator exists at all.

**Leading indicator:** `npm run catches` — 4 distinct pins in the current partial week, unchanged
since yesterday; nothing upstream moved today. The window now reads 4 weeks rather than 3, and the
dry count is of **completed** weeks only.

### Evangelism read (binding constraint: retention / word-of-mouth)

- **The core problem, in the user's words:** *"I cited a number and I don't know if it's still the
  right number."* Widely-read index pages go stale silently — erdosproblems.com/36 has shown a
  superseded bound since January.
- **The would-tell-a-friend moment:** a maintainer accepts a correction we found. **It has happened
  once** — Terence Tao merged PR #141 on 11 August. Distance from the next one: A-16 is written,
  reviewed and verified by two independent routes, and sitting behind David's gate.
- **Retention / word-of-mouth trend:** the instrument is `npm run traffic` (W-6) plus the north-star
  count. 3 unique viewer-days since the repo went public on 8 August — an **upper bound**, 7 days
  into a 90-day window, so it answers nothing yet. No review or referral surface exists; for this
  lane the tell-a-friend act *is* the merge button.
- **Advocacy signal this session:** none new. PR #141 remains the only one.

### Harvest — the one unshipped move (seeds tomorrow's work-list)

**A "What this ledger has caught" section in the README**, linking the verified findings with one
line each and the check a reader can run in a minute. `docs/findings/` is where this lane's actual
product lives and it is invisible from the front door — the README explains what the machine *is*
and never shows what it *found*. WHO: the mathematician arriving from upstream, which is the only
arrival channel we have evidence for (PR #141 came through a merge button, not a landing page).
WHY NOT TODAY: it is public-facing, so it needs its own adversarial review under the standing rule.
SHIP-SHAPE: **S, under 60 minutes** including review — the findings are already written; this is an
index, not new prose. SIGNAL: referrer-separated arrivals in `npm run traffic`, and a second
unsolicited upstream acknowledgement.

### Supersedes

Nothing. First close-out for this target date.
