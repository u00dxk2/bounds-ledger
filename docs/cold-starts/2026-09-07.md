<!-- GENERATED — regenerating REPLACES everything outside the hand: markers, so put prose inside them. Provenance stamp at the foot of this block. -->

<!-- hand:begin banner -->
> **KEEP THIS BLOCK UNDER 2000 CHARS.** The kickoff quotes it whole below that and cuts the MIDDLE
> above it, so a long banner loses its own instructions while keeping its head and tail.
>
> **`npm run verify` HAS TWO REDS AND ONLY ONE IS PRE-EXCUSED. READ THE RECEIPT, NOT THE EXIT CODE.**
> **Exit 3 with `check:brief` as the ONLY entry in `failedGates` is `A-41` and is NOT yours.** Any
> other code, or exit 3 with a second leg, is real and IS yours. `tmp/.verify-receipt.json` names the
> leg — read that field, never infer it. Clearing condition: if it ever exits 0 the auth model changed
> and `A-41` has moved.
>
> **START LINE 4 OF THE FIRST ACTION BEFORE READING THIS DOCUMENT** — three minutes, all network:
> ```
> npm run verify > tmp/verify-out.txt 2>&1
> ```
>
> **`A-2` IS DUE TODAY** — the standing drift-resolution log. Both legs its readCommand names ran green
> at 18:00Z on 09-06, so this is a decision, not a dig.
>
> **RE-RUN THE GATE AT HEAD; DO NOT CARRY A RECEIPT.** `check:deferrals` reads `continuity/items.json`,
> so ANY ledger edit — a note, a date, a mint — puts your change inside a verify leg's blast radius,
> and a receipt from before that edit cannot say which leg is red now. This cost three re-runs on
> 09-06 and the argument against it was wrong both times it was made.
>
> **DO NOT QUOTE `check-resolvability`'s PERCENTAGE.** It prints its own UNCALIBRATED banner: the
> value/non-value split drops real short bounds out of the denominator and admits one non-value, so the
> ratio is wrong in both directions by an unbounded amount. The MISSES list is the output, and `A-45`'s
> 14-day clause cannot start counting until that split is calibrated.
>
> **A red drift run is the alarm working.** Verify against PRIMARY SOURCES before `--snapshot`, then
> `npm run resnap`, then a SECOND `render-site.mjs` AFTER the commit.
<!-- hand:end banner -->

First command (also below, in place):

```bash
node ../skylark-site/scripts/update-david-board.mjs --list --json --full-ids --project bounds-ledger
```

### Primer — bounds-ledger, 2026-09-07

What ONLY this primer holds comes first; the FULL runnable **First action** block
sits at the foot, below the reads. The single command above is the same block's
first line, repeated so it can be running while you read — it replaces nothing.

_**Composed against HEAD commit `0bac107336ea5fbba7c4c415de15a2c8002a4010` at 2026-09-06T18:15:08.212Z.** Nothing that landed after that instant is in this file. David's board answers especially: an answer he gives later cannot be here, and this primer will go on reading as though the work were not owed — that is exactly how two lanes concluded an OTA was not owed on 2026-09-04. Read them live, never off this page: `node ../skylark-site/scripts/answered-cards.mjs --project bounds-ledger --full`._

#### Owed gates — 1 due on/before 2026-09-07

_Ledger read at 2026-09-06T18:15:08.212Z by `cc-due-gates.collectDue` — the same resolver `check-due-gates-dispositioned` uses, so any difference is TIMING, not two answers. Rows filed or re-dated after that are NOT READ at generation — the close-out commit lands after this, so the COUNT above is stale by construction and this list cannot claim completeness. The list below, not the count, is the artifact; re-derive both before acting: `node ../skylark-site/scripts/check-due-gates-dispositioned.mjs --print --today=2026-09-07`._

Unfiltered as of the read time above — nothing was curated out of what the read could
see; a row absent here may simply have been filed after it ran.

- `A-2` (nextCheckDate=2026-09-07, open) — STANDING: drift-resolution log for the adopted surfaces (re-verification CI shipped 2026-0
  - read: `node scripts/reverify.mjs --check # the drift alarm itself. Literal output 2026-08-25: "No drift. 113 files match upstream 5c4aeee95699d72e1603d04d0987b1a7b6097535." (that 40-hex token is an UPSTREAM teorth/optimizationproblems commit; it does not resolve in this repo), exit 0. Any non-zero exit means a cycle is owed and this row is the log for it. Positive control that the command can say otherwise: reverify.test.mjs plants synthetic drift in 10a.md every run and asserts it is detected, so the detecting path is exercised on every npm test. WHICH PART OF THE QUOTED LITERAL IS THE INVARIANT (added 2026-08-31): the invariant is the words No drift. plus the FILE COUNT. The 40-hex upstream sha in that sentence MOVES ON ITS OWN — upstream commits that touch no mirrored file change it while every mirrored byte holds, which is exactly what happened between 2026-08-25 (upstream sha 5c4aeee) and 2026-08-31 (upstream sha 3a14910), 113 files both times. A changed sha alone is NOT a catch and must not be read as one. Recorded because the 2026-08-25 rule that a verification note records literal output rather than a paraphrase — adopted to stop a false PASS — sets up a false ALARM wherever the literal contains a moving token, and the rule carried no clause saying which tokens those are.

 SECOND LEG ADDED 2026-09-05, because the first one is BLIND to the artifact this row is the log for. reverify.mjs --check compares the mirror; it cannot see whether the README catch table — the PUBLIC rendering of this very log — has fallen behind the resolution cycles it lists. That is checked by: node scripts/render-state-block.mjs --check # which on 2026-09-05, with mirror, pins, state block and pages all in sync, still failed with "catch table: STALE — README.md claims 15 upstream drifts, but mirror history has 16 resolution cycles". Literal output after the row was added, run 2026-09-05 and pasted rather than paraphrased: "catch table: 16 cycles, and README.md says 16." at exit 0. The first draft of this field asserted it printed "catch table: current" — a string that command never produces. It was written from the verdict rather than the run, caught by running it, and is recorded here rather than quietly replaced. POSITIVE CONTROL that this leg can say otherwise: it fired on exactly the day a cycle landed and went quiet once the row was written, which is both polarities on one artifact in one day.` · last run: 2026-08-31T15:03:22.232Z

#### What shipped since 24 hours ago

_Read at 2026-09-06T18:15:08.212Z; commits landing after that — the close-out especially — are not here. Re-derive: `git log --oneline --since='24 hours ago'`._

- commit `0bac107` — A-34's onTrigger was telling its next reader the proposal was due on a day already past
- commit `e31b4ff` — A-45's readCommand stops saying the thing it names does not exist
- commit `36a6b77` — The new indicator's first run found a value the paper proves and our page could not find
- commit `06f5b46` — A-42's own probe would have reported the shipped fix as a broken probe
- commit `13a8f72` — The guards I added to prove the fix could not themselves fail
- commit `5f9f021` — The page's most repeated sentence was the one claim we could not support
- commit `444d565` — The indicator was approved the same morning it was proposed, and the row says it does not exist yet
- commit `fa5a45b` — The drift leg this lane can never pass, written down with what would falsify it
- commit `3c63268` — docs(close-session): cold-start primer close-out 2026-09-06
- commit `5cd1375` — Instance four, and the claim was TRUE - which is why it is the instructive one
- commit `cb4be7c` — A-44 closed same day on its ship branch, and the probe that argued against it is recorded as wrong
- commit `48b57a5` — The receipt now names the leg that actually failed
- commit `91d59d4` — The three continuity findings closed, and one of them by waiving rather than editing

**Yesterday's report:** `docs/daily/2026-09-06-prelaunch.md` (coverage 2026-09-06, the prior day). No findings fence declared in it.

**Today's report (owed):** write `docs/daily/2026-09-07-prelaunch.md` — the artifact of record for service day 2026-09-07. Check it exists at close: `node ../skylark-site/scripts/check-daily-report-exists.mjs --date 2026-09-07` (it sweeps the fleet and applies each lane's coverageOffset; it takes no --project).

#### State — as of 2026-09-06T18:15:08.212Z (re-run the release command; never trust the value)

- **ledger**: 58 item(s): 37 closed · 21 open · as of 2026-09-06T18:15:08.212Z — release: `node ../skylark-site/scripts/gen-primer-first-action.mjs --repo . --target-date 2026-09-07 --json`

#### Id glossary — 22 id(s) this primer's day already has words for

_Each gloss is the ledger row's OWN title, not a re-derivation — paste it. Rows = every open ledger row plus every id named above. `(no ledger row)` means the id is named here and the ledger carries nothing under it: that is a finding, not a formatting gap._

- `A-2` — STANDING: drift-resolution log for the adopted surfaces…
- `A-7` — engineering-health P1s — four of five shipped 2026-08-09;…
- `A-9` — engineering-health P2 backlog (fix-on-touch, not scheduled…
- `A-20` — Should the fetch layer retry with backoff on 429/502? —…
- `A-29` — secret_scanning_validity_checks will not enable via API —…
- `A-31` — Should the public page publish be CI-gated? It is not, and…
- `A-33` — A change to a file we have filed an upstream report…
- `A-34` — David asked where else this ledger can have the kind of…
- `A-39` — A mid-run 429 becomes up to 339 requests in two seconds at…
- `A-41` — The brief leg became permanently unverifiable on…
- `A-42` — Two user-facing readers still key off OUR pin history and…
- `A-43` — Upstream moved Haugland's minimum-overlap bound to its…
- `A-44` — The verify receipt names the WRONG leg, not merely a vague…
- `A-45` — TIER-1 LEADING INDICATOR (orchestrator-approved…
- `G-2` — GOAL: contribute a verified bound improvement to the…
- `G-4` — GOAL (Tier-0 from G-3 close): an outside party acts on a…
- `W-3` — Watch for acknowledgement of the erdosproblems.com/36…
- `W-4` — KP-78 applies to every new detector: demonstrate BOTH…
- `W-6` — Read window for the README report-an-error channel — the…
- `W-8` — Does a materially amended outward artifact go back to…
- `W-9` — Does this lane's detection method find what the lane…
- `W-10` — reverify.test.mjs alarm-title leg failed once and would…

### First action — bounds-ledger, 2026-09-07

Every line in the block below is a COMMAND, not a claim. A command re-reads at the
moment you run it and cannot go stale; a state line written last night can.

```bash
node ../skylark-site/scripts/update-david-board.mjs --list --json --full-ids --project bounds-ledger
git rev-parse HEAD origin/main
git rev-list --count origin/main...HEAD
npm run verify > tmp/verify-out.txt 2>&1
node ../skylark-site/scripts/check-ci-status.mjs --workflow reverify.yml
```

Morning release commands — deliberately NOT READ at generation (a value written
last night would already be stale); run each, never trust a remembered one:
- `git -C . log -1 --format=%h%x20%s` — HEAD
- `git -C . status --porcelain` — working tree
- `node ../skylark-site/scripts/check-ci-status.mjs --workflow reverify.yml` — CI
- **deploy read: NOT AVAILABLE** — no read-only deploy-state script in this lane's package.json (looked for ops:deploy-state, deploy-state, check-deploy, render-deploys, deploy:status, daily-snapshot). The fleet's check-deploy-state.mjs is not a substitute — it requires --fix <sha> --deploy <sha> and exits 1 without them

<!-- hand:begin narrative -->
_(orchestrator narrative — this region survives regeneration)_
<!-- hand:end narrative -->

<!-- generated by gen-primer-first-action.mjs (skylark-site) @ commit b7905498 · lane bounds-ledger · for 2026-09-07 · at 2026-09-06T18:15:08.212Z -->
<!-- writtenAtSha: `0bac107336ea5fbba7c4c415de15a2c8002a4010` — the lane's HEAD commit when this primer was written. Provenance anchor for check-primer-freshness leg ii; NOT a claim about HEAD now (the close-out commit that carries this file lands after generation). -->
<!-- reading this block: a generated line that is wrong in a way you can see FIXED in skylark-site's git log after the commit above is STALE, not a finding — regenerate before filing it. `git -C ../skylark-site log --oneline b7905498..HEAD -- scripts/gen-primer-first-action.mjs src/lib/cc-primer-commands.mjs src/lib/cc-due-gates.mjs src/lib/cc-report-artifact.mjs src/lib/cc-hand-edit-merge.mjs src/lib/cc-cold-readability.mjs src/lib/cc-skeleton-gloss.mjs` -->
