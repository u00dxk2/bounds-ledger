<!-- GENERATED — regenerating REPLACES everything outside the hand: markers, so put prose inside them. Provenance stamp at the foot of this block. -->

<!-- hand:begin banner -->
> **THIS LANE IS LAUNCHED** — David ruled it 2026-08-27 (*"agreed on bounds-ledger"*). You get the full `daily` prompt. Instruments a launched app has and this one does not — retention, active users, Sentry, revenue — are answered **"not an instrument on this lane, by design"**, never with a zero. The real reads are `npm run reports` (G-4's primary) and `npm run traffic`.
>
> **The report path stays `docs/daily/<date>-prelaunch.md`.** Historical filename, not a lifecycle claim; the fleet registry resolves to exactly it. Do not helpfully fix it.
>
> **THE MATHSTODON POST IS CLOSED, UNSENT, AND MUST NOT BE RE-PROPOSED.** David answered on 2026-08-28: *"Let's make this an uncaged-minds post - please send all the details to the orchestrator to send to the uncaged-minds agent for a draft."* Nothing was ever posted anywhere in that artifact's fifteen-day life. The material was handed to the orchestrator (bus `10086869`) and `A-37` tracks it to acknowledgement. **We are not the drafter and must not become one** — David named a specific agent. `A-15` is closed; do not reopen it.
<!-- hand:end banner -->

### First action — bounds-ledger, 2026-08-29

Every line in the block below is a COMMAND, not a claim. A command re-reads at the
moment you run it and cannot go stale; a state line written last night can.

```bash
node ../skylark-site/scripts/update-david-board.mjs --list --json --full-ids --project bounds-ledger
git rev-parse HEAD origin/main
git rev-list --count origin/main...HEAD
npm run verify > tmp/verify-out.txt 2>&1
node ../skylark-site/scripts/check-ci-status.mjs --workflow reverify.yml
```

#### Owed gates — 0 due on/before 2026-08-29

_None due. This is a real zero: 1 clock store(s) were read and returned no row due on/before 2026-08-29._

#### What shipped since 24 hours ago

- commit `c84a36d` — Hygiene 2026-08-28: CLAUDE.md 49202 -> 44902, and the compression found two stale claims
- commit `8d70a26` — A-37 names the hand-off message id, so a search can prove it landed
- commit `de8eb84` — David answered: not Mathstodon, an Uncaged Minds post — A-15 closes unsent
- commit `77eb6fe` — Daily report 2026-08-28, and A-35's trigger gets a read anybody can run
- commit `cea25f3` — W-6 traffic sample 2026-08-28 — 3 unique viewers, and the clone figure is still ours
- commit `87991df` — A-36: three rotations wrote "not browser-verified" in prose and nothing tracked it
- commit `888a1e5` — The page's loudest report link sent visitors OFF the path G-4 is measured on
- commit `83210ce` — P2 continuity: the two items I filed today were invisible to every date-framed sweep
- commit `c698c10` — A-35: the CLAUDE.md budget overrun, deferred to P5 in the machine-readable form
- commit `4179f1e` — The first action now reads David's board, because today it nearly didn't
- commit `01948a9` — Mathstodon v2.1: the post now carries the evidence the first draft left out
- commit `9d81aeb` — A-15 now names a live board card, which is the half A-28 said was missing
- commit `4b59085` — Mathstodon v2: rewritten around the retraction on David's ruling — reviewed, NOT SENT
- commit `c71b8c5` — P5: the forcing rule overrode a pre-registered gate, and the sibling sweep found the fix I half-did
- commit `c62bfe4` — Close-out: continuity WARN cleared — every commit in the window now linked or given a rationale
- commit `d85c8a7` — docs(close-session): cold-start primer 2026-08-28
- commit `9a5f200` — P5 hygiene: the second fleet trap line, and the engineering zero-state
- commit `fcf056d` — Fleet rule recorded: never kill a node process by name, age, or "looks orphaned"
- commit `a3c8fe1` — P4: four waits forced, and a 34-day David-blocker that was never one
- commit `2e1b5d0` — David ruled LAUNCHED — recorded on A-13, the item that defined the question
- commit `970aa13` — Back-patch the P3 ship into today's report, inside the findings fence
- commit `a4558ed` — The page said "fetched <date>" and a reader takes that as "last looked at" — it is not
- commit `78ed7f5` — P2 continuity: the A-34 threshold committed before its count, and the prediction rebuilt as one clause
- commit `c504d37` — A self-attributed cause now carries its control on the line, enforced at commit time
- commit `0dfe218` — Report BLUF carries the 3-line judgment block, and the lead's "repo stays private" is false
- commit `1e02cf3` — Daily report 2026-08-27, A-30 closed, A-2 note 11, and a closed goal still cited in an instrument's output
- commit `7547c01` — Catch table: the twelfth cycle — an attribution RETRACTED by upstream
- commit `ef7ff97` — Declare the first-action fence and adopt the generated primer — but NOT one of the two offered fences
- commit `561964d` — Re-render the page so 2a publishes its real date, not "date unknown"
- commit `3907e7a` — Drift 2026-08-27: upstream RETRACTS the "acknowledges Jin" attribution on 2a — every leg verified

#### State — as of 2026-08-28T18:33:17.506Z (re-run the release command; never trust the value)

- **HEAD**: commit `c84a36d` on `main` · as of 2026-08-28T18:33:17.506Z — release: `git -C . log -1 --format=%h%x20%s`
- **working tree**: 0 uncommitted path(s) · as of 2026-08-28T18:33:17.506Z — release: `git -C . status --porcelain`
- **ledger**: 50 item(s): 30 closed · 20 open · as of 2026-08-28T18:33:17.506Z — release: `node ../skylark-site/scripts/gen-primer-first-action.mjs --repo . --target-date 2026-08-29 --json`
- **CI**: NOT READ at generation (a morning question) — release: `node ../skylark-site/scripts/check-ci-status.mjs --workflow reverify.yml`
- **deploy**: NOT READ at generation (a morning question) — release: `node ../skylark-site/scripts/check-deploy-state.mjs`

<!-- hand:begin narrative -->

## Session close-out — 2026-08-28 ~18:40Z

### The one thing to understand about today

The public page's loudest reporting link was **competing with its own measurement**. Its intro said
*"that is a bug worth [reporting]"* and pointed at the bare GitHub issues list — but `report-rate.mjs`
classifies an arrival by the prefilled `Row looks wrong:` title or the `Ledger mirror: upstream` body
marker, so an issue filed from that link has `arrivalKind() === null` and lands in `outsideOther`,
**never as an arrival**. G-4 — the Tier-0 goal that an outside party acts on a watched record without
us filing the report — is measured on exactly the path the page was steering people away from. Fixed
in `888a1e5`; the intro now names the per-row **looks wrong?** control.

It was the residue of the 2026-08-22 rotation: the better affordance shipped and the older instruction
stayed in the highest-attention block on the page.

### Load-bearing ships

- `888a1e5` — **user-visible.** The report-link fix above. KP-78 both ways with the mutation proven to
  land first; restored from a file COPY, not `git checkout`, because the fix was uncommitted. The
  assertion is scoped to an href *ending* in `/issues`, so the prefilled `/issues/new` links keep passing.
- `4179f1e` — **the first action now reads David's board.** He answered a card at 02:27Z and this pane
  found it only because a bus dispatch happened to carry it. On a quiet bus the whole cadence would have
  run past a live answer. The other four commands all ask *is the code green*; none can answer *does
  David want something*.
- `83210ce` — two ledger rows filed that morning were invisible to every date-framed sweep.
- `c84a36d` — CLAUDE.md **49202 → 44902** bytes (`A-35` closed). The keep-list caught a string I broke;
  the pass also found *"Pre-launch."* and a closed-`A-13` flip exit still live in the first fifteen lines.

### Tomorrow's first action

Run the declared block at the top of this file, then the ordinary steward cadence. **Nothing is owed to
David and no gate is due tomorrow.**

### CI

**`reverify.yml`: GREEN @ `c84a36d`** — 1 completed non-scheduled success, 0 failures, 0 pending, read at
close. Run-level verdict; read the JOB layer before trusting it in either direction during a platform
outage.

### What NOT to touch

- **The `manual: true` → UNVERIFIED-never-green mechanism** on `C-7`/`C-9`. It refuses to launder an
  unverifiable fact into a pass; automating it away on 2026-07-25 turned CI red in four minutes.
- **The Mathstodon artifact.** Closed unsent — see the banner.
- **Other panes' processes.** The Codex health probe reports orphaned `codex.exe` PIDs; read-only checks
  showed neither belongs to this lane. Post PIDs on the bus, never kill by name or age.

### Watch items maturing soon

`A-2` (drift-resolution log), `A-32` (lane-brief voice check) — both **2026-08-31**. `A-18`, `A-20` —
**2026-09-01**. `A-33`, `A-34`, `A-37` — **2026-09-02**. Nothing due 2026-08-29.

### Tier-0 status and the evangelism read

**The ONE core problem, in the user's words:** *"I cited a number. I have no idea if it's still the
current one."*

**The would-tell-a-friend moment:** *"the ledger caught a drift the field's own index missed."*

**G-4 = 0**, and it is a MEASURED zero: `npm run reports` fetched 26 issues, attributed all 26 to us,
and reconciled 26 + 0 + 0 = 26. Day 20 since the public flip.

**Distance from the moment, honestly:** on 2026-08-23 an outside contributor fixed a record citing our
filed report and the maintainer closed it — that is the moment happening, but its causal path ran
through an artifact we wrote and sent, which is why it satisfies the CLOSED G-3 and not G-4.

**Retention / word-of-mouth instrument:** *not built, and deliberately not being built.* At 3 unique
viewers in the trailing 14 days a return-rate or share-rate would manufacture a number rather than
measure one. The binding constraint is still arrival (`W-6`). Do not write a retention rec you cannot read.

**Advocacy signal this session:** none. No review, referral, share or unsolicited contact.

### Stuck-ask inventory

- **DOABLE-NOW** — `A-33` leg 1 (poll the state of every outward report we have filed; a state-diff, no
  map needed). The orchestrator approved pulling it forward from 2026-09-02. Hold its design constraint:
  fire on the TRANSITION, not the state, or it becomes a permanently-red alarm.
- **MISTAKEN** — none. Nothing on this ledger is waiting on something that already happened.
- **REAL** — `A-36` (phone-width check; un-stuck by David connecting the Chrome extension, deliberately
  not carded) and `A-37` (the Uncaged Minds hand-off; un-stuck by one bus follow-up if unacknowledged by
  2026-09-02).
- **UNVERIFIABLE-ON-OUR-SIDE** — `A-29` (GitHub accepts the validity-checks PATCH with HTTP 200 and
  silently ignores the field; the paid-entitlement explanation is a HYPOTHESIS with no positive control,
  because no second repo is known to have the feature on) and the **reply leg** of `W-3` (we cannot read
  a maintainer's inbox). `W-3`'s *page* leg IS ours and runs on every local check — today it read HTTP
  200, unchanged. Not being able to see something is never evidence it resolved.

### Supersedes

Supersedes the 2026-08-28 primer's read on `A-15` and `A-28`: both are now CLOSED. `A-28`'s "figures
have gone stale" finding was answered structurally — no figure in the final artifact can decay.

<!-- hand:end narrative -->

<!-- generated by gen-primer-first-action.mjs (skylark-site) · lane bounds-ledger · for 2026-08-29 · at 2026-08-28T18:33:17.506Z -->
