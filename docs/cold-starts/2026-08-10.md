# Cold-start primer for 2026-08-10 (MT)

Multi-session primer — each /close-session run appends a new section below. Later sections
supersede earlier ones explicitly.

## Session close-out — 18:50Z (2026-08-09, ~12:50 MT)

You are the bounds-ledger agent. Read this, then `AGENTS.md` + `CLAUDE.md`, then
`continuity/items.json`. The repo is PUBLIC (since 2026-08-08); A-13 is closed.

### Tomorrow's first action

The steward cadence, unchanged, and it is still first:

1. `gh run list --limit 1` then **read the LOG, not the badge**. Expect `No drift. 111 files match
   upstream dee1660…` (an UPSTREAM sha — do not `git show` it locally), `229 claim(s): 227 hold, 0
   broken/unreachable, 2 unverified (manual)`, and **10** selftest PASS lines (was 9 — a tenth
   shipped today). Green → **streak day 18 of 30**.
2. `npm run check` locally **with `CC_PROMPTS_PIN` set** — the only run that gives a real advisory
   read on the two manual claims.
3. **`npm run traffic`** — new today, and it is now part of the cadence. GitHub keeps only 14 days
   of traffic, so a skipped fortnight loses those days permanently; `continuity/traffic.json` is
   the only copy of anything older.

Then the standing technical increment: **G-2 round three, the Earth-Moon search.** Deliberately not
started today (two orchestrator dispatches consumed the session). Both prior rounds agree from
opposite directions that the record is a *designed* object — seed the growth with structure (a
clique joined to an odd cycle), hold it fixed in the layers, double-greedy only the rest, and watch
the leftover column at n≥12.

### Today's load-bearing ships

- **`56a8586` — the arrival counter (W-6).** `npm run traffic` accumulates GitHub's 14-day
  views/clones window into `continuity/traffic.json`. It renders **no verdict**; the fair-read
  judgment stays with whoever adjudicates the watch. `gh` holds the credential, so no traffic token
  enters this public repo and the sampler never runs in CI — only its merge is guarded there,
  because a merge keeping just the incoming window would silently delete every day older than 14.
- **`0a29081` — four engineering-health controls (A-7 leg a).** Branch protection ruleset on `main`
  (deletion + non-fast-forward blocked, no bypass actors), pre-commit secret scan via
  `core.hooksPath`, `repo-health.json`, and `package-lock.json` + `npm ci` in CI.
- **`2b751f5` — a live artifact UUID removed from `items.json`.** Fourth recurrence of the
  redaction pattern; the rule is now in `CLAUDE.md` (see below).

### CI

**`reverify`: success @ `2b751f5`.** Green all day, 6 runs, every one read by log. Local `main` and
`origin/main` agree. Tree clean.

### What changed about how you work here

- **`git config core.hooksPath .githooks` is per-clone.** A fresh clone has NO pre-commit scan until
  you set it. Now in `AGENTS.md`.
- **A rejected force-push is the control working, not a broken remote.** `main` carries ruleset
  `main-history-integrity` with no bypass actors, so even the owner is blocked. A deliberate history
  rewrite must disable the ruleset first; that friction is the point.
- **New `CLAUDE.md` rule on redactions**, and it is the highest-value thing in this close-out: *a
  redaction scoped to one SURFACE leaves the string on the others*, and the record of the redaction
  is itself a surface. Four recurrences. Enumerate BY SHAPE from a fresh clone of the remote; never
  search for the string you already know about, because that search cannot find the copy you do not
  know about.

### Decisions taken, so nobody re-derives them

- **A-7's R6 push-protection SPEND question is DEAD**, not deferred — the public flip granted the
  control free, so its pre-committed 2026-08-16 re-arm can never fire. What did NOT die: both
  controls are enabled and **neither has been shown to fire**; a settings toggle is not a
  demonstration here.
- **R16's fire demonstration was WAIVED by orchestrator ruling**, with the reason recorded. Grounds:
  `rulesets/rule-suites` returns server-side `result: pass` records for today's pushes, which is
  within-path evidence a rung above a config read, and producing a rejection means attempting the
  operation the ruleset exists to prevent. **The waiver is scoped** — it does not license recording
  any control as demonstrated on a state read, and it does not cover the `deletion` rule (never
  exercised) or secret scanning / push protection.
- **A-7 stays OPEN on R7 alone** — the scheduled verified history sweep, which is fleet-owned. The
  orchestrator said leg (a) closes; the item's own `closeWhen` names five conditions and R7 is one
  of them, so closing it would mean closing against a contract we do not meet. Offered them the
  clean alternative (close A-7, respawn R7 as its own fleet item) and it is unanswered — **if they
  say yes tomorrow, do exactly that.**

### Nothing is blocked on David

All three board cards are **answered**, including the rewritten-history one (board card `4ea78b83` — a card id, not a commit): his word
was *"Go ahead and strip it from that commit message too"*, it was carried out, and the second yes
followed. Verified today from a fresh anonymous clone — 135 commits, zero personal address in any
commit body, any author/committer field, or any tracked file; the three pre-rewrite SHAs are absent.
**If a board pass still lists that card as open, it is stale — say so rather than re-asking him.**

### Tier 0 / binding constraint

**North star: 0 externally-acknowledged corrections. UNCHANGED, and nothing today moved it.**
PR **#141** open, 0 comments, 0 reviews, untouched since 8/05 — **check, do not poll, do not nudge**
(his standing word). The July email is unanswered; W-3's two page legs both read unchanged locally
today. Our own issue tracker is empty on **day 1 of W-6's 90** — arithmetic, not a verdict.

**Evangelism read, honestly:** the ledger *is* the product and it has no users yet. The
would-tell-a-friend moment would be a stranger finding a record we caught moving before their own
source did — nobody has arrived to have it. Retention/word-of-mouth instrument: **stood up today**
(the traffic sampler) and reading 1 unique viewer since the flip. The 25 "unique cloners" are mostly
us, from Friday's own clone-verification, plus crawlers. Advocacy signal this session: none.

**Measurement limit found today, and it changes how W-6 must be read:** GitHub dedupes uniques only
*within* a 14-day window, so any 90-day figure is a SUM of per-day uniques that counts a returning
visitor once per day. It is an UPPER BOUND — under 100 conclusively settles the threshold is not
met; over 100 settles nothing. W-6's `closeWhen` now says so.

### What NOT to touch

- Do not re-automate C-7/C-9 — the erdosproblems 403 is IP-dependent, a local 200 proves nothing
  about CI, and only a green CI run is evidence.
- Do not put `CC_PROMPTS_PIN` in repo secrets. Public repo; this is load-bearing now, not a
  precaution.
- Do not re-propose the public comment on /36 — decided NO on 2026-08-02 and it is not a deferral.
- Do not hand-edit the mirror or the generated pins; do not weaken the 27b verifier to make a
  candidate pass; do not claim any G-2 result without exact re-verify + literature check +
  adversarial review + David's gate.
- Do not allowlist `history-sweep.mjs`'s four hits or neuter its fixture — it exits 1 by design and
  shipped public that way, adjudicated in writing.

### State at close

`main` at `2b751f5`, tree clean, local and origin agree, 135 commits, PUBLIC. 7 commits today.
10/10 self-tests. CI green. Daily report `docs/daily/2026-08-09-prelaunch.md`, 7/7 linters PASS.
Open continuity items: 10. Zero cards waiting on David. Codex calls: 0.
