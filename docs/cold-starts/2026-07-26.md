# Cold-start primer for 2026-07-26 (MT)

Multi-session primer — each /close-session run appends a new section below.

## Second-session update — 21:15Z (2026-07-25)

Steward pass ran early (15:00 MT). What you inherit:

- **CI verified honest, by log not badge**: overnight scheduled run `30153551921` AND post-revert HEAD run both show `223 claim(s): 222 hold, 0 broken/unreachable, 1 unverified (manual)`, no drift, mirror at `a002311`. Issues #3/#4/#5 already closed. Green-streak day 2 intact.
- **C-7 re-verified from a residential IP, page unchanged**: HTTP 200, `0.380876` present, `0.380868` absent, last edited 23 January 2026. No W-3 trigger.
- **Instrument defect #5 (the 7/25 series continues): the "local `npm run check` re-verifies C-7" sentence below was FALSE when written** — `check-claims.mjs` skipped `manual:` claims without fetching, on any machine; the 7/25 "local Node fetch" was an ad-hoc one-liner, not the script. Fixed in `7e07228`: the manual branch now does an **advisory fetch** — result printed as an indented line, counts/exit untouched, claim stays UNVERIFIED (deliberately NOT the reverted C-7 automation). CI run `30175057184` green with the advisory-403 line in the log, which now re-proves the runner-403 fact daily for free. Finding: `docs/findings/2026-07-25-npm-check-never-fetched-c7.md`. From here on, the sentence is true: a local `npm run check` genuinely checks C-7's pin — read the advisory line.
- Everything else below stands. A-6 remains untouched and highest-leverage.

## Third update — A-6 SHIPPED and CLOSED (2026-07-25 evening)

David greenlit the reshaped A-6 in-pane; it shipped the same session. Supersedes the A-6 sections below.

- **Phase-0 verdict first**: the bounds do NOT live in `teorth/erdosproblems` (README.md + problems.yaml grepped raw for all three /36 values — absent; no per-problem prose in the tree). A bound correction can never be a PR there. `CONTRIBUTING.md` accepts metadata-table edits and points mathematical content at the site's per-problem comment pages.
- **The ship**: second stewarded surface = `data/problems.yaml`, via **C-8** — an entry-level pin on problem 36 (`number → prize → informal_status.state`, stops before the churning `last_update`). **Deliberately NO byte-level mirror**: the repo pushes near-daily; a whole-file drift alarm would be permanently red = no information. Zero new scripts — rides `check-claims.mjs`. Ledger is now **224 claims / 223 hold / 1 UNVERIFIED** (C-7, by design). `extract-pins.mjs` executed and proven to preserve C-8.
- **C-8 ID caveat**: an unrelated reverted claim used the C-8 ID briefly earlier on 7/25 (the 403 episode). The claim's own note disambiguates.
- **If C-8 BREAKS in CI**: problem 36's entry changed upstream — likely status flip or prize change. Re-verify against the repo, re-pin deliberately. If the status flipped to solved/disproved, that's real news for our reconciliation — read the repo commit that did it.
- **Open paths not built** (deliberate): the site-vs-Tao bulk diff (needs residential fetches — local-session pipeline, pick up only if David wants more correction candidates); the /36 public-comment fallback (waits for W-3's ~8/24 decision point; outward-gated).
- Open items now: **G-1, A-2, W-3** only.

## Session close-out — 20:40Z (2026-07-25)

You are the bounds-ledger agent. Read this, then `CLAUDE.md`, then `continuity/items.json`. This lane is a **steward** of drifting mathematical records — a ledger that alarms when a cited record moves. Lite rail, pre-launch, private repo. **No self-rating** (no `/daily-close`).

**Yesterday's theme, and it should shape how you work today: every single thing we found was an instrument that lied.** Not one record drifted. Four separate defects, all in our own checking apparatus. Read that as a standing warning, not a war story.

### Today's load-bearing ships

| Commit | What |
|---|---|
| `0d60535` | Daily report + adopted the canonical H2 report format (linter now 4/4 PASS, first time) |
| `58e1799` | Housekeeping — README brought current, two CRLF/pipe gotchas, dead refs cleared |
| `7c0f2c9` | **A-4**: alarm now files an "Instrument failure" issue when a self-test breaks (was filing nothing) |
| `c5dda00` | **A-5**: alarm no longer calls a network flake a `Drift:`, and now says *why* a check failed |
| `228bd46` | C-7 automated / C-8 added — **wrong, reverted below**; the finding it produced is the keeper |
| `605f1a2` | gitignore Playwright scratch |

**CI:** green on every push except `605f1a2`, which went red exactly as it should have — it caught my bad C-7 change (`UNREACHABLE … HTTP 403`) and auto-filed the issue. Reverted and green again; **verify HEAD yourself before trusting this line.**

**Open GitHub issues #3, #4, #5** are all the same false alarm from that episode (one filed per push) — triage and close them if I left any open. That repetition also falsifies the premise on which **P-3 (one rolling issue)** was deferred: it was assumed to only matter during a multi-day real drift, but *any* repeated red files a new issue per push. Consider picking P-3 up.

### The one thing to understand before touching anything

**The erdosproblems.com 403 is REAL, and it is IP-dependent.** I got this wrong yesterday, shipped the wrong conclusion, and CI caught it in four minutes. The episode is worth your two minutes because the trap is easy to re-enter.

- From a **residential IP** (David's machine, your local session): plain Node `fetch` → **HTTP 200**, bound and edit-date readable.
- From a **GitHub Actions runner**: same URL → **HTTP 403**.

I tested locally, concluded the four-day-old "site 403s bots" convention was stale, automated C-7, added a C-8, declared *"224 claims, zero UNVERIFIED"* — and the next CI run returned `UNREACHABLE C-7 / C-8 — HTTP 403`. **Reverted**: C-7 is `manual: true` again, C-8 removed, ledger back to **223 claims / 222 hold / 1 UNVERIFIED**.

**What survives, and it is a genuine upgrade:** an agent running `npm run check` on a residential connection *does* re-verify C-7 mechanically. The page-check is no longer a screenshot round-trip — it is a local command any session can run. Three independent reads agreed on 7/25 (David's hand-check, local fetch, Playwright): `0.379005 < c < 0.380876`, unchanged, last edited 23 January 2026.

**Do NOT re-automate C-7 on the strength of a local 200.** It has been tried; it fails in CI. The only evidence that counts is a green CI run.

Finding: `docs/findings/2026-07-25-the-403-that-wasnt.md` — the corrected version; the lesson is *test a capability from the environment that will exercise it.*

### Tomorrow's first-action

**Check the overnight scheduled run — and read the log, not the badge.**

```
gh run list --limit 3 --json databaseId,status,conclusion,event,createdAt
```

- **Green →** say so and move on. G-1's real green-streak reaches **day 3** (day 1 = 2026-07-24). Confirm the log shows `223 claim(s): 222 hold` with **1 UNVERIFIED** (C-7, by design — CI is 403ed, that is correct, not a failure).
- **Red on a claim →** ⚠ read *which* claim. A break on one of the 216 generated pins means a bounds table moved. **C-7 can never break in CI** (it is UNVERIFIED there) — the way you learn the erdosproblems page changed is a **local `npm run check`**, which is worth running once here since you are on a residential connection. If the upper bound is now `0.380868`, that is **the lane's north-star event, not a bug**: follow **W-3's `onTrigger`** runbook in `continuity/items.json` step by step. That is G-1's second close condition landing.
- **Red on a mirror drift →** re-verify against **primary sources**, then a deliberate `--snapshot`, then **re-run `extract-pins.mjs` and commit** (the post-snapshot ratchet). Never snapshot to silence the alarm.
- **"Check error" title →** a transient fetch failure, not a drift. A-5 made that title honest. Confirm the next run is green; don't chase.

### Pending decision-blocked items

- **W-3** — the only thing owed by David is **a maintainer reply**, which lands in his inbox and nowhere we can reach. The *page* leg no longer needs his eyes: run `npm run check` locally and C-7 re-verifies mechanically. I recommended (offered 2026-07-25, **he has not picked dates**) that if no reply and no page change by **~2026-08-24**, we close W-3 recording that no response came rather than letting it age out. Closing costs little: C-7 stays pinned and any local check keeps surfacing the page state. G-1's 30-day CI leg ends ~2026-08-22, so they mature together. **Raise this with him; don't decide it alone.**

### The opportunity worth your attention — A-6

Filed today, **not started**, and it is the highest-leverage thing on the board.

G-1 needs one **externally-acknowledged** correction. That metric is 0 and currently rests entirely on one email to one busy maintainer. Verified yesterday:

- erdosproblems.com pages fetch at 200 — the **whole site** is machine-readable, not just `/36`.
- `github.com/teorth/erdosproblems` is **public, actively maintained** (pushed 2026-07-25T17:27Z), and is the community database behind the site.
- It ships `data/problems.yaml` (396KB), `schema/`, `scripts/`, and a **21KB `CONTRIBUTING.md`** — it accepts contributions.
- **Caveat, and the most likely way this is wrong:** `problems.yaml` is *metadata only* (number, prize, status, oeis, tags). The numeric bounds live in the site's page prose. **If the prose is not community-editable, the PR channel does not apply to bound corrections** — verify this before building anything on it.

Two independent wins if it holds: *scale the catches* (systematically diff the field's most-used index against Tao's curated table — that divergence is the lane's founding finding, and it would turn one lucky catch into a pipeline), and *change the channel* (an accepted PR is a public, dated, attributable acknowledgement that cannot be silently ignored; email put the north-star metric behind an inbox, a PR puts it behind a merge button).

Sequencing is in A-6's `note3`. Read `CONTRIBUTING.md` first. **Any PR is outward contact — adversarial refute-it review, then David's outward-send gate. Unchanged.**

### What NOT to touch

- **Don't hand-edit `ledger/`** — machine mirror; refresh only via `--snapshot`, then re-run `extract-pins.mjs`.
- **Don't "upgrade" generated pins to record-row claims** — they assert *listing position*, deliberately. Numeric ranking is defeated by symbolic cells, negatives and asymptotics.
- **Don't re-automate C-7.** It has been tried and it fails in CI (403 from runners). Only a green CI run is evidence.
- **Don't build a Playwright poller.** David asked for one; the plain-fetch claim in CI supersedes it and costs zero code.
- **Don't post self-ratings** — lite rail has no rating contract.
- **P-2/P-3** alarm polish stays deferred; they only pay on a multi-day real drift.
- **Don't start A-6 casually** — it is a real build, and outward contact is gated.

### Two traps that cost me time yesterday — both now in CLAUDE.md

1. **Files on disk are CRLF.** A Node string-replace written with `\n` **silently no-ops** — no error, no match, your edit never happened. This produced a *false* "the pipefail guard is inert" verdict; the guard was fine. **`git diff` to prove a test mutation landed before believing the result.**
2. **Never read an exit code through a pipe** — `node x.mjs | tail -3` reports *tail's* 0. The CI form of this is the lane's founding defect and I reproduced the local form while auditing it.

### Tier 0 / evangelism read (pre-launch)

- **Core problem, in the user's words:** "I'm citing a number — is it still current?"
- **Would-tell-a-friend moment:** the alarm catches a stale citation before the citer is embarrassed by it.
- **North-star metric — externally-acknowledged corrections: 0.** One correction *sent* 7/24; sending is not acknowledgement.
- **Retention / word-of-mouth:** no instrument, by design — no users, no consumption surface (private repo; the only way to read the ledger is to clone and run `npm run check`). The instrument to stand up is the **public status surface**, and the public flip is David-gated. The honest nearest proxy is the external-ack metric, already instrumented as W-3.
- **Advocacy signal this session:** none. Correct for day 4 pre-launch.

### State at a glance

| Item | What | Status |
|---|---|---|
| G-1 | GOAL: steward a drifting record inventory | open — real green-streak **day 2**; external-ack **0** |
| A-2 | Re-verification CI | open — armed, legible, **223 claims**, 1 UNVERIFIED (C-7, by design) |
| A-4 | Alarm silent on self-test failure | **closed 7/25** (`7c0f2c9`) |
| A-5 | Alarm called a network flake a drift | **closed 7/25** (`c5dda00`) |
| A-6 | **Adopt erdosproblems.com + PR channel** | **open — not started; highest leverage** |
| W-3 | Watch for the correction's acknowledgement | open — **reply leg David's**; page leg via a LOCAL `npm run check` |
| A-1 / A-3 / W-2 | reconciliation / correction sent / Haugland | closed 7/22, 7/24, 7/23 |
