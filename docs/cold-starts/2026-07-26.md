# Cold-start primer for 2026-07-26 (MT)

Multi-session primer — each /close-session run appends a new section below.

## Session close-out — CONSOLIDATED 2026-07-25 (~22:00Z; supersedes the three earlier blocks)

You are the bounds-ledger agent. Read this, then `CLAUDE.md`, then `continuity/items.json`. This lane is a **steward** of drifting mathematical records — a ledger that alarms when a cited record moves. Lite rail, pre-launch, private repo. **No self-rating** (no `/daily-close`).

Three sessions ran on 7/25. Cumulative state, one read:

### The ledger tonight

**224 claims / 223 hold / 0 broken / 1 UNVERIFIED (C-7, by design) — across TWO stewarded surfaces.** Mirror clean at upstream `a002311` (that's the teorth/optimizationproblems sha in `ledger/.../manifest.json`, not a local commit). Green-streak **day 2** (day 1 = 7/24). No open GitHub issues.

**CI:** reverify: completed/success @ `30beea2` — conclusion observed AND log read: `224 claim(s): 223 hold, 0 broken/unreachable, 1 unverified (manual)`.

### Today's load-bearing ships (all pushed, all CI-green)

| Commit | What |
|---|---|
| `462e082` / `f0af079` | **A-4 / A-5 closed** (session 1): alarm now files an Instrument-failure issue when a self-test breaks, and no longer titles a network flake `Drift:` |
| `293cec9` → `00d1dcd` | C-7 automation shipped on a local 200 and **reverted when CI 403ed** — the 403 is IP-dependent; finding `docs/findings/2026-07-25-the-403-that-wasnt.md` |
| `edde4d8` | **Instrument defect #5**: every doc said "local `npm run check` re-verifies C-7" — the script never fetched `manual:` claims. Now real: an **advisory fetch** (result printed, counts/exit untouched, stays UNVERIFIED). Finding: `docs/findings/2026-07-25-npm-check-never-fetched-c7.md` |
| `9247d2b` | **A-6 phase-0 verdict**: bounds do NOT live in teorth/erdosproblems (grepped raw, all three /36 values absent) — bound corrections can never be PRs there; metadata edits + site comment pages are the sanctioned channels |
| `30beea2` | **A-6 SHIPPED + CLOSED** (David greenlit in-pane): second surface = `teorth/erdosproblems:data/problems.yaml` via **C-8**, an entry-level pin on problem 36. Deliberately NO mirror — the repo pushes near-daily; an always-red drift alarm carries no information. Zero new scripts |

### Tomorrow's first-action

**Check the overnight scheduled run — read the LOG, not the badge** (`gh run list --limit 3`, then `gh run view <id> --log`). Expect: `No drift`, **`224 claim(s): 223 hold, 0 broken/unreachable, 1 unverified (manual)`**, and under C-7 the line `advisory fetch failed (HTTP 403) — expected from datacenter IPs (CI)` (that line re-proves the runner-403 fact daily, for free). Green → streak **day 3**; say so and move on.

- **Red on C-8** → problem 36's entry changed upstream (status flip? prize? restructure?). Read the upstream commit that did it, re-verify, re-pin deliberately. A status flip to solved/disproved is real news for reconciliation #1.
- **Red on a generated pin** → a bounds table moved: primary sources → deliberate `--snapshot` → **re-run `extract-pins.mjs` + commit** (the post-snapshot ratchet).
- **`Check error:` title** → transient fetch flake (A-5 made the title honest). Confirm next run green; don't chase.
- Optionally run `npm run check` locally (residential): since `edde4d8` it **genuinely** checks C-7's pin — read the advisory line. If it says `0.380876 NOT FOUND` → hand-verify the page NOW; if the page shows **0.380868**, that is the **north-star event**: follow **W-3 `onTrigger`** in `continuity/items.json` step by step and tell David.

### Pending decision-blocked items

- **W-3** (the only watch): maintainer-reply leg lands in David's inbox — only he can supply it. The ~2026-08-24 no-response close date is **recommended, not David-confirmed**. NEW since A-6 phase-0: the fallback at that decision point is a **public comment on erdosproblems.com/36** (the community-sanctioned venue) rather than closing empty — outward contact, adversarial review + David gate.

### What NOT to touch

- **Don't re-automate C-7.** Tried, failed in CI (403 from runners), reverted. The advisory fetch is the ceiling of what's safe — it never touches counts or exit codes. Only a green CI run is evidence.
- **Don't add a mirror for teorth/erdosproblems.** Entry-level claims are the deliberate design (near-daily churn → permanently-red alarm → no information). Recorded in A-6's resolution and CLAUDE.md.
- **Don't hand-edit `ledger/`**; snapshot → re-pin only. **Don't "upgrade" generated pins to record-row claims.** **Don't post self-ratings.** **P-2/P-3 alarm polish stays deferred** (P-3's premise weakened — repeated reds file one issue per push — but it only pays during a multi-day red; pick up only if that happens).
- **Any outward contact** (PR, comment, email) = adversarial refute-it review, then David's send gate. Never on your own.

### Traps (all three bit real sessions this week)

1. **CRLF files**: a Node string-replace written with `\n` silently no-ops. `git diff` to prove a test mutation landed. Use `\r?\n` in patterns.
2. **Never read an exit code through a pipe** — locally or in CI. Redirect to a file and check `$?`; CI steps that pipe need `shell: bash` (guarded by `reverify.test.mjs`).
3. **Test a capability from the environment that will exercise it.** A local 200 proves nothing about a runner. And its inverse (defect #5): **a capability claim about an instrument is verified by executing the instrument** — not by remembering an ad-hoc test that resembled it.

### Tier 0 / evangelism read (pre-launch)

- **Core problem, user's words:** "I'm citing a number — is it still current?"
- **Would-tell-a-friend moment:** the alarm catches a stale citation before the citer is embarrassed by it.
- **North-star — externally-acknowledged corrections: 0.** Correction sent 7/24; sending ≠ acknowledgement. Two channels now exist toward the first ack: the email reply (passive, David's inbox) and a future metadata-catch PR to teorth/erdosproblems (active, gated).
- **Retention/word-of-mouth:** no instrument by design (no users; private repo). Honest proxy = the external-ack metric, instrumented as W-3.
- **Advocacy signal this session:** none. Correct for day 4 pre-launch.

### State at a glance

| Item | What | Status |
|---|---|---|
| G-1 | GOAL: steward drifting record inventories | open — streak day 2/30 (ends ~8/22); external-ack **0** |
| A-2 | Re-verification CI | open — **224 claims, 2 surfaces**; closes with the 30-day streak |
| W-3 | Ack watch for the /36 correction | open — reply leg David's; page leg = local `npm run check` (real since `edde4d8`) |
| A-4 / A-5 / A-6 | alarm fixes + second surface | **all closed 7/25** |
| A-1 / A-3 / W-2 | reconciliation / correction sent / Haugland | closed 7/22–7/24 |
