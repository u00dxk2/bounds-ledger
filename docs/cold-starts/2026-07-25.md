# Cold-start primer for 2026-07-25 (MT)

You are the bounds-ledger agent. Read this, then `CLAUDE.md`, then `continuity/items.json`. This lane is a **steward** — a reproducible, re-verified ledger of mathematical records that alarms when a cited record drifts. Lite rail, pre-launch, private repo.

**2026-07-24 was the lane's biggest day: its first outward artifact went out, and its one instrument was found fake and fixed.** Read the four points below before doing anything.

## What changed 2026-07-24 (all pushed to origin/main)

1. **The drift alarm was never armed — fixed.** Both CI check steps piped into `tee`, and the GitHub Actions default shell is `bash -e` with no `pipefail`, so a drift's exit-1 was reported as `tee`'s exit 0: green through drift, no issue ever filed. The lane's single instrument was fake for its first two days. Fixed with `shell: bash` on both piped steps (`e6f6e33`); `reverify.test.mjs` now guards it. **G-1's 30-day green-streak clock RESTARTS 2026-07-24** — the prior greens carried no information. Finding: `docs/findings/2026-07-24-drift-alarm-was-never-armed.md`.

2. **A-3 CLOSED — first outward artifact SENT.** David opened the outward gate and emailed the erdosproblems.com maintainer the correction: erdosproblems.com/36's upper bound 0.380876 is superseded by the curated, paper-cited 0.380868 (bracket `0.379005 < c < 0.380868`). **Sending is NOT acknowledgement** — G-1's external-ack metric is still **0**. The wait is now **W-3**.

3. **A near-miss that became a portfolio rule.** The outward draft claimed "cross-checked against the curated table *and the source-paper abstracts*" — false; none of the three cited abstracts state their numeric bounds (the clause was inherited from the W-2 Haugland work where the abstract *did* carry the value). It survived a 5-angle adversarial review because every angle attacked the *claim*, never the *method sentence*. David caught it at his pre-send read. Now a standing rule (CLAUDE.md) + Angle 6, propagating portfolio-wide: **audit the method sentence as its own angle.**

4. **Pin extension SHIPPED (evening — David unheld it in-pane: "Accidental hold. Please proceed.").** `scripts/extract-pins.mjs` generates 216 pins covering all 109 constants (1b stays hand-pinned as C-1/C-3); `ledger/claims.json` is now 223 claims, 222/222 fetchable HOLD live. Pins assert the **last-listed row** of each bounds table verbatim — NOT "the record" (numeric ranking is defeated by symbolic cells/negatives/asymptotics; a min/max prototype mis-picked 10a/21a/41a and was deleted). **New discipline: after every `--snapshot`, re-run `extract-pins.mjs` and commit** — pins deliberately don't auto-follow the mirror, so a moved row stays BROKEN until re-pinned. `check-claims.mjs` now fetch-caches per URL and counts (not lists) held claims so a real BREAK can't drown. Commit `22fa79c`.

5. **Peak-moment render read — the drift alarm renders garbled.** Fired a real synthetic drift, rendered `finding.txt` through GitHub's own GFM API: the diff's `+/-` direction is destroyed (old/new become a bullet + nested sub-bullet, no removed/added marker), `$…$` numbers render as italic math, two H1s, 12 irrelevant HOLDS below-fold. The instrument fires; the render fails the moment. Finding: `docs/findings/2026-07-24-peak-moment-render.md`.

## Tomorrow's first-action

**Check the overnight re-verification CI — now genuinely meaningful for the first time.**

```
gh run list --limit 3 --json databaseId,status,conclusion,event,createdAt
```

- **Green →** A-2 is accruing *real* green-streak days toward G-1 (day 1 = 2026-07-24). Say so, move on.
- **Red →** the alarm doing its job. It auto-files a GitHub issue. Triage: re-verify the changed value against **primary sources** (paper / Tao's table / Wikipedia — not just the diff), then either (a) legitimate record change → `node scripts/reverify.mjs --snapshot`, review, commit deliberately, or (b) suspicious → investigate. **Never snapshot to silence the alarm.**

## Alarm legibility — SHIPPED 2026-07-24 (David greenlit)

`reverify.yml`'s `Open finding` step now **code-fences the issue body** (the `-/+` drift diff renders verbatim in `<pre><code>` instead of GFM mangling it into nested bullets) and **titles the issue with the drifted file(s)** (P-1: `Drift: constants/1b.md (date)` not a bare date). Verified against GitHub's own GFM API. Commit `03ced9e`. **P-2 (verdict-before-diff) and P-3 (one rolling issue) were deliberately deferred** — polish that only pays on a multi-day real drift; pick them up only if a real drift makes them worth it. Nothing pending here.

## Pending David — W-3 (the only open watch)

**Watch for acknowledgement of the erdosproblems correction.** Two legs, **both David-owned by necessity**: the site 403s bots (claim C-7 stays `manual: true`, reports UNVERIFIED by design — a bot fetch proves nothing), and any reply lands in David's inbox. David pastes/forwards any reply, and re-checks the page by hand periodically. **Do NOT substitute an agent fetch for either leg.** If the page updates to 0.380868, flip C-7's pin in `ledger/claims.json` deliberately and record the acknowledgement (that's G-1's second close condition landing).

## New conventions locked today (in CLAUDE.md)

- **Never hand David an artifact link** (`claude.ai/code/artifact/…` is session-scoped, page-not-found in his browser — cost a re-host today). Lane briefs live at `https://skylarkcreations.com/t/lanes/bounds-ledger`; source is `docs/lane-brief.md`; edit there → post a bus note → orchestrator ports.
- **Audit the method sentence** on any outward artifact (Angle 6, above).
- **`shell: bash` on any piped CI step** (pipefail; else exit codes mask).

## State at a glance

| Item | What | Status |
|---|---|---|
| G-1 | GOAL: steward ≥1 drifting record inventory | open — real green-streak clock day 1 = 2026-07-24; first outward artifact sent, ack still 0 |
| A-1 | Reconcile min-overlap discrepancy (customer-zero) | closed 2026-07-22 |
| A-2 | Re-verification CI | open — live + **now genuinely armed**; legibility fix shipped (`03ced9e`); claim pins extended to all 109 constants (`22fa79c`) |
| A-3 | Report erdosproblems.com/36 staleness | **closed 2026-07-24 — correction sent by David** |
| W-2 | Haugland final-digit | closed 2026-07-23 — rounding convention |
| W-3 | Watch for the correction's acknowledgement | **open — David-owned legs; G-1's close condition** |

## What NOT to touch

- **Don't hand-edit `ledger/`** — machine mirror, refresh only via `--snapshot`.
- **Don't post self-ratings** — lite rail has no self-rating/audit contract. Write the daily report, run the linters, skip `/daily-close`.
- **Pin extension is DONE** (7/24 eve, David unheld it) — don't re-do it, and don't "upgrade" generated pins to record-row claims (see CLAUDE.md: listing position, never "the record"). After any `--snapshot`, re-run `extract-pins.mjs`.
- **Don't re-fetch erdosproblems.com to "confirm" anything** — it 403s bots; a 403 proves nothing. C-7 is David-hand-verified by design.

## Session close-out — 22:15Z (7/24 eve session; facts already folded into the sections above)

- **Ships:** `22fa79c` pin extension (216 generated pins, 223 claims total, 222/222 fetchable HOLD) + `e07c7ee` docs/discipline. **CI:** reverify: success @ `e07c7ee`.
- **Supersedes** the "pin extension is held" posture wherever an older doc still says it — David unheld it in-pane 7/24 eve.
- **Tomorrow's first-action: unchanged** (check the overnight 09:17 UTC cron — first *scheduled* green of the real streak, now exercising all 223 claims).
- **Don't touch:** no record-ranking rebuild (listing-position pins are a deliberate call, see CLAUDE.md); P-2/P-3 stay deferred; W-3 stays David-owned.
- **For the next /daily:** report-shape lint FAILs the lane's numbered prelaunch format (BLUF-not-first, no State Appendix). Pre-existing, informational until 2026-06-25-style tightening applies — decide whether to adopt the canonical H2 set or record the format as an intentional lite-rail deviation.
- **Evangelism read (pre-launch):** core problem = "is the number I'm about to cite current?"; would-tell moment = the alarm catching a stale cite before a citer embarrasses themselves; external-ack metric 0 (W-3); no users/retention instrument yet by design — the public flip is the gate, and it's David-gated.
