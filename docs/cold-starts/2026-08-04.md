# Cold-start primer for 2026-08-04 (MT)

Multi-session primer — each /close-session run appends a new section below.

## Session close-out — 19:05Z (2026-08-03, ~13:05 MT)

You are the bounds-ledger agent. Read this, then `AGENTS.md` + `CLAUDE.md`, then `continuity/items.json`.
Steward of drifting mathematical records; pre-launch, private repo, **PACED rail**, **no self-rating**.

### State

**229 claims / 227 hold / 0 broken / 2 UNVERIFIED (by design)** over a **111-file** mirror, clean at
upstream **`dee1660`**. **G-1 — the steward-credibility goal — is at green-streak day 11 of 30**
(day 1 = 7/24; a green 8/04 run makes it 12; ends ~8/22). North star (externally-acknowledged
corrections): **still 0**.

**CI:** `reverify: completed/success @ 9d5b941` — green at HEAD, tree clean, everything pushed.
**Open GitHub issues: 0.** Open continuity items: 9 (A-12 closed today).

### Today's load-bearing ships (4 commits, all pushed)

- **`13af4c4`** — **README-as-product**, the paced rail's launch increment. Rewritten for a stranger:
  what it is, the founding discrepancy, the four drifts each linked to its resolving commit, live
  counts, how to run it, four load-bearing rules, limits stated plainly. Zero internal jargon
  (grep-verified). **This completes the flip package's artifacts — 4 of 4.**
- **`9d5b941`** — **A-12 CLOSED on a credentialed probe.** Orchestrator ported the three missing
  blocks; our probe (with `CC_PROMPTS_PIN`) read `brief: in sync — all 4 dated block(s) present`,
  exit 0. First green read since the checker shipped 7/31. Issue #6 closed. **The 8/07 escalation is
  moot — do NOT re-arm it.**
- **`2210deb`** — `AGENTS.md` canonicalized (fleet convention). Cross-vendor invariants at repo root;
  `CLAUDE.md` imports it via `@AGENTS.md`. Commands block **moved**, not duplicated.
- **`3572d6e`** — daily report (4/4 lint PASS) + the Monday forced-decision pass.

### Tomorrow's first-action

**Steward cadence first**, as always: read the overnight run's **LOG, not the badge**
(`gh run list --limit 3`, `gh run view <id> --log`). Expect `No drift. 111 files match upstream
dee1660…`, `229 claim(s): 227 hold, 0 broken/unreachable, 2 unverified (manual)`, two expected `403`
advisory lines (C-7/C-9), and the brief job logging *"not runnable from CI … Not filing an issue."*
Green → streak day 12. Then `npm run check` locally **with `CC_PROMPTS_PIN` set**. If red: verify →
`--snapshot` → `extract-pins.mjs` → commit, in that order.

**Then the launch increment: the PACKAGE-LEVEL adversarial review.** This is the last gate before
David's flip decision, and it is *not* the sum of the per-artifact reviews — those each asked "is
this artifact right?"; this one asks what a stranger actually experiences landing on the repo cold.
Does the README's opening survive someone who has never heard of this? Do the four rules read as
discipline or as excuse-making? Does anything contradict anything else? **Run it treating my own
recent prose as an unverified source** — see the warning below; that is not boilerplate today.

Then it goes to David at streak day 14 (**2026-08-06**) as A-13 proposed.

### The warning that earned its place today

**A fabricated citation nearly shipped in the README.** I wrote arXiv `2601.11162` for TTT-Discover,
invented from a grep truncated mid-number; the real ID is **`2601.16175`**. It was caught only by
re-running the grep rather than trusting a sentence I had written a minute earlier. In a repo whose
thesis is *cited-not-checked*, that would have been the worst thing to publish. Two further defects
in the same draft were **method** overclaims — the class `CLAUDE.md` warns about — and both survived
until I attacked the sentence rather than the value. **Verify by executing extends to text you just
wrote. Recency feels like verification and is not.**

### Pending decision-blocked items

- **A-13 public flip — David's call, proposed for 2026-08-06.** Artifacts complete; package-level
  review still owed (that is ours, not his). Nothing to ask him until the review is done.
- **W-3** — the ack watch on the 7/24 email. **Not ours to force, monitor only.** Both legs read
  clean today (page unchanged since 23 January 2026).

### What NOT to touch

- **Do not re-propose the public /36 comment.** David decided on 8/02 that having sent the email, he did not want a comment added on top of it. Not wording, not venue, not timing.
- **Do not re-automate C-7/C-9** — the erdosproblems 403 is IP-dependent; a local 200 proves nothing
  about CI.
- **Do not put `CC_PROMPTS_PIN` in repo secrets.** The 7/31 "check-brief joins CI once the page reads
  green" clause was **retired 8/03, the day it first fired** — CI cannot run that check at all, and
  satisfying the clause would require exactly this forbidden move. The strikethrough and reason are
  in `CLAUDE.md`; don't restore the clause.
- **Do not re-arm A-12's 8/07 escalation.** Closed on evidence.
- **Do not re-run the history sweep as "the increment"** — done 8/02. **Do** re-run it immediately
  before the flip click; the clean claim is only true as of the commit it ran against.
- **Do not treat A-13's first-PR targets as live** — #129/#130/#135 all merged 8/02. New targets needed.

### Watch items maturing

- **W-5** — the mirror blind class: upstream's `README.md` is not mirrored, and it is where upstream
  states which records it stands behind (`dee1660` asterisked two as not-yet-peer-reviewed and our
  alarm was silent). Read its `onTrigger` first; the right shape is likely A-6's (pin specific
  cells), not a byte mirror of a generated 421KB file that would go permanently red.
- **A-7 R6** — GitHub push protection. Deliberately not carded: the flip makes it free. Re-arm only
  if the flip is declined or unresolved by ~2026-08-16.
- **A-11** — pin-a-negative for the two structured sources still open.

### Active-user read (requested by the 8/03 evening-close dispatch, for tomorrow's growth directive)

**Honest answer: zero users, zero funnel, and no instrument — by structure, not by neglect.**
This lane has no product surface, no signup, no analytics. The repo is **private**; the David-facing
brief at `/t/lanes/bounds-ledger` sits behind a Supabase sign-in wall, is noindex+nofollow,
`/t/`-robots-disallowed and unlinked. **Nobody outside can currently see this project at all.** The
only outward trace in its entire history is David's one email of 24 July, still unanswered.

So the honest analogue of "active users" here is **whether anyone outside relies on the ledger**, and
that number is **0** — not because retention is poor but because reach is structurally zero.

**The implication for a growth directive: standing up a retention/active-user instrument is NOT the
move, and building one would be measuring an empty room.** The move is the **public flip** (A-13),
which is the single act that converts a private instrument into something a stranger can rely on.
It is one adversarial review away from David's decision. If the flip is declined or deferred, the
honest fallback is not more instrumentation either — it is picking a different route to external
acknowledgement (a correction-shaped PR to `teorth/optimizationproblems`, needing new targets since
the 8/02 merges; or A-6's scale-the-catches pipeline, currently permission-starved).

The would-tell-a-friend moment — *a drift alarm that catches a real record change before the citing
surface updates* — **demonstrably exists** (four drifts, one of them a genuine record improvement:
`5/3 → 7/√17`, `2.625622 → 2.6273856`, `>6.5143 → >6.5218`). The distance to it mattering is
entirely visibility, not capability.

### Tier 0 / binding constraint

Green streak day 11/30, ledger clean at `dee1660`. Posture: **suspect the instrument before the
record — and suspect its diagnosis too.** Today added a third turn of the same screw: **suspect your
own fresh prose.** The instrument was right all day; the thing that was nearly wrong was me.

## Session close-out — 06:07Z (2026-08-04, ~00:07 MT)

**Read the 19:05Z block above first — this one supersedes nothing in it.** Two late-night
orchestrator asks landed after that close-out, both fleet-wide hygiene, neither a rail phase.
**Today's first-action is unchanged: steward cadence, then the package-level adversarial review.**
No ledger state moved tonight — no drift run, no snapshot, no claims touched.

### What landed (2 commits, doc-only, both pushed)

- **`c2517fb`** — voice pass on `docs/lane-brief.md` against the new fleet standard
  (`skylark-site/docs/skylark-voice-standard.md` + `check-david-voice.mjs`). 52 em dashes to spaced
  short dashes, one status glyph dropped.
- **`b59846a`** — `CLAUDE.md` records which surfaces get that lint and why, so nobody re-derives it.

**CI:** `reverify: completed/success @ b59846a`. Tree clean, everything pushed. Bus:
`bda5f103` (voice), `c2907692` (memory-orphans).

### Two carve-outs — do NOT "fix" these

Both are reported upstream and deliberately left standing:

1. **The quoted email keeps its em dashes.** The `## What we said` blockquote reproduces, verbatim,
   what David sent the maintainer on 24 July. The lint does not exempt blockquotes. Rewriting it to
   satisfy a style rule would make the brief **misquote an outward artifact** — a correctness bug in
   this lane, not a style one. Same instinct as the mirror: byte-fidelity to what actually went out.
2. **`supersedes` flags as jargon and is not.** It is on the fleet list glossed as the bus flag
   ("replaces an earlier message"); our sentence is ordinary English about a number.

### What tonight taught about our own detector

**`check-brief.mjs` asserts block PRESENCE, never content fidelity.** I rewrote two block *headings*
in the source expecting to break its matching; it still read `in sync — all 4 dated block(s)
present`, exit 0, because it matches on the block DATE. Correct scope for a checker watching a page
it does not control — but the consequence is real: **after any `lane-brief.md` edit the hosted page
is stale in WORDING until the orchestrator re-ports.** Say that plainly rather than implying the two
match. A-12 stays closed (no block is missing) — recorded as its `note5`.

### Memory hygiene — clean, and the opposite problem

`check-memory-integrity.mjs` (at `caceeaa2`, the wiki-link fix): **1 file, 0 orphans, 0 orphaned
feedback files**, index 4 lines / 1113 bytes against a 200-line / 24985-byte cap. Nothing promoted,
nothing deleted. Worth knowing: this lane cannot have the fleet's orphan problem because it has one
13.2KB memory file rather than many small ones — the inverse risk is a rule going stale mid-blob
with nothing in the index to show it. Not acted on; flagged if a per-file convention arrives.
