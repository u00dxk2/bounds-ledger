# Cold-start primer for 2026-08-18

Multi-session primer — each close-out appends a section below.

## Session close-out — 2026-08-17 (P5 hygiene)

Paced rail, day 16 closed. **Fifteen commits, all pushed, main green at `836aa96`** and green again at every push after transport recovered. The day had one real defect, one false accusation I made against myself, and a GitHub outage underneath all of it.

### Tomorrow's first-action

```bash
cd C:/dev/skylark/bounds-ledger && git rev-parse --short HEAD origin/main && npm run check && node scripts/reverify.mjs --check && node scripts/check-claims.mjs
```

The steward cadence plus the credential-free visitor path. **The `git rev-parse` is new and load-bearing** — see the CI gotcha below.

Then: **check whether the cross-family review lane has replied on PR #21.** If it has, merge **#21 before #22** — that order is not cosmetic (below).

### The two held PRs — merge order is load-bearing

- **PR #21** `alarm-title-honesty` — fixes the alarm titling a transport failure as a record movement. Held for the review lane (request `8ecb03e8`).
- **PR #22** `guard-catch-count-v2` — asserts the README's catch count against mirror history. Reviewed 8/16, runner-validated 8/17.

Both merge cleanly (`git merge-tree --write-tree main <branch>` → 0 conflicts) and **both are CI-green on their merge commits**. Tracked as **A-17**, which now carries an `onTrigger` spelling out the whole procedure.

**Why #21 first:** every push to main triggers a network run, and until #21 lands, main's alarm titles an unreachable source as "records moved." It did that **seven times today**. Landing #22 first pushes a run under the dishonest alarm.

### What broke, and what I got wrong

**The defect (real, fixed, guarded):** the alarm grouped `UNREACHABLE` (never read the source) with `BROKEN` (read it, the value moved), so a rate-limited run announced `Drift: claims C-1,pin:12a:U …` — six records asserted moved, zero actually. This is **A-5's July defect surviving where A-5's own fix could not reach**, because `UNREACHABLE` matched before the `^error:` test. Finding: `docs/findings/2026-08-17-the-alarm-called-a-rate-limit-a-record-movement.md`.

**My false attribution (retracted — read this before repeating it):** I wrote "I caused the rate limit" into six places. **GitHub was in a critical outage from 2026-08-17T13:40:03.629Z** (githubstatus.com incident API, read directly); my first dispatch was 14:32Z, **fifty-two minutes later**. Disconfirming evidence I already held: a local ~450-fetch check ran **fully green at 14:28Z**, 48 min into the outage; a later *local* check failed though my dispatches were runner-side; and the errors included **502 and 503**, which volume does not produce. Retracted in the finding, `A-5 note4/note5`, `CLAUDE.md`, the report, and two public issue comments. **Self-blame felt like rigour and was just another unverified attribution — the kind nobody challenges you for.**

### Load-bearing gotchas earned today

- **`check-ci-status.mjs` reads LOCAL HEAD.** It printed a confident **GREEN** while I was on a feature branch, describing that branch's PR run while main was red. It failed in the *green* direction, which nobody double-checks. Always `git rev-parse --short HEAD origin/main` first; they must match. Documented in `docs/daily-config.md`.
- **A feature branch now DOES get a CI run** — b1 shipped today (`pull_request: branches: [main]`, `3f5bdfb`). Before it, a branch got **zero** runs however often you pushed, which is why `guard-catch-count` sat unvalidated for a day. Bonus: a `pull_request` run checks out the **merge commit**, not the branch tip, so it validates the sha that will actually land.
- **`gh api .../issues` counts PRs as issues** and lags ~10s after a close. Filter `.pull_request == null`; take two consistent reads.
- **Never restore a negative control with `git checkout --` while the fix is uncommitted** — it discarded the fix under test today. Commit or stash first, or restore with node.

### G-1 — the north star, and the date that matters

**Both legs are on course to be met on Saturday 2026-08-22.** Acknowledgement leg: **met since 2026-08-11** (Terence Tao merged `teorth/optimizationproblems#141`). Streak leg: **day 25 of 30**.

Today the closeWhen was **reworded** (leg (a) only; prior wording kept verbatim in `closeWhenPrevious`) because it said "30-day green CI" and that is not what has been counted — the 08-01 and 08-14 scheduled runs failed on *genuine drift* and the streak was correctly never reset. And a **`streakDayRule` was pre-registered before the outcome is known**: a day counts only if its scheduled run **reached a verdict**; a run that could not fetch **pauses the clock**, neither counting nor resetting. That rule delays our own goal, which is exactly why it was written today rather than on 08-22 with an incentive to count generously.

**Close evidence is PRE-STAGED** in `G-1.signalPrestaged` — the 25-run ledger, both reds explained, and the exact re-run command. The 08-22 read should be a decision, not a dig. **Closing G-1 is David's call, never a self-approval.**

### Do NOT touch

- **A run must stay RED when a cited source is unreachable.** After a day of transport failures the tempting fix is to make 429 non-fatal. That would launder an unverifiable result into a pass — the one thing this repo cannot do and remain worth anything. Only the *title* was ever wrong.
- **A-16** (upstream's Martinet row cites a witness whose arithmetic yields the previous record) — gate 2 is David's. Do not raise, do not PR, do not contact upstream.
- **A-15** (Mathstodon) — dormant by instruction. Do not ask.

### The pattern this week keeps producing

**Four stale blockers in two days**, all text written while a condition was live, all outliving it, all still reading as authoritative: A-7's "repo is private, needs paid add-ons" (fixed 8/16); **F-2's "needs the public-repo flip — David-gated"** (the flip happened 2026-08-08 — fixed today); **W-3's title claiming to be G-1's close condition** (satisfied 8/11 via a different channel — fixed today); **A-2's closeWhen** describing build work finished in July (fixed today). **All four found by reading, not by any check**, and three sat in `items.json`. Rule named in `key-user-flows.md`: **closing a gate must include a sweep for the text that cited it.**

### State

Mirror **113 files @ upstream `e70b4a4`** (an upstream `teorth/optimizationproblems` sha; it does not resolve in this repo). Ledger **233 claims — 231 hold, 0 broken, 2 UNVERIFIED by design**. `npm test` exit 0, 13 self-tests. **True open issues: 0** (eleven closed today: five false `Drift:` from transport, five resolved drifts nobody had closed, one I filed by accident). Catches indicator: current partial week 0, **0 completed dry weeks**. Candidate-correction queue depth **1 (A-16)** — new today, printed beside the movement rate. Arrivals: **3 unique viewer-days** since the flip, unreadable until 2026-11-06.

---

## Session close-out — 2026-08-17 (/close-session)

**Customer impact today: NONE.** Nothing deploys from this repo (`AGENTS.md`: "Nothing deploys — doc/script-only"), there are no users of a running service, and the only outward-facing surface is the public repo itself — which stayed correct all day. The seven false `Drift:` issues were visible on a public issue tracker for minutes to hours before being closed with evidence, which is the closest thing to impact that occurred: a visitor reading them mid-day would have seen our alarm claiming records moved when none had.

### Counts, each with the command that produced it

| Figure | Value | Command |
|---|---|---|
| Self-tests | **13/13 pass** | `npm test` (exit 0) |
| Report linters | **8/8 clean** | `node ../skylark-site/scripts/check-report-all.mjs docs/daily/2026-08-17-prelaunch.md` (exit 0) |
| Dead doc references | **0** across 61 paths | `node ../skylark-site/scripts/check-doc-references.mjs` (exit 0) |
| Cold-readability findings | **0** | `node ../skylark-site/scripts/check-cold-readability.mjs docs/daily/2026-08-17-prelaunch.md --soft` (exit 0) |
| Open GitHub issues | **0** | `gh api "repos/u00dxk2/bounds-ledger/issues?state=open&per_page=30" --jq '[.[]\|select(.pull_request==null)]\|length'` — note the `.pull_request == null` filter; without it PRs inflate the count |
| Uncarded decisions | **0** | `node ../skylark-site/scripts/check-unpromoted-decisions.mjs --soft` (exit 0; there is no `--project` flag — passing one exits GUARD DARK) |
| Dependabot alerts | **0** | `gh api repos/u00dxk2/bounds-ledger/dependabot/alerts --jq length` |
| Ledger | **113 files @ upstream `e70b4a4`; 233 claims, 231 hold, 0 broken, 2 UNVERIFIED by design** | `npm run check` |

### Engineering-zero: structurally n/a here, and here is the evidence

Not a flagged lane, and it cannot be: **`package.json` has `dependencies: {}` and `devDependencies: {}`** — an empty dependency set, so there is nothing for Dependabot to find (0 alerts). There is **no Sentry project** for this repo, which is why today's report frontmatter carries `sentry_open_p1: null` / `sentry_open_p2: null` rather than `0` — an absent instrument renders null, never a count. If a future session is asked for an engineering-zero re-triage, the honest answer is that both counters are structurally zero, not swept to zero.

### The GitHub outage, separated from our own process

**GitHub was in a critical outage 13:40:03Z–~16:26Z** (githubstatus.com incident API, read directly). What was theirs: six failed CI runs, workflow wall-times of 25–40 min against a 1–2 min baseline, HTTP 429/502/503 on the mirror sources, and `gh pr list` reporting `mergeable = UNKNOWN` while local `git merge-tree` returned clean for the same branches.

What was **ours, and would have happened on a quiet day**: the alarm titling `UNREACHABLE` as `Drift:` (a latent defect the outage merely exposed), my false "I caused the rate limit" attribution, and the four stale blockers. Do not let the outage absorb those three.

**Deploys are flowing again and push ≠ live still applies** — irrelevant here, since nothing deploys, but recorded so a future session does not go looking for a deploy row that never existed for this repo.

### Waker / listener — this pane did NOT hit the resume gap

There was a session resume at **18:59Z**. The **listener survived it**: `check-listener-process.mjs` reports `listener LIVE — pid 33596`, and 33596 is the same PID armed at 13:39Z, so it ran ~7 hours across the resume. **No `/listen` restart was needed.** The fixes in play are `a1deb8f0` + `e19946bc`.

**The waker exited three times, all legitimate** — none was the insta-exit churn: (1) `armed wake is 10min PAST DUE with no turn since (expired-unfired)`; (2) and (3) `actionable loop-death signal (unconsumed=1)`, each ~34 min after launch. Launch 2 logged `cleared a wake receipt that expired 56min before this launch` — **`e19946bc` working, observed live**; pre-fix that stale receipt is exactly what would have insta-exited it.

**If tomorrow's session resumes:** run `node ../skylark-site/scripts/check-listener-process.mjs` first. Exit 0 means the listener survived and `/listen` must NOT be re-run; only re-arm the waker (the SessionStart hook says so explicitly).

### Continuity hygiene done at close

Three closed items (**A-10, A-12, A-13**) carried no `closedAt`. Backfilled by **deriving** each from git — the author date of the first commit in which the item's status reads `closed` — and each carries a `closedAtProvenance` saying the date is derived and accurate to the day of the *commit*, which may lag the decision. Method self-checked: A-13 derived to **2026-08-08**, independently matching the known public-flip date. No duplicate ids (24 items); every open item has a `closeWhen`; five `onTrigger`s added earlier today.
