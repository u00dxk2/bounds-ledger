<!-- GENERATED — regenerating REPLACES everything outside the hand: markers, so put prose inside them. Provenance stamp at the foot of this block. -->

<!-- hand:begin banner -->
> **THIS LANE IS LAUNCHED** — David ruled it 2026-08-27. Instruments a launched app has and this
> one does not — retention, active users, Sentry, revenue — are answered **"not an instrument on
> this lane, by design"**, never with a zero. The real reads are `npm run reports` (`G-4`'s primary
> indicator) and `npm run traffic`.
>
> **The report path stays `docs/daily/<date>-prelaunch.md`.** Historical filename, not a lifecycle
> claim. Do not helpfully fix it.
>
> **THE MATHSTODON POST IS CLOSED, UNSENT, AND MUST NOT BE RE-PROPOSED.** Nothing is owed there.
>
> **`npm run verify` WILL EXIT 1, AND IT IS NOT YOURS TO FIX.** The hosted brief is missing the
> 31 August block; the port is the orchestrator's. Posted as `c507aab3`. Everything else was green
> all day 09-01. Confirm rather than assume — the leg names itself in the output:
> ```
> npm run verify > tmp/verify-out.txt 2>&1
> ```
> If a DIFFERENT leg fails, that is a real finding and this line does not cover it.
>
> **TWO GATES ARE DUE TODAY, BOTH PRE-STAGED, AND BOTH READS ARE ALREADY RUN.** Read the notes
> before running anything — the command is here, not just the pointer:
> ```
> grep -n "prestage2026_09_01" continuity/items.json
> ```
> `A-33` (cross-reference our own upstream reports against drift) and `A-34` (candidates for a
> second surface — **David's call, never self-approved; the row owes him CANDIDATES, not a
> decision**).
>
> **Both gates' `readCommand` separators were converted to `#` at pre-stage**, so unlike yesterday
> they will actually run. `A-34`'s `grep -c 'ledger/'` leg was DROPPED as non-discriminating: it
> counts prose mentions and reads the same at one surface or three.
>
> **`A-20` IS DECIDED — DO NOT REOPEN IT.** Its pre-registered discriminator resolved to SHIP on
> 09-01 and is spent. The implementation is built, tested and pushed on branch
> `a20-fetch-retry-backoff` at `8a4c2b8`, unmerged pending the cross-family review lane. Before
> spending a runner on it, check it still merges:
> ```
> git merge-tree --write-tree main a20-fetch-retry-backoff
> ```
>
> **A `readCommand` is written `command # prose`, never `command — prose`.** The runner executes
> the whole field with `#` comments stripped, so an em dash buries the command inside the prose.
> Convert a row's separator **when its gate comes due** — or, better, when you pre-stage it.
>
> **`CLAUDE.md` + `AGENTS.md` have ~37 bytes of headroom** against the 60,000 fleet budget.
> Anything you add must pay for itself. Measure, never estimate:
> ```
> node ../skylark-site/scripts/check-claude-md-sizes.mjs --stamp
> ```
>
> **`git rev-parse --short HEAD origin/main` exits 128.** Two revisions plus `--short`. Drop
> `--short`.
<!-- hand:end banner -->

### Primer — bounds-ledger, 2026-09-02

What ONLY this primer holds comes first; the runnable **First action** sits at the
foot of this block, below the reads — do not skip to it.

#### Owed gates — 2 due on/before 2026-09-02

_Ledger read at 2026-09-01T15:21:29.395Z by `cc-due-gates.collectDue` — the same resolver `check-due-gates-dispositioned` uses, so any difference is TIMING, not two answers. Rows filed or re-dated after that are NOT READ at generation — the close-out commit lands after this, so the COUNT above is stale by construction and this list cannot claim completeness. The list below, not the count, is the artifact; re-derive both before acting: `node ../skylark-site/scripts/check-due-gates-dispositioned.mjs --print --today=2026-09-02`._

Unfiltered as of the read time above — nothing was curated out of what the read could
see; a row absent here may simply have been filed after it ran.

- `A-33` (expectedSignalBy=2026-09-02, open) — A change to a file we have filed an upstream report against is a CANDIDATE CAUSAL EVENT, n
  - read: `gh issue list --repo teorth/optimizationproblems --author u00dxk2 --state all --limit 20 --json number,state,closedAt,title # SEPARATOR CONVERTED 2026-09-01 at pre-stage, em dash to hash, so the runner extracts the command rather than 900 bytes of prose. Literal output 2026-09-01, re-run at pre-stage: ONE row, 150 CLOSED 2026-08-23T17:15:35Z constants/87a.md: the current-record row quotes Martin's degree-8 discriminant, not the degree-12 construction that gives 857.5662. That single row IS the gap: it was true for three days and nothing surfaced it. POSITIVE CONTROL that the query can see our artifacts at all: it must return 150; an empty list means the author filter or the repo is wrong, not that we have never reported anything. NOTE THE TRAP THIS FIELD ALREADY AVOIDED: an equivalent gh pr list returns 141 from 2026-08-05 and is BLIND to 150, because our most recent outward artifact is an ISSUE; that miswrite was caught on W-8 the same day by running the command before writing it down.`
- `A-34` (expectedSignalBy=2026-09-02, open) — David asked where else this ledger can have the kind of impact G-3 just produced — bring h
  - read: `node scripts/reverify.mjs --check # SEPARATOR CONVERTED 2026-09-01 at pre-stage, em dash to hash. The grep leg was DROPPED from the executed command and its result recorded here instead: grep -c 'ledger/' continuity/items.json returns 21 on 2026-09-01, and the count is prose-inflated rather than structural, so it never discriminated one surface from two. The command that answers this row is the check, whose first line names the mirrored surface and its file count. Literal output 2026-09-01 14:30Z: No drift. 113 files match upstream commit 3a1491040076a3f67a52719e366f5e6b139e5c24 (an upstream teorth/optimizationproblems commit; it does not resolve in this repo -- git cat-file -t on its short form reports Not a valid object name, while the same command on a local commit returns commit) -- ONE surface. A second adopted surface shows up as a second mirror directory under ledger/ and a second line here. POSITIVE CONTROL that the check reads a real mirror rather than reporting an empty set: the file count must be non-zero and match the manifest; 113 today.`

#### What shipped since 24 hours ago

_Read at 2026-09-01T15:21:29.395Z; commits landing after that — the close-out especially — are not here. Re-derive: `git log --oneline --since='24 hours ago'`._

- commit `d244361` — The day's report, and A-20 re-dated because its gate fired rather than slipped
- commit `7ba4f3c` — Searchable, never displayed: the review refuted the visible half before it published
- commit `3145ed9` — The page asks if your number is still current and could not be searched by a number
- commit `a3dc9a5` — A-20's discriminator resolves to SHIP, and the instrument it named was blind to its own case
- commit `78c8e1d` — A-18 closes on the branch nobody was reading, and its gate gave three answers
- commit `348593f` — The one leg we cannot fix was masking the one leg we can
- commit `f219f55` — Close-out 2026-08-31: the P4 row that read surfaced+executed and never arrived, with its ids
- commit `fd7732d` — Hygiene 2026-08-31: tomorrow primer, today's five commits linked, and two reads a cold agent would otherwise redo
- commit `5f1b6ca` — The days report: two unreadable links on the reader's own verification path
- commit `c289a15` — A-32 closes: the brief said the metric was zero, and it has been two since 23 August
- commit `f96ba43` — P4 pre-stage: A-18 can close on evidence tomorrow, and its readCommand cannot say so
- commit `b3e7469` — Section B: A-32 was the last row prescribing the compound AGENTS.md dropped this morning
- commit `5011af8` — Both document links on every row now render; before today they were a wall of pipes

**Yesterday's report:** `docs/daily/2026-09-01-prelaunch.md` (coverage 2026-09-01, the prior day). No findings fence declared in it.

**Today's report (owed):** write `docs/daily/2026-09-02-prelaunch.md` — the artifact of record for service day 2026-09-02. Check it exists at close: `node ../skylark-site/scripts/check-daily-report-exists.mjs --date 2026-09-02` (it sweeps the fleet and applies each lane's coverageOffset; it takes no --project).

#### State — as of 2026-09-01T15:21:29.395Z (re-run the release command; never trust the value)

- **ledger**: 51 item(s): 35 closed · 16 open · as of 2026-09-01T15:21:29.395Z — release: `node ../skylark-site/scripts/gen-primer-first-action.mjs --repo . --target-date 2026-09-02 --json`

### First action — bounds-ledger, 2026-09-02

Every line in the block below is a COMMAND, not a claim. A command re-reads at the
moment you run it and cannot go stale; a state line written last night can.

```bash
node ../skylark-site/scripts/update-david-board.mjs --list --json --full-ids --project bounds-ledger
git rev-parse HEAD origin/main
git rev-list --count origin/main...HEAD
npm run verify > tmp/verify-out.txt 2>&1
node ../skylark-site/scripts/check-ci-status.mjs --workflow reverify.yml
```

Morning release commands — deliberately NOT READ at generation (a value written
last night would already be stale); run each, never trust a remembered one:
- `git -C . log -1 --format=%h%x20%s` — HEAD
- `git -C . status --porcelain` — working tree
- `node ../skylark-site/scripts/check-ci-status.mjs --workflow reverify.yml` — CI
- **deploy read: NOT AVAILABLE** — no read-only deploy-state script in this lane's package.json (looked for ops:deploy-state, deploy-state, check-deploy, render-deploys, deploy:status, daily-snapshot). The fleet's check-deploy-state.mjs is not a substitute — it requires --fix <sha> --deploy <sha> and exits 1 without them

<!-- hand:begin narrative -->
## Session close-out — 2026-09-01

### The one thing to understand about today

The public page asks "is the number you cited still current?" and could not be searched by a
number. It can now — paste `6.5143` or `2.288513` and you land on the constant. That is the ship.

**The half that did not ship is the more important half.** The same change also DISPLAYED the
previously-pinned value as *"our pinned row read X until then"*, and an adversarial review killed
it in the twenty minutes between commit and push. On Brun's constant, upstream **appended** a row
conditional on the Generalised Riemann Hypothesis (`2.1594`, `[D2025]`) *below* the unconditional
`2.288513` (`[PT2018]`). Both are still in the table — `constants/81a.md:13-14` — and upstream
says in terms that they are not comparable. A reader who cited `2.288513` would have been told
their still-correct number was superseded.

**The lesson is not about wording, and the commit body that argued it was is the evidence.** That
body defended the phrasing on the grounds that "our pinned row read X" is a statement about our
pin rather than about the record. It is, and it does not help: **a current value, a "value
changed" date and a previous value form a from-to pair, and a from-to pair is a movement claim
whatever noun sits in the sentence.** `8a4192a` refused this exact inference on this exact row on
2026-08-23; twelve days later it came back as a feature, past an author who quoted that rule in
the same commit. Checked across all eight rows that rendered a previous value: **eight of eight
were appends or restructures, not replacements.**

The fix keeps the whole product win: **searchable, never displayed.** Search asserts nothing.

### Three things a cold agent will otherwise re-derive

- **`gh run list` reports only the LATEST attempt.** A run that failed and was re-run to green
  vanishes from `--status failure` and reads `success`. Any failure count taken from it
  undercounts by exactly the re-run population — which is the population a transient-failure
  question is about. This is what resolved `A-20`: issue #25's originating run was
  `32157521934` attempt 1, `failure`, invisible for exactly that reason. Read attempts:
  `gh api repos/OWNER/REPO/actions/runs/<id>/attempts/<n> --jq .conclusion`. Finding:
  `docs/findings/2026-09-01-the-failure-count-could-not-see-its-own-population.md`.
- **GitHub Pages builds per PUSH, not per commit** — measured today, correcting `AGENTS.md`. A
  two-commit push produced ONE build row, for the tip. Positive control: rows exist for
  `7ba4f3c`, `a3dc9a5` and `348593f`, and none for `3145ed9`. This is why the refuted page was
  never served, and it means a multi-commit push cannot be assumed to publish its intermediates.
- **A hand-written predicate over `expectedSignalBy` says "3 date-less open rows"; the canonical
  resolver says 0 gates due and 0 rows without a command.** `A-2`, `A-9` and `W-4` are deliberately
  standing rows. Read the resolver, not the raw field.

### What NOT to touch

- **The `npm run verify` receipt gate.** It binds *the gate was green* to a **sha** rather than to
  memory, and the doc-only-ancestor carve-out means the ~450-request cost is paid once per code
  change, not once per commit. It earned it today by recording `exitCode 1` at `f219f55` instead
  of letting a remembered "green yesterday" carry into a day that opened red.
- **The self-contained-page assertion in `render-site.mjs`.** Still standing, still right, and
  today was its stress test.

### Tier-0 status

**`G-4` = 0 arrivals against 3 unique viewers in 14 days**, reported as arrivals over viewers and
never as a bare zero. The threshold was pre-committed on 08-31: a zero carries product information
only at **≥30 unique viewers / 14 days**. Measured, not assumed — 26 issues fetched, all 26
attributed to us, parts reconcile. **Consecutive instrument-facing days: 5.**

**The open recommendation is to give that count a threshold before it is read again.** It stands at
5 and nothing happens at any value; a counter that cannot change a decision is what this lane
refuses everywhere else. Proposed: 7 consecutive instrument-facing days with `npm run catches` at 0
numeric movements over the same window raises a David card for adopting a second surface — which
is also what `A-34` is about, so decide the two together rather than twice.

### Watch items maturing soon

`A-33` and `A-34` — **today**, both pre-staged above. `A-7`/`A-9` — 09-03. `A-2` — 09-07.
`A-20` — 09-08 (forced review OF THE REVIEW; the decision is made). `W-3` — 09-24.
<!-- hand:end narrative -->

<!-- generated by gen-primer-first-action.mjs (skylark-site) @ commit eb0dda96 · lane bounds-ledger · for 2026-09-02 · at 2026-09-01T15:21:29.395Z -->
<!-- reading this block: a generated line that is wrong in a way you can see FIXED in skylark-site's git log after the commit above is STALE, not a finding — regenerate before filing it. `git -C ../skylark-site log --oneline eb0dda96..HEAD -- scripts/gen-primer-first-action.mjs src/lib/cc-primer-commands.mjs src/lib/cc-due-gates.mjs src/lib/cc-report-artifact.mjs src/lib/cc-hand-edit-merge.mjs src/lib/cc-cold-readability.mjs` -->
