<!-- GENERATED — regenerating REPLACES everything outside the hand: markers, so put prose inside them. Provenance stamp at the foot of this block. -->

<!-- hand:begin banner -->
> **PULL, THEN START THE GATE, BEFORE READING THIS** — three minutes, all network:
> ```
> git pull --ff-only
> npm run verify > tmp/verify-out.txt 2>&1
> ```
> Expect **exit 0**. Any non-zero code is yours; read `tmp/.verify-receipt.json`'s `failedGates`,
> never infer the leg.
>
> **DO NOT TRUST ANY ID LIST IN THIS BANNER.** Run the resolver:
> `node scripts/sky.mjs check-due-gates-dispositioned.mjs --print --today=2026-09-12`.
>
> **GATE ORDER, WRITTEN ONCE, HERE — `A-39` first, then `A-33`, then `A-43`.** This is the only
> place the order lives; the 09-11 report points here rather than restating it (rule approved
> 2026-09-11 after two artifacts disagreed). `A-39` goes first because the retry MERGED on 09-11 at
> `12e0937`, so the condition it was filed to watch is now real: its own readCommand,
> `git grep -c RETRY_DELAYS_MS HEAD -- scripts/reverify.mjs`, should print a count against HEAD for
> the first time. The mechanism it fears — a 429 retried at a fixed 500ms with Retry-After ignored
> and no jitter — is F3 from `A-20`'s review, parked on `A-9`'s fix-on-touch backlog. The decision
> is whether `A-39` pulls F3 forward. If `--run` stamps any of these red, run the stored command
> through bash before believing it: the fleet runner's shell strips `^` (found 09-11, reported).
>
> **`A-48` IS DUE 2026-09-16 AND IS WORKABLE — the only commitment made directly to David.** All
> three citation indices answered at ~16:40Z on 09-11; the literal outputs and the three URLs are on
> the row at `A-48.reachability2026_09_11`. Re-run them before relying on this line. The probe says
> the read CAN be done; nobody has done it.
>
> **THE DENOMINATOR OF RECORD IS 673 CITED OF 770, NOT 543.** Release:
> `node scripts/depth-audit.mjs --corpus`. The audit read 8 rows as of 16:33Z on 09-11 — 2 sound,
> 0 defective, 4 unresolved, 2 unreachable — release: `node scripts/depth-audit.mjs`. Never quote
> any of it as a rate; the reader prints its sampling rule above the count for that reason.
>
> **`W-7` ROTATES.** 09-11's instrument was `A-42`'s readCommand under the fleet gate runner, so pick
> a different one, and write the line opening `W-7 —` that `docs/daily-config.md` now requires.
>
> **NOTHING IS WAITING ON DAVID** as of 16:39Z on 09-11. Release:
> `node scripts/sky.mjs answered-cards.mjs --project bounds-ledger --full`.
>
> **ARRIVAL, AND WHICH SESSION THIS DESCRIBES.** Written by session
> `cd22b602-3e49-4300-ac97-c123c8cdaa77`, which arrived on a clean handoff from the 09-10 session.
> Mid-afternoon the machine ran low on memory and killed all three waker ranks and one adversarial
> review; both were relaunched or recorded. Check your own strand rather than inheriting mine:
> `node ../skylark-site/scripts/read-listener-strand.mjs --project bounds-ledger`.
<!-- hand:end banner -->

First command (also below, in place):

```bash
git pull --ff-only
# commits after this primer was written are invisible to it — read them before acting:
git log --oneline 6feeebb430ac39c1e4ff8209d01ef54e3c01fff0..HEAD
```

### Primer — bounds-ledger, 2026-09-12

What ONLY this primer holds comes first; the FULL runnable **First action** block
sits at the foot, below the reads. The single command above is the same block's
first line, repeated so it can be running while you read — it replaces nothing.

_**Composed against HEAD commit `6feeebb430ac39c1e4ff8209d01ef54e3c01fff0` at 2026-09-11T16:53:09.544Z.** Nothing that landed after that instant is in this file. David's board answers especially: an answer he gives later cannot be here, and this primer will go on reading as though the work were not owed — that is exactly how two lanes concluded an OTA was not owed on 2026-09-04. Read them live, never off this page: `node scripts/sky.mjs answered-cards.mjs --project bounds-ledger --full`._

_Shared skylark-site scripts below are spelled `node scripts/sky.mjs …` — this lane vendors `scripts/sky.mjs` (S-20260906-75), so a shared script is named LANE-RELATIVE and resolves from a worktree too._

#### Owed gates — 3 due on/before 2026-09-12

_Ledger read at 2026-09-11T16:53:09.544Z by `cc-due-gates.collectDue` — the same resolver `check-due-gates-dispositioned` uses, so any difference is TIMING, not two answers. Rows filed or re-dated after that are NOT READ at generation — the close-out commit lands after this, so the COUNT above is stale by construction and this list cannot claim completeness. The list below, not the count, is the artifact; re-derive both before acting: `node scripts/sky.mjs check-due-gates-dispositioned.mjs --print --today=2026-09-12`._

Unfiltered as of the read time above — nothing was curated out of what the read could
see; a row absent here may simply have been filed after it ran.

- `A-33` (expectedSignalBy=2026-09-12, open) — A change to a file we have filed an upstream report against is a CANDIDATE CAUSAL EVENT, n
  - read: `gh issue list --repo teorth/optimizationproblems --author u00dxk2 --state all --limit 20 --json number,state,closedAt,title # SEPARATOR CONVERTED 2026-09-01 at pre-stage, em dash to hash, so the runner extracts the command rather than 900 bytes of prose. Literal output 2026-09-01, re-run at pre-stage: ONE row, 150 CLOSED 2026-08-23T17:15:35Z constants/87a.md: the current-record row quotes Martin's degree-8 discriminant, not the degree-12 construction that gives 857.5662. That single row IS the gap: it was true for three days and nothing surfaced it. POSITIVE CONTROL that the query can see our artifacts at all: it must return 150; an empty list means the author filter or the repo is wrong, not that we have never reported anything. NOTE THE TRAP THIS FIELD ALREADY AVOIDED: an equivalent gh pr list returns 141 from 2026-08-05 and is BLIND to 150, because our most recent outward artifact is an ISSUE; that miswrite was caught on W-8 the same day by running the command before writing it down. dead-ref-ok: the constants/87a.md token is inside a VERBATIM quotation of upstream issue 150's own title, so rewriting it to our mirrored path would make this field misquote the record it cites; our copy of that file is ledger/teorth-optimizationproblems/constants/87a.md and it exists (7502 bytes, checked 2026-09-02).` · last run: 2026-09-02T15:23:51.843Z
- `A-39` (expectedSignalBy=2026-09-12, open) — A mid-run 429 becomes up to 339 requests in two seconds at the limiter already refusing us
  - read: `git grep -c RETRY_DELAYS_MS HEAD -- scripts/reverify.mjs # Asks ONE question: has the retry merged into committed main yet? COMMIT-SCOPED ON PURPOSE. The obvious form, a plain grep over scripts/reverify.mjs, reads the WORKING TREE and was actively misleading on the day this row was filed: an uncommitted Codex edit had the retry constants in the tree while committed main had none, so the plain grep printed three hits and the honest answer was zero. LITERAL OUTPUTS, both run 2026-09-02 at this row's creation. Against HEAD: no output at all, exit 1 — the retry is NOT in main. POSITIVE CONTROL, the same command against the branch that has it: git grep -c RETRY_DELAYS_MS a20-fetch-retry-backoff -- scripts/reverify.mjs prints a20-fetch-retry-backoff:scripts/reverify.mjs:3 at exit 0. So exit 1 here means absent-from-main, NOT a broken pattern or a wrong path — the control proves the pattern matches when the thing is there. When this command starts printing a count against HEAD, the retry has merged and this row is live.` · last run: UNRUNNABLE
- `A-43` (expectedSignalBy=2026-09-12, open) — Upstream moved Haugland's minimum-overlap bound to its truncation — is that one wrong cell
  - read: `curl -s https://raw.githubusercontent.com/teorth/optimizationproblems/main/constants/1b.md — prints the live file. The H2016 row currently reads 0.380926; if it reads 0.380927 again upstream reverted and this row closes as moot. POSITIVE CONTROL that the fetch is the right document and not an error page: the response must also contain the string 'Erdos minimum overlap constant' heading text and the neighbouring value 0.380924, so an empty or redirected body cannot be misread as 'the row is gone'.` · last run: NEVER

#### What shipped since 24 hours ago

_Read at 2026-09-11T16:53:09.544Z; commits landing after that — the close-out especially — are not here. Re-derive: `git log --oneline --since='24 hours ago'`._

- commit `6feeebb` — A-20 closes on its first branch: the retry is merged, reviewed, and both answers are tested
- commit `dfe2b90` — A-20 F1+F2: the checker exited 0 having run nothing, and the retry's silent half was unguarded
- commit `95963f3` — P4: the ledger still carried 543 as the denominator of record, and slice 1's reading as 2 sound
- commit `c7f4246` — The denominator on the public pages could not be re-derived, and the audit's own verdicts were too generous
- commit `f1a3b2d` — P2: check:deferrals passed over a ledger it never read, and the fix sat uncarried for three days
- commit `22a1cf3` — A-42 closes: both readers were fixed, and the red on its gate came from the runner's shell
- commit `cf88e78` — docs(close-session): close-out 2026-09-10 — A-42's title asserted a state its own notes had superseded twice
- commit `c8473d8` — Hygiene pass 2026-09-10 — the day's report, and tomorrow's primer said David was still waiting
- commit `fd53cad` — The public page can now show what we have CHECKED, not only what we mirror
- commit `5ccd349` — The depth audit has a store and a first slice: 3 rows read against their sources, 2 sound
- commit `0cee023` — David dropped G-4's November verdict date — the goal is untouched and A-47 carries the meanwhile judgment

**Yesterday's report:** `docs/daily/2026-09-11-prelaunch.md` (coverage 2026-09-11, the prior day). Findings fence EMPTY — the BLUF is the whole story; nothing below it supersedes anything.

**Today's report (owed):** write `docs/daily/2026-09-12-prelaunch.md` — the artifact of record for service day 2026-09-12. Check it exists at close: `node scripts/sky.mjs check-daily-report-exists.mjs --date 2026-09-12` (it sweeps the fleet and applies each lane's coverageOffset; it takes no --project).

#### State — as of 2026-09-11T16:53:09.544Z (re-run the release command; never trust the value)

- **ledger**: 61 item(s): 43 closed · 18 open · as of 2026-09-11T16:53:09.544Z — release: `node scripts/sky.mjs gen-primer-first-action.mjs --repo . --target-date 2026-09-12 --json`

#### Id glossary — 21 id(s) this primer's day already has words for

_Each gloss is the ledger row's OWN title, not a re-derivation — paste it. Rows = every open ledger row plus every id named above. `(no ledger row)` means the id is named here and the ledger carries nothing under it: that is a finding, not a formatting gap._

- `A-2` — STANDING: drift-resolution log for the adopted surfaces…
- `A-7` — engineering-health P1s — four of five shipped 2026-08-09;…
- `A-9` — engineering-health P2 backlog (fix-on-touch, not scheduled…
- `A-20` — Should the fetch layer retry with backoff on 429/502? —…
- `A-33` — A change to a file we have filed an upstream report…
- `A-39` — A mid-run 429 becomes up to 339 requests in two seconds at…
- `A-42` — The date label and the sort key keyed off OUR pin history…
- `A-43` — Upstream moved Haugland's minimum-overlap bound to its…
- `A-45` — TIER-1 LEADING INDICATOR (orchestrator-approved…
- `A-46` — A close-out post announcing a FIX is textually identical…
- `A-47` — THE DEPTH AUDIT, David-ruled 2026-09-09: read our own…
- `A-48` — The second-area citation comparison David asked for,…
- `G-2` — GOAL: contribute a verified bound improvement to the…
- `G-4` — GOAL (Tier-0 from G-3 close): an outside party acts on a…
- `S-20260906-75` — (no ledger row)
- `W-3` — Watch for acknowledgement of the erdosproblems.com/36…
- `W-4` — KP-78 applies to every new detector: demonstrate BOTH…
- `W-6` — Read window for the README report-an-error channel — the…
- `W-8` — Does a materially amended outward artifact go back to…
- `W-9` — Does this lane's detection method find what the lane…
- `W-10` — reverify.test.mjs alarm-title leg failed once and would…

### First action — bounds-ledger, 2026-09-12

Every line in the block below is a COMMAND, not a claim. A command re-reads at the
moment you run it and cannot go stale; a state line written last night can.

```bash
git pull --ff-only
node scripts/sky.mjs update-david-board.mjs --list --json --full-ids --project bounds-ledger
git rev-parse HEAD origin/main
git rev-list --count origin/main...HEAD
npm run verify > tmp/verify-out.txt 2>&1
node scripts/sky.mjs check-ci-status.mjs --workflow reverify.yml
```

Morning release commands — deliberately NOT READ at generation (a value written
last night would already be stale); run each, never trust a remembered one:
- `git -C . log -1 --format=%h%x20%s` — HEAD
- `git -C . status --porcelain` — working tree
- `node scripts/sky.mjs check-ci-status.mjs --workflow reverify.yml` — CI
- `node scripts/sky.mjs check-deployed-sha-drift.mjs` — deploy
  - WHERE IT LOOKED: no read-only deploy-state script in this lane's package.json (looked for ops:deploy-state, deploy-state, check-deploy, render-deploys, deploy:status, daily-snapshot). The fleet's check-deploy-state.mjs is not a substitute — it requires --fix <sha> --deploy <sha> and exits 1 without them — so this is the FLEET read, which takes no required flags. service: NOT DECLARED — no row for "bounds-ledger" in src/data/cc-deploy-gates.json (generatedAt 2026-09-11T05:03:44.019Z, 17 repo row(s)) — nothing in this repo declares that lane's Render service. No `--service` flag is emitted: the lane SLUG is not the service name (antebellum-twitter's is disunion-feed), and a guessed flag exits 1.

<!-- hand:begin narrative -->
_(orchestrator narrative — this region survives regeneration)_
## Session close-out — 2026-09-11

_Appended at P5 on 2026-09-11 MT by session `cd22b602-3e49-4300-ac97-c123c8cdaa77`._

### The day, in one paragraph

The ledger turned its own method on itself and failed twice. The denominator on the public pages
(543) could not be re-derived — no predicate over the unchanged corpus reproduces it — and is now
673, computed by a committed command. Two of the previous day's three SOUND verdicts pinned rows
asserting a strict inequality their sources only prove non-strictly, and are now UNRESOLVED. Both
corrections are live on the public pages and in the ledger. Five more records were read against
their sources, drawn systematically for the first time.

### Load-bearing ships

- `c7f4246` — **the only user-visible ship**: five more audited rows on the constant pages and a
  denominator a reader can recompute. Four adversarial rounds, nine findings, all fixed first.
- `12e0937` — `A-20` merged: bounded retry, with its blocking review finding (a checker that exited
  0 through a junction having run nothing) fixed in `dfe2b90`.
- `f1a3b2d` — `check-deferrals` stopped passing over a ledger it never read.
- `22a1cf3` — `A-42` closed; `95963f3` — the ledger's 543 corrected, David's wording left intact.

### What NOT to touch tomorrow

- **The adversarial review on the working tree before a ship.** It caught three defects in guards I
  had written minutes earlier, including one that would have refused an intact corpus.
- **The corpus counter's refusals.** Each names the single failure it catches, and each was
  red-armed through the counter over a mutated fixture. Do not widen the table parser away from
  `extract-pins.mjs`'s definition to make a refusal go quiet; the pins would then disagree with it.
- **David's verbatim quotes on `A-47`, `A-34` and `G-4`.** They say "543 records" because that is
  the number we gave him. The corrections sit beside them, dated.

### Tier-0 binding constraint

`G-4` (an outside party acts on a watched record without us filing the report) reads 0 arrivals,
measured: `npm run reports` fetched 29 issues and accounted for all 29 as ours. The interim
yardstick is `A-47`'s coverage, 5 of 673 cited rows read against their source — coverage, never a
pass rate.

### Evangelism read

**The core problem, in a reader's words:** "I am about to cite a number and I do not know whether it
still holds." **Distance from the would-tell-a-friend moment: closer, and in an unexpected
direction** — the most persuasive thing this ledger can show a mathematician is that it holds its
own figures to the standard it applies to theirs, and today it published two corrections against
itself. **Retention and word-of-mouth: unmeasured and structurally unmeasurable** — the page fetches
nothing by construction; 1 unique repo viewer in the trailing 14 days (`npm run traffic`, 09-11).
No real-user advocacy signal this session.
<!-- hand:end narrative -->

<!-- generated by gen-primer-first-action.mjs (skylark-site) @ commit 8443d1800 · lane bounds-ledger · for 2026-09-12 · at 2026-09-11T16:53:09.544Z -->
<!-- writtenAtSha: `6feeebb430ac39c1e4ff8209d01ef54e3c01fff0` — the lane's HEAD commit when this primer was written. Provenance anchor for check-primer-freshness leg ii; NOT a claim about HEAD now (the close-out commit that carries this file lands after generation). -->
<!-- reading this block: a generated line that is wrong in a way you can see FIXED in skylark-site's git log after the commit above is STALE, not a finding — regenerate before filing it. `git -C C:/dev/skylark/skylark-site log --oneline 8443d1800^{commit}..HEAD -- scripts/gen-primer-first-action.mjs src/lib/cc-primer-commands.mjs src/lib/cc-due-gates.mjs src/lib/cc-report-artifact.mjs src/lib/cc-hand-edit-merge.mjs src/lib/cc-cold-readability.mjs src/lib/cc-skeleton-gloss.mjs` -->
