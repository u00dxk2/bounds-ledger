# Cold-start primer for 2026-08-08 (MT)

Multi-session primer — each /close-session run appends a new section below. Later sections
supersede earlier ones explicitly.

## Session close-out — 19:40Z (2026-08-07, ~13:40 MT)

You are the bounds-ledger agent. Read this, then `AGENTS.md` + `CLAUDE.md`, then
`continuity/items.json`. Steward of drifting mathematical records; pre-launch, private repo,
**PACED rail**, **no self-rating**. Two theses now: G-1 steward, G-2 steward + contributor.

### CI

**CI:** `reverify`: completed/**success** @ `cf89d99` (HEAD). Read the LOG not the badge; today's
runs all read `No drift. 111 files match upstream dee1660…` (that is an UPSTREAM sha — do not
`git show` it locally, it is not in this repo's object store), `229 claim(s): 227 hold, 0
broken/unreachable, 2 unverified (manual)`, and **9** selftest PASS lines.

### Today's load-bearing ships (3 commits, all pushed)

1. **`efac5e6` — G-2 round one.** `search/27b/propose-round.mjs`: 11 proposed constructions
   through 4 ordered gates. 0 certified, 11 eliminated (edge-bound 5, k9-free 2, chromatic 4).
   Finding: two unrelated families land **exactly one short**. `C5+K7` is over the `6n-12`
   budget by one edge with `omega=9`; paying that edge back from inside the clique fits exactly
   and drops `omega` to 8 but gives `chi=9`, paying it back from the join keeps `omega=9`.
   Circulants sized to fit top out at `C17(1..5)`, `chi=9`.
   `docs/findings/2026-08-07-g2-27b-round-one-the-one-edge-vise.md`
2. **`083de88` — G-2 round two, the inverted search.** `search/27b/round2-grow.mjs`: build
   biplanar graphs BY CONSTRUCTION (double-greedy two maximal planar layers out of `K_n`),
   measure `chi`. **Hard ceiling at `chi=8` across n=9..13, 300 seeds each — one colour BELOW
   the record.** Degrades as n grows (n=9: 286/300 reach 8; n=13: 54/300; leftover 1→15).
   `docs/findings/2026-08-07-g2-27b-round-two-density-is-not-the-thing.md`
3. **`cf89d99` — daily report updated for round two** (it had been written between rounds and
   still recommended "run round two"). 6/6 linters PASS.

### The one number to carry forward

**At n=11 the best round-two graph has exactly 50 edges and `chi=8`. Sulanke's graph has n=11,
exactly 50 edges and `chi=9`.** Same vertex count, same edge count, one colour apart, both
verified by the same code in the same run. **Density is not what the record is made of.** Round
two's own objective is measurably wrong — a clean kill, not an inconclusive one.

### Tomorrow's first-action

**Steward cadence first, as always.** `gh run list --limit 1`, read the LOG, expect the numbers
above. Green → **streak day 16 of 30**. Then `npm run check` locally **with `CC_PROMPTS_PIN`
set** (that is the only run that gives a real advisory read on the two manual claims).

**Then, BEFORE writing any "blocked on David" line: `node ../skylark-site/scripts/answered-cards.mjs --project bounds-ledger`.**
A new card is live (below). If he has answered it, act on his words — and note his 8/07 pattern:
he asked us to double-check a "send it" instruction and the check said we had already sent it,
so read the whole reply, not the first clause.

**Then G-2 round three: seed the growth with structure.** Both rounds now agree from opposite
directions that the record is a *designed* object — round one's structured constructions fail by
razor-thin margins, round two's unstructured ones do not get close at all. So stop growing from
a shuffled `K_n`. The next instrument accepts a **seed subgraph held fixed in the layers** (start
with the shape that produced the record: a clique joined to an odd cycle) and double-greedies
only the remaining edges. **Watch the leftover column** — at n≥12 it is already big enough to be
where the missing colour is going.

### Pending decision-blocked (David's — a card is NOW on his board; do not nag beyond it)

**Card `bb4df56c` (bus msg `75673773`), filed 2026-08-07 at close: "three calls only you can make
before the repository can go public."** These had sat in two daily reports without ever reaching
his board, which is the invisible-decision failure mode. All three, with a single recommended
reply drafted:
1. **Licence for our own code** (no LICENSE = all-rights-reserved; recommendation Apache-2.0 to
   match the mirror's, so the repo tells one licence story). The mirror's own Apache-2.0 is
   already handled and is NOT this question.
2. **His private words quoted verbatim in our decision docs** (recommendation: keep the
   decisions, paraphrase him out).
3. **The history legs** — his author email on every commit, 15 commit bodies with private
   session URLs, and `293cec9` which added a forum screenshot showing third-party usernames.
   Recommendation: keep the history and rewrite those three out. **The third is the one that is
   not ours to accept** — the other two are about him and he may not care.

**The flip stays ON HOLD** until he answers. His 8/05 words: not public until the issues are
addressed.

### Active in-flight / watch

- **#141** (upstream `teorth/optimizationproblems`, the six-line citation-key correction) —
  OPEN, 0 comments, 0 reviews, ~46h at close. **Check, do not poll, do not nudge** (his standing
  word). Sent 2026-08-05T20:45Z; commit `59b3a94` records the send.
- **W-3** (the ack watch) — no movement. Both advisory legs read HTTP 200 locally with the page
  unchanged: bound still `0.380876`, still `last edited 23 January 2026`.
- No Codex dispatches today. No deploys — nothing in this repo deploys.

### Tier 0 / binding constraint

**North star: 0 externally-acknowledged corrections.** Unchanged. A sent PR and a standing
harness are not acknowledgements. G-1 green streak day 15/30 at close. Open continuity items: 11.

### Evangelism read (required carry-forward)

- The would-tell-a-friend moment is unchanged — a stranger sees the alarm catch a record move —
  **plus the G-2 candidate moment**: a stranger watches a machine-found graph beat a 45-year-old
  record with a certificate they can run themselves. Today moved that *further away* honestly:
  two rounds, zero certificates, and round two could not even reach the record.
- Distance: repo private, reach zero. The card above is now the single gate between the lane and
  any audience at all.
- Retention / word-of-mouth instrument: none, correctly — no audience until the flip.

### What NOT to touch tomorrow

- **Do NOT weaken or "tune" the 27b verifier to make a candidate pass.** The verifier is the
  product's authority; a candidate that needs a softer judge is not a candidate.
- **Do NOT claim any G-2 mathematical result** without exact re-verify + literature/priority
  check + adversarial review + David's gate. Rounds one and two made NO mathematical claim and
  neither should round three until it has something that survives all four.
- **Do NOT read the `chi=9` near-misses as encouragement** — they merely re-derive the 1980
  bound. Landing one short twice is evidence about the obstacle's shape, not about a small push
  finishing it.
- **Do NOT pre-stage a second citation-key PR** while #141 has no outcome (10a/22b/46a/50a/52a
  wait).
- **Do NOT treat the `k9-free` gate as executed.** It is CITED (`theta(K9)=3`,
  Battle-Harary-Kodama 1962) and labelled so in code, console and JSON. Round two's `K9 - e`
  result is *consistent with* it and is **not** a proof of it.
- Do not re-automate C-7/C-9 (the 403 is IP-dependent; only a green CI run is evidence about
  CI). Do not put `CC_PROMPTS_PIN` in repo secrets. Do not re-propose the /36 public comment. Do
  not hand-edit the mirror or the generated pins.

### Two instrument lessons worth not relearning

- **A self-test licenses its own claim and nothing wider.** Round one's first run built two
  differently-labelled candidates as the *same graph* (a positional edge-drop hit only cross
  edges in a join). The self-test passed throughout — it tests the gates, not the candidate list
  pointed at them. Caught only by noticing two rows that should have differed did not, and the
  fix *changed the result*.
- **A script that exports functions must not run its main body on import.** Cross-checking the
  n=11 comparison by importing `round2-grow.mjs` silently re-ran the entire search and printed a
  second full results table into the middle of the check. Harmless only because the numbers
  agreed. Both round scripts now guard on being the entry point, proven both ways.

### Numbers that changed meaning today

- **Self-tests: 9**, up from 7 at day start (propose-round, round2-grow). Any doc saying 7 or 8
  is stale. `reverify.test.mjs` derives the list from `npm test` and fails if one is missing from
  the workflow — negative-controlled today, both ways.

## Session close-out — 00:50Z (2026-08-07 evening MT, ~18:50 MT)

**Supersedes the 19:40Z section's "Pending decision-blocked" block: David ANSWERED card `bb4df56c`
that evening, approving all three. Then, after seeing where it landed, he ruled that we hold off on making it public for the day. The flip is deferred by his own word — it is NOT waiting on him.**

### What he approved, and what landed

All three, verbatim as drafted. Two are shipped in `b6ad8da`:

1. **Licence — DONE.** Root `LICENSE` is Apache-2.0, byte-identical to the canonical text already
   vendored at `ledger/teorth-optimizationproblems/LICENSE` except the appendix copyright line
   (`Copyright 2026 Skylark Creations LLC`). Verified by diffing that one substitution back out.
   `NOTICE` tail rewritten — it had said our work was "not governed by the Apache License and is
   addressed separately, if at all", true before a root licence existed and false after.
   **If David would rather the copyright name read differently, that is a one-line change.**
2. **His quoted words — DONE for the decision docs.** Two verbatim quotes of his private speech
   existed in `docs/decisions/` (the 8/02 public-comment doc, the 8/05 first-PR doc). Both
   paraphrased with decision + reasoning intact. Verified none remain in that folder.
3. **History rewrite — NOT RUN. Staged, backed up, and blocked.**

### The history rewrite: everything is ready, do not re-derive it

**It was refused by the Claude Code safety classifier**, which is correct for an irreversible
history rewrite. Working around it (e.g. substituting `git filter-branch`) would defeat the intent
— **do not do that.** It needs David to approve the command when he is at this window.

- **Backup:** `C:\dev\skylark\bounds-ledger-backups\pre-rewrite-2026-08-07.bundle`, verified
  "records a complete history" at `b6ad8da`. Origin also still holds the pre-rewrite history until
  a force-push happens.
- **`git-filter-repo` IS installed** (`python -m git_filter_repo`, version a40bce548d2c).
- **Facts, already measured:** 117 commits, all authored under David's personal address; exactly **15**
  `Claude-Session: https://claude.ai/code/session_…` trailer lines (all the same session id); the
  forum snapshot is `.playwright-mcp/page-2026-07-25T20-13-43-020Z.yml`, added in `293cec9` and
  already deleted from the tree in `6bfaf3b` — so it is history-only.
- **His GitHub id decodes to 58274543, login `u00dxk2`** → noreply address
  `58274543+u00dxk2@users.noreply.github.com`.
- **The exact command:**

```
python -m git_filter_repo \
  --invert-paths --path .playwright-mcp/ \
  --email-callback 'return b"58274543+u00dxk2@users.noreply.github.com" if email == b"<DAVIDS-PERSONAL-ADDRESS>" else email' \
  --message-callback 'return __import__("re").sub(rb"\nClaude-Session: https://claude\.ai/code/[^\n]*", b"", message)' \
  --force
```

- **THE VERIFICATION THAT MATTERS:** the current files' content hashes to tree **`ab15ab3d`**
  (a tree object, not a commit). Tree hashes do not depend on history, so **if that hash is
  unchanged after the rewrite, the rewrite provably altered no current file.** Check it first.
  Then: authors → one noreply address, `Claude-Session` count → 0, `.playwright-mcp` objects → 0,
  commit count still 117.
- **Knock-on nobody has handled yet:** a rewrite changes every SHA, so every SHA cited in our docs
  (this primer, findings, `continuity/items.json` `linkedCommits[]`) goes stale.
  `git-filter-repo` writes `.git/filter-repo/commit-map` (old→new) — use it to fix those references
  in the same session. In a lane about references being correct, leaving them dead would be bad.
- `filter-repo` removes the `origin` remote; re-add
  `https://github.com/u00dxk2/bounds-ledger.git` before pushing.

### The open question his ruling did not cover

He was asked about "the decision docs" because that is how the blocker was filed, and that is what
got fixed. **A sweep then found his words quoted in roughly 35 more tracked files** — 13 in
`continuity/items.json`, the rest across the cold-start primers, the daily reports, and
`CLAUDE.md`. Fixing only the decision docs does not achieve what he asked for. Rewriting 35
historical records is materially bigger than what he approved, and several exist precisely to
record *why* a decision was made, so **it was surfaced rather than done on assumption. Unanswered.**

### State at this close

Repo **PRIVATE** (verified via `gh repo view`, not assumed). Tree clean, `main` in sync with
origin at `b6ad8da`, 117 commits. Nothing went public. Bus status `bcd0f56c` carries this and
supersedes the answered needs-decision `75673773`.

## Session close-out — 02:15Z (2026-08-07 late MT, ~20:15 MT)

**Supersedes the 00:50Z section's "history rewrite: NOT RUN". David approved it and IT IS DONE.**
Repo is still PRIVATE and the flip still needs his second yes — approving the rewrite was not
approving publication.

### The rewrite is complete and verified from a FRESH CLONE of the remote

`git-filter-repo` ran over all 119 commits. Verified against every pre-flight measurement, then
re-verified by cloning the remote fresh rather than trusting local state:

| check | before | after |
|---|---|---|
| author + committer email | David's personal address | `58274543+u00dxk2@users.noreply.github.com` |
| `Claude-Session:` trailers | 15 | **0** |
| `.playwright-mcp` objects (the forum snapshot) | 2 | **0** |
| commit count | 119 | 119 (+1 new = 120) |
| **HEAD tree hash** | `4d2a2056` | **`4d2a2056` — UNCHANGED** |

That last row is the one that matters: tree hashes do not depend on history, so an unchanged
hash proves the rewrite altered **no current file**. Author NAME was already `u00dxk2` before
and is unchanged — only the email moved (confirmed against the backup, since it had not been
measured pre-flight).

**Backups, both verified "records a complete history":**
`C:\dev\skylark\bounds-ledger-backups\pre-rewrite-2026-08-07.bundle` (at `61fc441`) and
`…-2026-08-07b.bundle` (at `0309467`, immediately pre-rewrite). To undo:
`git clone <bundle>` and force-push it back.

### THE TRAP THAT ALMOST SHIPPED HIS EMAIL ANYWAY — read this before any future rewrite

The rewrite succeeded, and then **the very next commit re-contaminated the repo.** The SHA-remap
commit was authored *after* `filter-repo` ran, using the repo's git config — which still held the
personal address. A fresh clone showed `yahoo hits: 1`. **Local checks would never have caught
it**, because they were run before that commit existed; only cloning the remote did.

Fixed: repo-local identity is now set (`git config user.email` →
`58274543+u00dxk2@users.noreply.github.com`, `user.name` → `u00dxk2`) and the offending commit was
re-stamped. **But this config is REPO-LOCAL.** A fresh clone on another machine, or `--global`
config, reverts to the personal address and the next commit re-contaminates the history.
**Anyone setting this repo up elsewhere must set the local identity first.**

### SHA references: 244 remapped, and a hazard worth knowing

The rewrite restamped all 119 commits, so every SHA cited in our docs and in `linkedCommits[]`
died. Remapped from `.git/filter-repo/commit-map` across 36 files.

**The hazard:** a `[0-9a-f]{7,40}` regex also matches numeric literals. README cites the value
`2.6273856`, whose tail `6273856` is indistinguishable from a short sha — a collision with a real
commit prefix would have **silently rewritten a record value in our own ledger.** It did not
happen, and that was *verified*, not assumed: all 710 decimal literals across the 63 doc files are
byte-identical before and after.

That verification failed twice, silently, before it worked — **both times looking like a result**:
1. The first comparison reported all 63 files differing. Cause: **Windows Python cannot resolve
   the MSYS path `/tmp/…`**, so the "original" side read empty and it compared against nothing.
2. The negative control then reported the check INERT — same cause, the mutation never landed. A
   **false inert verdict**, the same class `CLAUDE.md` warns about for CRLF-vs-`\n`, arriving by a
   different mechanism.
Redone through `git` and `sed` with the mutation proven to land first: the check FIRES on a single
altered digit and stays SILENT on the real data. **Generalise it: `/tmp` paths are invisible to
Windows Python in this environment — use the scratchpad dir or pipe through git/sed.**

Untouched and checked rather than assumed: the upstream mirror pin `dee1660` (outside the remap
and not in the commit map), `manifest.json`'s sha, foreign SHAs, and non-git ids.

### State

`main` at `fa4fb5b`, force-pushed, local and origin agree. 9/9 self-tests pass, continuity clean.
Repo **PRIVATE** (verified). **The flip still needs David's second yes after he has seen this.**
Still unanswered from earlier: nothing — he ruled on the quoted-words scope and the full sweep
landed in `0309467` (now restamped).
