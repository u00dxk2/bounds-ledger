<!-- GENERATED — regenerating REPLACES everything outside the hand: markers, so put prose inside them. Provenance stamp at the foot of this block. -->

<!-- hand:begin banner -->
> **THIS LANE IS LAUNCHED** — David ruled it 2026-08-27. Instruments a launched app has and this one
> does not — retention, active users, Sentry, revenue — are answered **"not an instrument on this
> lane, by design"**, never with a zero. The real reads are `npm run reports` (`G-4`'s primary
> indicator) and `npm run traffic`.
>
> **The report path stays `docs/daily/<date>-prelaunch.md`.** Historical filename, not a lifecycle
> claim. Do not helpfully fix it.
>
> **THE MATHSTODON POST IS CLOSED, UNSENT, AND MUST NOT BE RE-PROPOSED.** Nothing is owed here.
>
> **TWO GATES ARE DUE TODAY AND BOTH WERE PRE-STAGED YESTERDAY — read `A-18 note prestage2026_08_31`
> and `A-20 note prestage2026_08_31` BEFORE you run anything.** Both reads are already done and
> recorded with their positive controls, so today is a decision, not a dig. `A-18` in particular
> **can close on evidence**, and its own `readCommand` will tell you the opposite — see the narrative.
>
> **A `readCommand` is now written `command # prose`, never `command — prose`.** The gate runner
> executes the WHOLE field with shell `#` comments stripped, so an em dash buries the command inside
> 1200 bytes of explanation and the row can never be run or stamped. Convert a row's separator **when
> its gate comes due**, not in a bulk pass. Three rows use it (`A-2`, `A-32`, `W-8`/`A-33`).
>
> **`AGENTS.md` no longer prescribes `; echo "EXIT=$?"` after `npm run verify`.** The exit code is in
> `tmp/.verify-receipt.json` beside the sha it was green at. A `;` compound parks the permission
> classifier at a silent prompt that reads exactly like a hang.
>
> **`CLAUDE.md` + `AGENTS.md` have 37 bytes of headroom** against the 60,000 fleet budget. Anything
> you add today must pay for itself. Measure with `check-claude-md-sizes.mjs --stamp`.
>
> **`git rev-parse --short HEAD origin/main` fails.** Two revisions plus `--short`. Drop `--short`.
<!-- hand:end banner -->

### Primer — bounds-ledger, 2026-09-01

What ONLY this primer holds comes first; the runnable **First action** sits at the
foot of this block, below the reads — do not skip to it.

#### Owed gates — 2 due on/before 2026-09-01

_Ledger read at 2026-08-31T17:50:33.851Z by `cc-due-gates.collectDue` — the same resolver `check-due-gates-dispositioned` uses, so any difference is TIMING, not two answers. Rows filed or re-dated after that are NOT READ at generation — the close-out commit lands after this, so the COUNT above is stale by construction and this list cannot claim completeness. The list below, not the count, is the artifact; re-derive both before acting: `node ../skylark-site/scripts/check-due-gates-dispositioned.mjs --print --today=2026-09-01`._

Unfiltered as of the read time above — nothing was curated out of what the read could
see; a row absent here may simply have been filed after it ran.

- `A-18` (expectedSignalBy=2026-09-01, open) — Generated STATE block at the top of the cold-start primer (render-state-block.mjs --primer
  - read: `ls -1 docs/cold-starts/ | tail -10 ; grep -L "exit 0" docs/cold-starts/2026-08-1[7-9].md docs/cold-starts/2026-08-2*.md — lists primers IN THE ADOPTION WINDOW whose first-action carries no exit code; EMPTY means the property held. Read the listing too: a day with NO primer at all is invisible to the grep, and 2026-08-20 was one.`
- `A-20` (expectedSignalBy=2026-09-01, open) — Should the fetch layer retry with backoff on 429/502? — separate from the unreachable-stay
  - read: `grep -n -i -E "retry|backoff|429|502" scripts/reverify.mjs — literal output 2026-08-25: NO MATCHES, exit 1, so no retry layer exists and this question is unshipped. Positive control that the probe can match in that file: grep -c -i "fetch" on the same path returns 10. Without that control an empty result is indistinguishable from a wrong path.`

#### What shipped since 24 hours ago

_Read at 2026-08-31T17:50:33.851Z; commits landing after that — the close-out especially — are not here. Re-derive: `git log --oneline --since='24 hours ago'`._

- commit `5f1b6ca` — The days report: two unreadable links on the reader's own verification path
- commit `c289a15` — A-32 closes: the brief said the metric was zero, and it has been two since 23 August
- commit `f96ba43` — P4 pre-stage: A-18 can close on evidence tomorrow, and its readCommand cannot say so
- commit `b3e7469` — Section B: A-32 was the last row prescribing the compound AGENTS.md dropped this morning
- commit `5011af8` — Both document links on every row now render; before today they were a wall of pipes
- commit `d30fea2` — P2 continuity: the gate runner never stamped a row because our readCommands hide the command behind an em dash
- commit `b39ea07` — Close-out 2026-08-30: primer close-out section, and a runbook guard that calls every valid commit DEAD

**Yesterday's report:** `docs/daily/2026-08-31-prelaunch.md` (coverage 2026-08-31, the prior day). No findings fence declared in it.

**Today's report (owed):** write `docs/daily/2026-09-01-prelaunch.md` — the artifact of record for service day 2026-09-01. Check it exists at close: `node ../skylark-site/scripts/check-daily-report-exists.mjs --date 2026-09-01` (it sweeps the fleet and applies each lane's coverageOffset; it takes no --project).

#### State — as of 2026-08-31T17:50:33.851Z (re-run the release command; never trust the value)

- **ledger**: 51 item(s): 34 closed · 17 open · as of 2026-08-31T17:50:33.851Z — release: `node ../skylark-site/scripts/gen-primer-first-action.mjs --repo . --target-date 2026-09-01 --json`

### First action — bounds-ledger, 2026-09-01

Every line in the block below is a COMMAND, not a claim. A command re-reads at the
moment you run it and cannot go stale; a state line written last night can.

```bash
node ../skylark-site/scripts/update-david-board.mjs --list --json --full-ids --project bounds-ledger
git rev-parse HEAD origin/main
git rev-list --count origin/main...HEAD
npm run verify > tmp/verify-out.txt 2>&1
node ../skylark-site/scripts/check-ci-status.mjs --workflow reverify.yml
```

Morning release commands — deliberately NOT READ at generation (a value written
last night would already be stale); run each, never trust a remembered one:
- `git -C . log -1 --format=%h%x20%s` — HEAD
- `git -C . status --porcelain` — working tree
- `node ../skylark-site/scripts/check-ci-status.mjs --workflow reverify.yml` — CI
- **deploy read: NOT AVAILABLE** — no read-only deploy-state script in this lane's package.json (looked for ops:deploy-state, deploy-state, check-deploy, render-deploys, deploy:status, daily-snapshot). The fleet's check-deploy-state.mjs is not a substitute — it requires --fix <sha> --deploy <sha> and exits 1 without them

<!-- hand:begin narrative -->
## Session close-out — 2026-08-31

### The one thing to understand about today

The public page promises, in its own words, that *"every row links to its primary source so you can
check us in one hop"*. It could not do that on **either** link a row offers. `source` handed the
reader the CHECKER's URL — `raw.githubusercontent`, `content-type: text/plain` — and the constant's
name, the first link in every row, handed them our mirror off Pages as `text/markdown`. Both links
present, both pointing at the right file, both unreadable.

**Presence was never the question; what ARRIVED was.** Measuring the content-type rather than
checking that the link resolves is what separated those two, and opening one in a real browser
(`playwright-cli`) is what proved it: the document arrives as a single plain-text node with the
bound tables inline as `| $pi/2 = 1.57059$ | [SS2002] | |`. Fixed in `5011af8`; the live page now
carries 111 upstream blob links, 111 mirror blob links and **0** raw URLs.

### The finding to carry forward is about the gate runner

`0 of 18` open rows carried `readCommandLastRun`, and the cause was **not neglect**. The runner
executes the whole `readCommand` field with shell `#` comments stripped, and this lane house-styles
that field as `command — prose`. So every row extracted **1267 bytes of prose** as its command.
Changing `A-2`'s separator to `#` cut the extraction to 33 bytes and it stamped first try.

**Now 2 of 18.** Read that as "the two rows whose gates came due", never as "16 remain to be worked".

### Load-bearing ships

- `5011af8` — **user-visible**, verified live after the Pages build. The ceiling was named rather
  than quietly taken: upstream publishes typeset pages for all 111 constants (verified 111 of 111,
  fabricated id 404s as the negative control) and they are nicer, but linking there needs the
  self-contained-page assertion to separate an ASSET fetch from a NAVIGATION link. **Relaxing a
  verifier to admit a candidate is what this lane refused on 08-30.** Blob view needed no guard
  touched. That trade is available whenever we decide to split the guard on its own merits.
- `c289a15` — **`A-32` closed, both halves.** Voice check `3 blocking, 2 advisory` → `voice: clean`.
  The brief had said for five weeks that our one real metric was zero; it has been **2 since
  23 August**. **All three `claim` blockers were ordinary English**, not this lane's technical sense —
  glossing them as the technical term would have made the brief WRONG to quiet a linter, so they were
  reworded instead. A linter hit on a word is not proof the word was used in the sense the linter names.
- `f96ba43` — both of today's gates pre-staged.

### What NOT to touch

- **The self-contained-page assertion in `render-site.mjs`.** It rejected today's nicer candidate and
  was right to. It works because it makes the page's independence checkable rather than intended.
- **The `manual: true` → UNVERIFIED mechanism on `C-7`/`C-9`.** Both advisory fetches returned HTTP
  200 with their strings present today and both still printed UNVERIFIED.

### Two things a cold agent will otherwise re-derive

- **`check-daily-report-exists` reads the PUSHED remote, not your working tree.** It reported
  `PENDING` for a report that was written *and committed*; the push is what flipped it to present.
  Do not go looking for a missing file — push first, then re-run.
- **Today's P4 prompt never reached this pane.** The bus row records `surfacedAt 15:50:28Z`,
  `executedAt 16:03:19Z`, and `surfacedSession` = the live session, while every turn in that window
  was a bare idle tick. The pane was idle, **not** parked at a prompt. Reported to the orchestrator;
  nothing lane-side to fix. If a phase seems skipped, read `tmp/.bus-events.jsonl` before concluding
  nothing was sent.

### Tier-0 status

**`G-4` = 0 arrivals against 3 unique viewers in 14 days.** From today it is reported as
**arrivals over viewers**, never a bare zero, with the threshold pre-committed BEFORE the read: a
zero carries product information only at **≥30 unique viewers / 14 days** (rule-of-three; at n=3 the
bound rules out nothing). The zero is measured — 26 issues fetched, all 26 attributed to us, parts
reconcile. **Consecutive instrument-facing days: 4.**

### Watch items maturing soon

`A-18` and `A-20` — **today**, both pre-staged. `A-33` and `A-34` — **2026-09-02**; `A-34` is
dispositioned dated-and-deferred and its card is owed that day. `A-7`/`A-9` — 09-03. `A-2` — 09-07.
<!-- hand:end narrative -->

<!-- generated by gen-primer-first-action.mjs (skylark-site) @ commit 9f4bc5d4 · lane bounds-ledger · for 2026-09-01 · at 2026-08-31T17:50:33.851Z -->
<!-- reading this block: a generated line that is wrong in a way you can see FIXED in skylark-site's git log after the commit above is STALE, not a finding — regenerate before filing it. `git -C ../skylark-site log --oneline 9f4bc5d4..HEAD -- scripts/gen-primer-first-action.mjs src/lib/cc-primer-commands.mjs src/lib/cc-due-gates.mjs src/lib/cc-report-artifact.mjs src/lib/cc-hand-edit-merge.mjs src/lib/cc-cold-readability.mjs` -->
