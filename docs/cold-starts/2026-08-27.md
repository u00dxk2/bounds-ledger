# Cold-start primer for 2026-08-27

> **READ THE GATE `readCommand`s BEFORE THIS PROSE.** Run
> `node ../skylark-site/scripts/check-due-gates-dispositioned.mjs --print`, then run each due item's
> own `readCommand` from `continuity/items.json`. As of 2026-08-26 close, **every open item has a
> `readCommand`** — the gap scan reports zero — so this is now a paste rather than a hunt. The prose
> below explains *rationale*, which you only need once you have decided something.

## ⚠ ANSWERED — David ruled at 2026-08-26 17:04Z. G-3 IS CLOSED, G-4 is the new Tier-0.

**He answered the card the same afternoon it went up. Do not re-ask, and do not treat G-3 as live.**
His words, verbatim:

> "Yes - mark it done, and make the next goal whether it happens again without us filing the report.
> But also let's find more things to have an impact on? Where else can we have an impact like this?"

**Three things follow, and all three are already in `continuity/items.json`:**

1. **`G-3` closed MET** (card `8e8f547d`, decision `4e9f90a4`). Its `resolution` carries his words
   and the evidence.
2. **`G-4` opened, Tier-0** — an outside party acts on a watched record **without us filing the
   report**. Harder than G-3 on purpose: G-3's single instance ran entirely through an issue we
   wrote, so G-4 removes our own outbound effort. **Expect a long zero, and that is not failure** —
   it is bounded by discoverability, which is near zero (3 unique viewers per fortnight).
3. **`A-34` opened** — his third question. He is asking where else this ledger can have this kind of
   impact, which is the adopt-a-second-surface question arriving *from him*. Owed: a **ranked
   proposal with the reasoning and the cost**, not a brainstorm. Dated 2026-09-02.

**Release command** (it now confirms the answer rather than waiting for one):

```
node ../skylark-site/scripts/answered-cards.mjs --project bounds-ledger
```

**What the goal WAS:** `G-3` — that someone outside Skylark uses the ledger and acts on it — met on
2026-08-23.

**The evidence, which is not in dispute:**

| when (UTC) | what |
|---|---|
| 2026-08-20T22:38Z | we filed issue teorth/optimizationproblems#150 — the 87a record row cited the wrong witness |
| 2026-08-23T15:14:02Z | commit `7848802` by **Chessing234 (Taksh)** fixed it; his message ends **"Reported in issue 150."** |
| 2026-08-23T16:50:14Z | he cross-referenced our issue from PR #151 |
| 2026-08-23T17:15:36Z | **Terence Tao closed our issue `state_reason=completed`**; his follow-up `b1a28ac` landed 17:18:50Z |

Causation is stated by the actor in his own commit message. We are not inferring it from timing.

**Why it was carded rather than closed:** the two-person bar in `G-3.note3` was one this lane wrote
itself, and Tao is arguably already the single acknowledgement `G-1` produced when he merged PR #141
on 2026-08-05. A Tier-0 goal must not close because its author re-read his own threshold favourably.
**David settled it by not engaging with the counting question at all** — three readings were put to
him and he answered "mark it done". The resolution records it that way rather than as "the two-person
bar was met", because those are different statements and only the first is his.

**The part that should bother you more than the good news.** It happened on 2026-08-23 and was found
on 2026-08-26 **by accident**, while writing an unrelated `readCommand`. Our own mirror had already
ingested the fix — 87a.md moved inside the `5c4aeee` snapshot and the page classified it as a routine
text edit — so the ledger watched the bytes move and **no instrument connected them back to our own
report.** Nothing we run was watching the thing the top goal is about. That gap belongs to `W-9`
(the watch asking whether this lane's detection method finds what the lane exists to find) and it is
now that row's most concrete evidence.

## Tomorrow's first action — VERIFIED as a compound, exit 0, in the turn that wrote this

```bash
git rev-parse HEAD origin/main && npm run verify > tmp/verify-out.txt 2>&1; echo "EXIT=$?"
```

Run from the repo root. **No leading `cd`** — that compound cannot be statically resolved by the
permission classifier and parks at a silent prompt that reads exactly like a hang. Still
`git rev-parse HEAD origin/main`, **not** `--short`: it takes a single revision, dies
`fatal: Needed a single revision`, and in a leading `&&` position silently skips the whole cadence.
Six sessions have hit that trap, including ones that wrote the warning.

## CI is UNKNOWN, not green and not red — and read the JOB layer, not the run

**As of 2026-08-26 ~16:50Z: GitHub Actions `major_outage`, Pages `degraded_performance`**
(githubstatus.com/api/v2/summary.json, read directly). **Release command:**

```
node ../skylark-site/scripts/check-ci-status.mjs --workflow reverify.yml
```

**But do not stop at its verdict.** Today the run-level `conclusion` was wrong in **both directions
inside four minutes**:

- `8a35eaf` — run `completed/failure`, jobs `queued/null`, **zero steps executed**. A RED that is not
  a failure, and no issue was filed because the step that files them never ran.
- `1cb3f65` — run `startup_failure`, job `check` **`completed/success`, 29 of 29 steps**, including
  the state-block, drift and claims checks. A GREEN buried under a red run.

`check-ci-status`, `gh run list` and the badge all read the run level. **Read
`gh api repos/u00dxk2/bounds-ledger/actions/jobs/<id>` before believing either.** A failed run is not
a failed check — the CI-layer twin of this lane's `UNREACHABLE is not BROKEN`.

**The last real runner verdict is `1cb3f65`**, 29/29 green at 15:35Z; every commit after it is
doc-only prose. Local `npm run verify` is exit 0 at HEAD.

## Ten commits are stacked behind a degraded Pages, and they will publish together

The live page is still **`c25cecd`** (last successful build 2026-08-25T23:06:53Z). Four builds errored
today with `duration: 0`. When Pages recovers, **all ten publish at once with no check consulted at
any point** — this lane's publish fires on push and waits for nothing.

That backlog is **verified, not merely queued**: `1cb3f65` passed 29/29 on a runner and local verify
is green. Named anyway because the *mechanism* is unguarded and the next backlog may not be. Tracked
as `A-31` (should the publish be CI-gated), dated 2026-09-09 — deliberately not decided during an
outage.

**Release command:** `gh api repos/u00dxk2/bounds-ledger/pages/builds --jq '.[0:3]'`

## What is dated, and nothing is owed on 2026-08-27

Checked at close across every open item: **the next dated thing is 2026-08-31** (`A-2`, the standing
drift-resolution log; and `A-28`, the Mathstodon draft), then **2026-09-01** (`A-18`, `A-20`, `A-30`),
then **2026-09-09** (`A-31`). So after the steward cadence you are choosing the day's work.

**`A-28` was re-classified today and the correction matters:** it had been carried as though waiting
on David and names no board card, which by the waits gate means we never asked. It is **not** a
David-wait — the un-wait action is ours, because its figures decayed while it sat. Refresh them, then
card it.

## Two things to fix that are small and real

1. **`docs/lane-brief.md` fails its voice check** — `node ../skylark-site/scripts/check-david-voice.mjs
   docs/lane-brief.md` exits 3 on three unglossed terms (*drift*, *claim*, *bound*). Pre-existing, not
   a regression from today. That file is **wholly** David-facing, so the whole file is in scope.
2. **The brief is now materially out of date** — it predates the G-3 evidence above, which is the most
   interesting thing this lane has ever had to tell him. Tracked as `A-32`.

## The rule that came out of today, and this primer obeys it

**A hand-written statement of a live instrument's value carries an AS-OF and its read command — never
a bare present tense.** Approved by the orchestrator, flagged a portfolio candidate, recorded in
`CLAUDE.md` § Conventions.

Three statements were falsified by the same session that wrote them: yesterday's banner was true for
**seventeen seconds**, the 08-25 report called `A-18` the only declared deferral 82 minutes before
`A-30` was declared, and that primer contradicted itself on the same count. The structural cause is
that a banner sits **above** the append point, where the "later sections supersede earlier ones" rule
cannot reach it. **Every dated block above carries its release command for exactly that reason** — if
one is stale when you read it, run the command rather than believing the sentence.

## W-7 — rotate, and do not re-read these

Today's instrument was **`check-ci-status.mjs`** (its claim is "did CI pass"; what it reads is the
run-level conclusion, and those came apart in both directions today). Yesterday's was
`A-18.readCommand`. **Untouched candidates:** `check-posted-unpushed.mjs`, `history-sweep.mjs`,
`check-brief.mjs`, `traffic-snapshot.mjs`'s clone-attribution line, `report-rate.mjs`'s author
exclusion.

## State at close

**HEAD `39c510c`**, in sync with origin; the report and this primer land after it. `npm run verify`
**exit 0**, run four times today. Report linters **8 of 8 PASS**. Mirror **113 files at upstream
`5c4aeee`** (an upstream sha; it does not resolve in this repo — `git cat-file -t 5c4aeee` exits 128).
**233 claims: 231 hold, 2 UNVERIFIED** (`C-7`/`C-9`, the `manual: true` erdosproblems pins, correct by
design).

**Items: 43 total, 18 open.** `A-31` (publish gating) opened today; **0 open items missing a
`readCommand`, `closeWhen` or `onTrigger`** — six `readCommand` gaps were closed, each command run
before being written down and each carrying a positive control.

**Zero open issues, zero open PRs, zero Dependabot alerts.** No Sentry project — structural, because
nothing here runs as a service. Note that is a *different* fact from "nothing deploys", which was
false and was corrected today in four documents.

**Traffic: 3 unique viewers against 195 unique cloners** over the trailing 14 days, re-read today.
The clones are largely our own CI.

## The day's classification

**Instrument-facing, day seven** — every correction today was a defect in our own machinery. The last
record-facing catch remains 2026-08-14. **But that catch's OUTCOME landed on 2026-08-23**, which the
classification has no slot for and which answers the streak's own question from the other side: a lane
that only finds faults in its own tooling is maintaining itself — and this lane's last real catch just
changed a published mathematical record.
