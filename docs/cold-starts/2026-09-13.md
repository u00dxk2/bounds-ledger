<!-- GENERATED — regenerating REPLACES everything outside the hand: markers, so put prose inside them. Provenance stamp at the foot of this block. -->

<!-- hand:begin banner -->
> **PULL, THEN START THE GATE, BEFORE READING THIS** — three minutes, all network:
> ```
> git pull --ff-only
> npm run verify > tmp/verify-out.txt 2>&1
> ```
> Expect **exit 0**. Any non-zero code is yours; read `tmp/.verify-receipt.json`'s `failedGates`,
> never infer the leg.
>
> **BATCH YOUR LEDGER WRITES, THEN GATE ONCE — and never commit while the gate runs.** This cost
> two extra ~450-request runs on 09-12 and one refused post: a receipt taken while a commit lands
> carries `headMovedDuringRun`, and `agent-status` refuses a gates-green claim over it, correctly.
> The tempting move — re-run only the leg that reads the file you edited and argue the rest still
> holds — is the argument the receipt gate exists to refuse. It will usually give the right answer.
>
> **DO NOT TRUST ANY ID LIST IN THIS BANNER.** Run the resolver:
> `node scripts/sky.mjs check-due-gates-dispositioned.mjs --print --today=2026-09-13`.
>
> **TODAY'S ONE DUE GATE IS `A-45`, and its own read command carries two traps worth reading before
> you run it:** the ratio it prints is NOT quotable (the script says so in its own stdout), and it
> must be run LOCALLY — CI checks out at depth 1 and the script walks the whole history of
> `ledger/claims.json`, so a runner reports a smaller denominator for an infrastructural reason that
> looks exactly like a coverage change.
>
> **DAVID GETS THE TWO-INDEX CITATION COMPARISON ON 2026-09-16**, four days out and the only
> commitment made directly to him. Both inputs it needed now exist: reachability was proven 09-11,
> and `A-34`'s four criteria plus its candidate list were extracted onto the row itself on 09-12
> (`e0d8d6c`), so it starts without a 30 KB read of a closed row. **Progress: 0 of 3 candidate
> surfaces scored. Its denominator is surfaces × two indices × four criteria — NOT the 673 cited
> rows**, which belong to `A-47`. That conflation has now been relayed into this lane twice.
>
> **`A-43` OWES FOUR SOURCE LOOKUPS BY 2026-09-15** — the proved values behind the four
> machine-discovered rows in `ledger/teorth-optimizationproblems/constants/1b.md`. Record them as a
> SUSPICION-DRAWN audit slice, never folded into the systematic sample.
>
> **TWO AUDIT NUMBERS THAT ARE NOT THE SAME NUMBER.** The store holds **13 audits**; the public
> pages say **9 bound rows read**. The gap is three UNREACHABLE rows and one reference-entry leg,
> excluded by construction. Coverage is 9 of 673. Release: `node scripts/depth-audit.mjs` and
> `node scripts/depth-audit.mjs --corpus`.
>
> **`W-7` ROTATES.** 09-12's instrument was the gate resolver's own RESULT line, which printed
> `NOTHING-SWEPT` over a run that swept 61 rows and listed every due gate. Pick a different one, and
> write the line opening `W-7 —` that `docs/daily-config.md` requires.
>
> **NOTHING IS WAITING ON DAVID** as of 20:24Z on 09-12. Release:
> `node scripts/sky.mjs answered-cards.mjs --project bounds-ledger --full`.
>
> **ARRIVAL, AND WHICH SESSION THIS DESCRIBES.** Written by session
> `830878d4-cd6d-4861-a704-4d3f510f3a3b`, which arrived on a clean handoff, ran the full P1–P5 rail,
> and shipped depth-audit slice 3 plus `A-39`'s rate-limit fix. Check your own strand rather than
> inheriting mine: `node ../skylark-site/scripts/read-listener-strand.mjs --project bounds-ledger`.
<!-- hand:end banner -->

First command (also below, in place):

```bash
git pull --ff-only
# commits after this primer was written are invisible to it — read them before acting:
git log --oneline 443a3acedf7156c4c421ba94cb8d44fb28bb673d..HEAD
```

### Primer — bounds-ledger, 2026-09-13

What ONLY this primer holds comes first; the FULL runnable **First action** block
sits at the foot, below the reads. The single command above is the same block's
first line, repeated so it can be running while you read — it replaces nothing.

_**Composed against HEAD commit `443a3acedf7156c4c421ba94cb8d44fb28bb673d` at 2026-09-12T20:29:30.183Z.** Nothing that landed after that instant is in this file. David's board answers especially: an answer he gives later cannot be here, and this primer will go on reading as though the work were not owed — that is exactly how two lanes concluded an OTA was not owed on 2026-09-04. Read them live, never off this page: `node scripts/sky.mjs answered-cards.mjs --project bounds-ledger --full`._

_Shared skylark-site scripts below are spelled `node scripts/sky.mjs …` — this lane vendors `scripts/sky.mjs` (S-20260906-75), so a shared script is named LANE-RELATIVE and resolves from a worktree too._

#### Owed gates — 1 due on/before 2026-09-13

_Ledger read at 2026-09-12T20:29:30.183Z by `cc-due-gates.collectDue` — the same resolver `check-due-gates-dispositioned` uses, so any difference is TIMING, not two answers. Rows filed or re-dated after that are NOT READ at generation — the close-out commit lands after this, so the COUNT above is stale by construction and this list cannot claim completeness. The list below, not the count, is the artifact; re-derive both before acting: `node scripts/sky.mjs check-due-gates-dispositioned.mjs --print --today=2026-09-13`._

Unfiltered as of the read time above — nothing was curated out of what the read could
see; a row absent here may simply have been filed after it ran.

- `A-45` (expectedSignalBy=2026-09-13, open) — TIER-1 LEADING INDICATOR (orchestrator-approved 2026-09-06): stale-value resolvability — o
  - read: `node scripts/check-resolvability.mjs # BUILT AND RUN 2026-09-06 in 36a6b77; the earlier NOT-YET-BUILT caveat on this field is superseded by note6. Prints the coverage fraction, the search space on both sides, the MISSES with the digits a reader would actually type, and the excluded non-value pins. LITERAL OUTPUT pasted from the run, 2026-09-06 at 36a6b77: "resolvability: 140 of 141 (99.3%) — UNCALIBRATED, see below" then "*** THE FRACTION IS NOT YET QUOTABLE. Read the MISSES; do not quote the percentage. ***", exit 0. THE RATIO IS NOT THE READ AND MUST NOT BE QUOTED — the script says so in its own stdout, for a reason measured in both directions on its first run (it over-excludes real short bounds and leaks in one non-value; see note6). The MISSES list is the output that carries information. POSITIVE CONTROL that this can say otherwise rather than always printing a high number: an empty denominator exits 2 as NOTHING SWEPT rather than rendering as 100%, and the offline selftest (node scripts/check-resolvability.mjs --selftest, wired into npm test and into CI) proves a value absent from every haystack is reported as a MISS and that aliasing it in clears the miss, both polarities on one fixture with the mutation proven to land. RUN IT LOCALLY, NEVER READ IT FROM A CI RUN: CI checks out at fetch-depth 1 and this shells git log/git show over the whole history of ledger/claims.json, so a runner reports a smaller denominator for an infrastructural reason that looks like a coverage change.` · last run: NEVER

#### What shipped since 24 hours ago

_Read at 2026-09-12T20:29:30.183Z; commits landing after that — the close-out especially — are not here. Re-derive: `git log --oneline --since='24 hours ago'`._

- commit `443a3ac` — Report back-patch: the gate line named a receipt taken over a moving tree
- commit `884ef7a` — Daily report 2026-09-12 — five records held up, and the machinery that picked them was wrong twice
- commit `e035373` — P4: A-39 closes on today's fix, and three carried rows get evidence rather than a date bump
- commit `2a40d62` — A-39: a mid-run 429 no longer becomes a synchronized burst at the limiter refusing us
- commit `3d65a09` — Five more records read against their sources, and the draw that picked them is now executable
- commit `e0d8d6c` — A-48 carries A-34's four criteria and candidate shapes, extracted not retyped
- commit `4df975f` — P2: all three due gates read and stamped, and A-39 is live for the first time

**Yesterday's report:** `docs/daily/2026-09-12-prelaunch.md` (coverage 2026-09-12, the prior day). Findings fence: 3 line(s) under §State Appendix — OPEN IT; fence content supersedes the prose above it (first line: "**Appended after the report landed (20:25Z), correcting a State Appendix line above rather than rewr").

**Today's report (owed):** write `docs/daily/2026-09-13-prelaunch.md` — the artifact of record for service day 2026-09-13. Check it exists at close: `node scripts/sky.mjs check-daily-report-exists.mjs --date 2026-09-13` (it sweeps the fleet and applies each lane's coverageOffset; it takes no --project).

#### State — as of 2026-09-12T20:29:30.183Z (re-run the release command; never trust the value)

- **ledger**: 61 item(s): 44 closed · 17 open · as of 2026-09-12T20:29:30.183Z — release: `node scripts/sky.mjs gen-primer-first-action.mjs --repo . --target-date 2026-09-13 --json`

#### Id glossary — 21 id(s) this primer's day already has words for

_Each gloss is the ledger row's OWN title, not a re-derivation — paste it. Rows = every open ledger row plus every id named above. `(no ledger row)` means the id is named here and the ledger carries nothing under it: that is a finding, not a formatting gap._

- `A-2` — STANDING: drift-resolution log for the adopted surfaces…
- `A-7` — engineering-health P1s — four of five shipped 2026-08-09;…
- `A-9` — engineering-health P2 backlog (fix-on-touch, not scheduled…
- `A-33` — A change to a file we have filed an upstream report…
- `A-34` — David asked where else this ledger can have the kind of…
- `A-39` — A mid-run 429 becomes up to 339 requests in two seconds at…
- `A-43` — Upstream moved Haugland's minimum-overlap bound to its…
- `A-45` — TIER-1 LEADING INDICATOR (orchestrator-approved…
- `A-46` — A close-out post announcing a FIX is textually identical…
- `A-47` — THE DEPTH AUDIT, David-ruled 2026-09-09: read our own…
- `A-48` — The second-area citation comparison David asked for,…
- `G-2` — GOAL: contribute a verified bound improvement to the…
- `G-4` — GOAL (Tier-0 from G-3 close): an outside party acts on a…
- `S-20260906-75` — (no ledger row)
- `TIER-1` — (no ledger row)
- `W-3` — Watch for acknowledgement of the erdosproblems.com/36…
- `W-4` — KP-78 applies to every new detector: demonstrate BOTH…
- `W-6` — Read window for the README report-an-error channel — the…
- `W-8` — Does a materially amended outward artifact go back to…
- `W-9` — Does this lane's detection method find what the lane…
- `W-10` — reverify.test.mjs alarm-title leg failed once and would…

### First action — bounds-ledger, 2026-09-13

Every line in the block below is a COMMAND, not a claim. A command re-reads at the
moment you run it and cannot go stale; a state line written last night can.

```bash
git pull --ff-only
node scripts/sky.mjs update-david-board.mjs --list --json --full-ids --project bounds-ledger
git rev-parse HEAD origin/main
git rev-list --count origin/main...HEAD
npm run verify > tmp/verify-out.txt 2>&1
node scripts/sky.mjs check-ci-status.mjs --workflow reverify.yml
```

Morning release commands — deliberately NOT READ at generation (a value written
last night would already be stale); run each, never trust a remembered one:
- `git -C . log -1 --format=%h%x20%s` — HEAD
- `git -C . status --porcelain` — working tree
- `node scripts/sky.mjs check-ci-status.mjs --workflow reverify.yml` — CI
- `node scripts/sky.mjs check-deployed-sha-drift.mjs` — deploy
  - WHERE IT LOOKED: no read-only deploy-state script in this lane's package.json (looked for ops:deploy-state, deploy-state, check-deploy, render-deploys, deploy:status, daily-snapshot). The fleet's check-deploy-state.mjs is not a substitute — it requires --fix <sha> --deploy <sha> and exits 1 without them — so this is the FLEET read, which takes no required flags. service: NOT DECLARED — no row for "bounds-ledger" in skylark-site/src/data/cc-deploy-gates.json (generatedAt 2026-09-11T05:03:44.019Z, 17 repo row(s)) — nothing in this repo declares that lane's Render service. No `--service` flag is emitted: the lane SLUG is not the service name (antebellum-twitter's is disunion-feed), and a guessed flag exits 1.

<!-- hand:begin narrative -->
_(orchestrator narrative — this region survives regeneration)_
## Session close-out — 2026-09-12

_Appended at close on 2026-09-12 MT by session `830878d4-cd6d-4861-a704-4d3f510f3a3b`._

### The day, in one paragraph

Five more cited records were read against the sources they name and published, and for the first
time a whole slice held up: three matched outright, one matched once the paper's body was read, and
one could not be read at all because the publisher served a bot wall. Coverage went from 5 to 9 of
673. The machinery that PICKED them — all five listed in `continuity/depth-audit.json` as
`A-47-0009` to `A-47-0013` — was wrong twice, caught by adversarial review before the commit, and
the review also read further than I had and upgraded a verdict I had left unresolved.
`A-39` closed on its fix: a rate-limited response no longer becomes a synchronized burst at the
service that just refused us.

### Load-bearing ships

- `3d65a09` — **the only user-visible ship**: five audited rows on the constant pages, and a draw
  that is now executable (`depth-audit --draw`) rather than described in prose.
- `2a40d62` — `A-39`'s fix, red-armed through the production constant; `e035373` closed the row.
- `4df975f` — all three due gates read by RUNNING them, two of which had never produced a genuine read.
- `e0d8d6c` — `A-34`'s four criteria and candidate list extracted onto `A-48`, so tomorrow's work on
  the only commitment to David starts without a 30 KB read of a closed row.

### What NOT to touch tomorrow

- **The adversarial review on the working tree before the commit.** Both of today's blocking findings
  were defects in code written minutes earlier, and the row hash matched in both failure modes — no
  other check could have seen them.
- **The receipt gate's refusal.** It blocked a post whose receipt was taken while a commit landed, and
  the escape I had planned — re-run only the leg that reads the edited file — is the argument it
  exists to refuse. It would have given the right answer today.
- **The three verdict wordings on the public pages.** Read, reached-but-unsettled, and could-not-read
  each render differently, and unreachable rows are excluded from the read count by construction.

### Tier-0 binding constraint

`G-4` (an outside party acts on a watched record without us filing the report) reads 0 arrivals,
measured: `npm run reports` fetched 29 issues and accounted for all 29 as ours. The interim yardstick
is `A-47`'s coverage — 9 of 673 cited rows read against their source. Coverage, never a pass rate.

### Evangelism read

**The core problem, in a reader's words:** "I am about to cite a number and I do not know whether it
still holds." **Distance from the would-tell-a-friend moment: unchanged, and today is the first day
the honest answer to a mathematician is boring in the right way** — we read five of our own records
and all five stood up. **Retention and word-of-mouth remain structurally unmeasurable here**: the
published pages fetch nothing by construction, and `npm run traffic` reads repo views on github.com,
never page readership. 1 unique viewer in the trailing 14 days. No real-user advocacy signal.
<!-- hand:end narrative -->

<!-- generated by gen-primer-first-action.mjs (skylark-site) @ commit accf3f567 · lane bounds-ledger · for 2026-09-13 · at 2026-09-12T20:29:30.183Z -->
<!-- writtenAtSha: `443a3acedf7156c4c421ba94cb8d44fb28bb673d` — the lane's HEAD commit when this primer was written. Provenance anchor for check-primer-freshness leg ii; NOT a claim about HEAD now (the close-out commit that carries this file lands after generation). -->
<!-- reading this block: a generated line that is wrong in a way you can see FIXED in skylark-site's git log after the commit above is STALE, not a finding — regenerate before filing it. `git -C C:/dev/skylark/skylark-site log --oneline accf3f567^{commit}..HEAD -- scripts/gen-primer-first-action.mjs src/lib/cc-primer-commands.mjs src/lib/cc-due-gates.mjs src/lib/cc-report-artifact.mjs src/lib/cc-hand-edit-merge.mjs src/lib/cc-cold-readability.mjs src/lib/cc-skeleton-gloss.mjs` -->
