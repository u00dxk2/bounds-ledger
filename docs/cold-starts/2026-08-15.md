# Cold start — bounds-ledger — 2026-08-15 (MT)

Paced rail, day 14. Steward cadence first, then one increment. No self-rating.

## Session close-out — 19:45Z (2026-08-14, ~13:45 MT)

### Tomorrow's first action

Run the cadence. `npm run check` first, then read the overnight CI log for the **final pushed
commit** — which is `60b1820`, not whatever the badge shows.

```bash
cd C:/dev/skylark/bounds-ledger && npm run check
```

If it goes red, that is the alarm working. Verify against primary sources, then use the new
one-command ratchet rather than the three steps by hand:

```bash
cd C:/dev/skylark/bounds-ledger && npm run resnap
```

### The one thing that is David's, and it did not get answered

**A-15. He parked the Mathstodon post to 2026-08-14 — "let's revisit the post tomorrow" — and
that day passed without him in the pane.** The park **rolls forward**; the item is still open and
the answer is still owed. **A passed date is not a no.** Do not re-file a board card; the
orchestrator carried it in its surface-to-David set yesterday and whether it reached him is
unknown to this pane. Ask **once**, in **one sentence**, the next time he is here — and not twice.

The artifact is ready and unsent: draft at `docs/decisions/2026-08-13-mathstodon-announcement.md`,
nine-angle review at `docs/findings/2026-08-13-mathstodon-adversarial-review.md`. It does not
decay — every figure in it is historical inside a closed date window.

### What happened yesterday, in one paragraph

The overnight alarm fired and was right. Upstream `e70b4a4` — an upstream sha; it does not exist
in this repo, `git cat-file -t` returns *Not a valid object name* for it while our own `60b1820`
returns `commit` — added the page our mirror carries at
`ledger/teorth-optimizationproblems/constants/87a.md`, Martinet's constant for totally real fields.
One push by Terence Tao, not a burst. Resolved, re-pinned, state block re-rendered; 113 files,
233 claims, 231 hold, 2 manual.

### The find, and the thing about it worth carrying — A-16

Verifying the new page before snapshotting produced the day's real output. Its current-record row
offers an explicit witness, `rd ≤ 3^4·5^4·7^4·13^2·29^4·53^2·109^2 ≤ 857.5662`. That product is
a twenty-four-digit integer, so the inequality as typeset is false. Take the eighth root the
sentence itself invites and you get **913.492694** — to six significant figures, the value on the
row directly above, the record this one superseded. The witness offered for the new record is the
arithmetic of the old one.

**Held behind the outward gate.** Adversarial refute-it review FIRST, then David. Not urgent. If
it goes outward, audit the method sentence as its own angle: the claim is **internal
inconsistency**, and it must never be dressed up as having checked 857.567 against the paper.
`arXiv:1901.04354` is the right document — its abstract independently names the *refined
Golod–Shafarevich criterion* — but it states no numeric value.

**Say this plainly if you write about it: the alarm did not catch this and was never meant to.**
The mirror is byte-faithful including the defect. A human reading a new page found it, and there
is no detector behind that step.

### Shipped yesterday that changes how you work

- **`npm run resnap`** — the post-snapshot ratchet is THREE steps (snapshot → extract-pins →
  render-state-block) and the docs said two, which cost a red check. One command now. Content-
  idempotent when already in sync; only the manifest's `fetchedAt` moves, and you should not
  commit that alone.
- **The month-of-zeros rule was amended** (`60b1820`, orchestrator-approved). The zeros no longer
  fire the adopt-a-second-surface trigger alone — read `docs/findings/` over the same window
  first, and dry means no movements AND no findings. Adoption itself stays David's call. There is
  deliberately **no** counter over findings files; a number we can raise by writing more files is
  inflatable like a self-rating.

### Two habits that failed yesterday, both mine

- **The `verify-sha` gate caught the same unglossed upstream sha twice**, both times because I
  wrote the bus body first and glossed only after the refusal. Gloss `e70b4a4` at the point of
  writing.
- **`--supersedes` posted clean and did not drain the row.** I passed it on the second retro post;
  the CLI reported verified-on-bus; both rows still return from `/api/cc/agent-msg` with
  `excludedCount` 0. That projection exposes no `supersedes` field, so this pane cannot tell
  whether it was stored and ignored or never stored. Reported as an observation, not a diagnosis.
  **If you use `--supersedes`, verify the drain rather than trusting the POSTED line.**

### What NOT to touch

**`catch-rate.mjs`'s silence on added and removed pins.** It stayed correctly quiet while a
constant was added, which is exactly what its selftest asserts. It counts only *movement* in text
regenerated solely from the mirror, so it cannot be inflated by our own edits. After a day like
yesterday the temptation is to "improve" it to notice additions so the number reflects the work.
That would destroy the one property that makes the number trustworthy.

### State at close

`main` at `60b1820` plus the close-out commit, tree clean, local and origin agree, PUBLIC.
Six commits on 2026-08-14, all pushed. `npm test` exit 0 (13 self-tests); `npm run check` exit 0
with the pin set — no drift at 113 files against `e70b4a4`, 233 claims, 231 hold, 0 broken, 2
unverified and manual, state block in sync, brief in sync with all 4 dated blocks.

Open continuity items: **10**. **G-1 — the goal tracking externally-acknowledged corrections**
stands at 1 and stays open on the 30-day green streak alone. **W-3 — the watch on the
erdosproblems.com email** is unanswered and is a DIFFERENT channel from G-1; do not close it on
PR #141. **W-6 — the watch on arrivals** was sampled 8/14: 3 viewer-days since the flip, an upper
bound on people rather than a count, against a threshold of 90 days and 100 visitors. Run
`npm run traffic` again — the API keeps only 14 days, so a skipped day is lost permanently.

Cards waiting on David: **0**. **Codex calls: 0** — the probe read RED at 14:18Z and was later
confirmed a probe defect rather than an outage, so nothing was dispatched and nothing was
debugged solo.
