# Cold-start primer for 2026-08-23

Multi-session primer — each close-out appends a section below. Later sections supersede earlier ones.

## Session close-out — 2026-08-22

Paced rail, day 21. **G-1 closed. The lane has a new top goal and it reads zero.**

### Tomorrow's first-action — VERIFIED, exit 0 in the turn that wrote this

```bash
cd C:/dev/skylark/bounds-ledger && git rev-parse HEAD origin/main && npm run verify && node ../skylark-site/scripts/check-ci-status.mjs --workflow reverify.yml
```

Written to a file and executed verbatim, never retyped: printed `7fba042dd6…` twice, `npm run verify` **exit 0**, then CI **GREEN**. Whole chain **exit 0**.

**It is `git rev-parse HEAD origin/main`, NOT `--short`** — `--short` takes exactly one revision and exits 128, and its leading position in an `&&` chain silently skips everything after it. That defect has now been reproduced twice by people who had read the rule that morning.

### THE THING THAT WILL MISLEAD YOU FIRST

**`north_star_value` is 0 and that is a promotion, not a regression.** G-1 (the 30-day stewardship goal) closed today at value 1. G-3 — *someone outside Skylark uses the ledger and acts on it* — is Tier-0 now and legitimately reads zero, probably for months.

**Copy the report frontmatter from `docs/daily-config.md`, NOT from yesterday's report.** That block was added today precisely because the default behaviour is to inherit, and inheriting would headline a closed goal's flattering 1/green while the live goal reads nothing. The four fields are written out there with the reason.

The question G-3 asks on 2026-09-22 is **not** "has an outsider arrived" but "could we DETECT one if they did" — and that is now `G-3.readCommand`, five legs, run it rather than reasoning about it.

### TODAY — the named user-visible intent, before the ledger work

**Ship: let a visitor see which rows moved MOST RECENTLY, without scanning 111 of them. Item: `G-3`.**

One sentence of why, so you can disagree rather than inherit: every row already carries the date it last changed — that was the point of the page — but the only way to find the ones that moved this month is to read all 111. A researcher's real question is often *"has anything I rely on moved lately?"*, and the page holds the answer while making them do the work. Small, content-and-markup only, no new dependency, same shape as today's ship.

**Verify the premise against `index.html` BEFORE writing code.** Today's primer named a ship whose premise was false, and the check took ninety seconds.

### What a user can now see that they could not

**Every one of the 111 rows carries a "looks wrong?" link** (`1995839`) opening a GitHub issue pre-filled with that constant, its id and the mirror sha, plus prompts for what the source says and where they saw it. Before, one link in the intro served the whole page and carried no context, so a reader at row 87 had scrolled ~700 lines past it and then had to retype which row they meant. Verified on the SERVED page: 111 links, with the H1 present in the same fetch as the positive control.

Named principle: **Nielsen heuristic 6, recognition rather than recall.** A ledger whose product is honesty should make *being told it is wrong* the cheapest thing on the page.

### G-1 is closed — what that means mechanically

Both legs met; David approved on 2026-08-20 with a do-not-re-ask, so it was executed, not re-asked. **Honest limit on today's streak read, repeated here because the streak IS the claim:** `gh` returned exactly 200 rows, its `--limit` cap, so the read directly confirms 2026-08-02 onward (21 consecutive scheduled days, no gaps). The 07-24..08-01 leg rests on the API read recorded in `G-1.signalPrestaged` on 08-17. Raising `--limit` would re-verify it; that was not run.

**G-3 is Tier-0, G-2 is Tier-1.** G-2 gained `expectedSignalBy: 2026-09-22` and a `readCommand` today — **the lane set that date, not David, and it is flagged for overrule.** Its state at dating: round 1 is 11 rows all verdict DEAD but **4 of them scored, best χ 9**; round 2 best χ 8; round 3 best χ 9. **Best achieved anywhere equals the 1980 record and beats nothing.**

### Traps this session actually hit, in the order they will bite you

- **A written plan is not evidence about the artifact it describes.** Today's ship was named as "move the report link out of the footer, below all 111 rows." There was nothing in the footer: the link was at `index.html:41`, *above* the table. Shipping the sentence as written would have been a no-op indistinguishable from success. **Read the artifact before planning against it** — and when the claim is an absence, get a positive control first (the footer read returned its three paragraphs, proving the reader worked).
- **Do not call a background Codex job dead on one poll.** `codex-wait --job <id> --once` returned *no job found* and `git status` showed no diff, and I wrote it off as producing nothing and started redoing the work by hand. It was still running; it landed a few minutes later and did the job better than my plan. **Check the FILE, then check again after a pause, before concluding a dispatch failed** — and never start duplicating a subagent's scope on one negative poll.
- **A guessed field name is not one defect, it is a CLASS — and I proved that the hard way, twice in one hour.** The new `G-2.readCommand` first summed on `r.chi`, which rounds 2 and 3 do not have (their key is `bestChi`); it printed `best chi=0` for two rounds that had actually reached 8 and 9. Caught by the prove-the-zero rule forcing a look at the raw keys. **Then the FIX shipped the same bug mirrored:** testing only `bestChi` made round 1 report as carrying no chromatic value, when 4 of its 11 rows were scored at up to 9 — they died at the *chromatic* gate, not before reaching it. `propose-round.mjs` writes `out.chi`; the round-2/3 growers write `bestChi`. **Only the mandated sibling sweep caught the second one.** The command now tests both keys and, when neither is present, names the keys it checked instead of implying absence. Fixing the instance you noticed does not fix the class.
- **The `sha-file-claim` WARN is working as designed — ruled 2026-08-22.** A filename beside a commit sha IS a modification-claim shape; the upstream carve-out exempts only quoted linter output. **Interim writing rule until syntactic attachment ships: keep a filename either inside a `[linter-report]` quote or off the sha's line.** Not a licence to reword posts to dodge the linter.

### What is owed

- **Monday 2026-08-25 is heavy: four gates.** `A-25` (deferrals get an expiry field and a check that reads both) — **its single re-date is SPENT; Monday it ships or closes as declined, it does not roll again.** `A-26` (author the evangelism-bar doc and record a loop archetype). `A-18` (generated STATE block for the primer). `A-19` (two non-blocking review nits).
- **`W-7` is due 2026-08-24** — whether the 2026-08-15 instrument-audit proposal was ever tracked.
- Nothing is owed to David, and no David-blocker exists.

### The honest label on today's catches

**All three instrument-facing — the sixth consecutive day.** Last record-facing catch 2026-08-14. The standing prediction is carried forward **unchanged and uncontradicted**: the next record-facing catch will be a witness-value mismatch on a constant upstream added within ~30 days, found by a human recomputing a cited certificate and flagged by **no instrument we run**. If one arrives through an alarm instead, the prediction was wrong — say so.

### State at close

Tree clean, HEAD `b7f5f3c`, in sync with origin. **CI is RED at that commit and the red is CORRECT — read this before you investigate it.**

**The red is a TRANSPORT failure, not a record movement.** `pin:78a:U` (the Conway thrackle constant) came back **HTTP 503** from `raw.githubusercontent.com`. The run reported **233 claims: 230 hold, 1 unreachable, 0 BROKEN**. Zero broken means no cited surface moved. `UNREACHABLE` is not `BROKEN`, and the run correctly stays red anyway — unreachable means unverified, and a green there would launder an unverifiable into a pass. **Do NOT "fix" this by making a 503 non-fatal.**

The same commit ran `npm run verify` **green locally** (233 claims, 231 hold), so the source is reachable from a residential IP and the pin holds — that is the evidence it is transport and not drift.

**The alarm titled it correctly**, which is worth noting because it is the 2026-08-17 defect proven fixed in production: issue **#28, "Check error: bounds-ledger could not reach 1 cited source(s)"** — a `Check error:`, NOT a `Drift:`.

**RESOLVED before close, and this is the end state: the 503 was transient.** The next run to reach a verdict — the push run for `465c939`, which came free with the commit correcting these very documents rather than from a dispatch — was **GREEN**. **#28 is CLOSED**, naming that sha, because a permanently-open alarm issue is how a reader learns these can be ignored.

**So: HEAD `465c939`, CI GREEN, zero open issues.** Your first action re-reads CI live anyway, so trust that over this paragraph if they disagree.

**Do not dispatch `reverify.yml` to chase green** (~450 requests per run). The scheduled run at ~09:2x UTC re-checks on its own, and any push you make triggers one for free. Mirror 113 files at upstream `e70b4a4` (an upstream sha; it does not resolve in this repo). 233 claims — 231 hold, 2 UNVERIFIED (`C-7`/`C-9`, the `manual: true` erdosproblems pins, correct by design). Zero open issues, zero open PRs, zero open drift issues, zero Dependabot alerts. `CLAUDE.md` is back under budget at 40,314 bytes — the catch-rate derivation now lives in that script's own header, so **read `scripts/catch-rate.mjs` for the why, not CLAUDE.md**. Traffic: 4 unique viewers against 183 unique cloners; the clones are largely our own CI, and the viewer count is the only arrival figure that is not mostly us.

## Session close-out — 19:56Z (PM, close orders)

Supersedes the earlier block only where noted; everything else there still stands.

**A fresh-agent prompt is waiting, and it is the point of this block.**
`docs/cold-starts/fresh-agent-bounds-ledger-2026-08-22-pm.md` — 75 lines, paste-ready, one
user-visible build (surface the recently-moved rows so a visitor need not scan all 111). If you
are that cold session, read THAT file, not this one.

**Ledger hygiene done at close, all of it visible to machines now:**
- Three standing rows that had no clock — the drift-resolution log, the engineering-health P2
  backlog and the KP-78 watch — gained `nextCheckDate` (08-24, 09-03, 09-22). **Not
  `expectedSignalBy`**: a signal date promises a verdict these rows do not have, a re-read date
  promises only that someone looks. That distinction is written into each row; do not "upgrade"
  them to signal dates.
- Seven commits from the 08-21 session were in no item. All seven now linked, mapped by what each
  commit TOUCHED rather than by its subject line. Nothing went to `meta.untrackedCommits` — every
  one had an unambiguous owner once the file stats were read.
- W-8 (the watch on whether a materially amended outward artifact re-gates to David) gained a
  `closeWhen` with an explicit **Branch 0**: if no outward send happens by 09-22 the discriminator
  is inapplicable, and the row re-dates while SAYING the window was empty. Two consecutive empty
  windows means the outward channel has gone quiet, and that is the finding, not the original
  question.
- `continuity-check` ends at **status: OK**, zero CRITICAL and zero WARN.

**One waiver you should not undo.** W-8 carries `standingPatternWaived`. The detector matched the
phrase "standing rule" inside its `closeWhen` and read it as the row declaring itself
never-closeable; it says the opposite — the ANSWER becomes a standing rule and either answer
closes the row. Waived with the reasoning recorded, deliberately **not reworded**, because
rewording to slip past a phrase match is gaming the detector. Same defect class as the
`sha-file-claim` linter that fired on a P3 post earlier the same day.

**Nothing is waiting on David.** Awaiting-David query returned HTTP 200 with zero rows, and the
board carries no unread card. That is a read, not an assumption — a non-200 would have been
reported as a probe failure instead.

**State at this close:** HEAD `f328810` at the time of the ledger work, tree clean, CI GREEN,
zero open issues, zero open PRs, zero Dependabot alerts. The close-out commit follows this block.
