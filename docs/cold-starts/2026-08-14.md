# Cold-start primer for 2026-08-14 (MT)

Multi-session primer — each /close-session run appends a new section below. Later sections
supersede earlier ones explicitly.

## Session close-out — 20:20Z (2026-08-13, ~14:20 MT)

You are the bounds-ledger agent. Read this, then `AGENTS.md` + `CLAUDE.md`, then
`continuity/items.json`. The repo is PUBLIC.

### Tomorrow's first action

**`npm run check` FIRST, with `CC_PROMPTS_PIN` in the environment. Then read the overnight CI
log.** Two reads, two different questions; only the first is actionable at 07:45.

```powershell
npm run check
```

Expect: `No drift. 112 files match upstream 603052de…` (an **upstream** sha — like every
`teorth/…` sha in this file, it does not resolve in this repo; do not `git show` it),
`231 claim(s): 229 hold, 0 broken/unreachable, 2 unverified (manual)`, `state block: in sync`,
`brief: in sync`. Green → **streak day 22 of 30** (day 1 = 2026-07-24).

Then the log, for the **final pushed commit**:
`gh run view (gh run list --limit 1 --json databaseId --jq '.[0].databaseId') --log`

Then `npm run traffic` — GitHub keeps only 14 days, so a skipped fortnight is gone for good.

**New this session:** `npm run catches` is now part of the cadence. It is an indicator, not an
alarm — it renders a figure and no verdict, and never fails a build.

### Then the increment — the chain is staged, and the next link is named

**The Mathstodon adversarial review, and only the review.** The orchestrator confirmed the
sequencing today. It is the north-star-adjacent increment because arrival is the binding
constraint on a *second* acknowledgement, and the findability menu named Mathstodon first
*conditional on having a merged PR to point at* — we now have one.

**The review is ours to do; the send is David's.** When the review passes, surface ready-state
as a board card with the draft attached. **Do not card it before the review** — that skips the
gate, and 0 cards today was correct for exactly that reason.

**Sequenced after it, orchestrator-approved:** have the cold-start primer **generate** its own
"approved but unbuilt" section from `continuity/items.json`. Today's own failure is the argument:
the approved-and-unbuilt indicator was sitting in the primer's Tier-0 prose, not in the candidate
list, so a session that skimmed to "Then the increment" would have left it unbuilt a second day.
`render-state-block.mjs` is the pattern — generate the volatile part, hand-write only the judgment.

### Today's ships (3 commits, all pushed)

- **`6eec1e7`** — `scripts/catch-rate.mjs` + `npm run catches`: the Tier-1 leading indicator,
  approved 8/12 and unbuilt until today. **A catch is a GENERATED pin whose `expect` string
  changed** — mechanically record movement, because those pins are rewritten only from the
  mirror. Editorial drift leaves them byte-identical and correctly counts zero.
- **`d4a733d`** — the same indicator, relabelled after orchestrator feedback: it counts
  **MOVEMENTS**, a ceiling on catches, and the header now says so.
- **`09d9e3d`** — `docs/daily/2026-08-13-prelaunch.md`, 8/8 report linters PASS.

### The two defects I built and then found, because they are the reusable part

1. **I built the exact inflation `CLAUDE.md` warns about, one day after the rule was written.**
   The first version counted `pin:2a:U` three times for the 8/12 upstream burst — and the burst
   rule names *this indicator* as its victim. I caught it from the output, not from the rule.
   **The lesson the orchestrator named, which is worth more than the fix: when a rule names a
   specific corruption, adoption means a TEST THAT ASSERTS IT, not a resolution to re-read the
   rule.** Apply that to the next detector you ship.
2. **A pin can move without a record moving.** `pin:15a:U` moved on a citation-key rename with
   every bound value byte-identical — and that pin was *our own PR #141 landing*, not a catch.

**Deliberately not fixed, and the refutation is recorded so you do not spend a day rediscovering
it: DO NOT BUILD THE VALUE-PARSER.** Separating a value change from a citation change means
parsing the value out of the cell, which symbolic cells (`$K_{DR}+10^{-26}$`), negatives and
O(·) asymptotics defeat — a prototype already mis-ranked 10a/21a/41a this way on 7/24. It would
put an unverified mathematical judgment inside our own ledger. **The pin rule applies to the
indicator built on the pins.**

### How to read the new number without misreading it

**Quote the PER-WEEK figure, never the running total.** The total answers no question we ask of
it and only ever grows, so it reads as progress by construction. Today: weeks of 20 Jul / 27 Jul
/ 10 Aug = **0 / 4 / 4**.

**The positive side is an activity ceiling; the zero side is exact.** That asymmetry is what
makes the ceiling safe to act on: a zero on an upper bound is a true zero, and the decision rule
attached to it consumes only the zeros — **a month of zeros means adopt a SECOND surface, not try
harder on this one.**

**Recorded trigger, not yet built:** deduct our own merged PRs before any positive figure goes
*outward*, or once own-PR pins exceed roughly one a month. At N=1 it is not worth building, and
git cannot see whose commit upstream merged.

### CI

**`reverify`: success @ `d4a733d`**, read from the log and not the badge — real check output, and
the new `catch-rate.mjs --selftest` step is visible executing on the runner. 13 self-tests across
19 workflow steps.

### Tier 0 / binding constraint

**North star: 1** (Tao merged PR #141 on 8/11; G-1's acknowledgement condition is MET and the
item stays open on the 30-day green streak alone). **W-3 — the erdosproblems.com email — is a
DIFFERENT channel and is still unanswered; do not close it on the merge.** Both advisory legs
read unchanged today: the page still shows `0.380876`, still dated 23 January 2026.

**W-6 — arrival: 3 unique viewer-days since the repo went public, day 6 of 90, 0 referrers.**
Nothing on the internet links here. Viewer-days overcount distinct people, so 3 is an upper bound
on an upper bound. Threshold is 100 unique visitors across 90 days.

### Evangelism read, honestly

The ledger is the product and it still has no users. The would-tell-a-friend moment is a stranger
finding a record we caught moving before their own source did — nobody has arrived to have it.
The adjacent moment already happened: the maintainer of the surface we watch merged a correction
we found. **Retention/word-of-mouth instrument: the traffic sampler, day 6 of 90, cohort empty.
Advocacy signal: one merged PR, unchanged today.** The binding constraint is *arrival*, not
product quality — which is why the Mathstodon review is next and not another detector.

### Nothing is waiting on David

Awaiting rows for this project: **0** (HTTP 200, real probe, not an assumption). Board clear.
Zero cards filed today, deliberately.

### What NOT to touch

- **Do not build the value-parser** for the catches indicator (above). If a future session
  proposes it, the proposal is the alarm.
- **Do not hand-edit README's `state-block` region** — regenerate it with
  `node scripts/render-state-block.mjs`.
- **Do not touch the CI-wiring guard in `reverify.test.mjs`** — the assertion that every
  self-test `npm test` runs also appears as a workflow step. It caught today's new self-test in a
  negative control within seconds, and it works because it *derives* the expected set from
  `package.json` rather than checking a hand-maintained list. That claim was false for six days
  once (A-8); the guard is why it is a fact now.
- **Do not revive A-14.** It was refuted on the maintainer's own git history, not deprioritised.
- Do not re-automate C-7/C-9 — the erdosproblems 403 is IP-dependent; only a green CI run is
  evidence about CI.
- Do not put `CC_PROMPTS_PIN` in repo secrets. Public repo.
- Do not re-propose the public comment on /36 — decided NO on 2026-08-02.
- Do not send anything outward off your own bat; every Tier-2 findability move is David-gated.
- Do not hand-edit the mirror or the generated pins; do not weaken the 27b verifier.
- **Do not "upgrade" the generated-pin rule.** A pin asserts listing position, never "the record".

### Watch items maturing

None inside 24h. **A-7** — engineering health — is open on the fleet-owned scheduled history
sweep and is not ours to move; its 2026-08-16 date on the fleet wave is two days out.

### State at close

`main` at the primer commit, tree clean, local and origin agree, PUBLIC. 3 code/report commits
today plus this close-out. `npm test` exit 0 (13 self-tests). `npm run check` exit 0.
`npm run catches` reads 0 / 4 / 4 by week. Daily report `docs/daily/2026-08-13-prelaunch.md`,
8/8 linters PASS. Open continuity items: **8**. Dead references: 0. Cards waiting on David: 0.
**Codex calls: 0.**
