<!-- GENERATED — regenerating REPLACES everything outside the hand: markers, so put prose inside them. Provenance stamp at the foot of this block. -->

<!-- hand:begin banner -->
> **KEEP THIS BLOCK UNDER 2000 CHARS.** The kickoff quotes it whole below that and cuts the MIDDLE,
> so a long banner loses its own instructions while keeping its head and tail.
>
> **PULL, THEN START THE GATE, BEFORE READING THIS DOCUMENT** — three minutes, all network:
> ```
> git pull --ff-only
> npm run verify > tmp/verify-out.txt 2>&1
> ```
>
> **THE GATE HAS NO PRE-EXCUSED RED ANY MORE. ANY non-zero code is real and IS yours.** For four days
> this block told you exit 3 was `A-41` and not your problem; that pre-excuse is GONE because the red
> is gone — `A-41` closed 09-08 and the brief leg no longer contributes to the exit code. Expect
> **exit 0**. Read `tmp/.verify-receipt.json`'s `failedGates` for the leg, never infer it. The wall
> itself is unchanged: `node scripts/check-brief.mjs` still exits 3 and prints `BRIEF UNVERIFIABLE`
> every run — reported, never green, never counted. **If THAT command ever exits 0, the auth model
> changed and `A-41` reopens.**
>
> **THREE GATES DUE — `A-31`, `A-34`, `A-42`.** `A-34` is David's own question and is on its FOURTH
> slip; it has been outranked four days running. Either write it first, before anything else claims
> the day, or say plainly that the lane keeps choosing other work over it — a fifth silent re-date is
> the one outcome the row forbids.
>
> **THE CODEX-SHAPED TASK IS NAMED AND CARRIED:** `check:deferrals` reports `RESULT: PASS` over an
> EMPTY population — vacuously true, no zero-population branch. `check-resolvability` and
> `report-rate` both refuse a bare zero; the one leg inside the exit code is the one missing the
> guard. 09-08 posted a `probed-declined` zero in EVERY phase — dispatch this rather than
> hand-write it. (Deliberately not a count: an earlier draft said "five", which was already six
> by the close. A number that ticks after the document is written is the wrong thing to assert.)
>
> **BEFORE YOU BELIEVE ANY ZERO YOU MEASURED, RUN ITS CONTROL — 16 of 16 lanes hit this on 09-08.**
> The shape: a reading taken where the answer could not exist, written down as fact. Mine was a
> `node -e` probe that reported 0 radical rows because the shell ate its backslashes. **Two commands,
> not a reminder:** `node scripts/check-notation-coverage.mjs` exits **2 NOTHING SWEPT** rather than
> printing a clean zero when its patterns match nothing; and any backslash-bearing probe goes in a
> FILE — `node -e` with `\\` in it is the known-corrupting form, measured six times in this repo.
> If you write a new probe today, give it a control that refuses BEFORE you read its number.
>
> **DO NOT QUOTE `check-resolvability`'s PERCENTAGE.** It prints its own UNCALIBRATED banner; the
> MISSES list is the output. Calibration is dated 2026-09-13 and owned by this lane.
>
> **A red drift run is the alarm working.** Verify against PRIMARY SOURCES before `--snapshot`, then
> `npm run resnap`, then a SECOND `render-site.mjs` AFTER the commit.
<!-- hand:end banner -->

First command (also below, in place):

```bash
git pull --ff-only
# commits after this primer was written are invisible to it — read them before acting:
git log --oneline df460f0937450111d897231a7e018a42da9e82dd..HEAD
```

### Primer — bounds-ledger, 2026-09-09

What ONLY this primer holds comes first; the FULL runnable **First action** block
sits at the foot, below the reads. The single command above is the same block's
first line, repeated so it can be running while you read — it replaces nothing.

_**Composed against HEAD commit `df460f0937450111d897231a7e018a42da9e82dd` at 2026-09-08T23:14:46.266Z.** Nothing that landed after that instant is in this file. David's board answers especially: an answer he gives later cannot be here, and this primer will go on reading as though the work were not owed — that is exactly how two lanes concluded an OTA was not owed on 2026-09-04. Read them live, never off this page: `node scripts/sky.mjs answered-cards.mjs --project bounds-ledger --full`._

_Shared skylark-site scripts below are spelled `node scripts/sky.mjs …` — this lane vendors `scripts/sky.mjs` (S-20260906-75), so a shared script is named LANE-RELATIVE and resolves from a worktree too._

#### Owed gates — 3 due on/before 2026-09-09

_Ledger read at 2026-09-08T23:14:46.266Z by `cc-due-gates.collectDue` — the same resolver `check-due-gates-dispositioned` uses, so any difference is TIMING, not two answers. Rows filed or re-dated after that are NOT READ at generation — the close-out commit lands after this, so the COUNT above is stale by construction and this list cannot claim completeness. The list below, not the count, is the artifact; re-derive both before acting: `node scripts/sky.mjs check-due-gates-dispositioned.mjs --print --today=2026-09-09`._

Unfiltered as of the read time above — nothing was curated out of what the read could
see; a row absent here may simply have been filed after it ran.

- `A-31` (expectedSignalBy=2026-09-09, open) — Should the public page publish be CI-gated? It is not, and a malformed page reached main t
  - read: `gh api repos/u00dxk2/bounds-ledger/pages --jq '.build_type' # SEPARATOR CONVERTED 2026-09-02, em dash to hash, because this gate fires 2026-09-09. Returns 'legacy' while ungated, 'workflow' once gated. Literal output 2026-08-26: legacy. POSITIVE CONTROL that the endpoint is live rather than erroring silently: the same object's .html_url returns https://u00dxk2.github.io/bounds-ledger/ and .status returns a build state; an empty or null build_type means the read failed, not that no gate exists.` · last run: NEVER
- `A-34` (expectedSignalBy=2026-09-09, open) — David asked where else this ledger can have the kind of impact G-3 just produced — bring h
  - read: `node scripts/reverify.mjs --check # SEPARATOR CONVERTED 2026-09-01 at pre-stage, em dash to hash. The grep leg was DROPPED from the executed command and its result recorded here instead: grep -c 'ledger/' continuity/items.json returns 21 on 2026-09-01, and the count is prose-inflated rather than structural, so it never discriminated one surface from two. The command that answers this row is the check, whose first line names the mirrored surface and its file count. Literal output 2026-09-01 14:30Z: No drift. 113 files match upstream commit <upstream sha elided 2026-09-07 — reprint it with: node scripts/reverify.mjs --check> (an upstream teorth/optimizationproblems commit; it does not resolve in this repo -- git cat-file -t on its short form reports Not a valid object name, while the same command on a local commit returns commit) -- ONE surface. A second adopted surface shows up as a second mirror directory under ledger/ and a second line here. POSITIVE CONTROL that the check reads a real mirror rather than reporting an empty set: the file count must be non-zero and match the manifest; 113 today.` · last run: 2026-09-02T15:24:11.838Z
- `A-39` (expectedSignalBy=2026-09-09, open) — A mid-run 429 becomes up to 339 requests in two seconds at the limiter already refusing us
  - read: `git grep -c RETRY_DELAYS_MS HEAD -- scripts/reverify.mjs # Asks ONE question: has the retry merged into committed main yet? COMMIT-SCOPED ON PURPOSE. The obvious form, a plain grep over scripts/reverify.mjs, reads the WORKING TREE and was actively misleading on the day this row was filed: an uncommitted Codex edit had the retry constants in the tree while committed main had none, so the plain grep printed three hits and the honest answer was zero. LITERAL OUTPUTS, both run 2026-09-02 at this row's creation. Against HEAD: no output at all, exit 1 — the retry is NOT in main. POSITIVE CONTROL, the same command against the branch that has it: git grep -c RETRY_DELAYS_MS a20-fetch-retry-backoff -- scripts/reverify.mjs prints a20-fetch-retry-backoff:scripts/reverify.mjs:3 at exit 0. So exit 1 here means absent-from-main, NOT a broken pattern or a wrong path — the control proves the pattern matches when the thing is there. When this command starts printing a count against HEAD, the retry has merged and this row is live.` · last run: NEVER

#### What shipped since 24 hours ago

_Read at 2026-09-08T23:14:46.266Z; commits landing after that — the close-out especially — are not here. Re-derive: `git log --oneline --since='24 hours ago'`._

- commit `df460f0` — The reader holding sqrt(2) or 10^-335 got the empty state on 28 of 115 rows
- commit `61cecd9` — W-6 traffic sample 2026-09-08 — 1 viewer/14d, return-rate still INAPPLICABLE
- commit `4021830` — The front door exited 3 for every visitor, and the README blamed a credential

**Yesterday's report:** `docs/daily/2026-09-08-prelaunch.md` (coverage 2026-09-08, the prior day). Findings fence EMPTY — the BLUF is the whole story; nothing below it supersedes anything.

**Today's report (owed):** write `docs/daily/2026-09-09-prelaunch.md` — the artifact of record for service day 2026-09-09. Check it exists at close: `node scripts/sky.mjs check-daily-report-exists.mjs --date 2026-09-09` (it sweeps the fleet and applies each lane's coverageOffset; it takes no --project).

#### State — as of 2026-09-08T23:14:46.266Z (re-run the release command; never trust the value)

- **ledger**: 58 item(s): 39 closed · 19 open · as of 2026-09-08T23:14:46.266Z — release: `node scripts/sky.mjs gen-primer-first-action.mjs --repo . --target-date 2026-09-09 --json`

#### Id glossary — 21 id(s) this primer's day already has words for

_Each gloss is the ledger row's OWN title, not a re-derivation — paste it. Rows = every open ledger row plus every id named above. `(no ledger row)` means the id is named here and the ledger carries nothing under it: that is a finding, not a formatting gap._

- `A-2` — STANDING: drift-resolution log for the adopted surfaces…
- `A-7` — engineering-health P1s — four of five shipped 2026-08-09;…
- `A-9` — engineering-health P2 backlog (fix-on-touch, not scheduled…
- `A-20` — Should the fetch layer retry with backoff on 429/502? —…
- `A-31` — Should the public page publish be CI-gated? It is not, and…
- `A-33` — A change to a file we have filed an upstream report…
- `A-34` — David asked where else this ledger can have the kind of…
- `A-39` — A mid-run 429 becomes up to 339 requests in two seconds at…
- `A-42` — Two user-facing readers still key off OUR pin history and…
- `A-43` — Upstream moved Haugland's minimum-overlap bound to its…
- `A-45` — TIER-1 LEADING INDICATOR (orchestrator-approved…
- `G-2` — GOAL: contribute a verified bound improvement to the…
- `G-3` — GOAL (Tier-0 from G-1 close): someone outside Skylark uses…
- `G-4` — GOAL (Tier-0 from G-3 close): an outside party acts on a…
- `S-20260906-75` — (no ledger row)
- `W-3` — Watch for acknowledgement of the erdosproblems.com/36…
- `W-4` — KP-78 applies to every new detector: demonstrate BOTH…
- `W-6` — Read window for the README report-an-error channel — the…
- `W-8` — Does a materially amended outward artifact go back to…
- `W-9` — Does this lane's detection method find what the lane…
- `W-10` — reverify.test.mjs alarm-title leg failed once and would…

### First action — bounds-ledger, 2026-09-09

Every line in the block below is a COMMAND, not a claim. A command re-reads at the
moment you run it and cannot go stale; a state line written last night can.

```bash
git pull --ff-only
node scripts/sky.mjs update-david-board.mjs --list --json --full-ids --project bounds-ledger
git rev-parse HEAD origin/main
git rev-list --count origin/main...HEAD
npm run verify > tmp/verify-out.txt 2>&1
node scripts/sky.mjs check-ci-status.mjs --workflow reverify.yml
```

Morning release commands — deliberately NOT READ at generation (a value written
last night would already be stale); run each, never trust a remembered one:
- `git -C . log -1 --format=%h%x20%s` — HEAD
- `git -C . status --porcelain` — working tree
- `node scripts/sky.mjs check-ci-status.mjs --workflow reverify.yml` — CI
- `node scripts/sky.mjs check-deployed-sha-drift.mjs --service bounds-ledger` — deploy
  - WHERE IT LOOKED: no read-only deploy-state script in this lane's package.json (looked for ops:deploy-state, deploy-state, check-deploy, render-deploys, deploy:status, daily-snapshot). The fleet's check-deploy-state.mjs is not a substitute — it requires --fix <sha> --deploy <sha> and exits 1 without them — so this is the FLEET read, which takes no required flags. If `--service bounds-ledger` does not name exactly one Render service the script exits 1; drop the flag and read the whole fleet.

<!-- hand:begin narrative -->
_(orchestrator narrative — this region survives regeneration)_
<!-- hand:end narrative -->

<!-- generated by gen-primer-first-action.mjs (skylark-site) @ commit f37cfafcc · lane bounds-ledger · for 2026-09-09 · at 2026-09-08T23:14:46.266Z -->
<!-- writtenAtSha: `df460f0937450111d897231a7e018a42da9e82dd` — the lane's HEAD commit when this primer was written. Provenance anchor for check-primer-freshness leg ii; NOT a claim about HEAD now (the close-out commit that carries this file lands after generation). -->
<!-- reading this block: a generated line that is wrong in a way you can see FIXED in skylark-site's git log after the commit above is STALE, not a finding — regenerate before filing it. `git -C C:/dev/skylark/skylark-site log --oneline f37cfafcc^{commit}..HEAD -- scripts/gen-primer-first-action.mjs src/lib/cc-primer-commands.mjs src/lib/cc-due-gates.mjs src/lib/cc-report-artifact.mjs src/lib/cc-hand-edit-merge.mjs src/lib/cc-cold-readability.mjs src/lib/cc-skeleton-gloss.mjs` -->
