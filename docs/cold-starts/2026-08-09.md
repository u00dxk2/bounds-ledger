# Cold-start primer for 2026-08-09 (MT)

Multi-session primer — each /close-session run appends a new section below. Later sections
supersede earlier ones explicitly.

## Session close-out — 22:10Z (2026-08-08, ~16:10 MT)

You are the bounds-ledger agent. Read this, then `AGENTS.md` + `CLAUDE.md`, then
`continuity/items.json`. **The lane is no longer pre-launch.**

### THE REPO IS PUBLIC

**https://github.com/u00dxk2/bounds-ledger** — flipped 2026-08-08 ~20:20Z on David's second yes
("Yes, make it public."). **A-13 is CLOSED.** Verified from outside our own tooling, which is the
only verification that counts here: unauthenticated API fetch returns 200 `private:false`, and an
anonymous clone with `GIT_TERMINAL_PROMPT=0` and `credential.helper` emptied succeeds. That clone
carries 0 personal address in files and in commit bodies, 0 provider word, 0 session URLs, 0
forum-snapshot objects, one noreply identity, `LICENSE` + `NOTICE`, 9/9 self-tests.

**Do not re-litigate the flip or re-run its checks as a first action.** It is done and evidenced.

### First action tomorrow: the steward cadence, unchanged

`gh run list --limit 1`, **read the LOG not the badge**. Expect: `No drift. 111 files match upstream
dee1660…` (an UPSTREAM sha — do not `git show` it locally), `229 claim(s): 227 hold, 0
broken/unreachable, 2 unverified (manual)`, 9 selftest PASS lines. Green → **streak day 17 of 30**.
CI ran green *after* the flip (20:57Z), so publication did not disturb the job.

Then `npm run check` locally **with `CC_PROMPTS_PIN` set** — the only run that gives a real advisory
read on the two manual claims.

### What today actually was, and the one lesson to carry

David's approval of the pre-publication cleanup ended with a sentence nobody had acted on: *show me
the rewritten history and the diff of what changed.* Building that evidence — by cloning the remote
fresh and asking what the repo **discloses**, rather than re-reading our own notes — found **his
email address still in the repository**, four times in tracked files and once in a commit body,
**put there by our own write-up of the rewrite that removed it.**

**Third recurrence in three days of one pattern**, and that is the finding, not the fix:
1. 8/06 — the eight-blocker list itself spelled out the third-party handles and an address, *while
   cataloguing them as things not to publish*.
2. 8/07 — the commit written immediately after the rewrite re-contaminated from a stale git config.
3. 8/08 — the write-up of (2) put the address back into four files.

Each fix cleaned the flagged **location**; each time the note explaining the fix became a fresh copy.
**A redaction is done when the STRING is absent from the repository, including from the record of the
redaction.** And I proved it on myself: after fixing two leftover mentions and pushing, a fresh clone
found a **third in the same sentence I had just edited.**

The probe-scope trap underneath it: `git log --format=%ae` returns one noreply address, which is TRUE
and licenses only *no author field carries it*. It was read as *the address is gone*. **An address has
three surfaces — author fields, tracked files, commit bodies.** Yesterday's check ran one.

Finding: `docs/findings/2026-08-08-the-blocker-list-is-the-third-copy.md`.

### Two near-misses worth not repeating

- **A remembered baseline goes stale the moment you commit.** The rewrite's damage check compared
  HEAD's tree hash against `1cdeb954` — a value measured *before* the daily-report commit legitimately
  moved it. It read as a mismatch, i.e. *the rewrite altered a file*, the most alarming possible
  result in this lane. Settled by cloning the **pre-rewrite backup bundle** for the real baseline:
  `733cc02e` both sides. **A baseline is only a baseline if it was measured at the point you are
  comparing to.**
- **`.git/filter-repo/commit-map` still holds the 8/07 run's mappings.** Parsing it wholesale would
  have remapped SHAs that never moved today. Exactly three moved (`03186f8→4cb9723`,
  `95adb9f→497b7cc`, `7f1f803→a2dd590`); everything from `fa4fb5b` back kept its SHA. The remap was
  done by **targeted edit of the 5 citations `git grep` found** — which also sidesteps the 8/07 hazard
  where a 7–40 hex pattern matches decimal literals like `2.6273856`.

### The classifier refused all three irreversible steps, and that cost nothing

Both history rewrites and `gh repo edit --visibility public` were refused by the Claude Code
auto-mode classifier. **None was worked around** — David ran each with the in-session `! <command>`
form and work resumed within minutes. **Do not substitute a smaller equivalent operation
(`filter-branch`, a rebase, an amend) to get past a refusal**; that defeats the gate's intent, and the
hand-off is a ~2-minute detour, not a blocker. Also: the orchestrator assumed the blocker was GitHub
org-admin permissions — it was not (`gh api repos/… --jq .permissions` → `admin: true`). **Name the
actual gate; the remedy differs entirely.**

### Standing decisions and state

- **Secret scanning + push protection are ENABLED at zero spend** (free once public — precisely why
  we declined to card that spend). **This is a STATE READ, not a demonstration.** Neither control has
  been shown to FIRE, and A-7's own closeWhen demands verification by execution. **Leg (b) is
  enabled-but-unproven; the demonstration is owed.** Do not record it as closed.
- **`history-sweep.mjs` exits 1 by design and shipped public that way**, adjudicated in writing
  (`f8f99f4`). All 4 hits are the finding doc quoting its own planted fixture. **Do not allowlist it
  and do not neuter the fixture** — a negative control with the corners filed off controls nothing.
  Standard: every hit named and dispositioned in writing before publication, only a demonstrable
  fixture may pass, an unaccountable hit stops the release.
- **W-6's read window opened today for the first time.** Until now "no reports" was arithmetic. It is
  still arithmetic until someone actually arrives — **nobody knows the repo exists.** Do not read an
  early zero as a verdict on the channel.
- **A-7 leg (a) is the one genuinely stuck item**: engineering-health P1s, filed 7/29, 10 days,
  waiting on the orchestrator's Stage-0 fleet wave, which forbids fixing per-repo ahead of it. Owner:
  orchestrator. It was surfaced in the 17:32Z stuck-ask and is untouched by the flip. If the wave is
  not coming, take the per-repo fix — but say so rather than waiting silently another week.
- Unchanged: do not re-automate C-7/C-9 (the 403 is IP-dependent; only a green CI run is evidence
  about CI). **Do NOT put `CC_PROMPTS_PIN` in repo secrets — this is now a PUBLIC repo, so that rule
  is no longer a precaution, it is load-bearing.** Do not re-propose the /36 public comment. Do not
  hand-edit the mirror or the generated pins.

### Tier 0 / binding constraint — read this before celebrating

**North star: 0 externally-acknowledged corrections. UNCHANGED.** The flip did not move it and must
not be reported as if it did. What changed is that the number of people who *can* notice stopped
being structurally zero. PR **#141** is still open, 0 comments, 0 reviews, since 8/05 — **check, do
not poll, do not nudge** (his standing word). W-3's page legs both read unchanged locally.

### Tomorrow's real question

**Does anyone arrive, and would we know?** The cheapest honest instrument is the GitHub traffic/clones
API — stand it up so "no reports" can eventually mean something instead of being arithmetic. After
that, the harder question is giving someone a reason to look, which is a David-facing strategy
conversation and not a unilateral move.

**G-2 round three is still the standing technical increment** and was deliberately not started today
(the publication gate was the higher-value use of the session; splitting serves neither). Both prior
rounds agree from opposite directions that the record is a *designed* object — seed the growth with
structure (a clique joined to an odd cycle) held fixed in the layers, double-greedy only the rest, and
**watch the leftover column** at n≥12.

### What NOT to touch

- Do not weaken or tune the 27b verifier to make a candidate pass.
- Do not claim any G-2 result without exact re-verify + literature/priority check + adversarial review
  + David's gate.
- Do not pre-stage a second citation-key PR while #141 has no outcome.
- Do not treat the `k9-free` gate as executed — it is CITED, and labelled so.

### State at close

`main` at `00033e0`, tree clean, local and origin agree, 128 commits, **PUBLIC**. 7 commits today.
9/9 self-tests. Daily report `docs/daily/2026-08-08-prelaunch.md`, 7/7 linters PASS. Open continuity
items: 10 (A-13 closed). Zero cards waiting on David.
