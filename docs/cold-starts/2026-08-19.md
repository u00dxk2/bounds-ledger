# Cold-start primer for 2026-08-19

Multi-session primer — each close-out appends a section below. Later sections supersede earlier ones.

## Session close-out — 2026-08-18 (/close-session)

Paced rail, day 17 closed. **Every command in this section was run in the turn that wrote it, exit code kept** — that is today's own lesson applied to the artifact that caused it (see the finding below).

### Tomorrow's first-action — VERIFIED, exit 0 at close

```bash
cd C:/dev/skylark/bounds-ledger && git rev-parse HEAD origin/main && [ "$(git rev-parse HEAD)" = "$(git rev-parse origin/main)" ] && npm run check
```

Run verbatim at close: printed `489cfdb…` twice, then `npm run check` completed — **exit 0**. The two-sha compare is the sync guard; identical shas mean the checks below describe the commit you actually have. Then read the overnight CI log (that order — see `CLAUDE.md`).

**Nothing is held and nothing is owed to David today.** Both PRs merged; the open follow-ups all carry forced-review dates in the future.

### What happened today

**The catch: the primer's own first-action command exited 128 and skipped the whole steward cadence.** Yesterday's guard was written `git rev-parse --short HEAD origin/main`. `--short` takes **exactly one** revision; given two it exits 128 (`fatal: Needed a single revision`), and sitting at the head of an `&&` chain it silently skipped every check behind it. The working form was quoted one line above it in the same section's prose. Three surfaces in two files. Fixed in `e351b6b`; finding at `docs/findings/2026-08-18-the-guard-against-a-wrong-commit-read-never-ran.md`.

Two riders worth keeping: **a KP-78 "both answers demonstrated" note certifies only the command it actually ran** (yesterday's note sat under a two-line block and covered line two), and **`--short` cannot be repaired with `=N`** — `--short=7` exits 128 too, the arity limit is on the flag. **My first repair was also wrong** (`| uniq` breaks the never-read-an-exit-code-through-a-pipe invariant) and is recorded in the finding rather than quietly swapped.

The orchestrator adopted the rule fleet-wide the same day: *no command enters a primer without being run same-turn, exit code kept.*

**A-17 closed — both PRs merged in the required order.** The review lane returned APPROVE-WITH-NITS on #21 and released its hold. `#21 alarm-title-honesty → 95c12f1`, then `#22 guard-catch-count-v2 → f362ce7`. Both remote branches deleted, including the superseded `guard-catch-count`; `git ls-remote --heads origin` returns `refs/heads/main` only.

**The alarm fix proved itself in production three times today, unprompted.** Merging #21 triggered a push run that failed on `error: fetch constants/9a.md: 502`. It titled the issue **`Check error: … could not complete` (#25), not `Drift:`** — an hour earlier the same run would have asserted records moved. Verified locally (no drift, 231 of 233 claims holding), re-ran once → success, and githubstatus reported all systems operational with 0 unresolved incidents, **so no cause is attributed to that 502**. Issue #25 closed the same hour naming the evidence.

**Then it happened again at close.** The close battery's `npm run check` exited 1 with **two UNREACHABLE pins** (`pin:57c:U`, `pin:57c:L` — the Univalent Bloch constant page). **UNREACHABLE, not BROKEN: nothing moved.** One re-run of `check-claims.mjs` returned exit 0, 231 hold, 0 broken/unreachable. **Two independent transient transport failures in one day, on different files, each absorbed by exactly one retry** — that is the concrete case for A-20, and also the case for its caution (a retry already in place would have hidden both observations, so whatever ships must LOG that it retried).

### New items filed today

- **A-18** — generated STATE block at the top of the primer (`render-state-block.mjs --primer`). Orchestrator-approved in principle; **does not jump the review lane**, and `render-state-block.mjs` is itself a trusted-print instrument so its diff is in-class. Forced review 2026-08-25.
- **A-19** — the two non-blocking review nits. N-1: the alarm title counts unreachable *claim lines* while saying "cited source(s)". N-2: the SKIPPED path exits 0 on a bash-less machine, against this repo's own 8/17 lesson that a guard which cannot run must not report success. Forced review 2026-08-25.
- **A-20** — should the fetch layer retry with backoff on 429/502. Separate from the unreachable-stays-red invariant because a retry EXTENDS reach rather than softening a verdict. Forced review 2026-09-01.
- **W-7** — the 2026-08-15 instrument-audit recommendation was routed to "the weekly pass" and never became a tracker row, so the 8/17 pass never saw it. Forces the decision at the next pass, 2026-08-24. **It does not pre-approve the proposal.**

**The rule W-7 exists to enforce: a recommendation routed to a later pass gets its tracker row in the same commit as the report that proposes it.** I nearly re-committed the same defect within the hour by filing A-18 undated.

### G-1 — the north star

**Both legs reach their close condition on Saturday 2026-08-22.** Acknowledgement leg met since 2026-08-11 (Tao merged `teorth/optimizationproblems#141`). Streak leg **day 26 of 30** — today's scheduled run (`32121595508`, 09:26Z on `afed5e6`) completed and **reached a verdict**, so it counts under the pre-registered `streakDayRule`. Today's *push*-run failures do **not** touch the streak, which is measured on the scheduled run only.

**Closing G-1 is David's decision, never a self-approval and never a self-rating.** Evidence is pre-staged in `G-1.signalPrestaged`. Nothing currently answers what becomes Tier-0 the following Monday; that question is being surfaced *with* the close, not after — my input (G-2 as successor; candidate-correction queue depth as the better leading indicator) rides along as input only and is **not decided**.

### Do NOT touch

- **A run stays RED when a cited source is unreachable.** It fired honestly three times today. The temptation after a flaky day is to make 429/502 non-fatal; that would launder an unverifiable result into a pass. Only the *title* was ever wrong, and that is fixed.
- **A-16** (upstream's Martinet row cites a witness whose arithmetic yields the previous record) — gate 2 is David's. Do not raise, do not PR, do not contact upstream.
- **A-15** (Mathstodon) — dormant by instruction. Do not ask.

### State at close — each figure with the command that produced it

| Figure | Value | Command (exit) |
|---|---|---|
| HEAD / origin sync | `489cfdb` both | `git rev-parse HEAD origin/main` + `test` compare (0) |
| CI at HEAD | **GREEN** | `check-ci-status.mjs --workflow reverify.yml` (0) |
| Mirror | **113 files @ upstream `e70b4a4`**, no drift | `npm run check` (0 on re-run) |
| Ledger | **233 claims — 231 hold, 0 broken, 2 UNVERIFIED by design** | `check-claims.mjs` (0) |
| Catch table guard | "9 cycles, and README.md says 9" | `npm run check` (0) — #22's guard, live since today |
| Self-tests | **13 PASS** | `npm test` (0) |
| Dead doc references | **0** across 63 paths | `check-doc-references.mjs` (0) |
| True open issues | **0** | `gh api …/issues` filtered `.pull_request == null` |
| Open PRs | **0** | `gh pr list --state open` |
| Dependabot alerts | **0** | `gh api …/dependabot/alerts` |
| Uncarded decisions | **0** | `check-unpromoted-decisions.mjs --soft` (0) |
| Items | **28 total, 14 open** | `continuity/items.json` |

**Engineering zero-state: structurally zero, not swept to zero.** `package.json` has empty `dependencies` and `devDependencies`, so Dependabot has nothing to find; there is **no Sentry project** for this repo, which is why the report frontmatter carries `sentry_open_p1: null` rather than `0`. An absent instrument renders null, never a count. No waiver is needed because there is no alarm to waive.

`codex-health.mjs --reap-dead` exited 2 reporting one orphan pid (`61240`, 3.3h) — fleet-level, 0 reaped, nothing owed by this lane.
