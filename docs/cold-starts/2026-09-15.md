<!-- GENERATED — regenerating REPLACES everything outside the hand: markers, so put prose inside them. Provenance stamp at the foot of this block. -->

<!-- hand:begin banner -->
> **PULL, THEN START THE GATE, BEFORE READING THIS** — three minutes, all network:
> ```
> git pull --ff-only
> npm run verify > tmp/verify-out.txt 2>&1
> ```
> Expect **exit 0**. Any non-zero code is yours; read `tmp/.verify-receipt.json`'s `failedGates`,
> never infer the leg.
>
> **BATCH YOUR LEDGER WRITES, THEN GATE ONCE — and it cost a full extra run on 09-14 when I did not.**
> I committed a one-file traffic sample separately from the day's batch, which moved HEAD past the
> receipt, and `agent-status` refused the P4 post because a gates-green claim needs a receipt AT HEAD.
> That is ~450 requests spent on a sequencing choice. The gate was right; the argument "a re-run would
> change nothing" is the one it exists to refuse.
>
> **DO NOT TRUST ANY ID LIST IN THIS BANNER.** Run the resolver:
> `node scripts/sky.mjs check-due-gates-dispositioned.mjs --print --today=2026-09-15`.
> It should list `A-43` and `A-48`. Both were 2 due when this primer generated, 2026-09-14T20:3xZ.
>
> **`A-48` SURFACE 2 IS DUE TODAY** — "Graph Labeling", DOI `10.37236/27`. Surface 1 is SCORED
> (row field `surface1Scored`), and the whole comparison is promised to David for **2026-09-16**, which
> has not moved. Surface 1 carries TWO OPEN SUB-QUESTIONS into that delivery and they are the reason to
> start early: (a) is the survey PDF BODY mirrorable, untested; (b) does a correction path exist that is
> NOT a cold email — **not established**, the landing page carries only journal navigation and the PDF
> was never read. (b) is the criterion most likely to DISQUALIFY the candidate, so read it FIRST.
> The read is now runnable: `node scripts/citation-footprint.mjs <doi>`. It replaced an inline
> `node -e` one-liner that could never spawn here, so the row read `last run: NEVER` from 09-09 to 09-14.
>
> **THE INDICES DISAGREE IN ORDER, and that is the finding to carry to David.** Read 09-14:
> surface 1 Crossref 36 / Semantic Scholar 612 / OpenAlex 432; surface 2 Crossref 156 / 143 / 188.
> Two indices agree surface 1 is the more cited; **Crossref alone inverts it.** A Crossref-only
> comparison — which `A-48`'s own `onTrigger` forbids — would have ranked them backwards. Re-read both
> before quoting: `node scripts/citation-footprint.mjs 10.37236/21` and `… 10.37236/27`.
>
> **`A-43` IS ALSO DUE TODAY and is decidable.** Its four lookups are done and published on the 1b page;
> what remains is the report-shape decision. Any upstream draft needs the adversarial review AND
> David's gate before it goes anywhere.
>
> **THE INDEX NOW CARRIES A READ-BADGE, and the thing to protect is what its ABSENCE means.** Nine of
> 115 constants say a bound row was read against its cited source. No badge does NOT mean "not checked":
> it means no reading we can match to the row as it stands TODAY — either none was done, or the row moved
> and that constant's page still shows the reading as historical. The page says so in words. The first
> draft of that sentence said "we have not done that reading yet" and was caught by adversarial review;
> do not reintroduce it. Release: `node scripts/render-site.mjs --check`.
>
> **THE readCommand CORPUS IS 12,890 CHARS ACROSS 17 OPEN ROWS, longest 1,571** — as of 2026-09-14,
> from a `node -e` sum of every open row's `readCommand` length in `continuity/items.json`.
> **This REPLACES the "SEVEN readCommand FIELDS ARE STILL ESSAYS" line that stood here through 09-14.**
> That line could not be checked by anyone: "essay" is defined in no script, doc or threshold in this
> repo, and seven falls out only at an undeclared cut between 816 and 873 chars. A count whose predicate
> is unwritten is decoration. Quote the char total, which is reproducible, or quote nothing.
>
> **`W-7` ROTATES.** 09-14's instrument was `check-instrument-liveness.mjs`, which for this lane cannot
> return anything but NOTHING SWEPT — no Sentry feed and no declared report cache, so it has no subject
> here. Pick a different one and write the line opening `W-7 —`.
>
> **AN IMPORT-EXECUTES-CLI MECHANISM IS PROPOSED AND UNFUNDED.** The class has bitten three times:
> `lookup.mjs`, `render-site.mjs`, and `render-constant-pages.mjs` on 09-14, where importing ran
> `rmSync` over the published `c/` directory. All three now carry guards. The proposal — one selftest
> that imports every CLI module in one spawned process and asserts the import was inert — is in the P4
> post of 09-14 and the orchestrator offered to take it. Do not rebuild it unprompted; ask whether it was.
>
> **NOTHING IS WAITING ON DAVID** as of 2026-09-14. Release:
> `node scripts/sky.mjs answered-cards.mjs --project bounds-ledger --full`.
<!-- hand:end banner -->

First command (also below, in place):

```bash
git pull --ff-only
# commits after this primer was written are invisible to it — read them before acting:
git log --oneline c9791f9ad8b81640cc667c1d2ee73420174c6882..HEAD
```

### Primer — bounds-ledger, 2026-09-15

What ONLY this primer holds comes first; the FULL runnable **First action** block
sits at the foot, below the reads. The single command above is the same block's
first line, repeated so it can be running while you read — it replaces nothing.

_**Composed against HEAD commit `c9791f9ad8b81640cc667c1d2ee73420174c6882` at 2026-09-14T20:36:49.529Z.** Nothing that landed after that instant is in this file. David's board answers especially: an answer he gives later cannot be here, and this primer will go on reading as though the work were not owed — that is exactly how two lanes concluded an OTA was not owed on 2026-09-04. Read them live, never off this page: `node scripts/sky.mjs answered-cards.mjs --project bounds-ledger --full`._

_Shared skylark-site scripts below are spelled `node scripts/sky.mjs …` — this lane vendors `scripts/sky.mjs` (S-20260906-75), so a shared script is named LANE-RELATIVE and resolves from a worktree too._

#### Owed gates — 2 due on/before 2026-09-15

_Ledger read at 2026-09-14T20:36:49.529Z by `cc-due-gates.collectDue` — the same resolver `check-due-gates-dispositioned` uses, so any difference is TIMING, not two answers. Rows filed or re-dated after that are NOT READ at generation — the close-out commit lands after this, so the COUNT above is stale by construction and this list cannot claim completeness. The list below, not the count, is the artifact; re-derive both before acting: `node scripts/sky.mjs check-due-gates-dispositioned.mjs --print --today=2026-09-15`._

Unfiltered as of the read time above — nothing was curated out of what the read could
see; a row absent here may simply have been filed after it ran.

- `A-43` (expectedSignalBy=2026-09-15, open) — Upstream moved Haugland's minimum-overlap bound to its truncation — is that one wrong cell
  - read: `curl -s https://raw.githubusercontent.com/teorth/optimizationproblems/main/constants/1b.md — prints the live file. The H2016 row currently reads 0.380926; if it reads 0.380927 again upstream reverted and this row closes as moot. POSITIVE CONTROL that the fetch is the right document and not an error page: the response must also contain the string 'Erdos minimum overlap constant' heading text and the neighbouring value 0.380924, so an empty or redirected body cannot be misread as 'the row is gone'.` · last run: 2026-09-12T19:05:20.396Z
- `A-48` (expectedSignalBy=2026-09-15, open) — The second-area citation comparison David asked for, promised to him for 2026-09-16
  - read: `node scripts/citation-footprint.mjs 10.37236/21 — surface 1 ("Small Ramsey Numbers", EJC dynamic survey). Reads THREE independent indices (Crossref is-referenced-by-count, Semantic Scholar citationCount, OpenAlex cited_by_count) and prints figures with NO verdict. A non-200, or a 200 carrying no count field, prints BLOCKED and exits 2 — a fact about the INDEX, never a low or zero footprint for the surface. Exit 0 means at least the two independent indices this row requires answered. Indices disagree by an order of magnitude on the same work, so surfaces are compared by ORDER ACROSS INDICES, never by one index’s magnitude (note3).` · last run: 2026-09-14T19:23:34.768Z

#### What shipped since 24 hours ago

_Read at 2026-09-14T20:36:49.529Z; commits landing after that — the close-out especially — are not here. Re-derive: `git log --oneline --since='24 hours ago'`._

- commit `c9791f9` — W-6 traffic sample 2026-09-14: 1 unique viewer in the trailing 14 days
- commit `433282c` — P3: the index says which rows we actually opened the paper for
- commit `33ab324` — P2: a gate that could never have run, and A-2 down from 4,062 chars
- commit `88e8148` — docs(close-session): close-out 2026-09-13 — primer narrative and one stable history field

**Yesterday's report:** `docs/daily/2026-09-14-prelaunch.md` (coverage 2026-09-14, the prior day). No findings fence declared in it.

**Today's report (owed):** write `docs/daily/2026-09-15-prelaunch.md` — the artifact of record for service day 2026-09-15. Check it exists at close: `node scripts/sky.mjs check-daily-report-exists.mjs --date 2026-09-15` (it sweeps the fleet and applies each lane's coverageOffset; it takes no --project).

#### State — as of 2026-09-14T20:36:49.529Z (re-run the release command; never trust the value)

- **ledger**: 61 item(s): 44 closed · 17 open · as of 2026-09-14T20:36:49.529Z — release: `node scripts/sky.mjs gen-primer-first-action.mjs --repo . --target-date 2026-09-15 --json`

#### Id glossary — 18 id(s) this primer's day already has words for

_Each gloss is the ledger row's OWN title, not a re-derivation — paste it. Rows = every open ledger row plus every id named above. `(no ledger row)` means the id is named here and the ledger carries nothing under it: that is a finding, not a formatting gap._

- `A-2` — STANDING: drift-resolution log for the adopted surfaces…
- `A-7` — engineering-health P1s — four of five shipped 2026-08-09;…
- `A-9` — engineering-health P2 backlog (fix-on-touch, not scheduled…
- `A-33` — A change to a file we have filed an upstream report…
- `A-43` — Upstream moved Haugland's minimum-overlap bound to its…
- `A-45` — TIER-1 LEADING INDICATOR (orchestrator-approved…
- `A-46` — A close-out post announcing a FIX is textually identical…
- `A-47` — THE DEPTH AUDIT, David-ruled 2026-09-09: read our own…
- `A-48` — The second-area citation comparison David asked for,…
- `G-2` — GOAL: contribute a verified bound improvement to the…
- `G-4` — GOAL (Tier-0 from G-3 close): an outside party acts on a…
- `S-20260906-75` — (no ledger row)
- `W-3` — Watch for acknowledgement of the erdosproblems.com/36…
- `W-4` — KP-78 applies to every new detector: demonstrate BOTH…
- `W-6` — Read window for the README report-an-error channel — the…
- `W-8` — Does a materially amended outward artifact go back to…
- `W-9` — Does this lane's detection method find what the lane…
- `W-10` — reverify.test.mjs alarm-title leg failed once and would…

### First action — bounds-ledger, 2026-09-15

Every line in the block below is a COMMAND, not a claim. A command re-reads at the
moment you run it and cannot go stale; a state line written last night can.

```bash
git pull --ff-only
node scripts/sky.mjs update-david-board.mjs --list --json --full-ids --project bounds-ledger
git rev-parse HEAD origin/main
git rev-list --count origin/main...HEAD
npm run verify > tmp/verify-out.txt 2>&1
node scripts/sky.mjs check-ci-status.mjs --workflow reverify.yml
```

Morning release commands — deliberately NOT READ at generation (a value written
last night would already be stale); run each, never trust a remembered one:
- `git -C . log -1 --format=%h%x20%s` — HEAD
- `git -C . status --porcelain` — working tree
- `node scripts/sky.mjs check-ci-status.mjs --workflow reverify.yml` — CI
- `node scripts/sky.mjs check-deployed-sha-drift.mjs` — deploy
  - WHERE IT LOOKED: no read-only deploy-state script in this lane's package.json (looked for ops:deploy-state, deploy-state, check-deploy, render-deploys, deploy:status, daily-snapshot). The fleet's check-deploy-state.mjs is not a substitute — it requires --fix <sha> --deploy <sha> and exits 1 without them — so this is the FLEET read, which takes no required flags. service: NOT DECLARED — no row for "bounds-ledger" in skylark-site/src/data/cc-deploy-gates.json (generatedAt 2026-09-11T05:03:44.019Z, 17 repo row(s)) — nothing in this repo declares that lane's Render service. No `--service` flag is emitted: the lane SLUG is not the service name (antebellum-twitter's is disunion-feed), and a guessed flag exits 1.

<!-- hand:begin narrative -->
## Session close-out — 21:50Z

**primer re-read: `f805190`..`f805190` — no commits between P5 and this close, so the banner above stands unchanged; this block is appended.** Two bus-only corrections landed after P5 and are summarised below; neither contradicts the banner.

**CI:** reverify: completed/success @ `f805190` — `gh run list --branch main --limit 1`.

### Today's load-bearing ships
- `433282c` — the public index now shows which constants have a bound row read against its cited source (9 of 115), worst verdict wins, no checkmark. Live: HTTP 200, 115 rows, 9 badges on a fetch of the published page.
- `33ab324` — `A-48`'s gate could never spawn on this machine; replaced by `scripts/citation-footprint.mjs`, which reads three indices. `A-2`'s read command came down from 4,062 chars to 1,571 with its invariant kept.
- `f805190` — today's report, this primer, the "seven essays" count removed.

### Tomorrow's first action — names the FILE it acts on
Pull and gate (banner, top). Then read the front matter of the *Small Ramsey Numbers* survey PDF, reached from `https://www.combinatorics.org/ojs/index.php/eljc/article/view/DS1`, for a correction path that is NOT a cold email, and write the finding into **`continuity/items.json` → row `A-48` → field `surface1Scored`**, criterion (4). Do this BEFORE surface 2, because criterion (4) is the one that can disqualify surface 1 outright. Then `node scripts/citation-footprint.mjs 10.37236/27` for surface 2.

### Owed tomorrow — DERIVED, pasted verbatim
`node ../skylark-site/scripts/check-due-gates-dispositioned.mjs --print --today=2026-09-15`
```
due-gates: 2 gate(s) due on/before 2026-09-15 (read-only; no snapshot written)
  2026-09-15  A-43     [expectedSignalBy]  Upstream moved Haugland's minimum-overlap bound to its trunc
  2026-09-15  A-48     [expectedSignalBy]  The second-area citation comparison David asked for, promise
unread: 0 of 2 due: never-run 0 · timed-out 0 · no-readCommand 0 · errored-stamp 0 · waived 0
```

### Pending decision-blocked items
None on David. **One routing question is open with the orchestrator**: is the import-executes-CLI selftest this lane's to ship or theirs to take — asked in the EOD retro `2d0c8216-5e1f-4818-a57e-9740199cbb0c`. It has an owner and a date and **no row**; do not build it until that is answered.

### A standing correction to carry — the counts line
**`codexCalls:` and `adversarialReviews:` are separate lines and are NEVER summed** (fleet close broadcast 2026-09-14). A review is a gate you run, not work you hand off; declare it `— EXECUTED` or `— BY-INSPECTION (<why>)`. I summed them on two posts today and corrected both: `b0a849f4-d148-4d8e-8a83-a5749ff5a5d3` and `c79da1fd-9b68-48de-9d9a-e105e623c846`. The day's true totals were codexCalls: 0, adversarialReviews: 1 — EXECUTED.

### Do not trust tomorrow's close gate on the banner
**`npm run close:primer` PASSES on a primer whose banner is only the generator placeholder** — measured today by swapping this file's 4,675-byte banner region down to the 86-byte placeholder: `close:primer` exit 0, `check-next-primer-exists` exit 0. Until that gate reads the banner, the close must LOOK at the banner rather than trust the green. Proposed as a fix in the EOD retro; not shipped.

### What NOT to touch
- **The `usableAudit` → `identityVerified` → `isStale` chain in `scripts/render-constant-pages.mjs`, and the one shared `isBoundRead`.** The index badge and the constant pages must ask the same three identity questions; a second copy of that predicate is a second chance for the two surfaces to disagree.
- **The `manual: true` / UNVERIFIED rule for `C-7` and `C-9`.** A local advisory read of erdosproblems.com is real information and still does not make those claims green in CI.

### Tier 0 — G-4 (an outside party acts on a watched record without us filing the report)
**0, expected-zero, and a MEASURED zero** — `npm run reports` sees 29 issues and accounts for every one. **But read it for what it counts:** issues arriving through our per-row "looks wrong?" link. Someone who acts on a wrong row by fixing their own paper, or by emailing the upstream maintainer, is exactly G-4 and never appears there. Quote it as expected-zero, never as "G-4 has not happened".

### Evangelism read
1. **The core problem, in the user's words** (`docs/evangelism-bar.md`): *"I got this constant from a page that says it was last edited in January. I don't know if that means it's current or that nobody has looked at it since."*
2. **The would-tell-a-friend moment, and today's distance from it:** a citer looks up a constant and learns something about their own citation they could not have known — a dated "value changed", or a believable "unchanged since". That moment exists on every tracked row. Today added a deeper layer on 9 of 115: *we opened the paper and read the number there*. The distance is not the moment's quality; it is **coverage** (the bar's named #1 threat) and **arrivals**.
3. **Retention and word of mouth: UNMEASURED by design** — the page runs no analytics. Repo views read **1 unique viewer in the trailing 14 days** (`npm run traffic`, 2026-09-14T20:08:47Z), down from 3 on 2026-08-25; the return-rate proxy prints INAPPLICABLE at n=1. This loop is measured in months, so a thin fortnight is not a verdict — but a page improvement and a viewer count of 1 belong in the same sentence.
4. **Real-user advocacy this session:** none.
<!-- hand:end narrative -->

<!-- generated by gen-primer-first-action.mjs (skylark-site) @ commit accf3f567 · lane bounds-ledger · for 2026-09-15 · at 2026-09-14T20:36:49.529Z -->
<!-- writtenAtSha: `c9791f9ad8b81640cc667c1d2ee73420174c6882` — the lane's HEAD commit when this primer was written. Provenance anchor for check-primer-freshness leg ii; NOT a claim about HEAD now (the close-out commit that carries this file lands after generation). -->
<!-- reading this block: a generated line that is wrong in a way you can see FIXED in skylark-site's git log after the commit above is STALE, not a finding — regenerate before filing it. `git -C C:/dev/skylark/skylark-site log --oneline accf3f567^{commit}..HEAD -- scripts/gen-primer-first-action.mjs src/lib/cc-primer-commands.mjs src/lib/cc-due-gates.mjs src/lib/cc-report-artifact.mjs src/lib/cc-hand-edit-merge.mjs src/lib/cc-cold-readability.mjs src/lib/cc-skeleton-gloss.mjs` -->
