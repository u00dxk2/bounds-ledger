<!-- GENERATED — regenerating REPLACES everything outside the hand: markers, so put prose inside them. Provenance stamp at the foot of this block. -->

<!-- hand:begin banner -->
> **PULL, THEN START THE GATE, BEFORE READING THIS** — three minutes, all network:
> ```
> git pull --ff-only
> npm run verify > tmp/verify-out.txt 2>&1
> ```
> Expect **exit 0**. Any non-zero code is yours; read `tmp/.verify-receipt.json`'s `failedGates`,
> never infer the leg.
>
> **DO NOT TRUST ANY ID LIST IN THIS BANNER, INCLUDING BELOW.** On 09-09 this block named a gate
> that was never due while the GENERATED block below had it right, and the kickoff repeated the
> error as fact. Run the resolver:
> `node scripts/sky.mjs check-due-gates-dispositioned.mjs --print --today=2026-09-11`.
>
> **`A-42` IS DECIDABLE TODAY, NOT A DESIGN SESSION.** Both readers it names are now changed —
> the date label 09-06, the sort key 09-09 in `fa4b407` — each with KP-78 both ways. Its closeWhen
> asks exactly that. Read `instance4Shipped2026_09_09` on the row rather than re-deriving; then
> decide the two readers separately, which is what onTrigger means by not bundling them.
>
> **`A-20` IS ALSO DUE AND CARRIES A DO-NOT-SHIP REVIEW.** Run
> `git merge-tree --write-tree main a20-fetch-retry-backoff` BEFORE spending a runner on it: a
> verdict on a branch that cannot merge is a verdict about nothing. `A-39` sits at 09-12 to
> follow this decision, so take this one first.
>
> **`A-39` READS `NEVER-RUN` FOREVER AND THAT IS NOT A MISSED READ.** `git grep -c` exits 1 with
> no output on no match, which is that row's own answer. Do NOT make it always emit output — that
> trades a false NEVER-RUN for a read that cannot fail.
>
> **DAVID ANSWERED board card `f12ac518` AND IT IS CLOSED — this block said the opposite until P5 on
> 09-10, and a cold reader who obeyed it would have withheld work he had already funded.**
> His words: *"keep the goal as it is, drop the November verdict date, and judge the project
> meanwhile on how much of our own material stands up when we check it."* Applied and pushed.
> Nothing is waiting on David: confirm with
> `node scripts/sky.mjs answered-cards.mjs --project bounds-ledger --full` rather than
> trusting this line.
>
> **`A-47` NOW HAS A STORE AND HAS BEEN RUN — this block said it had none, true until P2 on
> 09-10.** Store `continuity/depth-audit.json`, read with `node scripts/depth-audit.mjs`; run it
> rather than quoting a figure from here. It refuses a bare zero, so a lapsed audit announces
> itself instead of reading as "no defects". **Do not quote its pass fraction as a rate** — the
> first slice deliberately hunted discrepancy and the script prints its own sampling rule above
> the count. Next job is more rows, not a coverage figure. `A-48`, the comparison promised him,
> is due **2026-09-16** and is the only outstanding commitment made directly to David.
>
> **ARRIVAL, AND WHICH SESSION THIS DESCRIBES.** The session that wrote this block is
> `806e0008-b671-48da-a6ec-f9af492172bd`, whose listener started 2026-09-10T19:07:15Z. It did
> NOT hang. The session BEFORE it went dark for about five hours: no turns from ~14:21Z, while
> its listener stayed connected and wrote 52 `no /loop detected` warnings that nothing could
> read. So a clean arrival here is a statement about this session only, and says nothing about
> the one it replaced. If you are reading this after a restart, check your own strand rather
> than inheriting mine: `node ../skylark-site/scripts/read-listener-strand.mjs --project bounds-ledger`.
<!-- hand:end banner -->
_bare item ids in the banner region above resolve via `node scripts/sky.mjs show-item.mjs <ID> --repo .` — never a whole-ledger grep._

First command (also below, in place):

```bash
git pull --ff-only
# commits after this primer was written are invisible to it — read them before acting:
git log --oneline c8473d89e41e272d4186100bcba9c07ca0872155..HEAD
```

### Primer — bounds-ledger, 2026-09-11

What ONLY this primer holds comes first; the FULL runnable **First action** block
sits at the foot, below the reads. The single command above is the same block's
first line, repeated so it can be running while you read — it replaces nothing.

_**Composed against HEAD commit `c8473d89e41e272d4186100bcba9c07ca0872155` at 2026-09-11T00:24:02.347Z.** Nothing that landed after that instant is in this file. David's board answers especially: an answer he gives later cannot be here, and this primer will go on reading as though the work were not owed — that is exactly how two lanes concluded an OTA was not owed on 2026-09-04. Read them live, never off this page: `node scripts/sky.mjs answered-cards.mjs --project bounds-ledger --full`._

_Shared skylark-site scripts below are spelled `node scripts/sky.mjs …` — this lane vendors `scripts/sky.mjs` (S-20260906-75), so a shared script is named LANE-RELATIVE and resolves from a worktree too._

#### Owed gates — 2 due on/before 2026-09-11

_Ledger read at 2026-09-11T00:24:02.347Z by `cc-due-gates.collectDue` — the same resolver `check-due-gates-dispositioned` uses, so any difference is TIMING, not two answers. Rows filed or re-dated after that are NOT READ at generation — the close-out commit lands after this, so the COUNT above is stale by construction and this list cannot claim completeness. The list below, not the count, is the artifact; re-derive both before acting: `node scripts/sky.mjs check-due-gates-dispositioned.mjs --print --today=2026-09-11`._

Unfiltered as of the read time above — nothing was curated out of what the read could
see; a row absent here may simply have been filed after it ran.

- `A-20` (expectedSignalBy=2026-09-11, open) — Should the fetch layer retry with backoff on 429/502? — separate from the unreachable-stay
  - read: `gh api repos/u00dxk2/bounds-ledger/actions/runs/32157521934/attempts/1 --jq .conclusion # prints failure. This is issue #25's originating attempt, the row that sat on the N_min=3 discriminator, and resolving it IS the decision this item registered. It is invisible to gh run list because that reports only the LATEST attempt and attempt 2 succeeded. Positive control that the endpoint reports real per-attempt conclusions: the same path with attempts/2 prints success on the same run id.` · last run: 2026-09-01T15:11:33.299Z
- `A-42` (expectedSignalBy=2026-09-11, open) — The date label and the sort key keyed off OUR pin history and read as claims about the REC
  - read: `node -e "const t=require('fs').readFileSync('index.html','utf8');const i=t.indexOf('id=\"c-1b\"');const s=t.slice(i,i+900);console.log(s.match(/tracked since[^<]*/g), s.match(/data-changed=\"[^\"]*\"/)[0])" # prints the founding record's live date label and sort key. REWRITTEN 2026-09-06 when A-42 instance 3 shipped in commit 5f9f021: this previously matched /first pinned[^<]*/, a string that no longer exists on the page, so the retired probe returned null on the label — and its own positive control read a null as a WRONG OFFSET rather than as the fix landing, i.e. it would have reported a shipped fix as a broken probe. LITERAL OUTPUT, pasted from the run rather than described, 2026-09-06 at commit 5f9f021: [ 'tracked since 2026-09-03 — no movement seen yet' ] data-changed="2026-09-03" , exit 0. On 2026-09-04 the retired form printed [ 'first pinned 2026-09-03 — unchanged since' ] and the same data-changed, which WAS the finding. STALENESS CORRECTED 2026-09-10: this field used to end 'the label half is FIXED and the sort-key half is deliberately UNCHANGED — instance 4 is what the 09-11 decision is still about', and that stopped being true when instance 4 shipped in commit fa4b407 on 2026-09-09. BOTH halves are now fixed. A probe whose prose asserts a state can go stale while the probe itself still runs, which is why this correction is dated rather than silently swapped. What the command now shows: the label wording, and a data-changed value that is still the row's own date — the ORDERING no longer keys on it alone, so a 1b date here is no longer evidence of a misrank. The ranking is read from data-moved and data-moved-date, which this probe does not print. THE SLICE WIDTH IS PART OF THE COMMAND, not an incidental: a first draft of this field used i+2000 and printed FOUR matches, because the wider window reaches the page's meta description and a multi-line tail; 900 returns the single row match above. The draft recorded the two-match output it expected rather than the four-match output it produced, and was caught by running it — the 2026-08-25 defect, one commit after a red-arm caught its sibling. POSITIVE CONTROL that this reads a real row rather than an empty slice: both halves must be non-null. A null on the LABEL now means the wording regressed or the offset moved; a null on data-changed means the id or offset is wrong.` · last run: NEVER

#### What shipped since 24 hours ago

_Read at 2026-09-11T00:24:02.347Z; commits landing after that — the close-out especially — are not here. Re-derive: `git log --oneline --since='24 hours ago'`._

- commit `c8473d8` — Hygiene pass 2026-09-10 — the day's report, and tomorrow's primer said David was still waiting
- commit `fd53cad` — The public page can now show what we have CHECKED, not only what we mirror
- commit `5ccd349` — The depth audit has a store and a first slice: 3 rows read against their sources, 2 sound
- commit `0cee023` — David dropped G-4's November verdict date — the goal is untouched and A-47 carries the meanwhile judgment
- commit `fc0df42` — P5 hygiene: the primer this lane actually owed, and A-42's second reader attached to its row
- commit `d6efd49` — P4: both overdue gates dispositioned by their own pre-registered instructions, and G-4's David-wait finally has a card
- commit `26dc939` — Cold-start primer for 2026-09-10 — the date rolled with no primer for the day it rolled into
- commit `80599ea` — Daily report 2026-09-09 — David ruled depth, and the survey control stopped answering the wrong question
- commit `90481bc` — Two due gates had never been RUN, and running them broke both readers
- commit `70d3917` — David ruled depth; A-34 closes on the ruling, and the sweep found it live in one doc
- commit `e5ff23b` — A-34: the proposal shipped to David, and the row stays open pending his ruling
- commit `fa4b407` — Asked what moved recently, the page answered what we started watching recently
- commit `e8668a8` — G-4: the 09-26 read is bound to detection health; the 11-06 read is left for David
- commit `618226a` — The receipt could not see the one transition the banner tells you to watch for
- commit `dd05d3e` — W-6 traffic sample 2026-09-09 — 1 viewer/14d at day 33, down from 2 at day 27

**Yesterday's report:** `docs/daily/2026-09-10-prelaunch.md` (coverage 2026-09-10, the prior day). No findings fence declared in it.

**Today's report (owed):** write `docs/daily/2026-09-11-prelaunch.md` — the artifact of record for service day 2026-09-11. Check it exists at close: `node scripts/sky.mjs check-daily-report-exists.mjs --date 2026-09-11` (it sweeps the fleet and applies each lane's coverageOffset; it takes no --project).

#### State — as of 2026-09-11T00:24:02.347Z (re-run the release command; never trust the value)

- **ledger**: 61 item(s): 41 closed · 20 open · as of 2026-09-11T00:24:02.347Z — release: `node scripts/sky.mjs gen-primer-first-action.mjs --repo . --target-date 2026-09-11 --json`

#### Id glossary — 23 id(s) this primer's day already has words for

_Each gloss is the ledger row's OWN title, not a re-derivation — paste it. Rows = every open ledger row plus every id named above. `(no ledger row)` means the id is named here and the ledger carries nothing under it: that is a finding, not a formatting gap._

- `A-2` — STANDING: drift-resolution log for the adopted surfaces…
- `A-7` — engineering-health P1s — four of five shipped 2026-08-09;…
- `A-9` — engineering-health P2 backlog (fix-on-touch, not scheduled…
- `A-20` — Should the fetch layer retry with backoff on 429/502? —…
- `A-33` — A change to a file we have filed an upstream report…
- `A-34` — David asked where else this ledger can have the kind of…
- `A-39` — A mid-run 429 becomes up to 339 requests in two seconds at…
- `A-42` — The date label and the sort key keyed off OUR pin history…
- `A-43` — Upstream moved Haugland's minimum-overlap bound to its…
- `A-45` — TIER-1 LEADING INDICATOR (orchestrator-approved…
- `A-46` — A close-out post announcing a FIX is textually identical…
- `A-47` — THE DEPTH AUDIT, David-ruled 2026-09-09: read our own…
- `A-48` — The second-area citation comparison David asked for,…
- `G-2` — GOAL: contribute a verified bound improvement to the…
- `G-4` — GOAL (Tier-0 from G-3 close): an outside party acts on a…
- `KP-78` — (no ledger row)
- `S-20260906-75` — (no ledger row)
- `W-3` — Watch for acknowledgement of the erdosproblems.com/36…
- `W-4` — KP-78 applies to every new detector: demonstrate BOTH…
- `W-6` — Read window for the README report-an-error channel — the…
- `W-8` — Does a materially amended outward artifact go back to…
- `W-9` — Does this lane's detection method find what the lane…
- `W-10` — reverify.test.mjs alarm-title leg failed once and would…

### First action — bounds-ledger, 2026-09-11

Every line in the block below is a COMMAND, not a claim. A command re-reads at the
moment you run it and cannot go stale; a state line written last night can.

```bash
git pull --ff-only
node scripts/sky.mjs update-david-board.mjs --list --json --full-ids --project bounds-ledger
git rev-parse HEAD origin/main
git rev-list --count origin/main...HEAD
npm run verify > tmp/verify-out.txt 2>&1
node scripts/sky.mjs check-ci-status.mjs --workflow reverify.yml
```

Morning release commands — deliberately NOT READ at generation (a value written
last night would already be stale); run each, never trust a remembered one:
- `git -C . log -1 --format=%h%x20%s` — HEAD
- `git -C . status --porcelain` — working tree
- `node scripts/sky.mjs check-ci-status.mjs --workflow reverify.yml` — CI
- `node scripts/sky.mjs check-deployed-sha-drift.mjs` — deploy
  - WHERE IT LOOKED: no read-only deploy-state script in this lane's package.json (looked for ops:deploy-state, deploy-state, check-deploy, render-deploys, deploy:status, daily-snapshot). The fleet's check-deploy-state.mjs is not a substitute — it requires --fix <sha> --deploy <sha> and exits 1 without them — so this is the FLEET read, which takes no required flags. service: NOT DECLARED — no row for "bounds-ledger" in src/data/cc-deploy-gates.json (generatedAt 2026-09-06T07:34:03.935Z, 17 repo row(s)) — nothing in this repo declares that lane's Render service. No `--service` flag is emitted: the lane SLUG is not the service name (antebellum-twitter's is disunion-feed), and a guessed flag exits 1.

<!-- hand:begin narrative -->
_(orchestrator narrative — this region survives regeneration)_
## Session close-out — 00:18Z

_Appended 2026-09-10 evening MT by session `806e0008-b671-48da-a6ec-f9af492172bd`. Earlier sections stand as the record; where this one disagrees with them, this one is later and wins._

**SESSION IDENTITY, because a liveness line that does not name its session is the thing the fleet retired today.** Two sessions ran this lane on 2026-09-10. `26ba7691-5930-4766-a6cf-0570a5ee03ae` ran the morning (P4, P5, morning close) and **stopped mid-life at 14:12:51Z** — its transcript ends on six `<task-notification>` records enqueued inside two milliseconds and never consumed. `806e0008-b671-48da-a6ec-f9af492172bd` started ~19:05Z and did everything after. **The banner above says this session did not hang; that is a claim about session `806e0008` only (a session id, not a commit), and the session it replaced was dark for 4h54m.** Read your own strand rather than inheriting either: `node ../skylark-site/scripts/read-listener-strand.mjs --project bounds-ledger`. <!-- pragma: allowlist sha -->

### Today's load-bearing ships

- `0cee023` — David dropped `G-4`'s November verdict date. The goal text is untouched; `A-47` carries the interim judgment. Retired text kept on the row, not deleted.
- `5ccd349` — the depth audit got a store (`continuity/depth-audit.json`), a reader, and its first slice. Its CI selftest is wired in the same commit.
- `fd53cad` — **the only user-visible ship.** Constant pages now show which rows we have read against their cited source. Two adversarial rounds said do not ship first; both were right.
- `c8473d8` — today's report, and two false state claims removed from THIS primer's banner.

### Tomorrow's first action

Run the first-action block at the foot of this file. Then take the two gates below — **`A-20` first**, because `A-39` (2026-09-12) is dated to follow its decision.

### Watch items maturing tomorrow — DERIVED, not authored

From `node ../skylark-site/scripts/check-due-gates-dispositioned.mjs --print --today=2026-09-11`, which swept 61 rows and printed **2 gates due on/before 2026-09-11**:

- **`A-20`** (should the fetch layer retry with backoff on 429/502) — `expectedSignalBy`. Its read is a per-attempt `gh api` call, and the resolver WARNs `READ_SCOPE_DISJOINT`: the command names no file token, so nothing relates its scope to the row. Read that WARN before treating a clean as information.
- **`A-42`** (the date label and the sort key) — `expectedSignalBy`, and the resolver flags **`[READ_NEVER_RUN]`**: its readCommand has never executed since the row was created 2026-09-04. A gate that never ran looks exactly like one that passes. Run it through the gate: `node ../skylark-site/scripts/check-due-gates-dispositioned.mjs --run A-42 --dry-run` (drop `--dry-run` to execute and stamp).

**`A-42` WAS RETITLED AT THIS CLOSE and the old title is why.** It read "Two user-facing readers **still** key off OUR pin history" — a present-tense state claim frozen on 2026-09-04, while the row's own notes record BOTH readers shipped (`5f9f021` on 09-06, `fa4b407` on 09-09). A list view shows the title, not the notes. Original kept at `A-42.titleRetired2026_09_10`. **The 09-11 gate is a close decision against `closeWhen`, not a design session.**

### Pending decision-blocked items

**None. Nothing is waiting on David.** Both cards he answered overnight are executed and dismissed — board card `f12ac518` (the November date) at commit `0cee023`, and board card `2f90980a` (depth over breadth) on the audit slice. Both of those are CARD ids, not commit shas. Board read at close: `answered-cards.mjs` returned no waiting/answered/pending-verify cards, and the raw awaiting set returned HTTP 200 with 0 rows.

### Active in-flight work

None. No Codex job, no open PR, no queued deploy. `A-48` — the second-area citation comparison **promised to David for 2026-09-16** — is the only outstanding commitment made directly to him; its predecessor slipped four dates before landing.

### CI

**reverify: completed/success @ `c8473d8`** — read at close with `node scripts/sky.mjs check-ci-status.mjs --workflow reverify.yml`. Re-run it rather than trusting this line; the close commit below lands after it.

### What NOT to touch tomorrow

- **The `agent-status` pre-POST gate set.** It refused four of my posts today for four different real defects and each refusal named the remedy. It refuses BEFORE the row lands, so there is nothing to retract. Do not soften it to a warning, and do not widen `--no-verify-sha` past out-of-portfolio shas.
- **The depth-audit reader's refusal to print a bare zero.** `exit 2` on a missing or empty store is the whole point: a lapsed audit must announce itself rather than read as "no defects".
- **The audit block's denominator and its not-checked sentence.** Both are load-bearing against the block reading as coverage, and both are red-armed. 3 of 543 is almost nothing and the page must keep saying so.

### Tier-0 binding constraint

`G-4` (an outside party acts on a watched record without us filing the report) reads **0 arrivals**, which is expected and not a verdict — its 30-viewer threshold is unreachable and David removed the date that would have forced a reading. The interim yardstick is now `A-47`: **3 rows audited, 2 sound, 1 unresolved** — run `node scripts/depth-audit.mjs`, and do not quote that fraction as a rate.

### Evangelism read

**The core problem, in a reader's words:** "I am about to cite a number and I do not know whether it still holds." **The would-tell-a-friend moment:** finding out a number you were about to publish has moved, or was never supported by the paper it cites. **Distance from it today: closer than yesterday, and measurable for the first time** — the pages now show which rows we have actually read against their sources, so a reader can tell mirroring from checking. **Retention / word-of-mouth: UNMEASURED and structurally unmeasurable here** — the published page is architecturally incapable of reporting usage (`render-site.mjs`'s selftest enforces that it fetches nothing), so the only arrival signals are a filed issue, a PR, or an email. 1 unique repo viewer in the trailing 14 days at the last sample. **No real-user advocacy signal this session.**

### Supersedes

This block supersedes the banner's original "WAITING ON DAVID" and "A-47 has no store" lines, both of which were corrected in place at `c8473d8` earlier this evening.

<!-- hand:end narrative -->
_bare item ids in the narrative region above resolve via `node scripts/sky.mjs show-item.mjs <ID> --repo .` — never a whole-ledger grep._

<!-- generated by gen-primer-first-action.mjs (skylark-site) @ commit 276a600f3 · lane bounds-ledger · for 2026-09-11 · at 2026-09-11T00:24:02.347Z -->
<!-- writtenAtSha: `c8473d89e41e272d4186100bcba9c07ca0872155` — the lane's HEAD commit when this primer was written. Provenance anchor for check-primer-freshness leg ii; NOT a claim about HEAD now (the close-out commit that carries this file lands after generation). -->
<!-- reading this block: a generated line that is wrong in a way you can see FIXED in skylark-site's git log after the commit above is STALE, not a finding — regenerate before filing it. `git -C C:/dev/skylark/skylark-site log --oneline 276a600f3^{commit}..HEAD -- scripts/gen-primer-first-action.mjs src/lib/cc-primer-commands.mjs src/lib/cc-due-gates.mjs src/lib/cc-report-artifact.mjs src/lib/cc-hand-edit-merge.mjs src/lib/cc-cold-readability.mjs src/lib/cc-skeleton-gloss.mjs` -->
