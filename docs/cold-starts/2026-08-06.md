# Cold-start primer for 2026-08-06 (MT)

Multi-session primer — each /close-session run appends a new section below.

## Session close-out — 21:05Z (2026-08-05, ~15:05 MT)

You are the bounds-ledger agent. Read this, then `AGENTS.md` + `CLAUDE.md`, then `continuity/items.json`.
Steward of drifting mathematical records; pre-launch, private repo, **PACED rail**, **no self-rating**.

### Tomorrow's first-action

**Steward cadence first, as always** — read the overnight run's **LOG, not the badge**
(`gh run list --limit 3`, `gh run view <id> --log`). Expect `No drift. 111 files match upstream
dee1660…`, `229 claim(s): 227 hold, 0 broken/unreachable, 2 unverified (manual)`, two expected `403`
advisory lines (C-7/C-9), and the brief job logging *"not runnable from CI … Not filing an issue."*
Green → **streak day 14**. Then `npm run check` locally **with `CC_PROMPTS_PIN` set**. If red:
verify → `--snapshot` → `extract-pins.mjs` → commit, in that order.

**Then: clear the flip blockers.** Concretely, start with the two that need no decision from anyone —
**add a `LICENSE`/`NOTICE` covering the 111 mirrored upstream files** (we redistribute someone else's
content with no licence statement at all), and **strip the disclosure defects from tracked files**
(`continuity/health.json`'s absolute local path; the security-posture inventory in
`docs/engineering-health-review-2026-07-29.md`; the portfolio PII description in `items.json` L302-305).

**Why this and not a product change, stated plainly as the rail requires:** this lane has **zero
reachable users** — the repo is private, so the contact route shipped yesterday can be opened by nobody.
Every product move here is downstream of publication, and publication is now blocked on these. This is
launch work, not instrument work: no detector, no lint, no re-run. **Cap it** — if it grows past clearing
blockers, stop and write the rest up rather than building.

### The state that changed today, and it changes the plan

**THE FLIP IS ON HOLD. David: *"let's not go public with the repository until the issues have been
addressed."*** The whole-project pre-publication review he asked for returned **eight blockers** and the
verdict *"I would not flip public with this tree and history."*
**The 2026-08-03/04 "package is complete, nothing owed by us" read is SUPERSEDED — do not act on it.**

Why five previous reviews missed all of it, because this is the reusable part: four artifact-level
reviews plus the package-level review each asked *is this artifact accurate?* **None asked *what does
this repository disclose?*** Those are different passes. Now in `CLAUDE.md`.

The eight, each verified locally rather than taken on the reviewer's word (it was wrong about something
else the same day):

1. **No `LICENSE` / `NOTICE` / `COPYING`** at root or in the mirror dir, while redistributing 111 upstream files.
2. `continuity/health.json` embeds an absolute local path.
3. `docs/engineering-health-review-2026-07-29.md` publishes **our own security posture** — branch protection off, secret-scanning state. A map of what is unprotected.
4. `continuity/items.json` L302-305 publicly describes the **portfolio-wide PII issue** (874 addresses / 18 private repos). Not ours to disclose.
5. Personal author email on every commit.
6. **15 commit bodies** contain private `claude.ai/code/session_…` URLs.
7. Commit `228bd46` added `.playwright-mcp/page-2026-07-25T20-13-43-020Z.yml`, a browser snapshot of erdosproblems.com containing **third-party forum user handles** (`/forum/user/holyterror`, `/forum/user/Desenyon`, `/forum/user/JJ_`). Removed later; still in history.
8. Board/card ids and David's private decision text across decision docs.

**1-4 and 8 are working-tree fixes. 5-7 are history-only and are NOT covered by his ruling — see below.**

### Today's load-bearing ships (11 commits, all pushed)

- **`02072a6`** — **the first pull request is SENT: `teorth/optimizationproblems` #141**, one file, +6/−6, five citation keys on upstream's `15a.md` (the matrix multiplication exponent page) that the page's own reference list never defines. David checked the page himself (screenshot) and said "send it now". Unlike the July email this **cannot be silently ignored**. Same commit records the flip hold.
- **`7b04896`** — correspondence redacted per his choice: the maintainer's name gone from 8 spots across 6 files, the sent email gone from 3 places, each replaced by its substance so the *why the wording changed* record survives.
- **`ad02f5a`** — README now tells a reader **how to say the ledger is wrong**. The lane's first inbound channel. Read window is **W-6** and it has not started (see below).
- **`9a197b8`** — the target set that produced #141: 10 citation-key defects across 6 mirrored files, found by a scan that reported 532 → 41 → 36 → 6, the first three being **my own parser**.
- Plus the daily report `docs/daily/2026-08-05-prelaunch.md` (4/4 lint PASS).

### North star — read this before writing any status

**Externally-acknowledged corrections: still 0.** A *sent* pull request is **not** an acknowledgement,
exactly as the July email was not. It moves on **merge**, or on a maintainer replying. **Do not read
"#141 is open" as progress**, and do not let it inflate a report. Check it, do not poll it.

### Pending / decision-blocked

- **#141** — with upstream. Nothing owed by us. If a maintainer questions our standing to file an editorial fix, the honest answer is already recorded: `CONTRIBUTING.md` documents only add-a-constant and update-a-bound, so the **doc** does not authorise it, but the **merge history** does (`fc933d8` against this exact file, plus `d2173be`, `bb73a16`). **Do not retreat to pretending the guide covers it.**
- **The flip** — blocked on the eight above. His hold is explicit.
- **W-3** — the July email is still unanswered. Monitor only; his standing word is wait.

### What NOT to touch tomorrow

- **Do not pre-stage a second upstream PR.** The other five files with the same defect (`10a`, `22b`, `46a`, `50a`, `52a`) stay unstaged until #141 has an outcome. The north star is 0, so the first real response teaches more than a second draft.
- **Do not treat David's "accept the history" as blanket clearance.** He ruled on the *maintainer's email address*, reasoning it was **published on the maintainer's own site**, so it was never private contact information. He had **not seen** blockers 5, 6 and 7 when he ruled. Those need his decision separately.
- **Do not edit the 16 citations of that author's published papers inside `ledger/teorth-optimizationproblems/`.** Those are ordinary scholarly references in upstream's public content; editing them corrupts the byte-mirror and turns the drift alarm red.
- **Do not re-automate C-7/C-9** — the erdosproblems 403 is IP-dependent; a local 200 proves nothing about CI.
- **Do not put `CC_PROMPTS_PIN` in repo secrets.**
- **`check-satisfied-closewhen` is CLOSED** — no more rounds, no more re-runs (fleet ruling).
- **Do not re-propose the public /36 comment** (David, 8/02) or a git-history rewrite for the maintainer's email (David, 8/05).

### The correction budget now binds

`skylark-site/docs/specs/error-taxonomy-and-correction-budget-2026-08-05.md`. Substrate capped at **~20%
of a day**; **a correction is a one-line log entry plus the fix, not a report**; one tuning round per tool
per week; every substrate ship names the product outcome it protects. Measured cause: 63% of the fleet's
bus traffic yesterday was substrate correcting itself. **A large share of that was this pane.**

### Watch items

- **W-6 (new)** — read window for the README report-an-error channel. **The clock has not started**: the repo is private, so the number of people who could file a report is **zero**. Do not evaluate before the flip; after it, silence is uninformative until **90 days AND 100 unique visitors**. Reading a quiet channel as "users have nothing to say" is the exact wrong conclusion this item exists to prevent.
- **W-3** — the ack watch. Both page legs read clean from this machine today (still 0.380876, still "last edited 23 January 2026").
- **W-4** — every detector ships demonstrating both answers.
- **W-5** — the mirror blind class (upstream's unmirrored `README.md`). Read its `onTrigger` first.

### A note on the SHAs above, so you don't chase them

`dee1660`, `fc933d8`, `d2173be` and `bb73a16` are **upstream `teorth/optimizationproblems` commits**, not
ours. `git rev-parse` fails on them here and that is correct, not a dead reference — all four were
verified live against the upstream repo when this primer was written. Everything else cited is local.

### CI

**`reverify`: completed/success @ `02072a6`.** Green at HEAD, tree clean, everything pushed.
Open GitHub issues: **0.** Open continuity items: **10** (W-6 added).

### Evangelism read (required carry-forward)

- **The one problem, in a user's words:** "I cited a number, and I have no idea whether it is still the number."
- **The would-tell-a-friend moment:** a stranger sees the alarm catch a record moving that they had already cited — and can check for themselves that it caught it. **Distance: we cannot reach it. The repo is private; nobody can see the alarm at all.**
- **Retention / word-of-mouth instrument:** none, and correctly none — there is no audience. The inbound channel shipped yesterday (`ad02f5a`) is the *first* half; the flip is the other half, and it is blocked.
- **Real-user advocacy signal this session:** zero. One outward artifact now exists that can produce one (#141), which is more than was true yesterday.

### Tier 0 / binding constraint

Green streak **day 13/30** heading into day 14; ledger clean at `dee1660`; reach still structurally zero,
but for the first time there is a channel that must answer. Posture unchanged: **suspect the instrument
before the record** — and, added today, **"is it true" and "should it be visible" are different passes.**
