# Cold-start primer for 2026-08-05 (MT)

Multi-session primer — each /close-session run appends a new section below.

## Session close-out — 22:30Z (2026-08-04, ~16:30 MT)

You are the bounds-ledger agent. Read this, then `AGENTS.md` + `CLAUDE.md`, then `continuity/items.json`.
Steward of drifting mathematical records; pre-launch, private repo, **PACED rail**, **no self-rating**.

### State

**229 claims / 227 hold / 0 broken / 2 UNVERIFIED (by design)** over a **111-file** mirror, clean at
upstream **`dee1660`**. Nothing moved today — no drift, no snapshot, no pins touched.
**G-1 — the steward-credibility goal — is at green-streak day 12 of 30** (day 1 = 7/24; ends ~8/22).
North star (externally-acknowledged corrections): **still 0**, day 11 since the email.

**CI:** `reverify: completed/success @ e32a2ce` — green at HEAD, tree clean, everything pushed.
**Open GitHub issues: 0.** Open continuity items: 9. Continuity check: OK, 18 items, no findings.

### Today's load-bearing ships (3 commits, all pushed)

- **`0e235b2`** — **the PACKAGE-LEVEL ADVERSARIAL REVIEW. A-13's last owed gate is CLEAR.**
  Verdict **SURVIVES WITH ONE REQUIRED DECISION**. Doc:
  `docs/decisions/2026-08-04-A13-package-adversarial-review.md`. **The flip package is complete;
  nothing further is owed by us.** Read F-1 before writing anything about the flip.
- **`0bb58ac`** — `SECURITY.md` now names its own residual supply chain (mutable action tags) instead
  of reassuring past it. F-2, resolved by prose rather than by a control.
- **`e32a2ce`** — **F-4: our access enumeration was incomplete.** Four GitHub Apps (`vercel`,
  `render`, `cursor`, `claude`) hold check-suite entitlements on this repo, at scopes unreadable
  without an app-authorized token, on a **User** account that has **no audit log at all**.
- Plus the daily report `docs/daily/2026-08-04-prelaunch.md` (4/4 lint PASS).

### Tomorrow's first-action

**Steward cadence first, as always.** Read the overnight run's **LOG, not the badge**
(`gh run list --limit 3`, `gh run view <id> --log`). Expect `No drift. 111 files match upstream
dee1660…`, `229 claim(s): 227 hold, 0 broken/unreachable, 2 unverified (manual)`, two expected `403`
advisory lines (C-7/C-9), and the brief job logging *"not runnable from CI … Not filing an issue."*
Green → **streak day 13**. Then `npm run check` locally **with `CC_PROMPTS_PIN` set**. If red:
verify → `--snapshot` → `extract-pins.mjs` → commit, in that order.

**Then the launch increment. A-13's review gate is DONE, so the remaining backlog is:**
**new first-PR targets** — the highest-value unblocked work in the lane. The three named candidates
(#129/#130/#135) all merged 8/02, so that item is dead as imagined and the correction-shaped-PR
channel currently has **no target**. Selection needs no gate; only a send would.
Other live options: **A-11** — pin-a-negative for the two structured sources; **W-5** — the mirror
blind class (read its `onTrigger` first).

### The decision now sitting with David — do not pre-empt it, do not re-file it

**A-13's flip goes to him at streak day 14 (2026-08-06) as already planned, and F-1 must be ON that
decision.** Do NOT file a separate card — the flip decision is the vehicle, and A-7 note3 is explicit
that a redundant card is the wrong move.

**F-1 in one paragraph:** the 8/02 history sweep is clean and correct — it swept for **credentials**.
**Nobody swept for people.** Going public publishes the erdosproblems maintainer by name alongside our
finding that his page is stale, the **verbatim text of the email David sent him** on 7/24
(`docs/lane-brief.md`, `## What we said`), that he has not replied, and the public-comment contingency
**David declined on 8/02** — across ~10 tracked files. Every artifact is individually accurate; the
exposure exists only in the SET, which is what four artifact-level reviews structurally could not see.
Three defensible options (flip as-is / redact the correspondence / tell the maintainer first — the
third is outward contact and David-gated). **We hold no recommendation: the email was his.**

### What NOT to touch

- **Do not re-open the two asks withdrawn tonight.** (a) SHA-pinning the two GitHub Actions —
  withdrawn on David's own 19:48Z words to two other panes: *"I am the only contributor on this
  project and the repo is private for now. Why do we need this?"* Same reasoning as A-7 note3. The
  pins remain a two-string change if fleet-wide controls ever land. (b) The four GitHub Apps (F-4) —
  **our** completeness defect, not his decision.
- **Do not re-propose the public /36 comment.** David decided 8/02. Not wording, not venue, not timing.
- **Do not re-automate C-7/C-9** — the erdosproblems 403 is IP-dependent; a local 200 proves nothing
  about CI.
- **Do not put `CC_PROMPTS_PIN` in repo secrets.** The clause that would have forced it was retired 8/03.
- **Do not treat A-13's first-PR targets as live** — #129/#130/#135 merged 8/02.
- **Do not re-run the history sweep as "the increment"** — done 8/02. **Do** re-run it immediately
  before any flip click; the clean claim is only true as of the commit it ran against. **And note
  what F-1 proves about it: a clean sweep is clean for CREDENTIALS only.**

### Deploy-gate state (fleet dispatch, settled — do not re-derive)

**DO-NOT-FLIP, because there is nothing to flip. This lane owns 0 Render services** — verified
against the Render API (46 services enumerated, 0 match), not against a doc. Nothing here deploys;
the only automation is the GitHub Actions drift job. **Precondition that would change this:** the lane
acquiring a deploy surface at all. If that ever happens it must be `checksPass` from its first commit
— a red run here does not mean a build broke, it means **a cited record moved**, and deploying past
it would publish a ledger asserting a value we already know is stale.

**`cancel-in-progress`: absent.** No `concurrency` block in either workflow (grepped, not remembered),
so frolic's silent deploy-loss hazard cannot apply here.

**Own-history check: 6 red CI runs on `main` (7/25 ×4, 8/01, 8/02) out of 100+, none shipped
anything** — 6 failures × 0 deploy surfaces. The billionaire-army incident class is structurally
impossible in this lane, verified rather than assumed.

### Verified-tonight facts worth not re-deriving

- `GET /commits/{ref}/status` is **blind to GitHub Actions** (legacy Status API) — this repo, with
  known-good CI, reads `pending` / 0 statuses. `GET /commits/{ref}/check-suites` is a **false
  positive**: 5 here, of which 4 are permanently-`queued` phantoms from account-level apps. The only
  honest probe is **`/check-runs` with a conclusion**.
- antebellum-twitter and forecast-lab genuinely have 0 workflows AND 0 check-runs (re-read on the
  correct endpoint). `srv-d9a0u4po3t8c7384pcg0` (disunion-feed) was flagged to the fleet as
  do-not-flip and was kept out of the central batch.

### Watch items maturing

- **W-3** — the watch for a reply to the 7/24 email: **not ours to force, monitor only.** Both legs
  clean today from this residential IP (page still 0.380876, still "last edited 23 January 2026").
  Its ~8/24 decision point is the next real checkpoint.
- **W-5** — the mirror blind class (upstream's unmirrored `README.md`). Read `onTrigger` first; the
  right shape is likely A-6's (pin specific cells), not a byte mirror of a 421KB generated file.
- **A-7 R6** — push protection. Still deliberately uncarded; re-arm only if the flip is declined or
  unresolved by ~2026-08-16.

### Known, unfixed, deliberately

`docs/lane-brief.md` fails `check-david-voice.mjs` with 2 BLOCKs (`claim`, `bound` un-glossed) and
2 em dashes. **The em dashes are the documented carve-out** — the verbatim quote of David's 7/24
email, where byte-fidelity beats style. The two jargon BLOCKs are **pre-existing and untouched today**
(the brief was not edited). Fixing them means the orchestrator must re-port the page, so it is a
deliberate deferral, not an oversight. Not urgent; do it as a brief-edit increment if one comes up.

### Tier 0 / binding constraint

Green streak day 12/30, ledger clean at `dee1660`, reach still structurally zero. Posture: **suspect
the instrument before the record — and suspect the summary you built on top of a verified probe.**
Today's own lesson, now in `CLAUDE.md`: proving one probe can fail does not validate the enumeration
around it. The instrument was right all day again. The thing that was incomplete was my account of it.
