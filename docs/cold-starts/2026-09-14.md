<!-- GENERATED — regenerating REPLACES everything outside the hand: markers, so put prose inside them. Provenance stamp at the foot of this block. -->

<!-- hand:begin banner -->
> **PULL, THEN START THE GATE, BEFORE READING THIS** — three minutes, all network:
> ```
> git pull --ff-only
> npm run verify > tmp/verify-out.txt 2>&1
> ```
> Expect **exit 0**. Any non-zero code is yours; read `tmp/.verify-receipt.json`'s `failedGates`,
> never infer the leg.
>
> **BATCH YOUR LEDGER WRITES, THEN GATE ONCE — and never commit while the gate runs.** It held on
> 2026-09-13: one gate run per code-changing phase, no `headMovedDuringRun` all day.
>
> **DO NOT TRUST ANY ID LIST IN THIS BANNER.** Run the resolver:
> `node scripts/sky.mjs check-due-gates-dispositioned.mjs --print --today=2026-09-14`.
> It should list `A-2` (the standing drift log, `nextCheckDate`) and `A-48` (surface 1, below).
> `A-2` owes nothing unless its read prints something other than `No drift.`
>
> **`A-48` SURFACE 1 IS DUE TODAY, 2026-09-14** — "Small Ramsey Numbers", EJC dynamic survey,
> DOI `10.37236/21`. It is the only commitment made directly to David: the whole comparison is promised
> for 2026-09-16, recorded in the row's `note` and `signalDateNote`, and it scored 0 surfaces on 09-13.
> The row carries ONE clock, `expectedSignalBy`, stepped per surface — 09-14 now, then 09-15, then the
> 09-16 delivery. Read the footprint from TWO indices, then A-34's four criteria in order, then re-date
> `expectedSignalBy` to 2026-09-15 for surface 2
> ("Graph Labeling", DOI `10.37236/27`, verified on 09-13: Crossref 156). The plan, and why shapes (c)
> and (d) are UNTESTED rather than struck: `node scripts/sky.mjs show-item.mjs A-48`, field `notes`.
> The single Crossref fetch of `10.37236/21` on 09-13 failed at the transport layer — a fact about the
> fetch, not the surface; re-read it.
>
> **`A-43` IS DECIDABLE AND DUE 2026-09-15.** Its four lookups are DONE and PUBLISHED on the 1b page
> (slice 4, `f95006d`): every neighbouring source states six decimals, so H2016 is the only cell where
> upstream must choose a rendering. What remains is the report-shape decision; any upstream draft needs
> the adversarial review AND David's gate before it goes anywhere.
>
> **COVERAGE READS 7, NOT 9, AND NOTHING WAS UN-AUDITED.** Two slice-1 rows were suspicion-drawn and
> left the coverage figure on 09-13. Release: `node scripts/depth-audit.mjs` — its `selection:` line
> prints both populations.
>
> **THE PAGE NOW SHOWS EACH CHECKED VALUE, AND ONLY WHEN IT IS PROVABLY THE ROW READ** (`2bd8292`):
> recorded line + recorded text + recorded hash. A row that fails renders as HISTORICAL — no live link,
> no support claim, not counted. If an upstream edit shifts audited lines, expect historical rows rather
> than a bug; the page says so in words.
>
> **`A-45` IS A FLOOR, NOT AN INDICATOR** (ruled 09-13). Report its MISSES count, never its ratio:
> `node scripts/check-resolvability.mjs`, run LOCALLY.
>
> **SEVEN readCommand FIELDS ARE STILL ESSAYS** (three cut on 09-13). List them by reading every open
> row's `readCommand` length in `continuity/items.json`; the remainder is bulk-shaped and the first thing
> worth delegating.
>
> **`W-7` ROTATES.** 09-13's instrument was a selection-sum refusal I wrote and deleted the same hour
> because it could never fail. Pick a different one and write the line opening `W-7 —`.
>
> **NOTHING IS WAITING ON DAVID** as of 09-13. Release:
> `node scripts/sky.mjs answered-cards.mjs --project bounds-ledger --full`.
<!-- hand:end banner -->
_bare item ids in the banner region above resolve via `node scripts/sky.mjs show-item.mjs <ID> --repo .` — never a whole-ledger grep._

First command (also below, in place):

```bash
git pull --ff-only
# commits after this primer was written are invisible to it — read them before acting:
git log --oneline 39a7ede496fc37b3abbe6689e03ab37a50f6b24d..HEAD
```

### Primer — bounds-ledger, 2026-09-14

What ONLY this primer holds comes first; the FULL runnable **First action** block
sits at the foot, below the reads. The single command above is the same block's
first line, repeated so it can be running while you read — it replaces nothing.

_**Composed against HEAD commit `39a7ede496fc37b3abbe6689e03ab37a50f6b24d` at 2026-09-13T18:08:04.802Z.** Nothing that landed after that instant is in this file. David's board answers especially: an answer he gives later cannot be here, and this primer will go on reading as though the work were not owed — that is exactly how two lanes concluded an OTA was not owed on 2026-09-04. Read them live, never off this page: `node scripts/sky.mjs answered-cards.mjs --project bounds-ledger --full`._

_Shared skylark-site scripts below are spelled `node scripts/sky.mjs …` — this lane vendors `scripts/sky.mjs` (S-20260906-75), so a shared script is named LANE-RELATIVE and resolves from a worktree too._

#### Owed gates — 2 due on/before 2026-09-14

_Ledger read at 2026-09-13T18:08:04.802Z by `cc-due-gates.collectDue` — the same resolver `check-due-gates-dispositioned` uses, so any difference is TIMING, not two answers. Rows filed or re-dated after that are NOT READ at generation — the close-out commit lands after this, so the COUNT above is stale by construction and this list cannot claim completeness. The list below, not the count, is the artifact; re-derive both before acting: `node scripts/sky.mjs check-due-gates-dispositioned.mjs --print --today=2026-09-14`._

Unfiltered as of the read time above — nothing was curated out of what the read could
see; a row absent here may simply have been filed after it ran.

- `A-2` (nextCheckDate=2026-09-14, open) — STANDING: drift-resolution log for the adopted surfaces (re-verification CI shipped 2026-0
  - read: `node scripts/reverify.mjs --check # the drift alarm itself. Literal output 2026-08-25: "No drift. 113 files match upstream <upstream sha elided 2026-09-07 — reprint with `node scripts/reverify.mjs --check`, or read it from ledger/teorth-optimizationproblems/manifest.json>." (that 40-hex token is an UPSTREAM teorth/optimizationproblems commit; it does not resolve in this repo), exit 0. Any non-zero exit means a cycle is owed and this row is the log for it. Positive control that the command can say otherwise: reverify.test.mjs plants synthetic drift in 10a.md every run and asserts it is detected, so the detecting path is exercised on every npm test. WHICH PART OF THE QUOTED LITERAL IS THE INVARIANT (added 2026-08-31): the invariant is the words No drift. ALONE. THE FILE COUNT IS INFORMATIONAL, NOT PART OF THE INVARIANT (corrected 2026-09-07, and this correction is instance four of the shape the paragraph below describes): the count read 113 on 2026-08-31 and 116 on 2026-09-07, because our own resolution cycles on 09-05 and 09-06 mirrored three further files. A count that has MOVED is not a catch; a count that moved with NO resolution cycle in our own history behind it is worth one look, and git log ledger/teorth-optimizationproblems/ is where you settle which you are holding. The 08-31 edit that added this paragraph fixed the sha half and PROMOTED the count to invariant in the same stroke, so a cold agent running this row against the invariant the row itself named would have read a benign three-file gap as a violation and re-verified a mirror that was fine. Any figure quoted here carries its as-of date for the same reason. The 40-hex upstream sha in that sentence MOVES ON ITS OWN — upstream commits that touch no mirrored file change it while every mirrored byte holds, which is exactly what happened between 2026-08-25 (upstream sha <upstream sha elided>) and 2026-08-31 (upstream sha <upstream sha elided>), 113 files both times. A changed sha alone is NOT a catch and must not be read as one. Upstream teorth/optimizationproblems shas were quoted in this field as values and are now ELIDED, marked, with the command that reprints them — a whole-ledger sweep on 2026-09-07 removed 5 such tokens across G-3, A-33 and A-34 as well. They resolve in no portfolio repo by construction, which is the very thing this paragraph is about, so check-staged-shas-resolve refuses them and the fleet ruling of 2026-09-07 is that the escape belongs on a PROSE line and NEVER inside JSON. An earlier version of this sentence claimed the line carried such a pragma; the elision made that claim false, and it is corrected here rather than quietly dropped. Recorded because the 2026-08-25 rule that a verification note records literal output rather than a paraphrase — adopted to stop a false PASS — sets up a false ALARM wherever the literal contains a moving token, and the rule carried no clause saying which tokens those are.

 SECOND LEG ADDED 2026-09-05, because the first one is BLIND to the artifact this row is the log for. reverify.mjs --check compares the mirror; it cannot see whether the README catch table — the PUBLIC rendering of this very log — has fallen behind the resolution cycles it lists. That is checked by: node scripts/render-state-block.mjs --check # which on 2026-09-05, with mirror, pins, state block and pages all in sync, still failed with "catch table: STALE — README.md claims 15 upstream drifts, but mirror history has 16 resolution cycles". Literal output after the row was added, run 2026-09-05 and pasted rather than paraphrased: "catch table: 16 cycles, and README.md says 16." at exit 0. The first draft of this field asserted it printed "catch table: current" — a string that command never produces. It was written from the verdict rather than the run, caught by running it, and is recorded here rather than quietly replaced. POSITIVE CONTROL that this leg can say otherwise: it fired on exactly the day a cycle landed and went quiet once the row was written, which is both polarities on one artifact in one day.` · last run: 2026-08-31T15:03:22.232Z
- `A-48` (expectedSignalBy=2026-09-14, open) — The second-area citation comparison David asked for, promised to him for 2026-09-16
  - read: `node -e "fetch('https://api.crossref.org/works/10.37236/21').then(r=>r.text().then(t=>console.log(r.status, (t.match(/is-referenced-by-count\":\\d+/)||['NO COUNT FIELD'])[0])))" — reads ONE index for ONE known DOI to say only whether the citation-footprint path is reachable today. A non-200, or a body with no count field, means BLOCKED — never a low or zero footprint. The positive control and the rest are on this row in readCommandHistory2026_09_13.` · last run: NEVER

#### What shipped since 24 hours ago

_Read at 2026-09-13T18:08:04.802Z; commits landing after that — the close-out especially — are not here. Re-derive: `git log --oneline --since='24 hours ago'`._

- commit `39a7ede` — Primer 2026-09-14, and A-48's first surface on a clock the resolver reads
- commit `a717eba` — Hygiene pass 2026-09-13: a guard that tested a retired number, and today's commits on their rows
- commit `afac67d` — Report 2026-09-13: the 9-to-7 movement names its baseline
- commit `2bd8292` — P4: the checked value is on the page, and only when it is provably the row read
- commit `ae5bd3a` — Daily report 2026-09-13 — four records held, and the coverage figure drops to 7
- commit `f95006d` — Four more records read, and the page now says how each was chosen
- commit `80b9942` — P2: A-45 calibrated by pin kind, then ruled a regression floor
- commit `8172726` — Close-out 2026-09-12 — appended to tomorrow's primer
- commit `5e16670` — Sibling sweep: the third copy of the bare cross-repo path
- commit `7a7a636` — Hygiene pass 2026-09-12 — tomorrow's primer, the draw command documented, and today's commits attached to their rows
- commit `443a3ac` — Report back-patch: the gate line named a receipt taken over a moving tree
- commit `884ef7a` — Daily report 2026-09-12 — five records held up, and the machinery that picked them was wrong twice
- commit `e035373` — P4: A-39 closes on today's fix, and three carried rows get evidence rather than a date bump
- commit `2a40d62` — A-39: a mid-run 429 no longer becomes a synchronized burst at the limiter refusing us
- commit `3d65a09` — Five more records read against their sources, and the draw that picked them is now executable
- commit `e0d8d6c` — A-48 carries A-34's four criteria and candidate shapes, extracted not retyped
- commit `4df975f` — P2: all three due gates read and stamped, and A-39 is live for the first time

**Yesterday's report:** `docs/daily/2026-09-13-prelaunch.md` (coverage 2026-09-13, the prior day). No findings fence declared in it.

**Today's report (owed):** write `docs/daily/2026-09-14-prelaunch.md` — the artifact of record for service day 2026-09-14. Check it exists at close: `node scripts/sky.mjs check-daily-report-exists.mjs --date 2026-09-14` (it sweeps the fleet and applies each lane's coverageOffset; it takes no --project).

#### State — as of 2026-09-13T18:08:04.802Z (re-run the release command; never trust the value)

- **ledger**: 61 item(s): 44 closed · 17 open · as of 2026-09-13T18:08:04.802Z — release: `node scripts/sky.mjs gen-primer-first-action.mjs --repo . --target-date 2026-09-14 --json`

#### Id glossary — 22 id(s) this primer's day already has words for

_Each gloss is the ledger row's OWN title, not a re-derivation — paste it. Rows = every open ledger row plus every id named above. `(no ledger row)` means the id is named here and the ledger carries nothing under it: that is a finding, not a formatting gap._

- `A-2` — STANDING: drift-resolution log for the adopted surfaces…
- `A-7` — engineering-health P1s — four of five shipped 2026-08-09;…
- `A-9` — engineering-health P2 backlog (fix-on-touch, not scheduled…
- `A-33` — A change to a file we have filed an upstream report…
- `A-34` — David asked where else this ledger can have the kind of…
- `A-39` — A mid-run 429 becomes up to 339 requests in two seconds at…
- `A-43` — Upstream moved Haugland's minimum-overlap bound to its…
- `A-45` — TIER-1 LEADING INDICATOR (orchestrator-approved…
- `A-46` — A close-out post announcing a FIX is textually identical…
- `A-47` — THE DEPTH AUDIT, David-ruled 2026-09-09: read our own…
- `A-48` — The second-area citation comparison David asked for,…
- `G-2` — GOAL: contribute a verified bound improvement to the…
- `G-3` — GOAL (Tier-0 from G-1 close): someone outside Skylark uses…
- `G-4` — GOAL (Tier-0 from G-3 close): an outside party acts on a…
- `S-20260906-75` — (no ledger row)
- `W-3` — Watch for acknowledgement of the erdosproblems.com/36…
- `W-4` — KP-78 applies to every new detector: demonstrate BOTH…
- `W-6` — Read window for the README report-an-error channel — the…
- `W-7` — The 2026-08-15 instrument-audit proposal was never tracked…
- `W-8` — Does a materially amended outward artifact go back to…
- `W-9` — Does this lane's detection method find what the lane…
- `W-10` — reverify.test.mjs alarm-title leg failed once and would…

### First action — bounds-ledger, 2026-09-14

Every line in the block below is a COMMAND, not a claim. A command re-reads at the
moment you run it and cannot go stale; a state line written last night can.

```bash
git pull --ff-only
node scripts/sky.mjs update-david-board.mjs --list --json --full-ids --project bounds-ledger
git rev-parse HEAD origin/main
git rev-list --count origin/main...HEAD
npm run verify > tmp/verify-out.txt 2>&1
node scripts/sky.mjs check-ci-status.mjs --workflow reverify.yml
```

Morning release commands — deliberately NOT READ at generation (a value written
last night would already be stale); run each, never trust a remembered one:
- `git -C . log -1 --format=%h%x20%s` — HEAD
- `git -C . status --porcelain` — working tree
- `node scripts/sky.mjs check-ci-status.mjs --workflow reverify.yml` — CI
- `node scripts/sky.mjs check-deployed-sha-drift.mjs` — deploy
  - WHERE IT LOOKED: no read-only deploy-state script in this lane's package.json (looked for ops:deploy-state, deploy-state, check-deploy, render-deploys, deploy:status, daily-snapshot). The fleet's check-deploy-state.mjs is not a substitute — it requires --fix <sha> --deploy <sha> and exits 1 without them — so this is the FLEET read, which takes no required flags. service: NOT DECLARED — no row for "bounds-ledger" in skylark-site/src/data/cc-deploy-gates.json (generatedAt 2026-09-11T05:03:44.019Z, 17 repo row(s)) — nothing in this repo declares that lane's Render service. No `--service` flag is emitted: the lane SLUG is not the service name (antebellum-twitter's is disunion-feed), and a guessed flag exits 1.

<!-- hand:begin narrative -->
_(orchestrator narrative — this region survives regeneration)_
## Session close-out — 2026-09-13

_Appended at close on 2026-09-13 MT by session `efb98365-da6c-4bfa-ae69-7ce758586801`._

### The day, in one paragraph

Four more cited records were read against their sources and published — all four held up — and the
public page now shows the VALUE each audit checked, but only where the stored row can be proved to be
the row that was read. The published coverage figure fell from 9 to 7 and that fall is the most
trustworthy number the lane has printed: two rows had been chosen BECAUSE something looked wrong and
were sitting inside a figure that is supposed to mean "drawn by position". `A-45` was calibrated by
pin kind and then ruled a regression floor rather than a leading indicator. Six adversarial rounds ran
against today's own work and five returned a real defect — every one in code or notes written minutes
earlier.

### Load-bearing ships

- `f95006d` — **the user-visible ship**: slice 4 on the 1b page, four suspicion-drawn rows read against
  their sources, plus the dead upstream citation for [YKLBMWKCZGS2026] recorded as its own leg; every
  audited row now says how it was CHOSEN, and the two populations are counted apart.
- `2bd8292` — the checked value on the page, gated on recorded line + recorded text + recorded hash; a
  row that cannot prove identity renders as HISTORICAL with no live link, no support claim, no count.
- `80b9942` — `A-45` calibrated (population by pin KIND, not digit count) and ruled a FLOOR, with
  `A-47` coverage named as the successor leading read.
- `a717eba` — a self-test that asserted the page does not render "543", a figure retired on 2026-09-11:
  a guard over a dead value that could never have caught the live one. Now reads the live figure from
  the store.
- `953d570` — `A-48` back to ONE clock after `continuity-check` caught the second one I had just added.

### The one owed piece of work tomorrow

After the generated **First action** block above (`git pull --ff-only` … `npm run verify > tmp/verify-out.txt 2>&1`), this is the day's owed work:

**`A-48` surface 1 — "Small Ramsey Numbers", DOI `10.37236/21`.** Read its citation footprint from TWO
independent indices (Crossref, Semantic Scholar, OpenAlex — all three answered on 2026-09-11), score it
against `A-34`'s four criteria in order, write the result onto the row, then re-date `expectedSignalBy`
to 2026-09-15 for surface 2. The file this acts on is `continuity/items.json`, row `A-48`, field `notes`
for the scoring and `expectedSignalBy` for the clock. Its `readCommand` has NEVER been run, so run it
through the gate: `node scripts/sky.mjs check-due-gates-dispositioned.mjs --run A-48 --dry-run`, then
drop `--dry-run`.

### Watch items maturing tomorrow — DERIVED, pasted from the command

`node scripts/sky.mjs check-due-gates-dispositioned.mjs --print --today=2026-09-14` prints:

```
due-gates: 2 gate(s) due on/before 2026-09-14 (read-only; no snapshot written)
  2026-09-14  A-2      [nextCheckDate]  STANDING: drift-resolution log for the adopted surfaces (re-
  2026-09-14  A-48     [expectedSignalBy]  The second-area citation comparison David asked for, promise
unread: 1 of 2 due: never-run 1 · timed-out 0 · no-readCommand 0 · errored-stamp 0 · waived 0
RESULT: PASS (exit 0)
```

`A-2` owes nothing unless `node scripts/reverify.mjs --check` prints something other than `No drift.`

### Active in-flight work

**None.** No Codex job is running, no branch is held, and no deploy is pending. `gh pr list --repo
u00dxk2/bounds-ledger --state open` returned no rows.

### CI

**CI:** reverify.yml: completed/success @ `953d570` — `node scripts/sky.mjs check-ci-status.mjs
--workflow reverify.yml` → "1 completed non-scheduled success(es) for HEAD, 0 failures, 0 pending".
Pages: `39a7ede` built; the tip `953d570` is doc-and-ledger only.

### What NOT to touch tomorrow

- **The identity rule behind the displayed value.** It took four blocking review rounds to get right,
  and each round found the claim surviving in a sentence the previous fix had not touched. If an
  upstream edit shifts audited lines, the page will show HISTORICAL rows — that is the rule working,
  not a bug.
- **`depth-audit.mjs --corpus` and its four narrow checks.** A fifth check was added today and deleted
  within the hour: it asserted that two selection counts sum to the total, which is true by
  construction. What makes the other four work is that each names the single failure it catches.
- **The suspicion/systematic split.** It is why coverage reads 7 and not 9; folding them back together
  would restore a figure that flatters.

### Tier-0 binding constraint

`G-4` (an outside party acts on a watched record without us filing the report) reads **0 arrivals**,
measured: `npm run reports` fetched 29 issues and accounted for all 29 as ours. The interim yardstick
is `A-47` coverage — **7 of 673** cited rows drawn by position and read against their sources, with 6
more chosen for suspicion counted apart.

### Evangelism read

**The core problem, in a reader's words:** "I am about to cite a number and I do not know whether it
still holds." **Distance from the would-tell-a-friend moment:** closer in one specific way — a reader
can now see the NUMBER we checked, not just that we checked something, and can see whether it was
picked in order or picked because we suspected it. **Retention and word-of-mouth remain structurally
unmeasurable here:** the published pages fetch nothing by construction, and `npm run traffic` reads
repo views on github.com, never page readership — 1 unique viewer in the trailing 14 days. No
real-user advocacy signal today.

<!-- hand:end narrative -->

<!-- generated by gen-primer-first-action.mjs (skylark-site) @ commit accf3f567 · lane bounds-ledger · for 2026-09-14 · at 2026-09-13T18:08:04.802Z -->
<!-- writtenAtSha: `39a7ede496fc37b3abbe6689e03ab37a50f6b24d` — the lane's HEAD commit when this primer was written. Provenance anchor for check-primer-freshness leg ii; NOT a claim about HEAD now (the close-out commit that carries this file lands after generation). -->
<!-- reading this block: a generated line that is wrong in a way you can see FIXED in skylark-site's git log after the commit above is STALE, not a finding — regenerate before filing it. `git -C C:/dev/skylark/skylark-site log --oneline accf3f567^{commit}..HEAD -- scripts/gen-primer-first-action.mjs src/lib/cc-primer-commands.mjs src/lib/cc-due-gates.mjs src/lib/cc-report-artifact.mjs src/lib/cc-hand-edit-merge.mjs src/lib/cc-cold-readability.mjs src/lib/cc-skeleton-gloss.mjs` -->
