<!-- GENERATED — regenerating REPLACES everything outside the hand: markers, so put prose inside them. Provenance stamp at the foot of this block. -->

<!-- hand:begin banner -->
> **THIS BLOCK IS UNDER THE 2000-CHAR KICKOFF CAP ON PURPOSE — KEEP IT THERE.** On 09-03 and again
> on 09-04 the kickoff quoted it truncated and the cut fell in the MIDDLE, splitting `A-34` so its
> headline arrived and its instruction did not. Ordering cannot fix that: the composer keeps the
> head and the tail and cuts the middle, so length is the only lever. Test every line by "does a
> cold agent do something differently because it read this?" — everything else lives below.
>
> **`A-34` IS DUE TODAY AND HAS SLIPPED TWICE. It needs a session, not a spare hour.** Its trigger:
> the proposal goes to David as a board card with the ranking and the costs. `signalDateNote` calls
> the research a day's work. The row holds candidate shapes and criteria but NO per-candidate
> citation-footprint evidence — that gap is the work. If it is not done, give a date; do NOT send a
> thinner version, because a ranked list with no evidence is a brainstorm wearing a ranking.
>
> **`A-33` is also due today** — leg 1 shipped early on 09-04 (`npm run upstream`), so today decides
> close-with-leg-2-declined versus re-date. It is a decision, not a dig.
>
> **`npm run verify` WILL EXIT 3, and that is not yours to fix today.** The brief leg cannot pass:
> `/t/*` went Google-session-only on 09-04 and `cc_pin` was retired, so `x-cc-pin` signs nobody in.
> Every other leg passes. That is `A-41`, dated 09-08. Clearing condition: if it ever exits 0, the
> auth model changed and `A-41` has moved.
>
> **START LINE 4 OF THE FIRST ACTION BEFORE READING THIS DOCUMENT** — three minutes, all network,
> answer lands in a receipt keyed to a sha:
> ```
> npm run verify > tmp/verify-out.txt 2>&1
> ```
>
> **CODEX CANNOT WRITE UNDER `.git/`.** Brief it to run NO git command and prove mutations with
> `grep -c`. You stage and commit. Diff its output against the BRIEF, not the tree.
>
> **`git rev-parse --short HEAD origin/main` exits 128.** Two revisions plus `--short`. Drop it.
<!-- hand:end banner -->

First command (also below, in place):

```bash
node ../skylark-site/scripts/update-david-board.mjs --list --json --full-ids --project bounds-ledger
```

### Primer — bounds-ledger, 2026-09-05

What ONLY this primer holds comes first; the FULL runnable **First action** block
sits at the foot, below the reads. The single command above is the same block's
first line, repeated so it can be running while you read — it replaces nothing.

#### Owed gates — 2 due on/before 2026-09-05

_Ledger read at 2026-09-04T19:06:24.172Z by `cc-due-gates.collectDue` — the same resolver `check-due-gates-dispositioned` uses, so any difference is TIMING, not two answers. Rows filed or re-dated after that are NOT READ at generation — the close-out commit lands after this, so the COUNT above is stale by construction and this list cannot claim completeness. The list below, not the count, is the artifact; re-derive both before acting: `node ../skylark-site/scripts/check-due-gates-dispositioned.mjs --print --today=2026-09-05`._

Unfiltered as of the read time above — nothing was curated out of what the read could
see; a row absent here may simply have been filed after it ran.

- `A-33` (expectedSignalBy=2026-09-05, open) — A change to a file we have filed an upstream report against is a CANDIDATE CAUSAL EVENT, n
  - read: `gh issue list --repo teorth/optimizationproblems --author u00dxk2 --state all --limit 20 --json number,state,closedAt,title # SEPARATOR CONVERTED 2026-09-01 at pre-stage, em dash to hash, so the runner extracts the command rather than 900 bytes of prose. Literal output 2026-09-01, re-run at pre-stage: ONE row, 150 CLOSED 2026-08-23T17:15:35Z constants/87a.md: the current-record row quotes Martin's degree-8 discriminant, not the degree-12 construction that gives 857.5662. That single row IS the gap: it was true for three days and nothing surfaced it. POSITIVE CONTROL that the query can see our artifacts at all: it must return 150; an empty list means the author filter or the repo is wrong, not that we have never reported anything. NOTE THE TRAP THIS FIELD ALREADY AVOIDED: an equivalent gh pr list returns 141 from 2026-08-05 and is BLIND to 150, because our most recent outward artifact is an ISSUE; that miswrite was caught on W-8 the same day by running the command before writing it down. dead-ref-ok: the constants/87a.md token is inside a VERBATIM quotation of upstream issue 150's own title, so rewriting it to our mirrored path would make this field misquote the record it cites; our copy of that file is ledger/teorth-optimizationproblems/constants/87a.md and it exists (7502 bytes, checked 2026-09-02).`
- `A-34` (expectedSignalBy=2026-09-05, open) — David asked where else this ledger can have the kind of impact G-3 just produced — bring h
  - read: `node scripts/reverify.mjs --check # SEPARATOR CONVERTED 2026-09-01 at pre-stage, em dash to hash. The grep leg was DROPPED from the executed command and its result recorded here instead: grep -c 'ledger/' continuity/items.json returns 21 on 2026-09-01, and the count is prose-inflated rather than structural, so it never discriminated one surface from two. The command that answers this row is the check, whose first line names the mirrored surface and its file count. Literal output 2026-09-01 14:30Z: No drift. 113 files match upstream commit 3a1491040076a3f67a52719e366f5e6b139e5c24 (an upstream teorth/optimizationproblems commit; it does not resolve in this repo -- git cat-file -t on its short form reports Not a valid object name, while the same command on a local commit returns commit) -- ONE surface. A second adopted surface shows up as a second mirror directory under ledger/ and a second line here. POSITIVE CONTROL that the check reads a real mirror rather than reporting an empty set: the file count must be non-zero and match the manifest; 113 today.`

#### What shipped since 24 hours ago

_Read at 2026-09-04T19:06:24.172Z; commits landing after that — the close-out especially — are not here. Re-derive: `git log --oneline --since='24 hours ago'`._

- commit `f6192e2` — The one-pass audit found two more, and both of them are on the page
- commit `7e741c8` — A-33 leg 1: the alarm that has to be able to go quiet
- commit `356987f` — The reader holding the stale number was the one reader the search turned away
- commit `a962921` — A verdict reached the bus and never reached the row it was a verdict about
- commit `f5f54c0` — The cold review found the same defect one document upstream, and it had been reasoning it away
- commit `9710655` — Rotation 8 records what the live page actually served, not what the tree contained
- commit `3b08874` — The render that dates the row this ledger opened on, and A-40 closes
- commit `b2aa427` — One object, one address - and the constant this repo opened on is finally on the page

**Yesterday's report:** `docs/daily/2026-09-04-prelaunch.md` (coverage 2026-09-04, the prior day). No findings fence declared in it.

**Today's report (owed):** write `docs/daily/2026-09-05-prelaunch.md` — the artifact of record for service day 2026-09-05. Check it exists at close: `node ../skylark-site/scripts/check-daily-report-exists.mjs --date 2026-09-05` (it sweeps the fleet and applies each lane's coverageOffset; it takes no --project).

#### State — as of 2026-09-04T19:06:24.172Z (re-run the release command; never trust the value)

- **ledger**: 55 item(s): 36 closed · 19 open · as of 2026-09-04T19:06:24.172Z — release: `node ../skylark-site/scripts/gen-primer-first-action.mjs --repo . --target-date 2026-09-05 --json`

#### Id glossary — 21 id(s) this primer's day already has words for

_Each gloss is the ledger row's OWN title, not a re-derivation — paste it. Rows = every open ledger row plus every id named above. `(no ledger row)` means the id is named here and the ledger carries nothing under it: that is a finding, not a formatting gap._

- `A-2` — STANDING: drift-resolution log for the adopted surfaces…
- `A-7` — engineering-health P1s — four of five shipped 2026-08-09;…
- `A-9` — engineering-health P2 backlog (fix-on-touch, not scheduled…
- `A-20` — Should the fetch layer retry with backoff on 429/502? —…
- `A-29` — secret_scanning_validity_checks will not enable via API —…
- `A-31` — Should the public page publish be CI-gated? It is not, and…
- `A-33` — A change to a file we have filed an upstream report…
- `A-34` — David asked where else this ledger can have the kind of…
- `A-39` — A mid-run 429 becomes up to 339 requests in two seconds at…
- `A-40` — The table's cite block emits a non-canonical URL for a…
- `A-41` — The brief leg became permanently unverifiable on…
- `A-42` — Two user-facing readers still key off OUR pin history and…
- `G-2` — GOAL: contribute a verified bound improvement to the…
- `G-3` — GOAL (Tier-0 from G-1 close): someone outside Skylark uses…
- `G-4` — GOAL (Tier-0 from G-3 close): an outside party acts on a…
- `W-3` — Watch for acknowledgement of the erdosproblems.com/36…
- `W-4` — KP-78 applies to every new detector: demonstrate BOTH…
- `W-6` — Read window for the README report-an-error channel — the…
- `W-8` — Does a materially amended outward artifact go back to…
- `W-9` — Does this lane's detection method find what the lane…
- `W-10` — reverify.test.mjs alarm-title leg failed once and would…

### First action — bounds-ledger, 2026-09-05

Every line in the block below is a COMMAND, not a claim. A command re-reads at the
moment you run it and cannot go stale; a state line written last night can.

```bash
node ../skylark-site/scripts/update-david-board.mjs --list --json --full-ids --project bounds-ledger
git rev-parse HEAD origin/main
git rev-list --count origin/main...HEAD
npm run verify > tmp/verify-out.txt 2>&1
node ../skylark-site/scripts/check-ci-status.mjs --workflow reverify.yml
```

Morning release commands — deliberately NOT READ at generation (a value written
last night would already be stale); run each, never trust a remembered one:
- `git -C . log -1 --format=%h%x20%s` — HEAD
- `git -C . status --porcelain` — working tree
- `node ../skylark-site/scripts/check-ci-status.mjs --workflow reverify.yml` — CI
- **deploy read: NOT AVAILABLE** — no read-only deploy-state script in this lane's package.json (looked for ops:deploy-state, deploy-state, check-deploy, render-deploys, deploy:status, daily-snapshot). The fleet's check-deploy-state.mjs is not a substitute — it requires --fix <sha> --deploy <sha> and exits 1 without them

<!-- hand:begin narrative -->
_(orchestrator narrative — this region survives regeneration)_
<!-- hand:end narrative -->

<!-- generated by gen-primer-first-action.mjs (skylark-site) @ commit b92fd0b1 · lane bounds-ledger · for 2026-09-05 · at 2026-09-04T19:06:24.172Z -->
<!-- writtenAtSha: `f6192e274215d11a821d4bfa8faa440a616d0b2c` — the lane's HEAD commit when this primer was written. Provenance anchor for check-primer-freshness leg ii; NOT a claim about HEAD now (the close-out commit that carries this file lands after generation). -->
<!-- reading this block: a generated line that is wrong in a way you can see FIXED in skylark-site's git log after the commit above is STALE, not a finding — regenerate before filing it. `git -C ../skylark-site log --oneline b92fd0b1..HEAD -- scripts/gen-primer-first-action.mjs src/lib/cc-primer-commands.mjs src/lib/cc-due-gates.mjs src/lib/cc-report-artifact.mjs src/lib/cc-hand-edit-merge.mjs src/lib/cc-cold-readability.mjs src/lib/cc-skeleton-gloss.mjs` -->

<!-- hand:begin closeout -->
### Close-out append — 2026-09-04 20:45 MT

Added after the P5 sections above; nothing above was overwritten.

**The stuck-ask read found one row and it is `G-4`.** Its 2026-09-26 gate is now known-uninterpretable
in advance and the row says so itself (`readDateKnownUninterpretable2026_09_04`): the threshold is 30
unique viewers, the read on 2026-09-04 was 2 in the trailing 14 days, and the denominator reaches
roughly 8-10 by that date. **This was NOT re-dated and NOT re-pointed** — both are working-model
changes and CLAUDE.md reserves them for David. It is routed into `A-34`, which is due TODAY.

**`W-8` was nearly filed as mistakenly dated and is not.** Its `closeWhen` already carries BRANCH 0:
if no outward send has occurred by 2026-09-22 the discriminator is INAPPLICABLE, re-date once and
record the empty window — because its trigger is an EVENT, not a date. Reading the row rather than a
summary of it is what caught that; the other six wait rows are REAL and owed no line.

**Uncarded decisions: zero for this lane.** `check-unpromoted-decisions` exits 3 fleet-wide on one
row that belongs to animoment, not here.
<!-- hand:end closeout -->
