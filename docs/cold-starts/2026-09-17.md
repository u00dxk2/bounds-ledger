<!-- GENERATED — regenerating REPLACES everything outside the hand: markers, so put prose inside them. Provenance stamp at the foot of this block. -->

<!-- hand:begin banner -->
> **PULL, THEN START THE GATE, BEFORE READING THIS** — three minutes, all network:
> ```
> git pull --ff-only
> npm run verify > tmp/verify-out.txt 2>&1
> ```
> Expect **exit 0**. Any non-zero code is yours; read `tmp/.verify-receipt.json`'s `failedGates`,
> never infer the leg. `check:brief` reading UNVERIFIABLE is the permanent state on `A-41`, not a failure.
>
> **BOTH GATES DUE TODAY HAVE NEVER BEEN READ — NOT ONCE SINCE THE ROWS WERE CREATED ON 2026-07-29.**
> `A-7` and `A-9` are the engineering-health rows, and `check-due-gates-dispositioned --print --today=2026-09-17`
> reported `unread: 2 of 2 due: never-run 2` at 17:43Z yesterday. A gate that never executed looks exactly
> like one that passes. Run each through the gate runner so the read is stamped:
> `node scripts/sky.mjs check-due-gates-dispositioned.mjs --run A-7` (then `--run A-9`).
> **`A-7`'s read carries its own trap, written on the row: the grep pattern matches the self-test that
> already ships, so JUDGE THE HITS, never the hit count.**
>
> **`A-51` IS THE ONLY OUTWARD CANDIDATE THIS LANE HOLDS, and it is dated tomorrow, 2026-09-18.** It is
> upstream's `[YKLBMWKCZGS2026]` reference citing a page that returns GitHub Pages' "Site not found" as
> HTTP 200. Re-read whether it is still dead before anything else — `curl -sIL https://test-time-training.github.io/discover.pdf`,
> and read the Content-Type, not the status line, because the dead page answers 200. `A-43`'s rounding
> correction CLOSED refuted on 2026-09-16; do not reopen that research leg.
>
> **`A-47` SLICE 6 POSITIONS ARE ALREADY FIXED: 125, 175, 225, 275, 325.** They are the untaken 25-offset
> steps below 350. Draw them with `node scripts/depth-audit.mjs --draw 125 175 225 275 325` BEFORE reading
> any row — the whole point of the ladder is that the draw precedes the reading.
>
> **THE PAGE AND THE STORE COUNT DIFFERENT POPULATIONS, AND THAT IS NOT A BUG.** The public page says 10
> rows drawn by position; `depth-audit.mjs` says 24 audited, 15 systematic. The page counts only rows whose
> source was actually READ and dropped the 6 unreachable. On 2026-09-16 that difference hid a real defect:
> an entry with a non-http `source` was silently dropped from the page while the store still counted it.
> `A-53` holds that gap, dated 2026-09-24. **Do not "fix" it by making the numbers match.**
>
> **DO NOT TOUCH `usableAudit` / `isBoundRead` in `scripts/render-constant-pages.mjs`.** They are the one
> shared definition of a row read against its source, and the reason the index badge and the constant page
> cannot disagree. Yesterday's defect was not in them.
>
> **ONE CARD IS OPEN ON DAVID'S BOARD: `ba297b25`** — whether to adopt a second area to watch, recommendation
> NOT YET with a one-step trial first. Nothing stalls while it sits. Read it live, never off this page:
> `node scripts/sky.mjs answered-cards.mjs --project bounds-ledger --full`.
>
> **IF ALL THREE WAKER RUNGS DIE AT ONCE, IT IS THE MACHINE, NOT YOU.** That happened on 2026-09-15 and
> again on 2026-09-16 (~17:25Z) — low system memory, fleet-wide. Relaunch all three and check each prints
> a fresh `armed` line; a rung whose predecessor is alive refuses with `already live (pid N)` instead.
<!-- hand:end banner -->

First command (also below, in place):

```bash
git pull --ff-only
# commits after this primer was written are invisible to it — read them before acting:
git log --oneline 68e97645a60ac0478c3e1f21a280faae3ea54617..HEAD
```

### Primer — bounds-ledger, 2026-09-17

What ONLY this primer holds comes first; the FULL runnable **First action** block
sits at the foot, below the reads. The single command above is the same block's
first line, repeated so it can be running while you read — it replaces nothing.

_**Composed against HEAD commit `68e97645a60ac0478c3e1f21a280faae3ea54617` at 2026-09-16T17:45:44.123Z.** Nothing that landed after that instant is in this file. David's board answers especially: an answer he gives later cannot be here, and this primer will go on reading as though the work were not owed — that is exactly how two lanes concluded an OTA was not owed on 2026-09-04. Read them live, never off this page: `node scripts/sky.mjs answered-cards.mjs --project bounds-ledger --full`._

_Shared skylark-site scripts below are spelled `node scripts/sky.mjs …` — this lane vendors `scripts/sky.mjs` (S-20260906-75), so a shared script is named LANE-RELATIVE and resolves from a worktree too._

#### Owed gates — 2 due on/before 2026-09-17

_Ledger read at 2026-09-16T17:45:44.123Z by `cc-due-gates.collectDue` — the same resolver `check-due-gates-dispositioned` uses, so any difference is TIMING, not two answers. Rows filed or re-dated after that are NOT READ at generation — the close-out commit lands after this, so the COUNT above is stale by construction and this list cannot claim completeness. The list below, not the count, is the artifact; re-derive both before acting: `node scripts/sky.mjs check-due-gates-dispositioned.mjs --print --today=2026-09-17`._

Unfiltered as of the read time above — nothing was curated out of what the read could
see; a row absent here may simply have been filed after it ran.

- `A-7` (expectedSignalBy=2026-09-17, open) — engineering-health P1s — four of five shipped 2026-08-09; OPEN on R7 alone (scheduled veri
  - read: `grep -rn 'history-sweep' .github/workflows/ # R7 (a SCHEDULED verified full-history sweep) is the only open leg. Literal output 2026-08-26: THREE hits, and all three are the --selftest step plus a comment ABOUT R7; no scheduled sweep exists, so R7 is unshipped. READING THE HIT COUNT IS THE TRAP here — the pattern matches the self-test that already ships, so judge the hits, never the number. POSITIVE CONTROL that the glob reads workflow files at all: grep -c 'schedule' .github/workflows/reverify.yml returns 3.` · last run: NEVER
- `A-9` (nextCheckDate=2026-09-17, open) — engineering-health P2 backlog (fix-on-touch, not scheduled work)
  - read: `git log --oneline --since='30 days ago' -- docs/engineering-health-review-2026-07-29.md # this row is a fix-on-touch BACKLOG, not scheduled work, so there is no state to poll and age is not decay. The only honest machine read is whether the review doc has been touched. POSITIVE CONTROL: the file gained a 'Superseded fact' note on 2026-08-26 (the Auto-deploy-on-push row went stale when Pages was turned on), so a 30-day window MUST return at least that commit; an empty result means the path is wrong, not that nothing happened.` · last run: NEVER

#### What shipped since 24 hours ago

_Read at 2026-09-16T17:45:44.123Z; commits landing after that — the close-out especially — are not here. Re-derive: `git log --oneline --since='24 hours ago'`._

- commit `68e9764` — P4: the store-vs-page gap gets a row, A-47 re-dated after its read, and today's gate stamp
- commit `55297f7` — A-43 closes: the rounding correction is refuted, written down, and not sent
- commit `739ce16` — A-47 slice 5: five more records read against their sources, and the first one this lane could recompute
- commit `271cfe0` — A-48 closes: the second-surface comparison reached David on the promised date
- commit `e7fbcd2` — A-48: the second-surface comparison, both surfaces scored, recommending a one-step trial rather than adoption
- commit `c5a964b` — P2: four promises that lived only in bus posts get ledger rows, and repo viewers stop reading as page visitors
- commit `5f3beb1` — continuity: A-47 read output from the close-out gate run
- commit `829e505` — docs(close-session): close-out 2026-09-15 — tomorrow's owed gates read PASS, and the close appended to the primer
- commit `8153032` — Report 2026-09-15: FIRST ACTION names the file, not the gate, for the first time in seven days
- commit `bd61fc9` — Hygiene pass 2026-09-15 — the day we refuted our own correction, written down
- commit `2af6082` — W-11: the record held the refutation and the analysis did not read it
- commit `9c23621` — The row this ledger opened on is on the page, and the case against upstream is weaker than we wrote

**Yesterday's report:** `docs/daily/2026-09-16-prelaunch.md` (coverage 2026-09-16, the prior day). No findings fence declared in it.

**Today's report (owed):** write `docs/daily/2026-09-17-prelaunch.md` — the artifact of record for service day 2026-09-17. Check it exists at close: `node scripts/sky.mjs check-daily-report-exists.mjs --date 2026-09-17` (it sweeps the fleet and applies each lane's coverageOffset; it takes no --project).

#### State — as of 2026-09-16T17:45:44.123Z (re-run the release command; never trust the value)

- **ledger**: 67 item(s): 46 closed · 21 open · as of 2026-09-16T17:45:44.123Z — release: `node scripts/sky.mjs gen-primer-first-action.mjs --repo . --target-date 2026-09-17 --json`

#### Id glossary — 24 id(s) this primer's day already has words for

_Each gloss is the ledger row's OWN title, not a re-derivation — paste it. Rows = every open ledger row plus every id named above. `(no ledger row)` means the id is named here and the ledger carries nothing under it: that is a finding, not a formatting gap._

- `A-2` — STANDING: drift-resolution log for the adopted surfaces…
- `A-7` — engineering-health P1s — four of five shipped 2026-08-09;…
- `A-9` — engineering-health P2 backlog (fix-on-touch, not scheduled…
- `A-33` — A change to a file we have filed an upstream report…
- `A-43` — Upstream moved Haugland's minimum-overlap bound to its…
- `A-45` — TIER-1 LEADING INDICATOR (orchestrator-approved…
- `A-46` — A close-out post announcing a FIX is textually identical…
- `A-47` — THE DEPTH AUDIT, David-ruled 2026-09-09: read our own…
- `A-48` — The second-area citation comparison David asked for,…
- `A-49` — npm run close:primer passes when tomorrow's banner is…
- `A-50` — Importing one of our CLI scripts can run it — one selftest…
- `A-51` — Upstream's [YKLBMWKCZGS2026] reference in the…
- `A-52` — Write down what an outside party acting on a record could…
- `A-53` — The audit store and the public page can disagree about how…
- `G-2` — GOAL: contribute a verified bound improvement to the…
- `G-4` — GOAL (Tier-0 from G-3 close): an outside party acts on a…
- `S-20260906-75` — (no ledger row)
- `W-3` — Watch for acknowledgement of the erdosproblems.com/36…
- `W-4` — KP-78 applies to every new detector: demonstrate BOTH…
- `W-6` — Read window for the README report-an-error channel — the…
- `W-8` — Does a materially amended outward artifact go back to…
- `W-9` — Does this lane's detection method find what the lane…
- `W-10` — reverify.test.mjs alarm-title leg failed once and would…
- `W-11` — Before asserting a claim is decision-ready, search this…

### First action — bounds-ledger, 2026-09-17

Every line in the block below is a COMMAND, not a claim. A command re-reads at the
moment you run it and cannot go stale; a state line written last night can.

```bash
git pull --ff-only
node scripts/sky.mjs update-david-board.mjs --list --json --full-ids --project bounds-ledger
git rev-parse HEAD origin/main
git rev-list --count origin/main...HEAD
npm run verify > tmp/verify-out.txt 2>&1
node scripts/sky.mjs check-ci-status.mjs --workflow reverify.yml
```

Morning release commands — deliberately NOT READ at generation (a value written
last night would already be stale); run each, never trust a remembered one:
- `git -C . log -1 --format=%h%x20%s` — HEAD
- `git -C . status --porcelain` — working tree
- `node scripts/sky.mjs check-ci-status.mjs --workflow reverify.yml` — CI
- `node scripts/sky.mjs check-deployed-sha-drift.mjs` — deploy
  - WHERE IT LOOKED: no read-only deploy-state script in this lane's package.json (looked for ops:deploy-state, deploy-state, check-deploy, render-deploys, deploy:status, daily-snapshot). The fleet's check-deploy-state.mjs is not a substitute — it requires --fix <sha> --deploy <sha> and exits 1 without them — so this is the FLEET read, which takes no required flags. service: NOT DECLARED — no row for "bounds-ledger" in skylark-site/src/data/cc-deploy-gates.json (generatedAt 2026-09-11T05:03:44.019Z, 17 repo row(s)) — nothing in this repo declares that lane's Render service. No `--service` flag is emitted: the lane SLUG is not the service name (antebellum-twitter's is disunion-feed), and a guessed flag exits 1.

<!-- hand:begin narrative -->
## Session close-out — 2026-09-16

**What yesterday was.** The day this lane checked a record by redoing the mathematics instead of reading
someone's word for it, and the record held. The account is `docs/daily/2026-09-16-prelaunch.md`; the
one-line version is that the depth audit's method now has a demonstrated second mode — recomputation —
and it found nothing wrong on its first use.

### Load-bearing commits
- `c5a964b` — four promises that lived only in bus posts became rows (`A-49`–`A-52`), and the traffic
  sampler stopped calling repository viewers page visitors.
- `e7fbcd2` / `271cfe0` — the second-surface comparison delivered to David and the row closed. Card
  `ba297b25`. Recommendation: not yet, run a one-step trial first.
- `739ce16` — **the day's user-visible ship**: depth-audit slice 5 live on the public constant pages,
  including the recomputed `10c` certificate.
- `55297f7` — `A-43` closed on its own refutation, with `A-51` minted first so the close could not carry
  the dead-link correction along.
- `68e9764` — `A-53` filed for the store-vs-page gap; `A-47` re-dated to 2026-09-18 after its read.

### What NOT to touch
- **`usableAudit` / `isBoundRead`** in `scripts/render-constant-pages.mjs` — the single shared definition
  of "read against its source". Repeated in the banner because it is the thing most likely to be
  "improved" by someone reconciling two counts that are not supposed to match.
- **The `sourceRead` text on `A-47-0019`** (the H2016 row, verdict UNRESOLVED). Unchanged from the
  2026-09-15 close and still the one sentence on the public page explaining how a row can be read against
  its source and be neither supported nor refuted.

### Three defects found yesterday, all instrument-facing
1. The traffic sampler's headline lines called repository viewers page visitors; the report repeated it.
   Fixed in `c5a964b`, and five sentences in two standing docs were corrected with it.
2. An audit entry was counted by the store and silently dropped from the page (non-http `source`). Found
   by counting the store against the rendered sentence by hand. `A-53` holds the unguarded gap.
3. An audit entry credited a reference it had not read, so the page contradicted itself in one sentence.
   **Found by the adversarial review, not by me** — which is the argument for running them.

### Two guards refused me, both correctly
A padded 40-character sha (`check-ci-status`, nothing queried) and a content hash pasted into a ledger
note (the pre-commit sha check). Both would have put a fabricated-looking token into the record.

### One instruction that did not apply
The close asked about an **open-redirect finding from the morning fleet sweep**. This lane never received
one: the only mentions in 426 delivered bus rows are a 2026-09-14 broadcast about *frame-dial's* finding
and the close instruction itself, and this lane publishes a static Pages site with no server routes and no
redirect sink (`*.html`, `c/*.html`, `scripts/*.mjs`, workflows: 8 hits, none a redirect). Said back to the
orchestrator rather than silently dropped.

### Gate 2
`gate 2: NOT APPLICABLE — this lane has no doc-only CI fast path.` Every push to `main` runs the full
`reverify.yml`; there is no path filter and no fast-path workflow to prove a green on.
<!-- hand:end narrative -->

<!-- generated by gen-primer-first-action.mjs (skylark-site) @ commit accf3f567 · lane bounds-ledger · for 2026-09-17 · at 2026-09-16T17:45:44.123Z -->
<!-- writtenAtSha: `68e97645a60ac0478c3e1f21a280faae3ea54617` — the lane's HEAD commit when this primer was written. Provenance anchor for check-primer-freshness leg ii; NOT a claim about HEAD now (the close-out commit that carries this file lands after generation). -->
<!-- reading this block: a generated line that is wrong in a way you can see FIXED in skylark-site's git log after the commit above is STALE, not a finding — regenerate before filing it. `git -C C:/dev/skylark/skylark-site log --oneline accf3f567^{commit}..HEAD -- scripts/gen-primer-first-action.mjs src/lib/cc-primer-commands.mjs src/lib/cc-due-gates.mjs src/lib/cc-report-artifact.mjs src/lib/cc-hand-edit-merge.mjs src/lib/cc-cold-readability.mjs src/lib/cc-skeleton-gloss.mjs` -->
