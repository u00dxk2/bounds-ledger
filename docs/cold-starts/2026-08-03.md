# Cold-start primer for 2026-08-03 (MT)

Multi-session primer — each /close-session run appends a new section below.

## Session close-out — 20:50Z (2026-08-02, ~14:50 MT)

You are the bounds-ledger agent. Read this, then `CLAUDE.md`, then `continuity/items.json`. Steward
of drifting mathematical records; pre-launch, private repo, **PACED rail**, **no self-rating**.

Yesterday was the lane's busiest day: 8 commits, a real drift, two greenlit ships, one David
decision, and two instrument defects caught — one of them mine.

### State

**229 claims / 227 hold / 0 broken / 2 UNVERIFIED (by design)** over a **111-file** mirror, clean at
upstream **`dee1660`**. **G-1 green-streak: day 10 of 30** (day 1 = 7/24; a green 8/03 run makes it
11; ends ~8/22). North star (externally-acknowledged corrections): **still 0**.

**CI:** `reverify: completed/success @ 4e8ad8e` — green at HEAD, tree clean, everything pushed.

### Today's load-bearing ships (8 commits, all pushed)

- **`da17be3`** — fourth drift resolved `621429c → dee1660`, and the **first that is purely records
  moving**. 10c $5/3 \to 7/\sqrt{17}$, 38a $2.625622 \to 2.6273856$, 71a $>6.5143 \to >6.5218$.
  Verified by recomputation before snapshot (71a's claimed "gain of exactly 1/133" reproduces to
  double precision). 3 pins moved, issue #9 closed.
- **`44e313f`** — A-13 **no-secrets history sweep: CLEAN** (75 commits, 13 patterns) + A-12 re-scoped
  to the machine credential. Flip package now **3 of 4**.
- **`6c9d059`** / **`ce93464`** — the brief checker was blaming the orchestrator for a login wall;
  fixed, then the fix's own CI side-effect fixed.
- **`183686e`** — David declined the public /36 comment. **`4e8ad8e`** — R6 push protection
  deliberately not carded.

### Tomorrow's first-action

**Steward cadence first**, as always: read the overnight run's **LOG, not the badge**
(`gh run list --limit 3`, `gh run view <id> --log`). Expect `No drift. 111 files match upstream
dee1660…`, `229 claim(s): 227 hold, 0 broken/unreachable, 2 unverified (manual)`, two expected
`403` advisory lines (C-7/C-9), and the brief job logging *"not runnable from CI … Not filing an
issue"*. Green → streak day 11. Then `npm run check` locally **with `CC_PROMPTS_PIN` set** (see
gotcha below). If red: verify → `--snapshot` → `extract-pins.mjs` → commit, in that order.

**Then the launch increment — and it should be README-as-product.** This is the paced rail's quota
and it is now the critical path: the flip package is 3 of 4 (SECURITY.md ✅, history sweep ✅,
adversarial review of the correction ✅ — but the *package-level* review is still owed), and with the
public comment declined the flip is the only launch-shaped work left. Write it for a stranger's
first read: what this is, what the alarm has actually caught (four drifts, five instrument defects),
live counts, how to run it. Then the package-level adversarial review, then it goes to David.

Do **not** re-run the history sweep as "the increment" — it is done. Do re-run it immediately before
the flip click; the claim is only true as of the commit it ran against.

### Watch items maturing

- **A-12** — hosted brief. Escalation **re-armed on true evidence**: 3 of 4 blocks genuinely missing
  (26 Jul, 28 Jul, 1 Aug), confirmed via the credentialed probe. **If still behind after 2026-08-07,
  escalate to David naming those three dates.** Issue #6 is the standing alarm; do not re-post daily.
- **W-5 (new)** — README.md is not mirrored, and it is where upstream states which records it stands
  behind (`dee1660` asterisked two as not-yet-peer-reviewed and our alarm was silent). Read its
  `onTrigger` before building anything: the right shape is probably A-6's (pin specific cells), not a
  byte mirror of a generated 421KB file that would go permanently red.
- **W-3** — monitor only. Both page legs read clean today (page unchanged since 23 Jan).
- **A-7 R6** — re-arm the spend question only if the flip is declined or unresolved by ~2026-08-16.

### What NOT to touch

- **Do not re-propose the public /36 comment.** David decided on 8/02 that having sent the email, he did not want a comment added on top of it. Not wording, not venue, not timing — he does not want a second contact
  on the same correction. `docs/decisions/2026-08-02-public-comment-erdosproblems-36.md` opens with
  this.
- **Do not re-automate C-7/C-9** — the erdosproblems 403 is IP-dependent; a local 200 proves nothing
  about CI.
- **Do not put `CC_PROMPTS_PIN` in repo secrets** to "fix" the CI brief check. This repo is heading
  for a public flip, and a local run already covers it.
- **Do not treat A-13's first-PR targets as live** — #129/#130/#135 all merged 8/02 (A-13 note5).
  New targets needed.
- **Generated pins stay listing-position pins.** Curated vs live: never blend. UNVERIFIED stays at 2.

### The one gotcha that will bite you

`check-brief.mjs` **needs `CC_PROMPTS_PIN` in env.** `/t/lanes/*` is behind a sign-in wall and **the
sign-in page returns HTTP 200** — so an anonymous run looks successful and reports every block
missing. That is exactly how the checker spent yesterday morning accusing the orchestrator of a
stale port for a page it had never received. An anonymous local run proves nothing either way.

### Evangelism / binding-constraint read

This lane has no users and no funnel; the honest analogue is **whether anyone outside relies on the
ledger**. Core problem, in a user's words: *"the number I'm citing may have moved and nobody told
me."* The would-tell-a-friend moment is a drift alarm that catches a real record change before the
citing surface updates — **that moment now demonstrably exists** (four drifts, one of them a genuine
record improvement). Distance to it mattering: nobody outside can see it. The repo is private, the
brief is gated, and the north star (externally-acknowledged corrections) is **0**.

**No retention instrument, and standing one up is not the move — the flip is.** With the public
comment declined, the north star rests on: (a) the 7/24 email, unanswered, not ours to force; (b) a
correction-shaped PR to `teorth/optimizationproblems` (channel selected, targets gone, new ones
needed); (c) A-6's scale-the-catches pipeline (permission-starved, inventoried, not started).
**Making the repo public is what converts a private instrument into something a stranger can rely
on** — which is why README-as-product is the right next increment rather than more correction work.

### Tier 0 / binding constraint

Green streak day 10/30, ledger clean at `dee1660`. Posture: **suspect the instrument before the
record — and suspect its diagnosis too.** Yesterday added the variant: an alarm can be confidently
wrong about the *cause*, not just the answer, and a number moving in the expected direction for an
unexpected reason is worse than no number.
