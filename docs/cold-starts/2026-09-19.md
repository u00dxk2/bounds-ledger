<!-- GENERATED — regenerating REPLACES everything outside the hand: markers, so put prose inside them. Provenance stamp at the foot of this block. -->

<!-- hand:begin banner -->
2026-09-18 close. **`A-33` is the only dated row due tomorrow** — the row that says a change to a file we have filed an upstream report against is a candidate causal event, not a text edit. Leg 1 shipped 2026-09-04; leg 2 (the map that makes a movement in a reported-against file readable as such) is the open half. Decide it or ship it; do not re-date it silently, and read note4 first — it must fire on the TRANSITION, because a check that fires every day once issue #150 is closed is this lane's founding defect in a new hat. Clearing condition for this paragraph: once `A-33` is shipped, closed or re-dated with a written reason, delete it.

> **Do NOT promote R(5, 13) ≤ 1138 or R(8, 14) ≤ 41525 to SOUND on the arithmetic.** Both reproduce exactly from the survey's own smaller bounds, and both are UNRESOLVED because the paper they are credited to could never be opened and the value's own source is a personal communication. The orchestrator's P4 feedback names this explicitly: do not let a future round quietly promote them. What WOULD settle either: a readable copy of Discrete Mathematics 307 (2007) 760-763, which is in Elsevier's open archive — an ordinary browser session may reach what an automated fetch could not. Clearing condition: once a human reads that paper, or the rows are re-verdicted with a reason, delete this paragraph.

> **Waker ladder — relaunch ranks 1, 2 and 3 at session start.** The commands are listed in `/listen` step 3b, one per rank; run them as separate background calls, never joined. A rung whose predecessor is alive refuses with `already live (pid N)` rather than arming a duplicate, so relaunching ranks 1, 2 and 3 is always safe. A context compact does not re-run `/listen`, which is how this pane lost its ladder twice on 2026-09-17.
<!-- hand:end banner -->

First command (also below, in place):

```bash
git pull --ff-only
# commits after this primer was written are invisible to it — read them before acting:
git log --oneline db57062766148e80049407ac9466cfb815c2cb98..HEAD
```

### Primer — bounds-ledger, 2026-09-19

What ONLY this primer holds comes first; the FULL runnable **First action** block
sits at the foot, below the reads. The single command above is the same block's
first line, repeated so it can be running while you read — it replaces nothing.

_**Composed against HEAD commit `db57062766148e80049407ac9466cfb815c2cb98` at 2026-09-18T19:35:40.337Z.** Nothing that landed after that instant is in this file. David's board answers especially: an answer he gives later cannot be here, and this primer will go on reading as though the work were not owed — that is exactly how two lanes concluded an OTA was not owed on 2026-09-04. Read them live, never off this page: `node scripts/sky.mjs answered-cards.mjs --project bounds-ledger --full`._

_Shared skylark-site scripts below are spelled `node scripts/sky.mjs …` — this lane vendors `scripts/sky.mjs` (S-20260906-75), so a shared script is named LANE-RELATIVE and resolves from a worktree too._

#### Owed gates — 1 due on/before 2026-09-19

_Ledger read at 2026-09-18T19:35:40.337Z by `cc-due-gates.collectDue` — the same resolver `check-due-gates-dispositioned` uses, so any difference is TIMING, not two answers. Rows filed or re-dated after that are NOT READ at generation — the close-out commit lands after this, so the COUNT above is stale by construction and this list cannot claim completeness. The list below, not the count, is the artifact; re-derive both before acting: `node scripts/sky.mjs check-due-gates-dispositioned.mjs --print --today=2026-09-19`._

Unfiltered as of the read time above — nothing was curated out of what the read could
see; a row absent here may simply have been filed after it ran.

- `A-33` (expectedSignalBy=2026-09-19, open) — A change to a file we have filed an upstream report against is a CANDIDATE CAUSAL EVENT, n
  - read: `gh issue list --repo teorth/optimizationproblems --author u00dxk2 --state all --limit 20 --json number,state,closedAt,title # SEPARATOR CONVERTED 2026-09-01 at pre-stage, em dash to hash, so the runner extracts the command rather than 900 bytes of prose. Literal output 2026-09-01, re-run at pre-stage: ONE row, 150 CLOSED 2026-08-23T17:15:35Z constants/87a.md: the current-record row quotes Martin's degree-8 discriminant, not the degree-12 construction that gives 857.5662. That single row IS the gap: it was true for three days and nothing surfaced it. POSITIVE CONTROL that the query can see our artifacts at all: it must return 150; an empty list means the author filter or the repo is wrong, not that we have never reported anything. NOTE THE TRAP THIS FIELD ALREADY AVOIDED: an equivalent gh pr list returns 141 from 2026-08-05 and is BLIND to 150, because our most recent outward artifact is an ISSUE; that miswrite was caught on W-8 the same day by running the command before writing it down. dead-ref-ok: the constants/87a.md token is inside a VERBATIM quotation of upstream issue 150's own title, so rewriting it to our mirrored path would make this field misquote the record it cites; our copy of that file is ledger/teorth-optimizationproblems/constants/87a.md and it exists (7502 bytes, checked 2026-09-02).` · last run: 2026-09-12T19:04:47.204Z

#### What shipped since 24 hours ago

_Read at 2026-09-18T19:35:40.337Z; commits landing after that — the close-out especially — are not here. Re-derive: `git log --oneline --since='24 hours ago'`._

- commit `fc0bbba` — A-54 phase 4: the first five Small Ramsey Numbers bounds read against their sources, on the page
- commit `f1c5e85` — A-54 phase 4: the draw for the first Small Ramsey Numbers depth slice, committed before any read
- commit `70ebd63` — P2 2026-09-18: A-7 closes on 533 of 533, A-51 is two files and goes to a draft, A-47's slice 6 is drawn first
- commit `5ebdb54` — Primer 2026-09-18: David's standing answer on shipping into silence, carried into tomorrow's pick
- commit `a7c7fc5` — Report 2026-09-17: the Gates line names the receipt taken AFTER the report's own commit
- commit `b5a73b7` — Hygiene 2026-09-17: the day's report, tomorrow's primer, and the blind spot the new area created

**Yesterday's report:** `docs/daily/2026-09-18-prelaunch.md` (coverage 2026-09-18, the prior day). No findings fence declared in it.

**Today's report (owed):** write `docs/daily/2026-09-19-prelaunch.md` — the artifact of record for service day 2026-09-19. Check it exists at close: `node scripts/sky.mjs check-daily-report-exists.mjs --date 2026-09-19` (it sweeps the fleet and applies each lane's coverageOffset; it takes no --project).

#### State — as of 2026-09-18T19:35:40.337Z (re-run the release command; never trust the value)

- **ledger**: 68 item(s): 47 closed · 21 open · as of 2026-09-18T19:35:40.337Z — release: `node scripts/sky.mjs gen-primer-first-action.mjs --repo . --target-date 2026-09-19 --json`

#### Id glossary — 23 id(s) this primer's day already has words for

_Each gloss is the ledger row's OWN title, not a re-derivation — paste it. Rows = every open ledger row plus every id named above. `(no ledger row)` means the id is named here and the ledger carries nothing under it: that is a finding, not a formatting gap._

- `A-2` — STANDING: drift-resolution log for the adopted surfaces…
- `A-7` — engineering-health P1s — four of five shipped 2026-08-09;…
- `A-9` — engineering-health P2 backlog (fix-on-touch, not scheduled…
- `A-33` — A change to a file we have filed an upstream report…
- `A-45` — TIER-1 LEADING INDICATOR (orchestrator-approved…
- `A-46` — A close-out post announcing a FIX is textually identical…
- `A-47` — THE DEPTH AUDIT, David-ruled 2026-09-09: read our own…
- `A-49` — npm run close:primer passes when tomorrow's banner is…
- `A-50` — Importing one of our CLI scripts can run it — one selftest…
- `A-51` — Upstream's [YKLBMWKCZGS2026] reference in the…
- `A-52` — Write down what an outside party acting on a record could…
- `A-53` — The audit store and the public page can disagree about how…
- `A-54` — Adopt Small Ramsey Numbers (Electronic Journal of…
- `G-2` — GOAL: contribute a verified bound improvement to the…
- `G-4` — GOAL (Tier-0 from G-3 close): an outside party acts on a…
- `S-20260906-75` — (no ledger row)
- `W-3` — Watch for acknowledgement of the erdosproblems.com/36…
- `W-4` — KP-78 applies to every new detector: demonstrate BOTH…
- `W-6` — Read window for the README report-an-error channel — the…
- `W-8` — Does a materially amended outward artifact go back to…
- `W-9` — Does this lane's detection method find what the lane…
- `W-10` — reverify.test.mjs alarm-title leg failed once and would…
- `W-11` — Before asserting a claim is decision-ready, search this…

### First action — bounds-ledger, 2026-09-19

Every line in the block below is a COMMAND, not a claim. A command re-reads at the
moment you run it and cannot go stale; a state line written last night can.

```bash
git pull --ff-only
node scripts/sky.mjs update-david-board.mjs --list --json --full-ids --project bounds-ledger
git rev-parse HEAD origin/main
git rev-list --count origin/main...HEAD
npm run verify > tmp/verify-out.txt 2>&1
node scripts/sky.mjs check-ci-status.mjs --workflow reverify.yml
```

Morning release commands — deliberately NOT READ at generation (a value written
last night would already be stale); run each, never trust a remembered one:
- `git -C . log -1 --format=%h%x20%s` — HEAD
- `git -C . status --porcelain` — working tree
- `node scripts/sky.mjs check-ci-status.mjs --workflow reverify.yml` — CI
- `node scripts/sky.mjs check-deployed-sha-drift.mjs` — deploy
  - WHERE IT LOOKED: no read-only deploy-state script in this lane's package.json (looked for ops:deploy-state, deploy-state, check-deploy, render-deploys, deploy:status, daily-snapshot). The fleet's check-deploy-state.mjs is not a substitute — it requires --fix <sha> --deploy <sha> and exits 1 without them — so this is the FLEET read, which takes no required flags. service: NOT DECLARED — no row for "bounds-ledger" in skylark-site/src/data/cc-deploy-gates.json (generatedAt 2026-09-17T06:11:22.338Z, 17 repo row(s)) — nothing in this repo declares that lane's Render service. No `--service` flag is emitted: the lane SLUG is not the service name (antebellum-twitter's is disunion-feed), and a guessed flag exits 1.

<!-- hand:begin narrative -->
## Session close-out — 2026-09-18

**What yesterday's Recommendation asked for, quoted, beside what happened to it.** Yesterday said:
*"Tomorrow: close `A-7`, decide `A-51`, take slice 6."* Two of the three landed and the third was
deliberately re-pointed:

- **`A-7` CLOSED** on its own closeWhen, by execution. The scheduled sweep printed `533 reachable
  commit(s) per git rev-list --all --count; the scan walked 533` and CLEAN with five dispositioned
  fixture hits, and `git rev-list --count --remotes=origin` returns 533 locally as an independent
  control.
- **`A-51` DECIDED**, not re-dated: still dead (HTTP 404 now, where 2026-09-05 read 200, body title
  `Site not found`), and the W-11 search changed the correction — the same dead link is in
  `ledger/teorth-optimizationproblems/constants/1a.md` as well as the mirrored 1b file, so a fix
  drafted from the row's title would have left the 1a citation dead. Draft, review and David's send decision are dated 2026-09-21.
- **Slice 6 DRAWN, not read.** Positions 125, 175, 225, 275 and 325 are printed and stored on `A-47`; the reads move to
  2026-09-21 because the day's depth-reading capacity went to the second area instead.

### The day's ship, and the one judgment inside it worth re-reading

The second watched area got its first depth readings and they are live on `ramsey.html`
(`db57062`, PR #38). Five bounds, drawn by a rule committed in `f1c5e85` **before any paper was
opened**: R(3, 11) ≤ 50 [GoeR1] SOUND, R(7, 8) ≥ 219 [Tat] SOUND by recomputation, R(4, 12) ≥ 128
[SuLL] UNREACHABLE, and the two [HW+] bounds UNRESOLVED.

**The judgment: a reading agent proposed SOUND for both [HW+] bounds and was overruled.** Its
arithmetic was right — both values reproduce exactly, twice, and 24 of the 25 HW+ cells reproduce
from the table's own inputs. But SOUND in this ledger means the credited source was read, and it
was not. The orchestrator called this the single best judgment in the P3 post. The recomputation
is kept in full on the rows, so the label costs nothing but the claim it would have made falsely.

### The number that will lie to you tomorrow

`npm run catches` still reads only the mirror's generated pins, so it cannot see the second area
at all — and as of today there are TWO depth stores with two populations: `continuity/depth-audit.json`
(A-47, frame 673 cited bound rows of the constants mirror) and `continuity/depth-audit-ds1.json`
(A-54, frame 122 credited bounds of Table Ia). `scripts/ds1-depth.mjs` refuses a reading whose bound
the committed table does not print, so the two cannot merge by accident, but no counter spans them.
`A-54` phase 2 on 2026-09-21 owns the choice: a detection counter for the second area, or an explicit
re-scoping of the printed figure where it is printed.

### Load-bearing commits from 2026-09-18

- `70ebd63` — the P2 ledger batch: `A-7` closed, `A-51` dispositioned, slice 6 drawn.
- `f1c5e85` — the draw, committed before any paper was opened. The order is the evidence.
- `fc0bbba` / `db57062` — the readings, the per-bound page, and the guards around them.

### What NOT to touch

- **The `--scheduled` refusals in `scripts/history-sweep.mjs`.** They are why `A-7` closed on two
  measured numbers rather than on a workflow file existing.
- **`storeGate` and the identity match in `scripts/render-ramsey.mjs`.** The guard matches each
  displayed reading to its stored row by (k, l, bound, value, ref, verdict), and refuses a missing
  or malformed store. Both were written because a count-based guard passed a verdict moved to the
  wrong bound, and an absent store republished every reading as "not yet read" at exit 0.

### Findings classification: every defect found today was instrument-facing

Nine of them, six from seven adversarial review rounds (the seventh approved with no material
findings), two from rendering the real readings rather than the fixtures, one from reading an
instrument against its own claim. **Consecutive instrument-facing days: 3.** The record-facing work —
five bounds read against their sources — found no defect in the records, which is a result, not a
blank. The standing prediction is unchanged and now owes one correction: today's strongest check was
a recomputation of a cited CONSTRUCTION, not of a certificate on the constants mirror, so the
predicted mechanism is broader than the sentence says.
<!-- hand:end narrative -->

<!-- generated by gen-primer-first-action.mjs (skylark-site) @ commit f6f392d9b · lane bounds-ledger · for 2026-09-19 · at 2026-09-18T19:35:40.337Z -->
<!-- writtenAtSha: `db57062766148e80049407ac9466cfb815c2cb98` — the lane's HEAD commit when this primer was written. Provenance anchor for check-primer-freshness leg ii; NOT a claim about HEAD now (the close-out commit that carries this file lands after generation). -->
<!-- reading this block: a generated line that is wrong in a way you can see FIXED in skylark-site's git log after the commit above is STALE, not a finding — regenerate before filing it. `git -C C:/dev/skylark/skylark-site log --oneline f6f392d9b^{commit}..HEAD -- scripts/gen-primer-first-action.mjs src/lib/cc-primer-commands.mjs src/lib/cc-due-gates.mjs src/lib/cc-report-artifact.mjs src/lib/cc-hand-edit-merge.mjs src/lib/cc-cold-readability.mjs src/lib/cc-skeleton-gloss.mjs` -->
