<!-- GENERATED — regenerating REPLACES everything outside the hand: markers, so put prose inside them. Provenance stamp at the foot of this block. -->

<!-- hand:begin banner -->
> **THIS LANE IS LAUNCHED.** David ruled it at 14:28 MT 2026-08-27, verbatim: *"agreed on bounds-ledger"* — recorded on `A-13` note23. You get the full `daily` prompt, not `dailyPrelaunch`. It reads instruments a launched app has; **answer each with the instrument you DO have or an explicit "not an instrument on this lane, by design" — never a zero**, because a zero from an absent instrument is indistinguishable from a measured one. Retention/active-users, Sentry and revenue are all the by-design answer here; the real reads are `npm run reports` (G-4's primary indicator) and `npm run traffic`.
>
> **The report path stays `docs/daily/<date>-prelaunch.md`.** The suffix is a historical filename, NOT a lifecycle claim, it already matches the fleet's `REPORT_CONFIGS`, and it is deliberately not being renamed. Do not helpfully fix it.
<!-- hand:end banner -->

### First action — bounds-ledger, 2026-08-28

Every line in the block below is a COMMAND, not a claim. A command re-reads at the
moment you run it and cannot go stale; a state line written last night can.

```bash
git rev-parse HEAD origin/main
git rev-list --count origin/main...HEAD
npm run verify > tmp/verify-out.txt 2>&1
node ../skylark-site/scripts/check-ci-status.mjs --workflow reverify.yml
```

#### Owed gates — 0 due on/before 2026-08-28

_None due. This is a real zero: 1 clock store(s) were read and returned no row due on/before 2026-08-28._

#### What shipped since 24 hours ago

- commit `0dfe218` — Report BLUF carries the 3-line judgment block, and the lead's "repo stays private" is false
- commit `1e02cf3` — Daily report 2026-08-27, A-30 closed, A-2 note 11, and a closed goal still cited in an instrument's output
- commit `7547c01` — Catch table: the twelfth cycle — an attribution RETRACTED by upstream
- commit `ef7ff97` — Declare the first-action fence and adopt the generated primer — but NOT one of the two offered fences
- commit `561964d` — Re-render the page so 2a publishes its real date, not "date unknown"
- commit `3907e7a` — Drift 2026-08-27: upstream RETRACTS the "acknowledges Jin" attribution on 2a — every leg verified
- commit `1e4c297` — chore(hooks): npm prepare re-wires core.hooksPath — the primer pre-commit survives a fresh clone
- commit `f59c907` — hooks(pre-commit): the primer checks run when the primer is COMMITTED, not the morning after

#### State — as of 2026-08-27T19:46:40.226Z (re-run the release command; never trust the value)

- **HEAD**: commit `0dfe218` on `main` · as of 2026-08-27T19:46:40.226Z — release: `git -C . log -1 --format=%h%x20%s`
- **working tree**: 0 uncommitted path(s) · as of 2026-08-27T19:46:40.226Z — release: `git -C . status --porcelain`
- **ledger**: 47 item(s): 27 closed · 20 open · as of 2026-08-27T19:46:40.226Z — release: `node ../skylark-site/scripts/gen-primer-first-action.mjs --repo . --target-date 2026-08-28 --json`
- **CI**: NOT READ at generation (a morning question) — release: `node ../skylark-site/scripts/check-ci-status.mjs --workflow reverify.yml`
- **deploy**: NOT READ at generation (a morning question) — release: `node ../skylark-site/scripts/check-deploy-state.mjs`

<!-- hand:begin narrative -->

## Session close-out — 2026-08-27 21:35Z

### The one thing to understand about today

Upstream **retracted an attribution it had published for fifteen days**, and our own file already
said we could not verify that claim. On 2026-08-12 upstream described Lorist–Schwenninger as a
proof "which acknowledges Jin"; we could not read the paper body (ar5iv served the abstract page),
so we recorded *"unverified by us — not contradicted"* rather than repeating it. Today upstream
withdrew it themselves, saying the old text *"read a citation of prior public posting as a
statement of dependence, and was incorrect"*. **Nobody told them.** The lane was right by
declining to guess, which is the only kind of right it is trying to be.

First **record-facing** day since 2026-08-14; it ends an eight-day instrument-facing run.

### Load-bearing ships

- `3907e7a` + `561964d` + `7547c01` — the twelfth drift cycle, verified against Crossref and both
  arXiv versions before snapshotting. **Trap for whoever re-checks that quote:** upstream quotes
  LS2026 **v1**, where it is verbatim; **v2 rewords the clause**, so checking against v2 alone
  reads as a misquotation and is not one. I got that wrong first.
- `a4558ed` — **user-visible.** The page said `fetched <date>`, which a reader takes as "last
  checked". It is not: `fetchedAt` moves only on drift, and the longest frozen window measured over
  mirror history is **nine days** (08-14 to 08-23), during which the job verified all 222 rows
  daily. A steady record was indistinguishable from a dead project. Now labelled, with the run
  history linked as the live read. Verified live on the published page, not just locally.
- `c504d37` (+ `78ed7f5` for its CI step) — a self-attributed cause must carry `control:` on the
  same line. Trigger list **measured** against the whole corpus; `exhausted` dropped for producing
  the only false positives.
- `a3c8fe1` — pre-commit refuses a staged `index.html` that is not what `render-site` would
  produce. Both polarities proven at the hook layer.

### Tomorrow first action

Run the declared first-action block at the top of this file. Then **ship `A-20`** (retry-with-backoff
on 429/502) — forced to a dated decision today, and tomorrow is the date. Its ship bar is in the
item: a test demonstrating BOTH answers — the retry recovers a single injected 502, AND a source
failing every attempt still returns UNREACHABLE and still reds the run. **Retry EXTENDS REACH; it
must never soften a verdict.** Making a 429 non-fatal is the forbidden change and this is not that.

### What NOT to touch

- **`C-7` / `C-9`, the two `manual: true` claims.** They report UNVERIFIED and never green. This
  machine gets HTTP 200 from that page — today included — and the block is datacenter-IP-dependent,
  so a local success says nothing about CI. Twice that local 200 has tempted an automation CI
  disproved within minutes. The rule is load-bearing *because* it overrides a passing local read.
- **The `-prelaunch` report filename.** See the banner.
- **Never kill a node process by name, age, or "looks orphaned"** — the process table is shared by
  24 panes; three lanes did this today and killed the fleet listeners. **Never run `doppler
  configure` / `doppler whoami`** — they print the token into the transcript. Both are in
  CLAUDE.md Gotchas.

### Waiting on David

**`A-28` — the Mathstodon announcement, written and adversarially reviewed, unsent for 14 days,
figures now stale.** Carded today. I recommended rewriting it around today’s retraction, which is
far stronger material than the draft carries. Do not send anything outward without his answer.

### Evangelism read

- **Core problem, in the user’s words:** *"I cited a number. I have no idea if it is still the
  current one."*
- **Would-tell-a-friend moment:** *"the ledger caught a drift the field’s own index missed."*
  Distance today: **closer than the metric shows.** Upstream corrected a claim we had flagged as
  unverifiable — the story exists; nobody has been told it.
- **Retention / word-of-mouth:** not instrumented, by design — static page, no analytics stack. The
  honest proxies are `npm run reports` (**0 arrivals, MEASURED**: 26 issues seen, 26 reconciled) and
  `npm run traffic` (**2 unique viewers / 14d** against 183 cloners that are mostly our own CI).
- **Advocacy signal this session:** none new. The G-3 instance (2026-08-23, a contributor fixing
  `87a.md` citing our issue #150) closed 08-26 and **does not count toward G-4**, whose whole
  condition is a causal path containing no artifact we authored and sent.

### Tier-0 status

`G-4` — an outside party acts on a watched record without us filing the report — reads **0**, a
measured zero, posture unchanged. The `A-34` decision rule was written today **before** its third
data point arrives: if the third record-facing catch is again citation-metadata rather than a bound
moving, the lane self-description gets rewritten and the 09-02 proposal is depth, not breadth.
Standing at 2 of the last 3.

### CI

**CI:** reverify: completed/success @ `9a5f200`.
_(orchestrator narrative — this region survives regeneration)_
<!-- hand:end narrative -->

<!-- generated by gen-primer-first-action.mjs (skylark-site) · lane bounds-ledger · for 2026-08-28 · at 2026-08-27T19:46:40.226Z -->
