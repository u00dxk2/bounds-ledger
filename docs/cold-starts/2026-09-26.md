<!-- GENERATED — regenerating REPLACES everything outside the hand: markers, so put prose inside them. Provenance stamp at the foot of this block. -->

<!-- hand:begin banner -->
Written 2026-09-25 late morning MT, at the close of 2026-09-25's rail (P1 to P5 in one session). **Three rows are due on 2026-09-26. `A-56` comes first**: settle 46a by reading the published body of Bourgain's GAFA 1991 paper, pp. 149–187. Read its routes and its stop condition, not your memory of them:

```bash
node scripts/sky.mjs show-item.mjs A-56 --fields onTrigger,waitJustification.unWait,closeWhen
```

If no route reads the body, close the row as "no reachable copy" and leave 46a UNRESOLVED. Do not re-date it. Nothing goes upstream: a correction to the mirrored entry is outward contact, so it needs adversarial review first and then David's gate. **The reading brief sends no email address or name to any service.** Today an agent sent David's email to Unpaywall because the brief listed that service, so Unpaywall is now off every brief.

> **`A-52` (write down how an outsider could act on a record without filing an issue) is OVERDUE on its own trigger.** Its onTrigger said to write the `arrivalShapes` field on 2026-09-23, and `show-item.mjs A-52 --fields arrivalShapes` printed "(absent)" on 2026-09-25. It is a definition, so nothing gets built: for each shape, name its trace and the command that reads it, or write "no trace we can see". Write it before `G-4`'s read, because that read decides whether any trace is worth reading routinely.

> **`G-4` (an outside party acts on a watched record without us filing the report) reads the same day:** run `npm run reports`. Today it printed 30 raw issues, all 30 ours, 0 outside arrivals, and the parts reconcile.

> **What today shipped, so you do not redo it.** `A-54` (the Small Ramsey Numbers rebuild, the second watched area) ran census session 1:
> - `876caa4` fixed the census order and plan and put the freeze in code: census rows are held off `ramsey.html` and `copying.html`.
> - `7bd7ec9` stored the readings. `node scripts/ds1-depth.mjs` printed "census: 10 of 122 bound(s), across 5 of 42 credited paper(s) — 6 sound, 0 defective, 1 unresolved, 3 unreachable".
> - Session 2 is on 2026-09-28, after `A-47` slice 8 on 2026-09-27, the audit that renders on the public pages. Read its plan and its blocked-source rule in one call: `node scripts/sky.mjs show-item.mjs A-54 --fields rebuildScope2026_09_21,blockedSourceRule2026_09_25`.
> - The reading brief template is `tmp/census1-brief.md`. It is gitignored, so it exists only on this machine.

> **The freeze still governs.** `ramsey.html` and `copying.html` stay as written until the rebuild lands, planned for 2026-10-16. Census rows must record the reference KEY and never a survey bibliography entry, or `render-ramsey.mjs --check` refuses the store. `AGENTS.md` says why.

> **On arrival, Glob `docs/cold-starts/` for the newest file, not only today's date.** Then relaunch the waker ladder (ranks 1, 2 and 3) with three separate background calls from `/listen` step 3b, never joined into one command.
<!-- hand:end banner -->

UNVERIFIED FIRST ACTION — matching execution evidence and current governing dependencies have not been established by these release commands.
Recovery read (PowerShell or bash, cwd: lane root): `git status --short` followed by `git log -5 --oneline`; reconcile the authored action in docs/daily-config.md with the current ruling and ledger before running it.

First command (also below, in place):

```bash
git pull --ff-only
# commits after this primer was written are invisible to it — read them before acting:
git log --oneline f43b679f0d0e71ff2f69891ca7c29fdef48ce0a7..HEAD
```

### Primer — bounds-ledger, 2026-09-26

What ONLY this primer holds comes first; the FULL runnable **First action** block
sits at the foot, below the reads. The single command above is the same block's
first line, repeated for reference — it replaces nothing. Establish the governing action and its preconditions before running release commands.

_**Composed against HEAD commit `f43b679f0d0e71ff2f69891ca7c29fdef48ce0a7` at 2026-09-25T14:23:54.105Z.** Nothing that landed after that instant is in this file. David's board answers especially: an answer he gives later cannot be here, and this primer will go on reading as though the work were not owed — that is exactly how two lanes concluded an OTA was not owed on 2026-09-04. Read them live, never off this page: `node scripts/sky.mjs answered-cards.mjs --project bounds-ledger --full`._

_Shared skylark-site scripts below are spelled `node scripts/sky.mjs …` — this lane vendors `scripts/sky.mjs` (S-20260906-75), so a shared script is named LANE-RELATIVE and resolves from a worktree too._

#### Owed gates — 3 due on/before 2026-09-26

_Ledger read at 2026-09-25T14:23:54.105Z by `cc-due-gates.collectDue` — the same resolver `check-due-gates-dispositioned` uses, so any difference is TIMING, not two answers. Rows filed or re-dated after that are NOT READ at generation — the close-out commit lands after this, so the COUNT above is stale by construction and this list cannot claim completeness. The list below, not the count, is the artifact; re-derive both before acting: `node scripts/sky.mjs check-due-gates-dispositioned.mjs --print --today=2026-09-26`._

Unfiltered as of the read time above — nothing was curated out of what the read could
see; a row absent here may simply have been filed after it ran.

- `A-52` (expectedSignalBy=2026-09-26, open) — Write down what an outside party acting on a record could look like besides filing an issu
  - read: `node scripts/sky.mjs show-item.mjs A-52 — prints this row with one "## <field>" heading per field. A LINE STARTING WITH "## arrivalShapes" means the list is written. The bare word arrivalShapes appears in this row's onTrigger, closeWhen and this text whether or not the list exists, so finding the word proves nothing.` · last run: 2026-09-25T00:04:16.187Z
- `A-56` (nextCheckDate=2026-09-26, open) — Settle 46a's depth-audit entry: read the published body of Bourgain's GAFA 1991 paper, pp.
  - read: `node scripts/depth-audit.mjs # the A-47-0030 line carries 46a's verdict. UNRESOLVED means this row is still open; DEFECTIVE or SOUND means the published body was read and this row closes naming it.` · last run: NEVER
- `G-4` (expectedSignalBy=2026-09-26, open) — GOAL (Tier-0 from G-3 close): an outside party acts on a record this ledger watches WITHOU
  - read: `npm run reports # SEPARATOR CONVERTED 2026-09-02 (bonus cycle), em dash to hash. G-3's leading indicator becomes G-4's primary one, because it counts exactly the arrivals that qualify here: outside issues filed through the per-row links, excluding ours by AUTHOR. Literal output 2026-08-25 when it shipped: '25 + 0 + 0 = 25' — raw issues fetched, ours excluded, outside arrivals — so the zero is MEASURED rather than a dead probe. POSITIVE CONTROL is built into the script and must not be removed: it refuses to print a bare zero, exits 2 on an empty fetch and 3 if the classified parts do not sum to the raw total. Read the denominator beside the count, always; a bare arrival figure from this command is worthless by construction. DETECTION VALID FROM 2026-08-28 (note6): this command could not have counted an arrival filed through the page's loudest link before that date, so read its zero against the clean-detection day count and never against the days-since-public-flip count.` · last run: NEVER

#### What shipped since 24 hours ago

_Read at 2026-09-25T14:23:54.105Z; commits landing after that — the close-out especially — are not here. Re-derive: `git log --oneline --since='24 hours ago'`._

- commit `f43b679` — Hygiene 2026-09-25: today's report lands, AGENTS.md learns the census and the freeze filter, A-54 links today's commits
- commit `3dd9b0c` — P4 2026-09-25: A-54 gets a written rule for blocked sources before census session 2
- commit `7bd7ec9` — A-54 census session 1: five credited papers read, ten bounds, 6 sound, 1 unresolved, 3 unreachable — re-derive: `git show 7bd7ec9`
- commit `876caa4` — A-54 census session 1 planned before any paper is opened: GG, Kery, Ka2, GrY, GR
- commit `89b4dd4` — P2 2026-09-25: A-54 gets its census-day receipt rule before the first census session runs
- commit `ef87a3d` — Hygiene 2026-09-24: today's report lands, tomorrow's primer leads with the missed census session, and tonight's commits are linked
- commit `b4003fa` — A-56: 46a's UNRESOLVED badge gets a named way out, dated 2026-09-26
- commit `c05232f` — A-47 slice 7: five more cited rows read against their sources, and 46a's reference looks wrong
- commit `0ed11f7` — A-47 slice 7 drawn before any source is opened: 46a, 52a, 5b, 71a, 80a
- commit `ea25512` — P2 2026-09-24: W-13 closes only on a workflow seen to fire and seen to stay silent
- commit `5243c1e` — Hygiene 2026-09-24: the 2026-09-22 report lands two days late and says so, tomorrow's primer leads with the dark window, and the four gates it swallowed are read

**Yesterday's report:** `docs/daily/2026-09-25-prelaunch.md` (coverage 2026-09-25, the prior day). No findings fence declared in it.
Prior report: historical; re-read its findings and later changes before treating its opener as current.

**Today's report (owed):** write `docs/daily/2026-09-26-prelaunch.md` — OUTPUT obligation due at close, the artifact of record for service day 2026-09-26. Check it exists at close: `node scripts/sky.mjs check-daily-report-exists.mjs --date 2026-09-26` (it sweeps the fleet and applies each lane's coverageOffset; it takes no --project). <!-- owed-output path="docs/daily/2026-09-26-prelaunch.md" service-day="2026-09-26" due="close" -->

#### State — as of 2026-09-25T14:23:54.105Z (re-run the release command; never trust the value)

- **ledger**: 72 item(s): 53 closed · 19 open · as of 2026-09-25T14:23:54.105Z — release: `node scripts/sky.mjs gen-primer-first-action.mjs --repo . --target-date 2026-09-26 --json`

#### Id glossary — 22 id(s) this primer's day already has words for

_Each gloss is the ledger row's OWN title, not a re-derivation — paste it. Rows = every open ledger row plus every id named above. `(no ledger row)` means the id is named here and the ledger carries nothing under it: that is a finding, not a formatting gap._

- `A-2` — STANDING: drift-resolution log for the adopted surfaces…
- `A-9` — engineering-health P2 backlog (fix-on-touch, not scheduled…
- `A-45` — TIER-1 LEADING INDICATOR (orchestrator-approved…
- `A-47` — THE DEPTH AUDIT, David-ruled 2026-09-09: read our own…
- `A-47-0030` — (no ledger row)
- `A-49` — npm run close:primer passes when tomorrow's banner is…
- `A-50` — Importing one of our CLI scripts can run it — one selftest…
- `A-52` — Write down what an outside party acting on a record could…
- `A-53` — The audit store and the public page can disagree about how…
- `A-54` — Adopt Small Ramsey Numbers (Electronic Journal of…
- `A-56` — Settle 46a's depth-audit entry: read the published body of…
- `G-2` — GOAL: contribute a verified bound improvement to the…
- `G-3` — GOAL (Tier-0 from G-1 close): someone outside Skylark uses…
- `G-4` — GOAL (Tier-0 from G-3 close): an outside party acts on a…
- `S-20260906-75` — (no ledger row)
- `W-3` — Watch for acknowledgement of the erdosproblems.com/36…
- `W-4` — KP-78 applies to every new detector: demonstrate BOTH…
- `W-6` — Read window for the README report-an-error channel — the…
- `W-8` — Does a materially amended outward artifact go back to…
- `W-11` — Before asserting a claim is decision-ready, search this…
- `W-12` — Watch: does upstream act on PR #194, the [YKLBMWKCZGS2026]…
- `W-13` — STANDING: someone must CALL the served-bytes check — A-55…

### First action — bounds-ledger, 2026-09-26

Release commands — subordinate to the governing action.
HANDOFF INCOMPLETE — governing action not authored.

UNVERIFIED FIRST ACTION — these commands have no matching execution evidence here. Command text alone establishes neither safety nor the promised evidence, shell compatibility, or tomorrow's priority.

```bash
git pull --ff-only
node scripts/sky.mjs update-david-board.mjs --list --json --full-ids --project bounds-ledger
git rev-parse HEAD origin/main
git rev-list --count origin/main...HEAD
npm run verify > tmp/verify-out.txt 2>&1
node scripts/sky.mjs check-ci-status.mjs --workflow reverify.yml
```

Morning release commands — deliberately NOT READ at generation (a value written
last night would already be stale); run each, never trust a remembered one:
- `git -C . log -1 --format=%h%x20%s` — HEAD
- `git -C . status --porcelain` — working tree
- `node scripts/sky.mjs check-ci-status.mjs --workflow reverify.yml` — CI
- `node scripts/sky.mjs check-deployed-sha-drift.mjs` — deploy
  - WHERE IT LOOKED: no read-only deploy-state script in this lane's package.json (looked for ops:deploy-state, deploy-state, check-deploy, render-deploys, deploy:status, daily-snapshot). The fleet's check-deploy-state.mjs is not a substitute — it requires --fix <sha> --deploy <sha> and exits 1 without them — so this is the FLEET read, which takes no required flags. service: NOT DECLARED — no row for "bounds-ledger" in skylark-site/src/data/cc-deploy-gates.json (generatedAt 2026-09-22T02:18:27.672Z, 17 repo row(s)) — nothing in this repo declares that lane's Render service. No `--service` flag is emitted: the lane SLUG is not the service name (antebellum-twitter's is disunion-feed), and a guessed flag exits 1.

<!-- hand:begin narrative -->
_(orchestrator narrative — this region survives regeneration)_
<!-- hand:end narrative -->

<!-- generated by gen-primer-first-action.mjs (skylark-site) @ commit a78edc48c · lane bounds-ledger · for 2026-09-26 · at 2026-09-25T14:23:54.105Z -->
<!-- writtenAtSha: `f43b679f0d0e71ff2f69891ca7c29fdef48ce0a7` — the lane's HEAD commit when this primer was written. Provenance anchor for check-primer-freshness leg ii; NOT a claim about HEAD now (the close-out commit that carries this file lands after generation). -->
<!-- reading this block: a generated line that is wrong in a way you can see FIXED in skylark-site's git log after the commit above is STALE, not a finding — regenerate before filing it. `git -C C:/dev/skylark/skylark-site log --oneline a78edc48c^{commit}..HEAD -- scripts/gen-primer-first-action.mjs src/lib/cc-primer-commands.mjs src/lib/cc-due-gates.mjs src/lib/cc-report-artifact.mjs src/lib/cc-hand-edit-merge.mjs src/lib/cc-cold-readability.mjs src/lib/cc-skeleton-gloss.mjs` -->
