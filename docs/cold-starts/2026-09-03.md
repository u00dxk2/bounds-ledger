<!-- GENERATED — regenerating REPLACES everything outside the hand: markers, so put prose inside them. Provenance stamp at the foot of this block. -->

<!-- hand:begin banner -->
> **THIS LANE IS LAUNCHED** — David ruled it 2026-08-27. Instruments a launched app has and this
> one does not — retention, active users, Sentry, revenue — are answered **"not an instrument on
> this lane, by design"**, never with a zero. The real reads are `npm run reports` (`G-4`'s primary
> indicator) and `npm run traffic`.
>
> **The report path stays `docs/daily/<date>-prelaunch.md`.** Historical filename, not a lifecycle
> claim. Do not helpfully fix it.
>
> **THE MATHSTODON POST IS CLOSED, UNSENT, AND MUST NOT BE RE-PROPOSED.** Nothing is owed there.
>
> **START LINE 4 OF THE FIRST ACTION BEFORE YOU READ THIS DOCUMENT.** `npm run verify` needs no
> context, takes about three minutes and writes its whole answer to a receipt keyed to a sha.
> Reading the primer ahead of it serialises that wait behind a six-thousand-token read — measured
> this morning, and it is the one process change approved yesterday. Background it, then read on:
> ```
> npm run verify > tmp/verify-out.txt 2>&1
> ```
> **This line dies when a primer generator emits the long command above the banner.**
>
> **`gen-primer-first-action.mjs` REPORTS `RESULT: PASS` AT EXIT 0 WHILE WRITING NOTHING** unless
> you pass `--out`. It prints the primer to stdout and appends a `CANDIDATE ONLY` line at the very
> bottom, below the PASS. Cost a full re-run on 09-02. The form that actually writes:
> ```
> node ../skylark-site/scripts/gen-primer-first-action.mjs --repo . --target-date <date> --out docs/cold-starts/<date>.md
> ```
>
> **CODEX CANNOT WRITE ANYTHING UNDER `.git/` ON THIS MACHINE — measured three times on 09-02.**
> `git add`, `git commit`, `git checkout`, `git worktree add` all fail `Permission denied`; a write
> probe to `tmp/` SUCCEEDS. So brief a dispatch to run **no git command at all** and to prove a
> mutation landed with `grep -c`, not `git diff --numstat`. You stage and commit. And **diff its
> output against the artifact the REVIEW read**, not the tree it was written on — on 09-02 a
> dispatch reimplemented `fetchWithRetry` weaker than the reviewed branch code (dropped
> `res.body?.cancel()`, narrowed the transient set) and `npm test` passed.
>
> **TWO GATES DUE TOMORROW, BOTH PRE-STAGED BELOW WITH THEIR READS ALREADY RUN.** `A-7`
> (engineering-health R7) and `A-9` (the fix-on-touch backlog). Read the notes before running:
> ```
> grep -n "prestage2026_09_02" continuity/items.json
> ```
>
> **`A-34` MUST NOT SLIP A THIRD TIME.** David asked on 26 August where else this ledger can have
> impact; it is now re-dated to 09-05 for the second time. The row's own note argues the answer may
> not be a second surface at all — the alarm is structurally blind to upstream being *wrong and
> staying wrong*, and we mirror 111 constants whose citations have almost never been checked by
> hand. Breadth versus depth is the real question for him.
>
> **`git rev-parse --short HEAD origin/main` exits 128.** Two revisions plus `--short`. Drop
> `--short`.
<!-- hand:end banner -->

### Primer — bounds-ledger, 2026-09-03

What ONLY this primer holds comes first; the runnable **First action** sits at the
foot of this block, below the reads — do not skip to it.

#### Owed gates — 2 due on/before 2026-09-03

_Ledger read at 2026-09-02T16:29:13.337Z by `cc-due-gates.collectDue` — the same resolver `check-due-gates-dispositioned` uses, so any difference is TIMING, not two answers. Rows filed or re-dated after that are NOT READ at generation — the close-out commit lands after this, so the COUNT above is stale by construction and this list cannot claim completeness. The list below, not the count, is the artifact; re-derive both before acting: `node ../skylark-site/scripts/check-due-gates-dispositioned.mjs --print --today=2026-09-03`._

Unfiltered as of the read time above — nothing was curated out of what the read could
see; a row absent here may simply have been filed after it ran.

- `A-7` (expectedSignalBy=2026-09-03, open) — engineering-health P1s — four of five shipped 2026-08-09; OPEN on R7 alone (scheduled veri
  - read: `grep -rn 'history-sweep' .github/workflows/ — R7 (a SCHEDULED verified full-history sweep) is the only open leg. Literal output 2026-08-26: THREE hits, and all three are the --selftest step plus a comment ABOUT R7; no scheduled sweep exists, so R7 is unshipped. READING THE HIT COUNT IS THE TRAP here — the pattern matches the self-test that already ships, so judge the hits, never the number. POSITIVE CONTROL that the glob reads workflow files at all: grep -c 'schedule' .github/workflows/reverify.yml returns 3.`
- `A-9` (nextCheckDate=2026-09-03, open) — engineering-health P2 backlog (fix-on-touch, not scheduled work)
  - read: `git log --oneline --since='30 days ago' -- docs/engineering-health-review-2026-07-29.md — this row is a fix-on-touch BACKLOG, not scheduled work, so there is no state to poll and age is not decay. The only honest machine read is whether the review doc has been touched. POSITIVE CONTROL: the file gained a 'Superseded fact' note on 2026-08-26 (the Auto-deploy-on-push row went stale when Pages was turned on), so a 30-day window MUST return at least that commit; an empty result means the path is wrong, not that nothing happened.`

#### What shipped since 24 hours ago

_Read at 2026-09-02T16:29:13.337Z; commits landing after that — the close-out especially — are not here. Re-derive: `git log --oneline --since='24 hours ago'`._

- commit `096e126` — P4: both gates dispositioned, and the delegated fix was a regression wearing a fix's name
- commit `4c2fd97` — Section A: a row we reported on now says so, beside the verdict that called it a text edit
- commit `b51a705` — P2: the return-rate instrument refused its own first reading, and the refusal was right

**Yesterday's report:** `docs/daily/2026-09-02-prelaunch.md` (coverage 2026-09-02, the prior day). Findings fence EMPTY — the BLUF is the whole story; nothing below it supersedes anything.

**Today's report (owed):** write `docs/daily/2026-09-03-prelaunch.md` — the artifact of record for service day 2026-09-03. Check it exists at close: `node ../skylark-site/scripts/check-daily-report-exists.mjs --date 2026-09-03` (it sweeps the fleet and applies each lane's coverageOffset; it takes no --project).

#### State — as of 2026-09-02T16:29:13.337Z (re-run the release command; never trust the value)

- **ledger**: 52 item(s): 35 closed · 17 open · as of 2026-09-02T16:29:13.337Z — release: `node ../skylark-site/scripts/gen-primer-first-action.mjs --repo . --target-date 2026-09-03 --json`

#### Id glossary — 17 id(s) this primer's day already has words for

_Each gloss is the ledger row's OWN title, not a re-derivation — paste it. Rows = every open ledger row plus every id named above. `(no ledger row)` means the id is named here and the ledger carries nothing under it: that is a finding, not a formatting gap._

- `A-2` — STANDING: drift-resolution log for the adopted surfaces…
- `A-7` — engineering-health P1s — four of five shipped 2026-08-09;…
- `A-9` — engineering-health P2 backlog (fix-on-touch, not scheduled…
- `A-20` — Should the fetch layer retry with backoff on 429/502? —…
- `A-29` — secret_scanning_validity_checks will not enable via API —…
- `A-31` — Should the public page publish be CI-gated? It is not, and…
- `A-33` — A change to a file we have filed an upstream report…
- `A-34` — David asked where else this ledger can have the kind of…
- `A-39` — A mid-run 429 becomes up to 339 requests in two seconds at…
- `G-2` — GOAL: contribute a verified bound improvement to the…
- `G-4` — GOAL (Tier-0 from G-3 close): an outside party acts on a…
- `W-3` — Watch for acknowledgement of the erdosproblems.com/36…
- `W-4` — KP-78 applies to every new detector: demonstrate BOTH…
- `W-6` — Read window for the README report-an-error channel — the…
- `W-8` — Does a materially amended outward artifact go back to…
- `W-9` — Does this lane's detection method find what the lane…
- `W-10` — reverify.test.mjs alarm-title leg failed once and would…

### First action — bounds-ledger, 2026-09-03

Every line in the block below is a COMMAND, not a claim. A command re-reads at the
moment you run it and cannot go stale; a state line written last night can.

```bash
node ../skylark-site/scripts/update-david-board.mjs --list --json --full-ids --project bounds-ledger
git rev-parse HEAD origin/main
git rev-list --count origin/main...HEAD
npm run verify > tmp/verify-out.txt 2>&1
node ../skylark-site/scripts/check-ci-status.mjs --workflow reverify.yml
```

Morning release commands — deliberately NOT READ at generation (a value written
last night would already be stale); run each, never trust a remembered one:
- `git -C . log -1 --format=%h%x20%s` — HEAD
- `git -C . status --porcelain` — working tree
- `node ../skylark-site/scripts/check-ci-status.mjs --workflow reverify.yml` — CI
- **deploy read: NOT AVAILABLE** — no read-only deploy-state script in this lane's package.json (looked for ops:deploy-state, deploy-state, check-deploy, render-deploys, deploy:status, daily-snapshot). The fleet's check-deploy-state.mjs is not a substitute — it requires --fix <sha> --deploy <sha> and exits 1 without them

<!-- hand:begin narrative -->
## Session close-out — 2026-09-02

### The one thing to understand about today

The public page gained a disclosure: on a row we ourselves reported upstream, it now says so and
links the report. On `87a` that line sits directly beneath our own classifier's verdict, so the
page reads `text edited 2026-08-23 — bound unchanged` above `we reported this row · closed
2026-08-23`. **That juxtaposition is `A-33`'s whole finding, handed to a reader instead of to us.**
The classifier answered *did a number move* correctly and had no way to ask *is this a file we
reported against*. A human seeing both lines can ask it in a second.

**It asserts no cause, and that is permanent.** `A-33` leg 2 as designed labels such a movement
`CANDIDATE CAUSAL EVENT`. On a public page that publishes a judgment about causation, which the
row's own note forbids. If leg 2 is ever built, it is internal-only.

### Three things a cold agent will otherwise re-derive

- **A delegated fix can be a regression wearing a fix's name, and every gate still passes.** A
  Codex dispatch asked to apply two reviewed fixes rebuilt `fetchWithRetry` from scratch instead,
  because the tree sat on `main` which has no retry. Its version dropped `res.body?.cancel()`
  (leaking an undrained response on every retry) and replaced `isTransientFetchError` — which
  carries `UND_ERR_SOCKET` plus a message-regex fallback — with a bare code-set lookup matching
  neither. `npm test` passed; the diff against `main` looked like a clean feature-plus-fix. Caught
  only by diffing against the BRANCH the review actually read. Its work is preserved at
  `tmp/codex-reverify.mjs`, `tmp/codex-check-claims.mjs`, `tmp/codex-reverify.test.mjs` — take the
  F1 guard and the F2 test from those, never the retry.
- **A self-attributed cause can carry a control clause that could not have failed.** The two lost
  Codex dispatches were first blamed on a race with my own commits, with a control attached, which
  is the form the rule asks for. The control did not discriminate. The timestamps did: my first git
  write was 15:23:34Z and the dispatch failed around 15:11Z. Corrected on the bus as `866163ce`.
  The lesson is not *check timestamps*; it is that a control which cannot come out the other way is
  decoration.
- **A `readCommand` that greps a working file answers a different question than the one it asks.**
  `A-39`'s first draft used a plain `grep` over `scripts/reverify.mjs` to ask whether the retry had
  merged into `main`. An uncommitted Codex edit had the constants in the tree while committed
  `main` had none, so it printed three hits when the honest answer was zero. It is now
  commit-scoped: `git grep -c RETRY_DELAYS_MS HEAD -- scripts/reverify.mjs`, with both polarities
  recorded on the row.

### What NOT to touch

- **The practice of diffing a delegated change against the reviewed artifact.** Not a script, and
  it should not become one — it asks a question no gate asks: *is this the code the review read?*
- **The scope caveat welded to the return-rate figure.** It prints REPO views, not page readers,
  in the same breath as the number. Separating them into a footnote is how the number starts
  meaning something it does not.

### Tier-0 status

**`G-4` = 0 arrivals against 3 unique viewers in 14 days** — measured, 26 issues fetched, all 26
ours, parts reconcile. The pre-committed threshold needs **30 unique viewers** before a zero says
anything about the product. **New today: the return rate reads 1.00 — three viewer-days over three
distinct viewers, so nobody came back twice.** Readable at n=3 in a way the arrival count is not.
**Consecutive instrument-facing days: 6.**

### Watch items maturing soon

`A-7` and `A-9` — **tomorrow**, both pre-staged above. `A-33` and `A-34` — 09-05. `A-20` — 09-08
(forced review OF THE REVIEW; F1 still blocking and the branch is untouched at `8a4c2b8`). `A-29`
— 09-08. `A-31` and `A-39` — 09-09. `G-2`, `W-8`, `W-9`, `W-10` — 09-22. `W-3` — 09-24. `G-4` —
09-26.
<!-- hand:end narrative -->

<!-- generated by gen-primer-first-action.mjs (skylark-site) @ commit 241d73cb · lane bounds-ledger · for 2026-09-03 · at 2026-09-02T16:29:13.337Z -->
<!-- reading this block: a generated line that is wrong in a way you can see FIXED in skylark-site's git log after the commit above is STALE, not a finding — regenerate before filing it. `git -C ../skylark-site log --oneline 241d73cb..HEAD -- scripts/gen-primer-first-action.mjs src/lib/cc-primer-commands.mjs src/lib/cc-due-gates.mjs src/lib/cc-report-artifact.mjs src/lib/cc-hand-edit-merge.mjs src/lib/cc-cold-readability.mjs src/lib/cc-skeleton-gloss.mjs` -->
