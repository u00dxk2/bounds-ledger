# Cold-start primer for 2026-08-24

Multi-session primer — each close-out appends a section below. Later sections supersede earlier ones.

## Session close-out — 2026-08-23

Paced rail, day 22. Two user-visible ships, three corrections to our own rules, and a weekday label that was wrong in yesterday's primer and got repeated.

### Tomorrow's first-action — VERIFIED, exit 0 in the turn that wrote this

```bash
cd C:/dev/skylark/bounds-ledger && git rev-parse HEAD origin/main && npm run verify && node ../skylark-site/scripts/check-ci-status.mjs --workflow reverify.yml
```

Executed from a file, never retyped: printed `137255f…` twice, `npm run verify` **exit 0**, CI **GREEN**. Whole chain **exit 0**.

It is `git rev-parse HEAD origin/main`, **not** `--short`. That has now been reproduced three times by people who read the warning the same morning.

**And run the gate unpiped.** `npm run verify | tail -40` is refused by the pre-commit corruption guard — correctly, because the pipe's exit code would be the pager's. The working shape is now written into the AGENTS.md command entry:

```bash
npm run verify > tmp/verify-out.txt 2>&1; echo "EXIT=$?"
```

I tripped that guard twice today before writing it down. You should trip it zero times.

### ⚠ CORRECTION to yesterday's primer — a weekday label, and it propagated

Yesterday's block said **"Monday 2026-08-25 is heavy: four gates."** **2026-08-25 is a TUESDAY.** Monday is 2026-08-24 — tomorrow. The dates on the items were right; only the weekday word was wrong, and I repeated it in a bus post before checking.

So the real shape of your week:

- **Monday 2026-08-24 (tomorrow) — two gates.** `A-2` (the standing drift-resolution log) re-reads, and `W-7` (whether the 2026-08-15 instrument-audit proposal was ever tracked) must be **DECIDED**: ship it, defer it with an explicit date, or strike it. Its `onTrigger` says routing it to "the next pass" again is the exact failure the watch exists to catch, so that is not an available option.
- **Tuesday 2026-08-25 — four gates.** `A-18` (a generated STATE block for the primer), `A-19` (two non-blocking review nits), `A-25` (give deferrals an expiry field and a check that reads it), `A-26` (author the evangelism-bar doc this repo lacks).
- **`A-25`'s single re-date is SPENT.** On Tuesday it ships or closes as declined. It does not roll again.

Check the weekday of any date you are about to put a day-name on. `node -e "console.log(new Date('2026-08-25T12:00:00Z').toLocaleDateString('en-US',{weekday:'long',timeZone:'UTC'}))"`.

### THE THING THAT WILL MISLEAD YOU FIRST

**The 111 dates on the public page.** 208 of 222 pins carry `2026-07-24` — the day the ledger first pinned them, **not** a day anything moved. Ten rows carry a later date; those are the ones that have genuinely moved. The page now says this in its own words, but the trap is live for anyone reading the raw data.

This killed today's planned ship and is the day's most transferable lesson: the primer named a *"changed in the last 90 days"* filter, told me to verify the premise, and I did — rows carry dates, premise true, **design still wrong**, because today minus 90 days reaches past the bootstrap date and the filter would have matched all 111 rows. **"Does the field exist" and "does the field discriminate" are different questions, and only the second can kill a design.** Shipped a sort instead. Nothing in the selftest, the state block or CI can see that a control does not discriminate.

### What a user can now see that they could not

Both verified live on the served page with a positive control on the H1 before any element was asserted present:

1. **An Order control** — *by constant id* (default) or *most recently updated first*. 111 rows publish the later of their two pin dates as a sort key.
2. **A search that matches nothing now explains itself** (`9208ff3`) — says whether we have never tracked that constant or whether it may be filed under an unfamiliar name, and offers a report link **prefilled with the search term**.

**Why #2 matters more than it looks, and what to watch for:** it manufactures a countable observable where none existed. A visitor who searches, misses and leaves is invisible to every instrument we have. An issue titled **`Constant not tracked: <term>`** is a row that cannot exist today, so N=1 is unambiguous evidence. **The orchestrator asked that its first arrival be surfaced on the bus the day it lands, not folded into a weekly line.** `G-3.readCommand` now searches for it.

### Three corrections to our own rules — one class, worth recognising

None of these were bugs. Each was a **rule pointing at the wrong place**, and the signature is that every individual part looks correct.

- **`123dd12`** — the dry-week rule read `docs/findings/`, which the classification stopped flowing into on 08-19 while the daily reports kept classifying. Read literally it would have called a busy fortnight dry. That leg exists to stop a zero movement count firing a second-surface decision on its own; pointed at an empty directory it *agrees* instead of dissenting. Now reads the reports' classification sentences.
- **`137255f`** — `W-6`'s visitor threshold was retired 08-20, and `traffic-snapshot.mjs` kept **printing it on every run for three days**, including the run twenty minutes before I found it. A printed line that names an item id reads as authoritative without being traced back. A-11 class.
- **`G-3.readCommand` was blind to a path we had just built** — it gained the `Constant not tracked:` search and a positive control on the served page. **Shipping a new detection path is not done until the goal's own read command reads it.**

### Traps this session actually hit

- **A test that identifies its subject by POSITION breaks when anything is inserted above it.** The hostile-name test matched the first `issues/new` href on the page; the new empty-state link now sits above the table, so it silently started asserting against the wrong URL. Locate a test's subject by identity — here, its prefilled title.
- **A stub DOM cannot prove what an injection assertion appears to prove.** Swapping `textContent` for `innerHTML` fails the test because the stub then never writes `textContent`, not because it parsed markup. Recorded beside the assertion (`0698ab3`) so the PASS line is not read as a browser-level guarantee.
- **I cited the wrong SHA in a receipt.** The `USER-VISIBLE:` line named HEAD instead of the commit carrying the change, which was a flow doc. Those coincide only when the user-visible commit is last, and mine was third of three. **`git show --stat <sha>` before citing it** — if the diff is all `docs/`, the SHA is wrong by construction.
- **I quoted a stale number twice.** "~4 unique viewers" came from yesterday's primer and rode into two bus posts before I re-read the source. Live figure is **3** in the trailing 14-day window. It changed no conclusion, and the practice was still wrong.

### The alarm-title test failed once and would not reproduce — READ THIS BEFORE INVESTIGATING

`reverify.test.mjs`'s alarm-title leg failed during a routine gate run: expected `Drift: constants/2a.md`, got the generic `Re-verification failed: bounds-ledger (2026-08-23)`. **Ten consecutive runs afterwards passed**, tree clean throughout. Tracked as `W-10`, read 09-22.

**Deliberately NOT diagnosed.** The fitting story is that the synthetic mutation into `2a.md` did not land — the CRLF trap — and it has precedent here, which is exactly why it is not written down as the cause.

**What IS established:** this is **not** the 2026-08-17 mode, where the guard returned an empty string and an absence assertion passed against nothing. Here the title was a real, well-formed fallback, so the alarm path ran and took the generic branch.

**If it recurs: capture the failing state BEFORE re-running.** A passing re-run destroys the evidence. Then check with `git diff --numstat` whether the mutation landed.

### State at close

**HEAD `137255f`, tree clean, in sync with origin, CI GREEN at that commit.** `npm run verify` exit 0; 16 of 16 self-tests. `continuity-check` **status OK**, zero WARN. Report linters **8 of 8 clean**. Dead-reference check clean (64 paths across 5 docs). All three continuity endpoints HTTP 200.

Mirror 113 files at upstream `e70b4a4` (an upstream sha; it does not resolve in this repo — `git cat-file -t` returns *not a valid object*). 233 claims: 231 hold, 2 UNVERIFIED (`C-7`/`C-9`, the `manual: true` erdosproblems pins, correct by design). Zero open issues, zero open PRs, zero Dependabot alerts.

**Items: 39 total, 19 open.** Two new watches today — `W-9` (does this lane's detection method find what the lane exists to find; the clock the orchestrator asked for on seven instrument-facing days) and `W-10` (above), both read 09-22.

**CLAUDE.md is 42,562 LF-normalized bytes against a warn threshold of 45,000 — UNDER, no compression needed.** Yesterday's "back under budget at 40,314" cited a number that is not the canonical threshold; the canonical check is `node ../skylark-site/scripts/check-claude-md-sizes.mjs`.

**Traffic: 3 unique viewers in the trailing 14-day window against 178 unique cloners.** The clones are largely our own CI, which checks the repo out daily, on every push and every PR — today had six pushes. The viewer count is the only arrival figure that is not mostly us, and the sampler states it as an upper bound.

### The honest label on today's catches

**All instrument-facing — the seventh consecutive day.** Last record-facing catch remains 2026-08-14. The standing prediction is **carried forward unchanged and still uncontradicted**: the next record-facing catch will be a witness-value mismatch on a constant upstream added within roughly 30 days, found by a human recomputing a cited certificate and flagged by no instrument we run. If one arrives through an alarm instead, the prediction was wrong — say so.

`W-9` now puts a clock on the question those seven days raise: read 09-22, three branches, one of which is that upstream simply went quiet and the window was empty. **Read the daily reports' classification sentences for that, not `docs/findings/`** — that is what `123dd12` corrected today.

### Nothing is owed to David

Board read fresh at close: no waiting, answered, or pending-verification card. No David-blocker.

## Session close-out — 17:2xZ (close orders)

Supersedes the earlier block only where noted; everything else there still stands.

**The Monday engineering-health wave does NOT touch this lane, and here is the evidence rather than the assurance.** The fleet wave runs Monday 2026-08-24 with the shared Atlas/Mongo database exposure first. This repo carries **zero dependencies and zero devDependencies**, no Supabase, no Firebase, no Mongo, and nothing deploys. A grep across the tree returns exactly two hits and neither is a usage: `docs/engineering-health-review-2026-07-29.md` recording R10 as N/A ("State is flat JSON plus a file mirror"), and a `mongodb://` pattern inside `history-sweep.mjs` — a secret-scanning regex that *detects* such URLs, which is a guard, not an exposure. **So your first action tomorrow is the ordinary one at the top of this file, not a wave task.** If someone tells you otherwise, ask which file they found.

**Engineering zero is structural here, not swept.** Zero dependencies means Dependabot has nothing to find; there is no Sentry project because nothing is deployed. Do not read those zeros as a clean-up you performed — they are properties of the repo, and the honest phrasing matters when the fleet gate asks.

**Retro was received and answered** — `0d25aef7`, posted 2026-08-23. Its Q0 is the part worth re-reading before you start: three of my own prior findings recurred today, all the same shape, and **both instances of "a correction that reaches one copy is not a correction" had the second copy sitting in a file I had already opened and edited.** Proximity is not protection. The first fix creates the feeling of being done, and that feeling is the defect.

**Two things carried to tomorrow's retro as portfolio candidates**, already accepted by the orchestrator: locate a test's subject by identity and never by ordinal (a positional match broke the moment the page grew upward); and the stub-DOM proof limit recorded beside its own assertion (`0698ab3`), so a PASS line cannot be misread as a browser-level guarantee.

**Nothing is waiting on David.** Board read fresh at close through `answered-cards.mjs`: no waiting, answered, or pending-verification card for this lane. Zero uncarded decisions — nothing was posted today that needed one.

**State at this close: HEAD `4714e32`, tree clean, in sync with origin, CI GREEN at that commit.** `npm run close:primer` PASS, `git status --porcelain` empty, `npm run verify` exit 0 with a receipt at that HEAD — the close battery in the order `docs/close-session-config.md` sets. Today's traffic sample is committed, which matters because that file is the only copy of anything outside the API's 14-day window.
