<!-- GENERATED — regenerating REPLACES everything outside the hand: markers, so put prose inside them. Provenance stamp at the foot of this block. -->

<!-- hand:begin banner -->
> **THIS LANE IS LAUNCHED** — David ruled it 2026-08-27 (*"agreed on bounds-ledger"*). Instruments a
> launched app has and this one does not — retention, active users, Sentry, revenue — are answered
> **"not an instrument on this lane, by design"**, never with a zero. The real reads are
> `npm run reports` (G-4's primary indicator) and `npm run traffic`.
>
> **The report path stays `docs/daily/<date>-prelaunch.md`.** Historical filename, not a lifecycle
> claim; the fleet registry resolves to exactly it. Do not helpfully fix it.
>
> **THE MATHSTODON POST IS CLOSED, UNSENT, AND MUST NOT BE RE-PROPOSED.** David answered 2026-08-28:
> make it an Uncaged Minds post, drafted by that agent. **`A-15` and `A-37` are BOTH CLOSED** —
> `A-37` closed 2026-08-29 on the orchestrator's acknowledgement (`d00cce6d`). The draft lives with
> the uncaged-minds lane as the Substack piece *"The Sentence Nobody Can Check"* (their commit
> `d5b9e3c` — an uncaged-minds repo sha; it does not resolve in this repo), **HELD for David's send**.
> Nothing is owed by this lane: do not draft it, do not card him, do not reopen the question.
>
> **TODAY'S SECTION A IS ALREADY CHOSEN: `A-38`.** On a phone the `looks wrong?` link — the control
> `G-4` is measured through — sits 365px past the right edge of a 390px screen, behind a horizontal
> scroll nothing cues. Measured 2026-08-29 against the live published page; the literal numbers are in
> the item's `readCommand`, so you start from evidence rather than a dig, and its positive control
> means the same probe reports a fix as well as the defect. Three candidate fixes are in `note4`, none
> chosen. **The desktop layout is currently correct and must not regress.**
>
> **The browser check is NOT blocked, and never was.** `playwright-cli` is installed machine-wide and
> needs no Chrome extension: `playwright-cli open <url>` then `resize 390 844` then `eval`. The Chrome
> MCP extension is still not connected; it was simply never the only route. Believing otherwise cost
> `A-36` two days. The preference for playwright-cli lives in **skylark-site's** `AGENTS.md`, not this
> lane's — grepping our own returns nothing.
<!-- hand:end banner -->

### First action — bounds-ledger, 2026-08-30

Every line in the block below is a COMMAND, not a claim. A command re-reads at the
moment you run it and cannot go stale; a state line written last night can.

```bash
node ../skylark-site/scripts/update-david-board.mjs --list --json --full-ids --project bounds-ledger
git rev-parse HEAD origin/main
git rev-list --count origin/main...HEAD
npm run verify > tmp/verify-out.txt 2>&1
node ../skylark-site/scripts/check-ci-status.mjs --workflow reverify.yml
```

#### Owed gates — 1 due on/before 2026-08-30

This is the whole LIST, not a selection — an omission from a curated subset carries
no information, because set-aside and never-seen look identical to you.

- `A-38` (expectedSignalBy=2026-08-30, open) — On a phone, G-4 detection path is 365px off-screen behind a horizontal scroll nothing cues
  - read: `playwright-cli open https://u00dxk2.github.io/bounds-ledger/ then playwright-cli resize 390 844 then playwright-cli eval to read the left edge of the looks-wrong? link against the 390px viewport. LITERAL OUTPUT 2026-08-29 when filed, so a future run has something to compare against rather than a verdict to trust: wrapperClientW 348, wrapperScrollW 1032, hiddenToRight 684, firstColRight 274, sourceLinkLeft 702, flagLinkLeft 755, flagLinkVisibleNow false, scrollNeededToSeeFlag 365, openCiteHeight 405, bodyScrollW 390, docScrollW 390. POSITIVE CONTROL that the probe can report the fixed state as well as the broken one: flagLinkVisibleNow reads true whenever the link left edge is under 390, so the same command that reports the defect today reports its absence after a fix -- it is not an assertion that can only fail.`

#### What shipped since 24 hours ago

- commit `8f4a7ff` — A-36 closed on the read: the blocker was never real, and the read found a defect on the Tier-0 path
- commit `ed1b369` — A-36's reason for not carding David expired when he answered the competing card
- commit `1830c45` — Rotation 7 recorded, and Rotation 6 had never been written into the flow doc
- commit `b1a73bd` — The page is named after citing a number and gave the reader nothing to cite
- commit `f859acd` — P2 continuity: G-4's zero was never a 31-day zero, and three commits owned no item

#### State — as of 2026-08-29T19:15:20.782Z (re-run the release command; never trust the value)

- **HEAD**: NOT READ at generation (stale by construction — the close-out commit lands after generation) — release: `git -C . log -1 --format=%h%x20%s`
- **working tree**: NOT READ at generation (stale by construction — the close-out commit lands after generation) — release: `git -C . status --porcelain`
- **ledger**: 51 item(s): 31 closed · 20 open · as of 2026-08-29T19:15:20.782Z — release: `node ../skylark-site/scripts/gen-primer-first-action.mjs --repo . --target-date 2026-08-30 --json`
- **CI**: NOT READ at generation (a morning question) — release: `node ../skylark-site/scripts/check-ci-status.mjs --workflow reverify.yml`
- **deploy**: NOT READ at generation (a morning question) — release: `node ../skylark-site/scripts/check-deploy-state.mjs`

<!-- hand:begin narrative -->
## Session close-out — 2026-08-29 ~19:20Z

### The one thing to understand about today

The page is **titled** *"Is the number you cited still current?"*, `docs/key-user-flows.md` states the
core problem as *"I cited a number"*, and it defines the visitor as someone who **quotes a record**.
Until today the page gave that person nothing to quote. Five rotations of the F-2 spot-check flow all
improved *reading* a row — finding it, dating it, ordering it, labelling it, disputing it — and not one
served the citation, which is the action the reader actually came to perform and the only one that
carries this ledger's work to anybody else. It is also the only `G-4` arrival shape (*someone cites the
public page*) that had zero affordance behind it.

Fixed in `b1a73bd`: every row carries a `cite` disclosure with both bounds, the upstream mirror sha,
each side's change label, and the row's own permalink.

**The design decision to preserve: the caveat lives INSIDE the citation.** A citation is the only
artifact on that page that *leaves* the page, so every honesty the page supplies in context — these are
last-listed table rows, this is a snapshot at a pinned sha — is stripped the moment the text is pasted
somewhere else. It travels in the text or it does not travel at all. A ledger whose product is catching
other people's stale citations must not become a source of them.

### Load-bearing ships

- `b1a73bd` — **user-visible.** The citation block above. Native `<details>` plus `user-select:all`, no
  JavaScript; a clipboard button was declined as gold-plating at 3 viewers a fortnight. **No "retrieved
  on" date, deliberately** — the page regenerates on commit and cannot know when it was last read, so
  the most conventional line in a citation would have been the dishonest one.
- `8f4a7ff` — **`A-36` closed on a real read, and the blocker was never real.** See the banner. What
  held at 390×844: body does not scroll horizontally (390 against 390), filter and Order control both
  fit, 111 citation blocks live. What did not: the `looks wrong?` link at x=755. Filed as `A-38`.
- `f859acd` — **`G-4`'s zero was never a 31-day zero.** Arrival detection has only been correct since
  `888a1e5` on 2026-08-28; before that an issue filed from the page's loudest link scored
  `arrivalKind() === null` by construction. The 2026-09-26 read now asks for clean-detection days (29),
  not days-since-flip (49). Window corrected, measure untouched.

### Tomorrow's first action

Run the declared block at the top of this file, then the steward cadence, then **`A-38` as Section A**.
One gate is due: `A-38` itself.

### CI

**`reverify.yml`: GREEN @ `1830c45`** — 1 completed non-scheduled success, 0 failures, 0 pending, read
at close. Run-level verdict; read the JOB layer before trusting it in either direction during a
platform outage.

### What NOT to touch

- **The record-claim guard in `render-site.mjs`** (`doesNotMatch(/is the record|current record|best
  known bound is/i)`). It rejected today's first draft of a *disclaimer* containing "current record" —
  it cannot parse negation, and that bluntness is the feature. The caveat was reworded; the guard was
  not loosened. Do not make it context-aware.
- **The `manual: true` → UNVERIFIED-never-green mechanism** on `C-7`/`C-9`.
- **The generated primer's `hand:` markers and the command-not-claim first-action fence.** Named in
  today's retro as the one artifact in the cold start that cost nothing.

### Watch items maturing soon

`A-38` — **2026-08-30 (tomorrow)**. `A-2` (the standing drift-resolution log) and `A-32` (the lane
brief's voice check) — **2026-08-31**. `A-18` (a generated state block in the primer) and `A-20`
(whether the fetch layer should retry with backoff) — **2026-09-01**. `A-33`, `A-34`, `A-37` —
**2026-09-02**.

**`A-18` is pre-staged and is a CLOSE, not a dig:** the `--primer` target shipped, and this very file
carries a generated `#### State` block with five release commands.

### Tier-0 status and the evangelism read

**The ONE core problem, in the user's words:** *"I cited a number. I have no idea if it's still the
current one."*

**The would-tell-a-friend moment:** *"the ledger caught a drift the field's own index missed."*

**`G-4` = 0**, a MEASURED zero: `npm run reports` fetched 26 issues, attributed all 26 to us, parts
reconcile (26 + 0 + 0 = 26). Day 21 since the public flip — but **29 days of clean detection** is the
honest denominator from tomorrow's arithmetic, and that distinction is now recorded on the item.

**Retention / word-of-mouth instrument:** *not built, and deliberately not being built.* At 3 unique
viewers per fortnight a return-rate or share-rate would manufacture a number rather than measure one.
Do not write a retention rec you cannot read.

**Advocacy signal this session:** none.

### Stuck-ask inventory

- **DOABLE-NOW** — `A-38`. Evidence gathered, three candidate fixes listed, positive control in place.
- **MISTAKEN** — none today, because the one that qualified was fixed: `A-36` waited two days on a
  David action that was never required.
- **REAL** — `A-37` (the Uncaged Minds hand-off; un-stuck by one bus follow-up if unacknowledged by
  2026-09-02).
- **UNVERIFIABLE-ON-OUR-SIDE** — `A-29` (GitHub silently ignores the validity-checks PATCH; the
  paid-entitlement explanation is a hypothesis with no positive control) and the **reply leg** of `W-3`
  (we cannot read a maintainer's inbox). `W-3`'s *page* leg IS ours and ran today: HTTP 200, unchanged.

### Supersedes

Supersedes the 2026-08-29 primer's read on `A-36`, which is now CLOSED — and specifically supersedes
its claim that the phone check was blocked on David connecting the Chrome extension. It was not.

---

## Session close-out — 2026-08-29 ~21:55Z (close-session)

**Supersedes the block above on `A-37`, and on the ledger counts: 19 open of 51, not 20 of 51.**

### A dead `readCommand` kept an item open for a day after it was told to close

Checking the bus at close found that `A-37`'s `readCommand` cited
`agent-status.mjs --project bounds-ledger --list 2>&1 | grep -i uncaged`. **`agent-status` has no
`--list` flag — it is write-only** and refuses unknown flags at exit 2. The command also pipes into
`grep`, so the exit code a reader sees is grep's: a total failure of the read renders as *"no reply
found"*. Both of this lane's oldest defects in one line — a documented command nobody ran, and an
exit code read through a pipe.

**What it hid:** the orchestrator posted `d00cce6d` on 2026-08-28 saying verbatim *"A-37's gate
condition is met; close it on your next boundary."* That sat unread for a day, and today's P4
stock-take carried `A-37` as Tier-B aging-on-signal dated 2026-09-02 — **true of the clock, false of
the state.** A stock-take can classify a row correctly against its date and still be wrong about it.

`A-37` is now closed with the evidence recorded, and its `readCommand` is replaced by a PIN-gated GET
against `/api/cc/agent-msg` that was **run before being written down** (HTTP 200, 50 rows, 7 hits).

### The lesson generalises past this row

**A `readCommand` is a claim, and this lane has now been bitten by an unrun one three times**
(2026-08-18 the primer's `git rev-parse`, 2026-08-25 `A-18`'s month-wide glob, tonight `A-37`). The
cheap habit: when you *write* a `readCommand`, run it and paste the literal output plus exit code
into the field. Tonight's replacement carries exactly that, plus a positive control showing it can
return zero.

### Also fixed tonight

- **`.gitignore` gained `.playwright-cli/`.** Today's phone-width read left a 257KB page snapshot
  untracked in a **public** repo. The ignore list already covered `.playwright-mcp/` — the rule
  covered one route to the artifact and I arrived by a second one, which is the same shape as
  `A-36`'s blocker. Nothing was ever committed; the close gate requires an empty `git status`.

### CI

**`reverify.yml`: GREEN @ `0258da4`** — 1 completed non-scheduled success, 0 failures, 0 pending,
read at close against the actual current HEAD.

### Nothing is waiting on David

Board clear and the awaiting set drained — `answered-cards` reports no unread David-word for this
lane, the awaiting GET returned **HTTP 200 with 0 rows**, and `check-unpromoted-decisions` is clean.
Both are real reads, not 401s skimmed as empty.
<!-- hand:end narrative -->

<!-- generated by gen-primer-first-action.mjs (skylark-site) · lane bounds-ledger · for 2026-08-30 · at 2026-08-29T19:15:20.782Z -->
