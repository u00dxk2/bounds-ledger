# Cold-start primer for 2026-08-01 (MT)

Multi-session primer — each `/close-session` appends a new section below. Later sections supersede earlier
ones explicitly.

You are the bounds-ledger agent. Read this, then `CLAUDE.md`, then `continuity/items.json`. This lane is a
**steward** of drifting mathematical records — a ledger that alarms when a cited record moves. Lite rail,
pre-launch, private repo. **No self-rating** (no `/rate-day`).

**This primer deliberately does not restate `CLAUDE.md`.** The conventions, the gotchas, and the
never-do-this list live there and are current.

## Session close-out — 2026-07-31

Full detail: `docs/daily/2026-07-31-prelaunch.md` (4/4 report linters PASS).

### State

**227 claims / 225 hold / 0 broken / 2 UNVERIFIED** over a **110-file** mirror, clean at upstream `c4f8386`
(the `teorth/optimizationproblems` sha in `manifest.json` — not a local commit). **G-1 green-streak: day 8 of
30** as of 7/31 (day 1 = 7/24); a green run on 8/01 makes it **day 9**, ending ~2026-08-22. Tree clean on
`main` at `ff476c2`, pushed, CI green (run `30633757959`). No issues, PRs, or deploys.

**North star — externally-acknowledged corrections: 0.** Sent 2026-07-24; sending is not acknowledgement.
Confirmed by a **real** local advisory fetch on 7/31 (residential IP, HTTP 200 on both pins): the bound
`0.380876` and `This page was last edited 23 January 2026.` are both still present. The maintainer has not
touched the page, seven days post-send. Things requiring David: **1** (forward a reply, if one arrives).

Both UNVERIFIED claims are `manual: true`, both the same blocked page, both by design: **C-7** pins the bound,
**C-9** pins the edit date. Two is correct — David ruled on 7/26 that it should be left as is.

### Today's ship

| Commit | What |
|---|---|
| `ff476c2` | KP-78 audit: pipefail guard's fire side demonstrated for the first time; built the guard that asserts every `npm test` self-test also runs in CI; finding doc; watch `W-4`; CLAUDE.md convention |

**Nothing in `ledger/` moved** — no `--snapshot`, so no post-snapshot re-pin ratchet is owed.

### Tomorrow's first-action

**Read the overnight run's LOG, not the badge** (`gh run list --limit 3`, then `gh run view <id> --log`). Expect:

- `No drift. 110 files match upstream c4f83863…`
- `227 claim(s): 225 hold, 0 broken/unreachable, 2 unverified (manual).`
- **TWO** `advisory fetch failed (HTTP 403)` lines, one under C-7 and one under C-9.

Green → say so, note the streak (day 9), move on. Then run `npm run check` locally — on a residential
connection that does a **real** advisory fetch of the page for both C-7 and C-9 (CI structurally cannot).
Redirect to a file and check `$LASTEXITCODE`; never read the exit code through a pipe.

**Second — the carried item, now two days stale.** The hosted brief at
`https://skylarkcreations.com/t/lanes/bounds-ledger` returns 200 but does **not** carry the **28 July** block
("the alarm went off for real, twice"), which has been in `docs/lane-brief.md` since `2fd740b` (7/29). Found
7/30, bus note posted; found again 7/31, escalated as a repeat failure. **Verify before acting** — fetch the
page and grep the response; do not trust this primer's claim. If it has landed, say so and drop it. If it has
not, this is now a third-day failure and worth naming to David directly rather than a third identical bus note.
**Never hand David an artifact link** — that is the 7/24 lesson.

#### If something is red, or a local advisory says NOT FOUND

Unchanged and still the highest-signal branch in the lane:

- **C-9 NOT FOUND locally** → *the maintainer edited the page.* Hand-verify, then follow **W-3's `onTrigger`**
  in `items.json` step by step. Do not re-pin the new date until the state is recorded as a finding — the old
  date is the evidence.
- **C-7 NOT FOUND** (the January bound gone) → check whether the bound is now the curated one. If so **that is
  the north-star event.** W-3 `onTrigger`, then tell David.
- **Red on C-8** → problem 36's entry moved in `teorth/erdosproblems`. Read the upstream commit, re-verify,
  re-pin deliberately.

### New standing rule as of today — read before shipping any detector

**`W-4` / KP-78: prove the instrument can fail.** Ship no test, guard, lint, probe, `--check` mode or alarm
without demonstrating at write time that it returns BOTH answers — fires when the condition is present, silent
when absent — and put both outputs in the finding or commit. **Prove the mutation landed with
`git diff --numstat` before believing any negative control**: a control written with `\n` against CRLF files
silently no-ops, which is exactly how a working pipefail guard got pronounced broken on 2026-07-25.

The 7/31 audit's most valuable output was not a fixed detector but a **missing** one — A-8's own fix (a
hand-added CI step) had nothing asserting it stays. **Apply the rule to recent commits, not just to files that
look like tests.** Full account: `docs/findings/2026-07-31-kp78-prove-the-instrument-can-fail.md`.

### Process note

No primer existed for 2026-07-31 — the 7/30 session shipped its report but not the primer, so the 7/31 agent
reconstructed carry-forward from the report. Cost was minutes, not correctness, but the primer is the designed
cold-start path and its absence is silent. This file exists so that does not repeat.

---

## Session close-out — 21:10Z

**Supersedes the block above on two points:** (1) the hosted-brief check is now AUTOMATED, so tomorrow's
first-action no longer includes hand-fetching that page; (2) the brief is stale by **two** blocks since
**26 July**, not one block since 29 July — the earlier account was wrong and is corrected below.

### Today's load-bearing ships

| Commit | What |
|---|---|
| `ff476c2` | KP-78 guard — every self-test in `npm test` must appear in the workflow (derived from `package.json`, so a new self-test cannot be CI-less) |
| `5776c40` | Both detector blind classes named; A-10 constants sweep closed |
| `90f4c8e` | `check-brief.mjs` — a drift check on our OWN hosted brief |
| `02b263e` | That check moved from npm-only to a scheduled CI job that surfaces via an issue |
| `4c7a62a` | Daily report (4/4 linters) + this primer |
| `a4beb54` / `6be4646` | Continuity: A-10 logged, A-11 corrected |

**CI:** reverify: completed/**success** @ `6be4646`. **G-1 green-streak day 8 of 30** (ends ~8/22).
Nothing in `ledger/` moved — no `--snapshot`, so no re-pin ratchet is owed.

### ⚠ TODAY'S CORRECTIONS — the part that is NOT in git

Each is something believed this morning and disproved by evening. Read before acting on anything above.

1. **The hosted brief is TWO blocks stale (26 July AND 28 July), since 26 July** — not one block since 29 July.
   I reported the wrong version twice, 7/30 and 7/31. **Why both hand-checks under-reported is the transferable
   part: each searched the page for the block the PRIMER named, so each could only ever find that one.** A
   hand-check that inherits its search term from a doc can only confirm that doc. The detector derives the
   expected set from `docs/lane-brief.md` and inherits nothing.
2. **A check wired only into `npm run check` is not a detector.** I shipped `check-brief.mjs` that way and
   justified it as deliberate. The reasoning was sound — CI-wired it goes permanently red, this lane's founding
   defect — but the mechanism was wrong. **A good reason does not make a manual check automatic.**
3. **"Can it fire?" and "can it see the failure that matters?" are DIFFERENT questions.** Both detectors here
   passed KP-78 and both were blind to a class this lane exists to catch.
4. **A closed finding is not a closed defect class.** A-5 was closed as fixed; its exact failure mode was still
   reachable through a different input shape — a row reorder fired the alarm with a totally empty body.
5. **Apply KP-78 to recent FIXES, not just the test directory.** Auditing existing tests would have scored
   3-of-4 and missed the real hole: A-8's own fix was a hand-added CI step with nothing asserting it stays.
6. **A-11 was never a David-blocker** — no card was ever created, so it was a question we never sent. Defaulted
   to (c), documented. Do not re-raise it as blocked on him.

### Tomorrow's first-action

**Read the overnight run's LOG, not the badge** (`gh run list --limit 3`, then `gh run view <id> --log`). Expect
`No drift. 110 files match upstream c4f83863…`, `227 claim(s): 225 hold, 0 broken/unreachable, 2 unverified
(manual)`, and **two** `advisory fetch failed (HTTP 403)` lines. Green → say so, note streak **day 9**, move on.

Then `npm run check` locally — on a residential IP that does a **real** advisory fetch of erdosproblems.com/36
for both C-7 and C-9, which CI structurally cannot. Redirect to a file and check `$LASTEXITCODE`; never read an
exit code through a pipe.

**Do NOT hand-check the brief page any more** — the `brief` CI job does it daily. Just read whether issue #6 is
still open.

### Held for a fresh session — briefs (do not half-start these)

**(i) Fetch timeout on the four network call-sites.** WHAT: no `AbortSignal` exists anywhere
(`reverify.mjs:29,46`, `check-claims.mjs:55,69`), so a hung upstream inherits the Actions job cap and the alarm
reports nothing for hours while looking healthy. PROOF: fire — a fixture server that never responds must exit
non-zero with a stated reason rather than hang; silent — a normal run unaffected. **TRAP: latent, not
observed** — nine days of scheduled runs never hit it, and a too-tight timeout turns a merely-slow upstream into
a red drift alarm, which is worse than the disease. Choose generously and write down what it was measured
against.

**(ii) Pin-a-negative for the two STRUCTURED off-mirror sources (A-11).** WHAT: `check-claims` is a presence
test, so it sees a pin disappear but never an ADDITION — a source that keeps the pinned text and adds a better
bound below it stays green forever. WHERE: C-6 (Wikipedia raw wikitext) and C-8
(`teorth/erdosproblems:data/problems.yaml`) — assert no row/field exists below the cited one. PROOF: fire — a
fixture with a row added below the pin; silent — the unchanged source. **TRAP: do NOT attempt this for C-4/C-5
(arXiv prose) or C-7/C-9 (erdosproblems.com, the CI-blocked 403 surface)** — those stay at (c) permanently. The
blind class is already named on `holds()` with a passing selftest assertion: **that test passing IS the defect,
not the fix. Do not "fix" the assertion.**

### Journey-walk verdict: **N/A — no product surface exists**

Not a PARTIAL, not a skip. Pre-launch: no app, no users, no flow to walk. The only outward surface is the
one-page brief, which is stale and now has its own alarm (A-12). **Walk contamination: none** — no walk was
run, so today's numbers are uncontaminated. When this lane does get a surface, the marking fix goes in *before*
the first walk, not after.

### Tier-0 / evangelism read

- **North star — externally-acknowledged corrections: 0.** Sent 7/24; sending is not acknowledgement. Confirmed
  untouched today by a real residential-IP fetch of both pins (HTTP 200, both strings present). Seven days out.
- **Core problem, in the user's words:** "I cited a number and I don't know if it's still true."
- **Would-tell-a-friend moment:** the alarm catches a record moving before the citer notices. It has fired for
  real twice (28 July). Distance: the repo is private with no public status surface, so nobody outside can
  experience it yet.
- **Retention / word-of-mouth instrument: not built, correctly** — there are no users. Leading indicator stays
  drift catches; lagging stays the external ack.

### Watch items maturing

- **W-3** — the acknowledgement watch on erdosproblems.com/36. `npm run check` reads both pins daily. Day 7, no
  movement.
- **A-12** — the hosted-brief alarm. **Issue #6 is the standing alarm; do not re-post a bus note each day.** If
  still red after **2026-08-07**, escalate to David as a real blocker (an outward artifact wrong for two weeks).

### What NOT to touch

- **Do not re-automate C-7 / C-9.** The 403 is IP-dependent; a local 200 proves nothing about CI. Tried
  2026-07-25, CI went red in four minutes. Only a green CI run is evidence.
- **Do not put `check-brief.mjs` into the reverify drift job** until the page first reads green — it would go
  red and stay red, and G-1's streak measures the ledger, not the porter.
- **Do not re-raise David's three settled questions** (wait on W-3; public comment approved in principle only;
  leave UNVERIFIED at 2 — decided 7/26), and do not re-raise A-11 as blocked on him.
- **Do not "fix" the blind-class assertion in `check-claims.mjs --selftest`.** It passes on purpose.
