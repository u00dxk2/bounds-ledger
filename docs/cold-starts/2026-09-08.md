<!-- GENERATED — regenerating REPLACES everything outside the hand: markers, so put prose inside them. Provenance stamp at the foot of this block. -->

<!-- hand:begin banner -->
> **KEEP THIS BLOCK UNDER 2000 CHARS.** The kickoff quotes it whole below that and cuts the MIDDLE
> above it, so a long banner loses its own instructions while keeping its head and tail.
>
> **PULL, THEN START THE GATE, BEFORE READING THIS DOCUMENT** — three minutes, all network:
> ```
> git pull --ff-only
> npm run verify > tmp/verify-out.txt 2>&1
> ```
> Named by COMMAND, never by line number. This banner used to say "START LINE 4"; on 09-07 the pull was
> inserted above it and that pointer went stale on the very edit that fixed what it described.
>
> **THE GATE HAS TWO REDS AND ONLY ONE IS PRE-EXCUSED. READ THE RECEIPT, NOT THE EXIT CODE.** Exit 3
> with `check:brief` as the ONLY entry in `failedGates` is `A-41` and is NOT yours. Any other code, or
> exit 3 with a second leg, is real and IS yours. `tmp/.verify-receipt.json` names the leg — read that
> field, never infer it. Clearing condition: if it ever exits 0 the auth model changed and `A-41` moved.
>
> **FOUR GATES ARE DUE TODAY — `A-20`, `A-29`, `A-34`, `A-41`.** `A-34` is the David-facing one: a
> proposal to send, not a dig, and it has already been re-dated once.
>
> **BATCH LEDGER EDITS, THEN RUN THE GATE ONCE.** `check:deferrals` reads `continuity/items.json`, so
> ANY ledger edit puts your change inside a verify leg's blast radius and a receipt from before it
> cannot say which leg is red. What batching buys, stated so the two numbers are comparable: on 09-07
> the MID-DAY window cost ONE run across four commits instead of four. The DAY still cost three —
> the opening first-action gate, that batched window, and the close battery — because those bookends
> are the rail's, not batching failures. An earlier draft of this line set 09-06's day total against
> 09-07's window count and read as a drop from three to one; it was not.
>
> **DO NOT QUOTE `check-resolvability`'s PERCENTAGE.** It prints its own UNCALIBRATED banner; the MISSES
> list is the output. Calibration is dated 2026-09-13 and owned by this lane.
>
> **A red drift run is the alarm working.** Verify against PRIMARY SOURCES before `--snapshot`, then
> `npm run resnap`, then a SECOND `render-site.mjs` AFTER the commit.
<!-- hand:end banner -->

First command (also below, in place):

```bash
git pull --ff-only
```

### Primer — bounds-ledger, 2026-09-08

What ONLY this primer holds comes first; the FULL runnable **First action** block
sits at the foot, below the reads. The single command above is the same block's
first line, repeated so it can be running while you read — it replaces nothing.

_**Composed against HEAD commit `91fedef22beb1176fd5873f901b5f05fee7ca254` at 2026-09-07T16:42:57.507Z.** Nothing that landed after that instant is in this file. David's board answers especially: an answer he gives later cannot be here, and this primer will go on reading as though the work were not owed — that is exactly how two lanes concluded an OTA was not owed on 2026-09-04. Read them live, never off this page: `node ../skylark-site/scripts/answered-cards.mjs --project bounds-ledger --full`._

#### Owed gates — 4 due on/before 2026-09-08

_Ledger read at 2026-09-07T16:42:57.507Z by `cc-due-gates.collectDue` — the same resolver `check-due-gates-dispositioned` uses, so any difference is TIMING, not two answers. Rows filed or re-dated after that are NOT READ at generation — the close-out commit lands after this, so the COUNT above is stale by construction and this list cannot claim completeness. The list below, not the count, is the artifact; re-derive both before acting: `node ../skylark-site/scripts/check-due-gates-dispositioned.mjs --print --today=2026-09-08`._

Unfiltered as of the read time above — nothing was curated out of what the read could
see; a row absent here may simply have been filed after it ran.

- `A-20` (expectedSignalBy=2026-09-08, open) — Should the fetch layer retry with backoff on 429/502? — separate from the unreachable-stay
  - read: `gh api repos/u00dxk2/bounds-ledger/actions/runs/32157521934/attempts/1 --jq .conclusion # prints failure. This is issue #25's originating attempt, the row that sat on the N_min=3 discriminator, and resolving it IS the decision this item registered. It is invisible to gh run list because that reports only the LATEST attempt and attempt 2 succeeded. Positive control that the endpoint reports real per-attempt conclusions: the same path with attempts/2 prints success on the same run id.` · last run: 2026-09-01T15:11:33.299Z
- `A-29` (expectedSignalBy=2026-09-08, open) — secret_scanning_validity_checks will not enable via API — likely a paid Secret Protection 
  - read: `gh api repos/u00dxk2/bounds-ledger --jq '.security_and_analysis.secret_scanning_validity_checks.status' # SEPARATOR CONVERTED 2026-09-02, em dash to hash, because this gate fires 2026-09-08 and the runner would otherwise execute the whole 900-byte field instead of the command. Returns disabled while unshipped, enabled when done. Positive control that the probe reads a real field: the same query with .secret_scanning.status returns enabled, so a disabled reading is the setting's state and not a missing key or a silent jq failure.` · last run: NEVER
- `A-34` (expectedSignalBy=2026-09-08, open) — David asked where else this ledger can have the kind of impact G-3 just produced — bring h
  - read: `node scripts/reverify.mjs --check # SEPARATOR CONVERTED 2026-09-01 at pre-stage, em dash to hash. The grep leg was DROPPED from the executed command and its result recorded here instead: grep -c 'ledger/' continuity/items.json returns 21 on 2026-09-01, and the count is prose-inflated rather than structural, so it never discriminated one surface from two. The command that answers this row is the check, whose first line names the mirrored surface and its file count. Literal output 2026-09-01 14:30Z: No drift. 113 files match upstream commit <upstream sha elided 2026-09-07 — reprint it with: node scripts/reverify.mjs --check> (an upstream teorth/optimizationproblems commit; it does not resolve in this repo -- git cat-file -t on its short form reports Not a valid object name, while the same command on a local commit returns commit) -- ONE surface. A second adopted surface shows up as a second mirror directory under ledger/ and a second line here. POSITIVE CONTROL that the check reads a real mirror rather than reporting an empty set: the file count must be non-zero and match the manifest; 113 today.` · last run: 2026-09-02T15:24:11.838Z
- `A-41` (expectedSignalBy=2026-09-08, open) — The brief leg became permanently unverifiable on 2026-09-04 — should it keep contributing 
  - read: `node scripts/check-brief.mjs # exits 3 BRIEF UNVERIFIABLE for as long as the Google-session wall stands. A 0 means the auth model changed back or the leg was re-scoped, and either way the question in this row has moved. POSITIVE CONTROL that the script reached the network rather than failing early: its output names the redirect target /t/signin?next=%2Ft%2Flanes%2Fbounds-ledger, a string it can only print from a real response — an early failure prints no redirect at all.` · last run: NEVER

#### What shipped since 24 hours ago

_Read at 2026-09-07T16:42:57.507Z; commits landing after that — the close-out especially — are not here. Re-derive: `git log --oneline --since='24 hours ago'`._

- commit `91fedef` — A-45's calibration gets a date that lands BEFORE the verdict, not after it
- commit `bd9d420` — A-44's close account moves into the field 87 consumers actually read
- commit `b5bd53d` — A reader holding 117/370 typed it in and the page said we do not track it
- commit `dd42146` — The field I was told to move it into is not read either
- commit `f1855c3` — fix(hooks): resolve the shared scripts through git's common dir, and gate staged SHAs at commit time
- commit `a3c4744` — Close-out 2026-09-06: tomorrow's gate had its readCommand edited today, so run it before leaning on it
- commit `d5f211b` — The report of record, tomorrow's primer, and the two lessons that cost three gate runs
- commit `0bac107` — A-34's onTrigger was telling its next reader the proposal was due on a day already past
- commit `e31b4ff` — A-45's readCommand stops saying the thing it names does not exist
- commit `36a6b77` — The new indicator's first run found a value the paper proves and our page could not find
- commit `06f5b46` — A-42's own probe would have reported the shipped fix as a broken probe
- commit `13a8f72` — The guards I added to prove the fix could not themselves fail
- commit `5f9f021` — The page's most repeated sentence was the one claim we could not support
- commit `444d565` — The indicator was approved the same morning it was proposed, and the row says it does not exist yet
- commit `fa5a45b` — The drift leg this lane can never pass, written down with what would falsify it

**Yesterday's report:** `docs/daily/2026-09-07-prelaunch.md` (coverage 2026-09-07, the prior day). No findings fence declared in it.

**Today's report (owed):** write `docs/daily/2026-09-08-prelaunch.md` — the artifact of record for service day 2026-09-08. Check it exists at close: `node ../skylark-site/scripts/check-daily-report-exists.mjs --date 2026-09-08` (it sweeps the fleet and applies each lane's coverageOffset; it takes no --project).

#### State — as of 2026-09-07T16:42:57.507Z (re-run the release command; never trust the value)

- **ledger**: 58 item(s): 37 closed · 21 open · as of 2026-09-07T16:42:57.507Z — release: `node ../skylark-site/scripts/gen-primer-first-action.mjs --repo . --target-date 2026-09-08 --json`

#### Id glossary — 23 id(s) this primer's day already has words for

_Each gloss is the ledger row's OWN title, not a re-derivation — paste it. Rows = every open ledger row plus every id named above. `(no ledger row)` means the id is named here and the ledger carries nothing under it: that is a finding, not a formatting gap._

- `A-2` — STANDING: drift-resolution log for the adopted surfaces…
- `A-7` — engineering-health P1s — four of five shipped 2026-08-09;…
- `A-9` — engineering-health P2 backlog (fix-on-touch, not scheduled…
- `A-20` — Should the fetch layer retry with backoff on 429/502? —…
- `A-29` — secret_scanning_validity_checks will not enable via API —…
- `A-31` — Should the public page publish be CI-gated? It is not, and…
- `A-33` — A change to a file we have filed an upstream report…
- `A-34` — David asked where else this ledger can have the kind of…
- `A-39` — A mid-run 429 becomes up to 339 requests in two seconds at…
- `A-41` — The brief leg became permanently unverifiable on…
- `A-42` — Two user-facing readers still key off OUR pin history and…
- `A-43` — Upstream moved Haugland's minimum-overlap bound to its…
- `A-44` — The verify receipt names the WRONG leg, not merely a vague…
- `A-45` — TIER-1 LEADING INDICATOR (orchestrator-approved…
- `G-2` — GOAL: contribute a verified bound improvement to the…
- `G-3` — GOAL (Tier-0 from G-1 close): someone outside Skylark uses…
- `G-4` — GOAL (Tier-0 from G-3 close): an outside party acts on a…
- `W-3` — Watch for acknowledgement of the erdosproblems.com/36…
- `W-4` — KP-78 applies to every new detector: demonstrate BOTH…
- `W-6` — Read window for the README report-an-error channel — the…
- `W-8` — Does a materially amended outward artifact go back to…
- `W-9` — Does this lane's detection method find what the lane…
- `W-10` — reverify.test.mjs alarm-title leg failed once and would…

### First action — bounds-ledger, 2026-09-08

Every line in the block below is a COMMAND, not a claim. A command re-reads at the
moment you run it and cannot go stale; a state line written last night can.

```bash
git pull --ff-only
node scripts/sky.mjs update-david-board.mjs --list --json --full-ids --project bounds-ledger
git rev-parse HEAD origin/main
git rev-list --count origin/main...HEAD
npm run verify > tmp/verify-out.txt 2>&1
node scripts/sky.mjs check-ci-status.mjs --workflow reverify.yml
```

Morning release commands — deliberately NOT READ at generation (a value written
last night would already be stale); run each, never trust a remembered one:
- `git -C . log -1 --format=%h%x20%s` — HEAD
- `git -C . status --porcelain` — working tree
- `node ../skylark-site/scripts/check-ci-status.mjs --workflow reverify.yml` — CI
- **deploy read: NOT AVAILABLE** — no read-only deploy-state script in this lane's package.json (looked for ops:deploy-state, deploy-state, check-deploy, render-deploys, deploy:status, daily-snapshot). The fleet's check-deploy-state.mjs is not a substitute — it requires --fix <sha> --deploy <sha> and exits 1 without them

<!-- hand:begin narrative -->
_(orchestrator narrative — this region survives regeneration)_
<!-- hand:end narrative -->

<!-- generated by gen-primer-first-action.mjs (skylark-site) @ commit e589f2c2b · lane bounds-ledger · for 2026-09-08 · at 2026-09-07T16:42:57.507Z -->
<!-- writtenAtSha: `91fedef22beb1176fd5873f901b5f05fee7ca254` — the lane's HEAD commit when this primer was written. Provenance anchor for check-primer-freshness leg ii; NOT a claim about HEAD now (the close-out commit that carries this file lands after generation). -->
<!-- reading this block: a generated line that is wrong in a way you can see FIXED in skylark-site's git log after the commit above is STALE, not a finding — regenerate before filing it. `git -C ../skylark-site log --oneline e589f2c2b..HEAD -- scripts/gen-primer-first-action.mjs src/lib/cc-primer-commands.mjs src/lib/cc-due-gates.mjs src/lib/cc-report-artifact.mjs src/lib/cc-hand-edit-merge.mjs src/lib/cc-cold-readability.mjs src/lib/cc-skeleton-gloss.mjs` -->
