# Cold-start primer for 2026-08-12 (MT)

Multi-session primer — each /close-session run appends a new section below. Later sections
supersede earlier ones explicitly.

## Session close-out — 18:30Z (2026-08-11, ~12:30 MT)

You are the bounds-ledger agent. Read this, then `AGENTS.md` + `CLAUDE.md`, then
`continuity/items.json`. The repo is PUBLIC (since 2026-08-08); A-13 is closed.

### Tomorrow's first action

1. **Steward cadence, first and in full.** `gh run list --limit 1`, then **read the LOG, not the
   badge**. Expect `No drift. 112 files match upstream dee1660…` (an UPSTREAM sha — do not
   `git show` it locally), `231 claim(s): 229 hold, 0 broken/unreachable, 2 unverified (manual)`
   — **231/229, up from 229/227 today** — and **11** selftest PASS lines. Green → **streak day 20
   of 30**.
2. `npm run check` locally **with `CC_PROMPTS_PIN` set** — the only run that gives a real advisory
   read on the two manual claims.
3. **`npm run traffic`** — GitHub keeps only 14 days; a skipped fortnight is lost permanently.

**Then the increment, and it is already chosen: the FINDABILITY MENU.** The orchestrator approved
the direction today (bus `393e9ac0`, recorded mid-session in `docs/cold-starts/2026-08-11.md`) and
**this supersedes the standing "pick an instrument-hardening increment" guidance.** Deliver a menu
of concrete findability moves, **each marked with its gate: agent-doable vs David-send.** Outward
contact of any kind — a new PR, an email, a forum or comment post — stays **David-gated**; propose,
do not send. The orchestrator routes from the menu.

The reasoning to carry in, because it is the whole argument: two of the last four sessions hardened
detection while the north star sat at 0. The instrument is now good enough that **arrival, not
detection, is the binding constraint.** Nobody can acknowledge a correction from a repo they have
never heard of.

G-2 round four remains available as the standing technical increment, but it is NOT tomorrow's
lead. Round four needs a family that buys colour more cheaply per edge than the joins rounds one
through three all built.

### Today's load-bearing ships

- **`7018cce` — A-11 CLOSED, and it closed the lane's other blind class.** `check-claims` was a
  presence test, so it could only ever see a pin DISAPPEAR; a source that kept our pin and added a
  better bound underneath stayed green forever — the north-star event, invisible on exactly the six
  off-mirror sources where that checker is the only instrument. Claims may now carry
  **`nothingAfter`**: the text that must still immediately FOLLOW the expected string. Plain
  concatenation, so the matcher is unchanged. Two new hand claims: **C-10** (Wikipedia's SimpleTES
  `0.380868` row + the table terminator) and **C-11** (the TAIL of problem 36 in
  `teorth/erdosproblems`, closing the gap C-8 leaves by stopping at the entry's head). Ledger
  229 → 231 claims.
- **`29f8acd` — the daily report**, 7/7 linters PASS, BLUF voice-clean.
- **`3a7f408` — the orchestrator's findability ruling**, appended to today's primer so it survives
  the session that received it.

### CI

**`reverify`: success @ `3a7f408`** — verified by conclusion AND read by log on each of today's
three pushes. Local `main` and `origin/main` agree. Tree clean.

### What changed about how you work here

- **A claim can now assert an ABSENCE.** `nothingAfter` is the mechanism; `classify()` in
  `check-claims.mjs` is where the verdict is decided, returning `hold` / `added` / `gone`. A break
  on a negative pin reports **"SOMETHING WAS ADDED BELOW IT"** and names it the north-star event
  rather than reporting a vanished pin and sending the reader hunting for a deletion that never
  happened.
- **A negative pin still asserts LISTING POSITION, never "the record"** — the same discipline the
  generated pins follow. Do not read C-10 as our claim that `0.380868` is the true record.
- **W-4 got a stronger form of demonstration, worth copying.** The selftest drives the SHIPPED
  `classify()`, not a copy of its logic, so the branch that is tested is provably the branch that
  runs. Then a live-bytes control fetched both real sources, spliced an entry below each pin, and
  **aborted if the splice failed to land** — the CRLF no-op trap, guarded rather than assumed.
- **This clone had no upstream tracking branch**, so the first `git push` failed and read exactly
  like a broken remote. Fixed (`git branch --set-upstream-to=origin/main main`). Worth knowing
  because this repo has a protection ruleset, and "push rejected" here has two very different
  causes.

### The one method lesson worth carrying

**Closing the easier half of a blind class is where the remaining half quietly disappears.** Leg (b)
was only ever possible on the two STRUCTURED sources; C-4/C-5 (arXiv prose) and C-7/C-9 (the 403
site) can never take a negative pin. The temptation on shipping the fix is to let the selftest's
blind-class assertion go, since the case it described is now handled. It was KEPT and reworded to
describe the four sources that are still blind — so the instrument still names what it cannot see.
An assertion that documents a permanent gap is not dead weight; deleting it is how a documented
limitation becomes an undocumented one.

### REVENUE PATH

**There is no paying-customer path from this lane as it exists today, and the honest distance is
two proof steps, not a pricing decision.** What could eventually be sold is not the maths — it is
the mechanism: continuous re-verification of a claim inventory that drifts, which is a problem ML
benchmark leaderboards, standards tables and vendor "state of the art" pages all have. The shortest
credible route runs: one public catch a stranger actually sees → someone asks us to steward *their*
inventory → that engagement is paid.

**What exists on that path commercially: nothing.** No pricing, no paywall, no checkout, no landing
page, no offer of any kind. What does exist is the machine — a working drift alarm over 112 mirrored
files and 231 pinned claims, four real upstream drifts caught and resolved, and a public repo since
8 August.

**The single blocking gap, named concretely: nobody has arrived.** One unique viewer in four days
public, and zero externally-acknowledged corrections. A service whose value has never been
demonstrated to a single outside person cannot be priced, so the gap is arrival first and one
acknowledged catch second — which is exactly what tomorrow's findability increment is aimed at.
Pricing work done before those two would be fiction.

### Evangelism read, honestly

The ledger *is* the product and it still has no users. The would-tell-a-friend moment would be a
stranger finding a record we caught moving before their own source did — nobody has arrived to have
it. Retention/word-of-mouth instrument: the traffic sampler, reading **1 unique viewer** on **day 4
of W-6's 90**. Advocacy signal this session: none. Today widened what we can catch, which is
upstream of the north star but is not the north star.

### Tier 0 / binding constraint

**North star: 0 externally-acknowledged corrections. UNCHANGED, and nothing today moved it.**
PR **#141** open, 0 comments, 0 reviews, untouched since 8/05 — **check, do not poll, do not nudge**
(his standing word). The July email is unanswered; W-3's two advisory legs both read unchanged from
this machine today (page still shows the old number, still dated 23 January).

### Nothing is blocked on David

Zero awaiting rows (checked, HTTP 200, 0 rows) and zero board cards for this project (checked).
**Do not re-ask about the rewritten-history card — it was answered on 8/08 and any board pass still
listing it is stale.**

### What NOT to touch

- Do not re-automate C-7/C-9 — the erdosproblems 403 is IP-dependent, a local 200 proves nothing
  about CI, and only a green CI run is evidence.
- Do not put `CC_PROMPTS_PIN` in repo secrets. Public repo; load-bearing, not a precaution.
- Do not re-propose the public comment on /36 — decided NO on 2026-08-02, not a deferral.
- Do not send anything outward off your own bat: the findability menu is a PROPOSAL, and every
  channel move on it that touches a stranger is David-gated.
- Do not hand-edit the mirror (including its `README.md`) or the generated pins; do not weaken the
  27b verifier to make a candidate pass; do not claim any G-2 result without exact re-verify +
  literature check + adversarial review + David's gate.
- Do not allowlist `history-sweep.mjs`'s four hits or neuter its fixture — it exits 1 by design and
  shipped public that way, adjudicated in writing.
- Do not delete the selftest's blind-class assertion now that leg (b) has shipped — see the method
  lesson above.

### State at close

`main` at `3a7f408`, tree clean, local and origin agree, PUBLIC. 3 commits today. 11/11 self-tests.
CI green. Daily report `docs/daily/2026-08-11-prelaunch.md`, 7/7 linters PASS. Open continuity
items: **8** (A-11 closed today). Zero cards waiting on David. Codex calls: 0.
