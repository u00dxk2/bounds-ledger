<!-- GENERATED — regenerating REPLACES everything outside the hand: markers, so put prose inside them. Provenance stamp at the foot of this block. -->

<!-- hand:begin banner -->
> **PULL, THEN START THE GATE, BEFORE READING THIS** — three minutes, all network:
> ```
> git pull --ff-only
> npm run verify > tmp/verify-out.txt 2>&1
> ```
> Expect **exit 0**. Any non-zero code is yours; read `tmp/.verify-receipt.json`'s `failedGates`,
> never infer the leg.
>
> **DO NOT TRUST ANY ID LIST IN THIS BANNER, INCLUDING BELOW.** On 09-09 this block named a gate
> that was never due while the GENERATED block below had it right, and the kickoff repeated the
> banner's error as fact. Run the resolver:
> `node scripts/sky.mjs check-due-gates-dispositioned.mjs --print --today=2026-09-10`.
>
> **BOTH GATES ARE NOW DISPOSITIONED — this block said they were not, and was true only until
> 08:23Z.** Read at P2 on 2026-09-10 the resolver returned **0 due**. No clock time is given here
> because the exact stamp was not captured and inventing one is the defect this block exists to
> correct; run the release command instead of trusting the line:
> `node scripts/sky.mjs check-due-gates-dispositioned.mjs --print`.
> `A-31` is CLOSED (its read stamped 05:36:28Z, exit 0). `A-39` was ruled INAPPLICABLE by its own
> pre-registered onTrigger and re-dated to **2026-09-12** to follow `A-20`. Kept rather than deleted
> because the banner sits ABOVE the append point, so a later section cannot supersede it — a stale
> line here is the one thing this primer promotes hardest and cannot correct.
>
> **`A-39` WILL READ `NEVER-RUN` FOREVER AND THAT IS NOT A MISSED READ.** `git grep -c` exits 1
> with no output on no match — exactly that row's own answer — so the runner cannot tell a real
> read from a failed spawn. It becomes stampable only when the answer flips. Do NOT make it always
> emit output: that trades a false NEVER-RUN for a read that cannot fail, this repo's founding
> defect.
>
> **DAVID RULED DEPTH on 09-09 and it is the biggest thing here.** *"Go with depth — start reading
> our own 543 records against their sources on a schedule, and bring me the second-area comparison
> when it is ready."* **The store now EXISTS — this block said it did not, and that was true until
> P2 on 2026-09-10.** It is `continuity/depth-audit.json`, and it read 3 audited / 2 sound / 0
> defective / 1 unresolved / 0 unreachable when this was written —
> `node scripts/depth-audit.mjs` is the release command; run it rather than quoting the line above.
> **Do not quote that 2-of-3 as a rate:** the sample deliberately hunted discrepancy, and the script
> prints its own sampling rule above the count for exactly that reason. Still owed: the coverage
> figure stays UNBUILT until more rows are audited — that ordering was right and has not changed.
> `A-48` is the comparison promised him for **2026-09-16**; missing it silently repeats the pattern
> that made `A-34` slip four times.
<!-- hand:end banner -->

First command (also below, in place):

```bash
git pull --ff-only
# commits after this primer was written are invisible to it — read them before acting:
git log --oneline 80599ea3723703e7c0ad71a5342c2ac950b2c77c..HEAD
```

### Primer — bounds-ledger, 2026-09-10

What ONLY this primer holds comes first; the FULL runnable **First action** block
sits at the foot, below the reads. The single command above is the same block's
first line, repeated so it can be running while you read — it replaces nothing.

_**Composed against HEAD commit `80599ea3723703e7c0ad71a5342c2ac950b2c77c` at 2026-09-10T06:12:44.839Z.** Nothing that landed after that instant is in this file. David's board answers especially: an answer he gives later cannot be here, and this primer will go on reading as though the work were not owed — that is exactly how two lanes concluded an OTA was not owed on 2026-09-04. Read them live, never off this page: `node scripts/sky.mjs answered-cards.mjs --project bounds-ledger --full`._

_Shared skylark-site scripts below are spelled `node scripts/sky.mjs …` — this lane vendors `scripts/sky.mjs` (S-20260906-75), so a shared script is named LANE-RELATIVE and resolves from a worktree too._

#### Owed gates — 2 due on/before 2026-09-10

_Ledger read at 2026-09-10T06:12:44.839Z by `cc-due-gates.collectDue` — the same resolver `check-due-gates-dispositioned` uses, so any difference is TIMING, not two answers. Rows filed or re-dated after that are NOT READ at generation — the close-out commit lands after this, so the COUNT above is stale by construction and this list cannot claim completeness. The list below, not the count, is the artifact; re-derive both before acting: `node scripts/sky.mjs check-due-gates-dispositioned.mjs --print --today=2026-09-10`._

Unfiltered as of the read time above — nothing was curated out of what the read could
see; a row absent here may simply have been filed after it ran.

- `A-31` (expectedSignalBy=2026-09-09, open) — Should the public page publish be CI-gated? It is not, and a malformed page reached main t
  - read: `gh api repos/u00dxk2/bounds-ledger/pages --jq .build_type # QUOTES REMOVED 2026-09-09 and that is the whole fix: the gate runner strips the # comment and hands the rest to the win32 shell, which does not honor the single quotes around the jq expression -- the first execution through the runner, on this row's own due date, died with `failed to parse jq expression (line 1, column 1) ... unexpected token "'"` and STAMPED that parse error as if it were the gate's reading. The command had never been run through the runner in the 14 days it sat here, so nobody had learned it could not be. Unquoted it runs: literal output 2026-09-09, `legacy`, exit 0. SEPARATOR CONVERTED 2026-09-02, em dash to hash, because this gate fires 2026-09-09. Returns 'legacy' while ungated, 'workflow' once gated. Literal output 2026-08-26: legacy. POSITIVE CONTROL that the endpoint is live rather than erroring silently: the same object's .html_url returns https://u00dxk2.github.io/bounds-ledger/ and .status returns a build state; an empty or null build_type means the read failed, not that no gate exists.` · last run: 2026-09-10T05:36:28.538Z
- `A-39` (expectedSignalBy=2026-09-09, open) — A mid-run 429 becomes up to 339 requests in two seconds at the limiter already refusing us
  - read: `git grep -c RETRY_DELAYS_MS HEAD -- scripts/reverify.mjs # Asks ONE question: has the retry merged into committed main yet? COMMIT-SCOPED ON PURPOSE. The obvious form, a plain grep over scripts/reverify.mjs, reads the WORKING TREE and was actively misleading on the day this row was filed: an uncommitted Codex edit had the retry constants in the tree while committed main had none, so the plain grep printed three hits and the honest answer was zero. LITERAL OUTPUTS, both run 2026-09-02 at this row's creation. Against HEAD: no output at all, exit 1 — the retry is NOT in main. POSITIVE CONTROL, the same command against the branch that has it: git grep -c RETRY_DELAYS_MS a20-fetch-retry-backoff -- scripts/reverify.mjs prints a20-fetch-retry-backoff:scripts/reverify.mjs:3 at exit 0. So exit 1 here means absent-from-main, NOT a broken pattern or a wrong path — the control proves the pattern matches when the thing is there. When this command starts printing a count against HEAD, the retry has merged and this row is live.` · last run: UNRUNNABLE

#### What shipped since 24 hours ago

_Read at 2026-09-10T06:12:44.839Z; commits landing after that — the close-out especially — are not here. Re-derive: `git log --oneline --since='24 hours ago'`._

- commit `80599ea` — Daily report 2026-09-09 — David ruled depth, and the survey control stopped answering the wrong question
- commit `90481bc` — Two due gates had never been RUN, and running them broke both readers
- commit `70d3917` — David ruled depth; A-34 closes on the ruling, and the sweep found it live in one doc
- commit `e5ff23b` — A-34: the proposal shipped to David, and the row stays open pending his ruling
- commit `fa4b407` — Asked what moved recently, the page answered what we started watching recently
- commit `e8668a8` — G-4: the 09-26 read is bound to detection health; the 11-06 read is left for David
- commit `618226a` — The receipt could not see the one transition the banner tells you to watch for
- commit `dd05d3e` — W-6 traffic sample 2026-09-09 — 1 viewer/14d at day 33, down from 2 at day 27

**Yesterday's report:** `docs/daily/2026-09-09-prelaunch.md` (coverage 2026-09-09, the prior day). Findings fence EMPTY — the BLUF is the whole story; nothing below it supersedes anything.

**Today's report (owed):** write `docs/daily/2026-09-10-prelaunch.md` — the artifact of record for service day 2026-09-10. Check it exists at close: `node scripts/sky.mjs check-daily-report-exists.mjs --date 2026-09-10` (it sweeps the fleet and applies each lane's coverageOffset; it takes no --project).

#### State — as of 2026-09-10T06:12:44.839Z (re-run the release command; never trust the value)

- **ledger**: 61 item(s): 40 closed · 21 open · as of 2026-09-10T06:12:44.839Z — release: `node scripts/sky.mjs gen-primer-first-action.mjs --repo . --target-date 2026-09-10 --json`

#### Id glossary — 23 id(s) this primer's day already has words for

_Each gloss is the ledger row's OWN title, not a re-derivation — paste it. Rows = every open ledger row plus every id named above. `(no ledger row)` means the id is named here and the ledger carries nothing under it: that is a finding, not a formatting gap._

- `A-2` — STANDING: drift-resolution log for the adopted surfaces…
- `A-7` — engineering-health P1s — four of five shipped 2026-08-09;…
- `A-9` — engineering-health P2 backlog (fix-on-touch, not scheduled…
- `A-20` — Should the fetch layer retry with backoff on 429/502? —…
- `A-31` — Should the public page publish be CI-gated? It is not, and…
- `A-33` — A change to a file we have filed an upstream report…
- `A-34` — David asked where else this ledger can have the kind of…
- `A-39` — A mid-run 429 becomes up to 339 requests in two seconds at…
- `A-42` — Two user-facing readers still key off OUR pin history and…
- `A-43` — Upstream moved Haugland's minimum-overlap bound to its…
- `A-45` — TIER-1 LEADING INDICATOR (orchestrator-approved…
- `A-46` — A close-out post announcing a FIX is textually identical…
- `A-47` — THE DEPTH AUDIT, David-ruled 2026-09-09: read our own…
- `A-48` — The second-area citation comparison David asked for,…
- `G-2` — GOAL: contribute a verified bound improvement to the…
- `G-4` — GOAL (Tier-0 from G-3 close): an outside party acts on a…
- `S-20260906-75` — (no ledger row)
- `W-3` — Watch for acknowledgement of the erdosproblems.com/36…
- `W-4` — KP-78 applies to every new detector: demonstrate BOTH…
- `W-6` — Read window for the README report-an-error channel — the…
- `W-8` — Does a materially amended outward artifact go back to…
- `W-9` — Does this lane's detection method find what the lane…
- `W-10` — reverify.test.mjs alarm-title leg failed once and would…

### First action — bounds-ledger, 2026-09-10

Every line in the block below is a COMMAND, not a claim. A command re-reads at the
moment you run it and cannot go stale; a state line written last night can.

```bash
git pull --ff-only
node scripts/sky.mjs update-david-board.mjs --list --json --full-ids --project bounds-ledger
git rev-parse HEAD origin/main
git rev-list --count origin/main...HEAD
npm run verify > tmp/verify-out.txt 2>&1
node scripts/sky.mjs check-ci-status.mjs --workflow reverify.yml
```

Morning release commands — deliberately NOT READ at generation (a value written
last night would already be stale); run each, never trust a remembered one:
- `git -C . log -1 --format=%h%x20%s` — HEAD
- `git -C . status --porcelain` — working tree
- `node scripts/sky.mjs check-ci-status.mjs --workflow reverify.yml` — CI
- `node scripts/sky.mjs check-deployed-sha-drift.mjs --service bounds-ledger` — deploy
  - WHERE IT LOOKED: no read-only deploy-state script in this lane's package.json (looked for ops:deploy-state, deploy-state, check-deploy, render-deploys, deploy:status, daily-snapshot). The fleet's check-deploy-state.mjs is not a substitute — it requires --fix <sha> --deploy <sha> and exits 1 without them — so this is the FLEET read, which takes no required flags. If `--service bounds-ledger` does not name exactly one Render service the script exits 1; drop the flag and read the whole fleet.

<!-- hand:begin narrative -->
_(orchestrator narrative — this region survives regeneration)_
<!-- hand:end narrative -->

<!-- generated by gen-primer-first-action.mjs (skylark-site) @ commit f37cfafcc · lane bounds-ledger · for 2026-09-10 · at 2026-09-10T06:12:44.839Z -->
<!-- writtenAtSha: `80599ea3723703e7c0ad71a5342c2ac950b2c77c` — the lane's HEAD commit when this primer was written. Provenance anchor for check-primer-freshness leg ii; NOT a claim about HEAD now (the close-out commit that carries this file lands after generation). -->
<!-- reading this block: a generated line that is wrong in a way you can see FIXED in skylark-site's git log after the commit above is STALE, not a finding — regenerate before filing it. `git -C C:/dev/skylark/skylark-site log --oneline f37cfafcc^{commit}..HEAD -- scripts/gen-primer-first-action.mjs src/lib/cc-primer-commands.mjs src/lib/cc-due-gates.mjs src/lib/cc-report-artifact.mjs src/lib/cc-hand-edit-merge.mjs src/lib/cc-cold-readability.mjs src/lib/cc-skeleton-gloss.mjs` -->
