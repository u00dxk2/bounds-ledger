# Cold-start primer for 2026-07-30 (MT)

Multi-session primer — each `/close-session` appends a new section below. Later sections supersede earlier
ones explicitly.

You are the bounds-ledger agent. Read this, then `CLAUDE.md`, then `continuity/items.json`. This lane is a
**steward** of drifting mathematical records — a ledger that alarms when a cited record moves. Lite rail,
pre-launch, private repo. **No self-rating** (no `/rate-day`).

**This primer deliberately does not restate `CLAUDE.md`.** The conventions, the gotchas, and the
never-do-this list live there and are current.

## Session close-out — 20:41Z

Covers the whole of 2026-07-29 (one session, the /daily rail: P0 only).

### State this morning

**227 claims / 225 hold / 0 broken / 2 UNVERIFIED** across two stewarded surfaces, over a **110-file** mirror.
Clean at upstream `c4f8386` (the `teorth/optimizationproblems` sha in `ledger/.../manifest.json` — not a local
commit). **G-1 green-streak: day 6 as of 7/29** (day 1 = 7/24) — a green run on 7/30 makes it **day 7 of 30**,
ending ~2026-08-22. Working tree clean at `main`, up to date with origin. No open issues, PRs, or deploys.

**North star — externally-acknowledged corrections: 0.** Sent 2026-07-24; sending is not acknowledgement. As of
the 7/29 local read, erdosproblems.com/36 still shows the January bound and still says `last edited 23 January
2026` — **the maintainer has not touched the page**, six days post-send. Things requiring David: **1** (forward
a reply, if one arrives).

Both UNVERIFIED claims are `manual: true`, both the same blocked page, both by design: **C-7** pins the bound,
**C-9** pins the edit date. Two is correct — David said "leave it" on 7/26.

### Today's load-bearing ships

One commit, pushed, tree clean:

| Commit | What |
|---|---|
| `2fd740b` | The 7/29 daily (4/4 report linters PASS) **+ `docs/lane-brief.md` gains a dated 28 July block** on the alarm's first two real firings — needs an orchestrator re-port |

**Nothing in `ledger/` moved** — no `--snapshot`, so no post-snapshot re-pin ratchet is owed.

### Tomorrow's first-action

**Read the overnight run's LOG, not the badge** (`gh run list --limit 3`, then `gh run view <id> --log`). Expect:

- `No drift. 110 files match upstream c4f83863…`
- `227 claim(s): 225 hold, 0 broken/unreachable, 2 unverified (manual).`
- **TWO** `advisory fetch failed (HTTP 403) — expected from datacenter IPs (CI)` lines, one under C-7 and one
  under C-9.

Green → say so, note the streak (day 7), move on. Then optionally `npm run check` locally — on a residential
connection that does a **real** advisory fetch of the page for both C-7 and C-9 (CI structurally cannot).
Redirect to a file and check `$LASTEXITCODE`; never read the exit code through a pipe.

**Second, new today:** confirm the brief re-port landed — fetch
`https://skylarkcreations.com/t/lanes/bounds-ledger` and check it carries the **28 July** block ("the alarm went
off for real, twice"). If it doesn't, re-post the bus note asking the orchestrator to re-port `docs/lane-brief.md`.
Not David-gated. **Never hand David an artifact link** — that is the 7/24 lesson.

#### If something is red, or a local advisory says NOT FOUND

Unchanged from the 7/29 primer, and still the highest-signal branch in the lane:

- **C-9 NOT FOUND locally** → *the maintainer edited the page.* Hand-verify, then follow **W-3's `onTrigger`**
  in `items.json` step by step. Do not re-pin the new date until the state is recorded as a finding — the old
  date is the evidence.
- **C-7 NOT FOUND** (the January bound gone) → check whether the bound is now the curated one. If so **that is
  the north-star event.** W-3 `onTrigger`, then tell David.
- **Red on C-8** → problem 36's entry moved in `teorth/erdosproblems`. Read the upstream commit, re-verify,
  re-pin deliberately.
- **Red on a generated pin** → a bounds table moved: primary sources → deliberate `--snapshot` → **re-run
  `extract-pins.mjs` + commit** (the post-snapshot ratchet).
- **`Check error:` title** → transient fetch flake. Confirm next run green.
- **`Instrument failure:` title** → a self-test failed before the checks ran. The alarm reporting its own
  breakage is A-4 working as designed.

### CI

**`reverify`: completed/success @ `2fd740b`** — conclusion observed, then the **log** read for the scheduled run
(`30442566840`): `No drift. 110 files match upstream c4f83863…` + `227 claim(s): 225 hold, 0 broken/unreachable,
2 unverified (manual).` + both self-tests PASS + the two expected advisory-403 lines. Not a badge read.

**What that bought:** yesterday's two absorptions are now confirmed by an *independent* scheduled run, not just
by the commit that made them. A-2's `closeWhen` ("drift detection demonstrated") was satisfied 7/28; today it
survived a full cycle.

### Decisions David ALREADY MADE — do not re-raise

Unchanged; he answered all three in-pane on 7/26.

1. **W-3's close date: he said WAIT.** `signalDate` **2026-09-24 is a CHECK-IN, not a deadline.** Closing the
   watch requires his decision, not a date.
2. **The public comment on erdosproblems.com/36: "yes but wait."** Approved *in principle*, **not now**; needs
   his explicit go plus a pre-send read. It would be this lane's **first genuinely public act**.
3. **UNVERIFIED at 2: "leave it."**

Also standing: **do not compress `continuity/items.json`** absent his word (optional call surfaced 7/28; my
recommendation was leave it, so the default on silence is do nothing).

### Pending decision-blocked items

**None new.** The single David-side action is unchanged and passive: forward a maintainer reply if one arrives.

### Active in-flight work

**None.** No Codex jobs (`codexCalls: 0`), no deploys, no open PRs, no open issues. One thing is *with the
orchestrator*, not with us: the `docs/lane-brief.md` re-port (see first-action).

### Watch items maturing within 24h

**None.** W-3's next check-in is 2026-09-24 and is **not** a deadline.

### What NOT to touch

Everything in `CLAUDE.md` § Gotchas is current — all four traps still bite: **CRLF silently no-ops `\n`
string-replaces** (`git diff` to prove a test mutation landed before believing a result); **never read an exit
code through a pipe**; **test a capability from the environment that will exercise it** (a local 200 proves
nothing about a runner); **`git status` is not a content diff** (check `--numstat` before believing it disagrees
with the drift report). Plus, specific to now:

1. **Do not re-automate C-7 or C-9** on the strength of a local 200. Tried 7/25; CI went red in four minutes.
2. **Do not re-raise David's three 7/26 decisions**, and do not compress `items.json`.
3. **Do not treat a red `reverify` as automatically a record event** — both of 7/28's were editorial and both
   still required the full verify → snapshot → re-pin cycle.
4. **Do not "upgrade" the generated `86a` pins to record claims.** That page's own contribution notes say it was
   AI-assisted and its pre-2021 rows were not checked against primary sources. Our pins are honest only because
   they assert **listing position, never correctness**.
5. **Flow-craft rotation stays skipped** until F-2 (*spot-check a claim*) has a surface — and building one **is**
   the David-gated public-repo flip. Do not invent a critique to fill the slot.

### Tier 0 / evangelism read (pre-launch)

- **Core problem, user's words:** *"I cited a number. I have no idea if it's still the current one."*
- **Would-tell-a-friend moment:** the alarm catches a stale citation before the citer is embarrassed by it.
  **Distance from it:** the alarm now demonstrably fires on real upstream movement (7/28, twice) — but both
  catches were the curated table correcting *itself*, so the moment has still not occurred.
- **North star — externally-acknowledged corrections: 0.** Not moved. Day 6 post-send.
- **Retention / word-of-mouth:** no instrument **by design** — no users, no public surface, private repo. The
  honest proxy is the external-ack metric, already instrumented as W-3, so the move is *not* "stand an
  instrument up"; it is to keep the one surface David reads (the hosted brief) current, which today's ship did.
- **Advocacy signal this session: none** — correct for day 8 pre-launch.

### State at a glance

| Item | What | Status |
|---|---|---|
| G-1 | GOAL: steward a drifting record inventory | open — streak day 6/30 (ends ~8/22); external-ack **0** |
| A-2 | Re-verification CI | open — 227 claims, 2 surfaces; `closeWhen` met 7/28, closes with the 30-day streak |
| W-3 | Ack watch for the /36 correction | open — reply leg David's; both page legs mechanical, read green 7/29 |
| A-1 / W-2 / A-3 / A-4 / A-5 / A-6 | reconciliation, Haugland, correction sent, alarm fixes, second surface | closed 7/22–7/25 |

**Continuity: no edit was owed today** and none was written — no new item, no gate moved, and A-2's notes
already carry both 7/28 drifts. Don't go looking for a missing entry.

**Supersedes:** the 7/29 primer's "Tomorrow's first-action" paragraph, which still expected the pre-drift
counts (`225 claim(s): 223 hold`, mirror `937f4e5`). The correct expectation is **227 claims / 225 hold at
`c4f8386`, 110 files** — as stated above.

---

## Second close-out — 04:20Z (engineering-health fleet review)

An orchestrator dispatch landed after the section above was written. **This supersedes that section's
"Continuity: no edit was owed today and none was written … Don't go looking for a missing entry."** An edit
*was* owed and three items were filed: **A-7, A-8, A-9**. Two commits also landed after the table above:
`2b8a438` (the review doc) and `d89e10f` (rename sweep + housekeeping ledger).

**The three things tomorrow-you needs:**

1. **The review is `docs/engineering-health-review-2026-07-29.md`** — this repo audited against
   `skylark-site/docs/engineering-health-standard.md` v1.0 (34 rules). Counts: **P0 0 · P1 6 · P2 6**.
   Carried as **A-7** (five fleet-shared control gaps — blocked on a coordinated wave, do NOT fix per-repo),
   **A-8** (the one lane-specific P1, actionable immediately), **A-9** (P2 fix-on-touch backlog).

2. **P0 state: there is no P0.** Not "fixed", not "held" — the review found zero, and that is a real finding
   rather than a gap in the review: this repo has no datastore, no service endpoint, no build output, no
   deploy, and no secret has ever been committed (checked across all 51 commits, not assumed). Do not go
   looking for a P0 fix to verify. **A-8 is the one thing here you can close tomorrow** — add a
   `node scripts/extract-pins.mjs --selftest` step to `reverify.yml`; the pipefail-guard hazard is already
   checked and cleared, see the item.

3. **The verdict sentence, which is the most useful thing the review produced:** *what would most alarm a
   skeptical staff engineer in their first 30 minutes here is not the code — it is that a repo whose founding
   finding is "an alarm that was never armed carries no information" ships a CI workflow that silently omits
   one of its own three self-tests.* That is A-8, and it is why A-8 is not just tidy-up.

**One live substrate fact, not repo state:** `/api/cc/*` GETs went PIN-gated on the evening of 7/29. Any
`listen.mjs` started *before* that gate keeps running but 401s on every reconnect — mine did, silently, for
~1h45m (last good SSE connect 02:08Z, first 401 ~02:23Z). Nothing in **this repo** calls the CC API, so there
is nothing here to fix; the symptom is a listener that looks alive in Task Manager while delivering nothing.
**If the bus goes quiet, check `tmp/.bus-listen.err` for 401 before assuming there is no traffic** — the fix
is restarting the listener with `CC_PROMPTS_PIN` in its environment.
