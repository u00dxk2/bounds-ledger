# Cold-start primer — 2026-07-24 (MT)

Lane day 3. You are the bounds-ledger agent. Read this first, then `CLAUDE.md`, then `continuity/items.json`. One-paragraph orientation: this lane is a **steward** — it keeps a reproducible, re-verified ledger of mathematical records and alarms when a cited record drifts. Day 1–2 stood the machine up; **today there is no queued build work**, and that is fine — the lane is in a watch/hold state. Do not invent feature work.

## First action today

**Check the overnight re-verification CI.** The `reverify` workflow runs daily at 09:17 UTC (≈03:17 MT), so a run fired before you woke up.

```
gh run list --limit 3 --json databaseId,status,conclusion,headBranch,createdAt
```

- **All green →** nothing to do on A-2; it is simply accruing days toward G-1's 30-day green-streak close. Say so and move on.
- **A run is red →** that is the drift alarm doing its job, not a bug. It auto-filed a GitHub issue with the drift/claim report. Triage: read the issue, re-verify the changed value against **primary sources** (the paper / Tao's table / Wikipedia — not just the diff), then either (a) it is a legitimate upstream record change → `node scripts/reverify.mjs --snapshot`, review the diff, and commit the updated ledger copy deliberately, or (b) it is suspicious → investigate before snapshotting. **Never re-snapshot to silence the alarm without verifying first.**

## Held for David — do NOT act without his gate

**A-3 — report erdosproblems.com/36 staleness upstream.** Staleness is hand-confirmed (David's screenshot 2026-07-23: page shows `0.379005 < c < 0.380876`, missing the current `0.380868`). The decision is filed and **recommends reporting upstream**, but the outward send is gated on (1) an adversarial refute-it review, then (2) David's outward-send gate. A copy-paste-ready draft correction sits in `docs/decisions/2026-07-23-A3-erdosproblems-report.md`. **Do not contact erdosproblems.com / Thomas Bloom without David's gate.** If the gate opens, run the adversarial review first.

## Don't-touch / standing rules

- **No self-rating.** Lite rail carries no self-rating/audit contract (David's standing call). Write the daily report + run the linters; do not run `/daily-close` scoring.
- **Don't hand-edit `ledger/`** — it is a machine mirror of 109 upstream files, refreshed only via `--snapshot`.
- **Extending claim-level pins** beyond the min-overlap constant (see the honest-gap note below) is *available* work but **not queued** — don't start it without a prompt.

## State at a glance

| Item | What | Status |
|---|---|---|
| G-1 | GOAL: steward ≥1 drifting record inventory w/ re-verification CI | open — 30-day green-CI clock started 2026-07-23 |
| A-1 | Reconcile the min-overlap discrepancy (customer-zero) | **closed** 2026-07-22 — `docs/reconciliations/2026-07-22-minimum-overlap.md` |
| A-2 | Re-verification CI for the adopted surface | **live + green**; closes on the scheduled streak |
| A-3 | Report erdosproblems.com/36 staleness upstream | open — confirmed, decision filed, **held for David gate** |
| W-2 | Haugland final-digit 0.380927 vs 0.380926 | **closed** 2026-07-23 — rounding convention, not a typo, no PR |

## Honest open gap (not a blocker)

Claim-level pins (`ledger/claims.json`) currently cover only the **one** min-overlap constant across 4 surfaces. The other ~108 constants in the adopted surface have **mirror-diff** coverage (change detection) but no claim-level value pins yet. Fine for now — min-overlap is the only constant with a live cross-surface discrepancy — but it is the natural next extension when build work is wanted.

Last commit at primer write: `758ece6`.
