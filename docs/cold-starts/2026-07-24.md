# Cold-start primer for 2026-07-24 (MT)

Multi-session primer — each /close-session run appends a new `## Session close-out` section below. Later sections supersede earlier ones on any overlapping item.

## Session close-out — 19:32Z (lane day 2, 2026-07-23)

You are the bounds-ledger agent. Read this, then `CLAUDE.md`, then `continuity/items.json`. One-paragraph orientation: this lane is a **steward** — it keeps a reproducible, re-verified ledger of mathematical records and alarms when a cited record drifts. Day 1–2 stood the machine up; **today there is no queued build work**, and that is fine — the lane is in a watch/hold state. Do not invent feature work.

### Today's load-bearing ships (all pushed to origin/main)

- `9ca5af7` — A-2 re-verification CI live: `scripts/reverify.mjs` + `.github/workflows/reverify.yml` (daily cron 09:17 UTC + push + manual); 109-file snapshot @ upstream `a002311`; network-free synthetic-drift self-test. Same commit **closed W-2** (Haugland 0.380927 is round-to-nearest, bound-safe — not a typo, no upstream PR).
- `7c0446a` — cross-surface claim checks: `scripts/check-claims.mjs` + `ledger/claims.json` (7 claims; 6 fetchable HOLD across Tao-at-HEAD/arXiv/ar5iv/Wikipedia; C-7 erdosproblems reports UNVERIFIED by design).
- `758ece6` — A-3 staleness hand-confirmed (David screenshot) + report-upstream decision filed, held for David's gate.
- `6056ffe` — housekeeping: this primer, `CLAUDE.md`, `package.json` (`npm test`/`npm run check`); untracked the linter cache; marked README first-artifact done.

**CI:** reverify: success @ `6056ffe` (green — nothing red crossing into tomorrow).

### Tomorrow's first-action

**Check the overnight re-verification CI.** The `reverify` workflow runs daily at 09:17 UTC (≈03:17 MT), so a run fired before you woke.

```
gh run list --limit 3 --json databaseId,status,conclusion,headBranch,createdAt
```

- **All green →** nothing to do on A-2; it is simply accruing days toward G-1's 30-day green-streak close. Say so and move on.
- **A run is red →** that is the drift alarm doing its job, not a bug. It auto-filed a GitHub issue with the drift/claim report. Triage: read the issue, re-verify the changed value against **primary sources** (the paper / Tao's table / Wikipedia — not just the diff), then either (a) legitimate upstream record change → `node scripts/reverify.mjs --snapshot`, review the diff, commit the updated ledger copy deliberately, or (b) suspicious → investigate before snapshotting. **Never re-snapshot to silence the alarm without verifying first.**

### Pending decision-blocked — do NOT act without David's gate

**A-3 — report erdosproblems.com/36 staleness upstream.** Staleness hand-confirmed (page shows `0.379005 < c < 0.380876`, missing the current `0.380868`). Decision filed and **recommends reporting upstream**, gated on (1) an adversarial refute-it review, then (2) David's outward-send gate. Copy-paste-ready draft correction sits in `docs/decisions/2026-07-23-A3-erdosproblems-report.md`. **Do not contact erdosproblems.com or its maintainer without David's gate.** If the gate opens, run the adversarial review first. (No `⚠ requires David at the pane` build-gate suffix applies — this is an outward-contact decision, not a mobile/EAS/secret gate.)

### Active in-flight work

None building. A-2 just accrues green-streak days. No Codex jobs, no deploys pending.

### What NOT to touch tomorrow + why

- **Don't hand-edit `ledger/`** — machine mirror of 109 upstream files, refreshed only via `--snapshot`.
- **Don't post self-ratings** — lite rail carries no self-rating/audit contract (David's standing call). Write the daily report + run the linters; skip `/daily-close` scoring.
- **Don't start extending claim-level pins** beyond the min-overlap constant without a prompt — it is available work, not queued (David held new feature work 2026-07-23).

### Binding-constraint / evangelism read (North Star)

The portfolio binding constraint is **retention + word-of-mouth (product-love), not the acquisition funnel**. This lane's analog: the would-tell-a-friend moment is *"the ledger caught a drift the field's own index missed."* (1) Core problem: record surfaces drift faster than observers track them. (2) Distance to the moment: the instrument now exists and has one confirmed real catch (erdosproblems.com/36 is two curated records behind) — but it has produced **0 externally-acknowledged corrections** so far, because the one outward report (A-3) is held for David's gate. (3) Metric = drift catches + external acknowledgements; instrument is live as of 2026-07-23, value 0 external acks. (4) No external advocacy signal yet (pre-launch, private repo). The move toward beloved is: get A-3's correction gated + sent, which would be the first external acknowledgement.

### State at a glance

| Item | What | Status |
|---|---|---|
| G-1 | GOAL: steward ≥1 drifting record inventory w/ re-verification CI | open — 30-day green-CI clock started 2026-07-23 |
| A-1 | Reconcile the min-overlap discrepancy (customer-zero) | **closed** 2026-07-22 — `docs/reconciliations/2026-07-22-minimum-overlap.md` |
| A-2 | Re-verification CI for the adopted surface | **live + green**; closes on the scheduled streak |
| A-3 | Report erdosproblems.com/36 staleness upstream | open — confirmed, decision filed, **held for David gate** |
| W-2 | Haugland final-digit 0.380927 vs 0.380926 | **closed** 2026-07-23 — rounding convention, not a typo, no PR |

### Honest open gap (not a blocker)

Claim-level pins (`ledger/claims.json`) currently cover only the **one** min-overlap constant across 4 surfaces. The other ~108 constants have **mirror-diff** coverage (change detection) but no claim-level value pins yet. Fine for now — min-overlap is the only constant with a live cross-surface discrepancy — but it is the natural next extension when build work is wanted.
