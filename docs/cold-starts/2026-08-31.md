<!-- GENERATED — regenerating REPLACES everything outside the hand: markers, so put prose inside them. Provenance stamp at the foot of this block. -->

<!-- hand:begin banner -->
> **THIS LANE IS LAUNCHED** — David ruled it 2026-08-27 (*"agreed on bounds-ledger"*). Instruments a
> launched app has and this one does not — retention, active users, Sentry, revenue — are answered
> **"not an instrument on this lane, by design"**, never with a zero. The real reads are
> `npm run reports` (`G-4`'s primary indicator) and `npm run traffic`.
>
> **The report path stays `docs/daily/<date>-prelaunch.md`.** Historical filename, not a lifecycle
> claim. Confirmed by command on 2026-08-30 rather than from memory: `check-daily-report-exists --date
> 2026-08-30` names exactly that file for this lane across 21 registered lanes. Do not helpfully fix it.
>
> **THE MATHSTODON POST IS CLOSED, UNSENT, AND MUST NOT BE RE-PROPOSED.** `A-15` and `A-37` are both
> closed; the draft lives with the uncaged-minds lane, held for David's send. Nothing is owed here: do
> not draft it, do not card him, do not reopen the question.
>
> **`A-38` IS CLOSED — the phone fix shipped 2026-08-30 (`ee19ee9`, closed `dd23ddd`).** Do not reopen
> it and do not re-run its probe as though it were still owed. If you touch the public page's layout,
> the numbers to hold are in `A-38 note6`: at 390×844 `flagLinkVisibleNow` must read **true** and
> `hiddenToRight` **0**, and at 1440×900 the `::before` labels must resolve to `"none"` so they do not
> leak onto desktop.
>
> **TWO GATES ARE DUE TODAY, and the second is the one that matters.** `A-32` — the lane brief — is
> the only artifact this lane writes *for David*, it fails its voice check, and it predates the `G-3`
> evidence. It is the stalest thing we own and today is its date.
>
> **Write the State Appendix LAST, from live commands, with an as-of and a release command on every
> value.** This is not style. A report cannot name the commit that lands it — proven 2026-08-30, where
> `8bcacd0` is the commit that created the 08-29 report and lands one commit after the `8f4a7ff` that
> report's appendix names as HEAD. Yesterday's report is the worked example.
>
> **`git rev-parse --short HEAD origin/main` exits 128.** Two revisions plus `--short` is the failure;
> drop `--short`. Documented 2026-08-18, re-encountered live 2026-08-30 at 16:07Z.
<!-- hand:end banner -->

### First action — bounds-ledger, 2026-08-31

Every line in the block below is a COMMAND, not a claim. A command re-reads at the
moment you run it and cannot go stale; a state line written last night can.

```bash
node ../skylark-site/scripts/update-david-board.mjs --list --json --full-ids --project bounds-ledger
git rev-parse HEAD origin/main
git rev-list --count origin/main...HEAD
npm run verify > tmp/verify-out.txt 2>&1
node ../skylark-site/scripts/check-ci-status.mjs --workflow reverify.yml
```

#### Owed gates — 2 due on/before 2026-08-31

_Read at 2026-08-30T16:22:23.323Z — the close-out commit lands after this, so a date added or dropped at close is not reflected here. Re-derive before acting: `node ../skylark-site/scripts/check-due-gates-dispositioned.mjs --print`._

This is the whole LIST, not a selection — an omission from a curated subset carries
no information, because set-aside and never-seen look identical to you.

- `A-2` (nextCheckDate=2026-08-31, open) — STANDING: drift-resolution log for the adopted surfaces (re-verification CI shipped 2026-0
  - read: `node scripts/reverify.mjs --check — the drift alarm itself. Literal output 2026-08-25: "No drift. 113 files match upstream 5c4aeee95699d72e1603d04d0987b1a7b6097535." (that 40-hex token is an UPSTREAM teorth/optimizationproblems commit; it does not resolve in this repo), exit 0. Any non-zero exit means a cycle is owed and this row is the log for it. Positive control that the command can say otherwise: reverify.test.mjs plants synthetic drift in 10a.md every run and asserts it is detected, so the detecting path is exercised on every npm test.`
- `A-32` (expectedSignalBy=2026-08-31, open) — docs/lane-brief.md fails its voice check and predates the G-3 evidence — the one artifact 
  - read: `node ../skylark-site/scripts/check-david-voice.mjs docs/lane-brief.md ; echo EXIT=$? — literal output 2026-08-26: '3 blocking, 2 advisory', exit 3, naming drift/claim/bound. Exit 0 closes the voice half. POSITIVE CONTROL that the checker reads this file rather than erroring silently: the same run prints the resolved path 'C:/dev/skylark/bounds-ledger/docs/lane-brief.md' in its header line; a run naming no path did not read the file and its verdict is void. The CONTENT half is not machine-checkable — grep -c '857.567' docs/lane-brief.md returns 0 today and a non-zero result is evidence the G-3 outcome reached the brief, but a rewrite could describe it without that figure, so treat the grep as a hint and read the file.`

#### What shipped since 24 hours ago

- commit `0d52bb2` — P4: the days report, and the first State Appendix written from live commands
- commit `dd23ddd` — A-38 closes on the live page, and a selftest picked the fix
- commit `ee19ee9` — A-38: on a phone the Tier-0 detection path was 365px off-screen; the table now stacks into cards
- commit `af9b17e` — P2 continuity: a report cannot name the commit that lands it, so its State Appendix is stale by construction
- commit `a8a6344` — AGENTS.md compression: loaded 64,479 -> 59,551 (fleet budget 60,000) - history moved byte-verbatim to docs/agents-md-archive.md
- commit `c278854` — A-37 closed a day late because its readCommand could never have run
- commit `0258da4` — Hygiene bookkeeping: link the A-36 close and the capability-note commits
- commit `8bcacd0` — Hygiene 2026-08-29: report, tomorrow primer, and the capability note that cost A-36 two days
- commit `8f4a7ff` — A-36 closed on the read: the blocker was never real, and the read found a defect on the Tier-0 path
- commit `ed1b369` — A-36's reason for not carding David expired when he answered the competing card
- commit `1830c45` — Rotation 7 recorded, and Rotation 6 had never been written into the flow doc
- commit `b1a73bd` — The page is named after citing a number and gave the reader nothing to cite
- commit `f859acd` — P2 continuity: G-4's zero was never a 31-day zero, and three commits owned no item

**Yesterday's report:** ⚠ no docs/daily-reports/ under C:/dev/skylark/bounds-ledger — not "no report exists"; this is a failed lookup.

#### State — as of 2026-08-30T16:22:23.323Z (re-run the release command; never trust the value)

- **HEAD**: NOT READ at generation (stale by construction — the close-out commit lands after generation) — release: `git -C . log -1 --format=%h%x20%s`
- **working tree**: NOT READ at generation (stale by construction — the close-out commit lands after generation) — release: `git -C . status --porcelain`
- **ledger**: 51 item(s): 33 closed · 18 open · as of 2026-08-30T16:22:23.323Z — release: `node ../skylark-site/scripts/gen-primer-first-action.mjs --repo . --target-date 2026-08-31 --json`
- **CI**: NOT READ at generation (a morning question) — release: `node ../skylark-site/scripts/check-ci-status.mjs --workflow reverify.yml`
- **deploy**: NOT READ at generation (a morning question) — release: `node ../skylark-site/scripts/check-deploy-state.mjs`

<!-- hand:begin narrative -->
## Session close-out — 2026-08-30 ~16:20Z

### The one thing to understand about today

The page is titled *"Is the number you cited still current?"* and its whole purpose is that a stranger
who spots a wrong row can tell us. On a phone they could not: the `looks wrong?` link — the control
`G-4` is measured through — sat at x=755 on a 390px screen, 365px past the edge, behind a horizontal
scroll with nothing cueing the gesture. That is fixed (`ee19ee9`). Rows now stack into cards below
56rem, which is the table's own `min-width`, so the breakpoint sits exactly where horizontal scroll
would otherwise begin rather than at a number someone picked.

**The finding to carry forward is not the CSS. It is that a selftest picked the fix.** `A-38` listed
three candidates. Candidate (a) — move the controls into the first cell for phones — means rendering
them twice and hiding one copy by media query, and `render-site.mjs` asserts one report link per row
(`flagLinks.length === rows.length`). It fails. **The right answer to a verifier rejecting a candidate
is a different candidate, never a weakened assertion.** This lane has applied that rule to mathematical
claims for weeks; this is the first time it decided a design question.

### Load-bearing ships

- `ee19ee9` / `dd23ddd` — **user-visible.** The phone card layout, verified on the LIVE published page
  after the Pages build reported `built` at 15:46:09Z. At 390×844: `flagLinkVisibleNow` false→**true**,
  `flagLinkLeft` 755→**87**, `hiddenToRight` 684→**0**. At 1440×900, desktop unregressed with
  `::before` content resolving to `"none"`. Negative control: breakpoint moved to 20rem reproduced the
  original failure exactly, `numstat`-proved before the verdict was believed.
- `af9b17e` — **a report cannot name the commit that lands it.** `8bcacd0` created the 08-29 report and
  lands one commit *after* the `8f4a7ff` its appendix names as HEAD, so the staleness is structural,
  not carelessness. The 08-29 report now carries a correction fence; `docs/daily-config.md` line 4
  carries the measured runtime; `G-4 note7` carries the viewport axis.
- `0d52bb2` — **the first State Appendix written from live commands**, every value with its read time
  and its release command. Use it as the template.

### The day's work, in order

Run the declared block above, then the steward cadence, then **`A-32` as the day's work**. Two gates are due:
`A-2` (the standing drift-resolution log — read the alarm, log a cycle only if it went red) and `A-32`.
`A-32` is the real one: the lane brief is the only artifact written *for David*, it fails its voice
check, and it predates the `G-3` evidence.

### One generated line above is a failed lookup, not a fact

The **Yesterday's report** line in the generated block reads *"⚠ no `docs/daily-reports/` under
…bounds-ledger"*. That directory has never existed here — this lane's reports live in `docs/daily/`,
and yesterday's is `docs/daily/2026-08-29-prelaunch.md`. The generator is a skylark-site script looking
in a path this lane does not use, and it is **not** telling you no report exists; it says so itself,
which is the instrument behaving well. Do not go looking for a missing report. Reported to the
orchestrator as a fleet-side row on 2026-08-30; nothing for this lane to fix.

### What NOT to touch

- **The `manual: true` → UNVERIFIED mechanism on `C-7`/`C-9`.** Today's runs are the argument: both
  advisory fetches returned HTTP 200 with their expected strings present, and both still printed
  UNVERIFIED. It refuses to convert a local success into a green.
- **The one-report-link-per-row assertion in `render-site.mjs`.** It rejected a candidate fix today and
  was right to. Do not relax it to enable a mobile duplicate.
- **The `--short`-free form of `git rev-parse HEAD origin/main`** in the first-action block.

### Watch items maturing soon

`A-2` and `A-32` — **today**. `A-18` (a generated state block in the primer — pre-staged as a CLOSE,
not a dig: the `--primer` target shipped and this very file carries the block) and `A-20` (whether the
fetch layer should retry with backoff; its `onTrigger` carries a Branch 0 with `N_min = 3`) —
**2026-09-01**. `A-33` and `A-34` — **2026-09-02**.

### Tier-0 status

**`G-4` = 0**, a MEASURED zero: `npm run reports` at 16:04Z fetched 26 issues, attributed all 26 to us,
parts reconcile (26 + 0 + 0 = 26). Day 22 since the public flip. **Clean-detection days now carry TWO
qualifiers**, not one: valid only since 2026-08-28 (the time axis, `note6`) **and desktop-only until
today** (the viewport axis, `note7`). The phone share of our readers renders **not-ready** — no device
breakdown exists — never 0 and never "all of them".

**Traffic:** 3 unique viewers against 179 unique cloners on the trailing 14 days, 35 days recorded.
The clone figure is mostly our own scheduled CI checkouts and is not an audience.

**Retention / word-of-mouth instrument:** not built, deliberately. At 3 viewers a fortnight a
return-rate would manufacture a number rather than measure one.

### Stuck-ask inventory

- **DOABLE-NOW** — `A-32` (the lane brief). Both halves are actionable today: the voice check is a
  command, and the content half needs the `G-3` outcome stated in plain English.
- **MISTAKEN** — none today.
- **REAL** — none. Nothing is waiting on David; the board returned 0 cards.
- **UNVERIFIABLE-ON-OUR-SIDE** — `A-29` (GitHub silently ignores the validity-checks PATCH) and the
  reply leg of `W-3` (we cannot read a maintainer's inbox). `W-3`'s page leg IS ours and ran twice
  today: HTTP 200, unchanged.
<!-- hand:end narrative -->

<!-- generated by gen-primer-first-action.mjs (skylark-site) · lane bounds-ledger · for 2026-08-31 · at 2026-08-30T16:22:23.323Z -->
