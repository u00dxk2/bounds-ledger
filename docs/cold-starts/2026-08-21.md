# Cold-start primer for 2026-08-21

Multi-session primer — each close-out appends a section below. Later sections supersede earlier ones.

## Session close-out — 2026-08-20

Paced rail, day 19. **This primer exists because yesterday's did not.** 2026-08-20 opened with no cold-start primer of its own, so the session began reconstructing state from the kickoff and the ledger before any work started — and nothing anywhere recorded the miss. That is now a gate; see *What is owed* below.

### Tomorrow's first-action — VERIFIED, exit 0 in the turn that wrote this

```bash
cd C:/dev/skylark/bounds-ledger && git rev-parse HEAD origin/main && [ "$(git rev-parse HEAD)" = "$(git rev-parse origin/main)" ] && npm run verify && node ../skylark-site/scripts/check-ci-status.mjs --workflow reverify.yml
```

Run verbatim at close from a file (never retyped): printed `f37b839…` twice, `npm run verify` → **exit 0** with the receipt written at that sha, then CI **GREEN**. Whole chain **exit 0**.

**It is `npm run verify`, not `npm run check`, and that changed today.** The receipt writer means `agent-status` **refuses the day's first bus post** unless a receipt exists at your *current* HEAD. Running the bare check leaves you refused later, at a worse moment — which is exactly what happened mid-session. A receipt for a different commit says nothing about the code you have.

**Do not reach for `--no-verify-receipt` or `CC_VERIFY_RECEIPT_DISABLED=1`.** The refusal prints both overrides, which is the most tempting shape a guard can take. Complying is what caused today's cadence to run at all.

**Nothing is held on you and nothing is owed to David tomorrow** — except that G-1's close lands Saturday and is his call, not ours.

### The day in one line

Instrument-heavy morning, corrected mid-day by a David ruling, then two user-visible things and a David-approved product build.

### DAVID RULING, mid-day — this is the standing frame now

David, verbatim: *"when are we going to start working 80% of the time on improving or introducing features and UI / UX improvements that will cause users to love our products instead of working on instrumentation seemingly every day?"* Four of twenty lanes shipped anything a user could see yesterday.

**The day's primary ship is a user-visible product improvement, named in the report as what a user now sees that they did not before. Substrate queues behind it, inside a 20% budget. "Stand the instrument up" is no longer an accepted P3 outcome.** The ruling was accurate for this lane: before it arrived, everything shipped that morning was instrument work.

### What a user can now see that they could not

**`ce25d76` — the README answers "is my number in here?"** 113 mirrored files sit in this repo, GitHub renders every one as a bounds table, and the front door linked to **none** of them. It now names twelve recognisable constants — Grothendieck, moving sofa, chromatic number of the plane, kissing number in dimension 5, sphere packing in R⁴, Lehmer, Bloch, Crouzeix, Hadwiger, two Erdős constants, biplanar chromatic number — each linking to the table we watch, readable in one click with no clone. Content only, no new code. All 15 links verified resolving, **and the link checker was negative-controlled first**.

### Held for the review lane — do NOT merge either without a verdict

Both are trusted-print instruments. Both are runner-validated. **GitHub Pages is deliberately NOT enabled**; publishing before the review would defeat the gate.

- **PR #26 — the `lookup.mjs` CLI** (on branch `f2-lookup-surface`; it is NOT on `main`, so that path will not resolve for you until #26 merges). F-2's CLI: pinned upper/lower rows for one constant, the date each pin last changed, primary source. Automatic PR run, both jobs pass.
- **PR #27 — `index.html`, the public page, stacked on #26.** David approved it today on the board: *"Yes - build the page."* All 111 constants, client-side filter, zero dependencies, nothing fetched at runtime. Dispatched run `32393859115` → success.
- **Merge order is #26, then #27.**
- **Known gap, stated on #27: nobody has looked at that page rendered in a browser.** The Chrome extension would not connect (tried twice). Structure and content are verified — 111 rows, 4 cells each, all links resolve, escaping proven; layout and mobile are not. Open `index.html` and look before merging.

### Catches today, and the honest label on them

**All instrument-facing, none in the mathematics** — the fourth straight day. That is the substance behind A-23.

- **A-21 CLOSED, and it closed properly.** Yesterday's proposal about the fleet's parked-item filter shipped upstream inside a day. Verified against its own pre-registered condition: `filteredCount` 0 → 2, W-3 and W-6 now excluded — **and proven endpoint-side**, because those rows' date fields are byte-identical between `3eecd37` and HEAD. **The loop closed only because W-7 forces the tracker row into the same commit as the report proposing it.** First time that ceremony paid out.
- **A-22 — the bus `sha-file-claim` linter reads sha-to-filename ADJACENCY as a modification claim.** Four false WARNs across today's posts. Five distinct relations it collapses; relation (e) is the sharpest — *the file that should have recorded a commit and did not* — because a documentation-gap report is by construction a sentence naming a commit beside a file it deliberately does not touch. Diagnosis only, approved as such. **2026-08-27** or it is the W-7 failure.
- **`f37b839` — a retracted cause was still living in `reverify.yml`.** Its b1 comment asserted our dispatches tripped rate limiting; that was retracted (GitHub was in a critical outage starting 52 minutes earlier). **The retraction reached `CLAUDE.md` and never reached the code comment.**
- **A-24 — a STACKED PR gets zero CI runs.** `pull_request: branches: [main]` filters the BASE branch, so PR #27 got no checks while #26 got both. b1's own defect reappearing directly beneath b1's own comment warning that a hold with no executable release condition outlives its reason. **Filed, not fixed** — it touches the workflow the streak is measured on. Release command: `gh workflow run reverify.yml --ref <branch>`. Decide by **2026-09-03**.

### Also shipped

- **`79f2d99` — the primer gate.** `npm run close:primer` fails when tomorrow's primer is missing. Close boundary only; **never** in `npm run check` or CI, where it would be red from midnight until close ran. Its self-test is in CI, and the CI wiring was negative-controlled (remove the step → `reverify.test.mjs` fails; restore → passes).
- **`f7aad95`** — the G-2 hunting-ground ranker, unread for 14 days, re-run and moved (+33/−13). G-2 becomes the only goal once G-1 closes.
- **`aeae6af`** — `key-user-flows.md` F-2 now reads *partial surface, started not done*.
- **`ec4e801`** — the 8/20 report, 8/8 linters clean, with a P3 finding back-patched inside a `findings:begin/end` fence.

### G-1 — the north star, and Saturday

**Streak day 28 of 30 as of today. Both close conditions land Saturday 2026-08-22** — acknowledgement met since 2026-08-11 (Tao merged `teorth/optimizationproblems#141`), streak leg landing on the 22nd. Evidence pre-staged in `G-1.signalPrestaged`.

**Closing it is David's decision — never a self-approval, never a self-rating.** The orchestrator is carrying it upward; do not decide it in-lane, and do not hold work on it.

**The succession question has a date and no owner.** Nothing names what replaces G-1 as Tier-0. One concrete input, surfaced as evidence rather than a proposal: **G-1's title is "re-verification CI + public status surface", but its close conditions are only the armed alarm and one acknowledgement — so it closes Saturday having never delivered the public surface named in its own title.** PR #27 is that surface.

### What is owed

- **Tomorrow's primer, before this session closes.** `npm run close:primer` now fails until the primer dated 2026-08-22 exists in `docs/cold-starts/`. This is the gate that yesterday's absence bought.
- **A-23, in tomorrow's report:** one sentence classifying the window's findings as record-facing or instrument-facing — **never a counter**, which would be inflatable like a self-rating — plus the orchestrator's addition: **name what the FIRST record-facing catch would look like** (what record, what class of drift, what would flag it). Due **2026-08-21**, i.e. tomorrow.
- Nothing else is owed to anyone outside the lane.

### Standing posture, unchanged

Suspect the instrument before the record. Verify by executing, never by reading. Read exit codes, never output, on any spawn that might not have happened — that trap appeared **twice** today, once as `bash` missing from PATH in PowerShell. And when a guard refuses a documented command, write the command to a file verbatim and run the file; never retype it to get past the guard.

**Pattern worth watching, deliberately not filed as a fourth row today:** three separate instruments this session could not distinguish an *assertion* from a *reference to something absent* — the bus `sha-file-claim` linter (A-22), the verify-receipt gate reading "no gate-green claim is made" as a gate-green claim, and the doc-reference checker flagging this primer's deliberate mentions of a primer that never existed and a file held on a branch. Each is individually defensible. If a fourth appears, the shared blind spot is the finding, not the instrument.


---

## FINAL close-out — 2026-08-20 (supersedes the section above where they differ)

The section above was written mid-afternoon. Everything below happened after it.

### Tomorrow's first action — unchanged, and still verified

The command in the section above stands. It was run verbatim from a file at close, printing `f37b839` twice, `npm run verify` exit 0 with the receipt at that sha, then CI GREEN — whole chain **exit 0**. HEAD has since moved to `883e108`; re-run it rather than trusting this line, which is the whole point of the sync guard.

### THE USER-VISIBLE THING TO SHIP TOMORROW — name it before touching anything else

**First choice, if the review lane has returned:** merge **PR #26 → PR #27** in that order and enable GitHub Pages. That puts the spot-check page David approved in front of actual visitors — the largest user-visible ship available to this lane, and it is built, runner-validated and waiting.

**If the review has NOT returned by mid-morning, do not idle on it.** Ship this instead, which is ungated and content-only: **show a real catch in the README's first screen instead of describing one.** The drift stories are currently a table of commit links — a visitor must click through to see the product working. Inline one before/after (`5/3 → 7/√17` from `da17be3` is the cleanest) so the would-tell-a-friend moment — *"the ledger caught a drift the field's own index missed"* — is visible in fifteen seconds rather than three paragraphs and a click.

**Do not let the day's first hour go to instruments again.** That is not a mood note; see the next line.

### Did the rail push toward instrument work today? Yes, and the mechanism is nameable

Three of five phases ask for instrument work before anything asks for product: P1 asks what would speed up tomorrow's cold start, P2 is continuity hygiene, and P3's Section **A** is called *substrate action* while product is Section **B** — second, and explicitly allowed to be zero. P3's "measurement-or-reject" clause also converts product ideas into "stand up the instrument" on a lane with no users, which is the outcome today's ruling now forbids while the clause producing it is unchanged. Full answer in the retro (`c377b03c`); the fleet is turning Q8 answers into a prompt fix tonight.

### What landed after the afternoon section

- **David ruled the succession** — *"make the new top goal whether someone outside uses the ledger, and keep improving a bound ourselves one level down."* Encoded same-hour as **`G-3`** (`883e108`), **staged**: it becomes Tier-0 when G-1 **closes**, which is Saturday and is his separate decision. **`G-2` is Tier-1.** His direction and my operationalization (close at **two** distinct outside people, not one — G-1 already produced exactly one) are marked separately in the row so they can be challenged separately.
- **`G-3` may read zero for months and the row says so.** Do **not** substitute an activity count, and never read the clone figure as arrivals — it is mostly our own CI.
- **P4 (`3483d208`)** — `W-6` re-pointed (`c2f349c`): its 100-unique-visitor threshold could never have filled at ~4 viewer-days in 12 days, so it is now a qualitative n=1 read with the date deliberately unchanged. `A-7` finally dated. Today's traffic sample committed (`738068f`) after being found uncommitted.
- **P5 (`64c797b2`)** — seven dated items had a deadline and **no response procedure**; all now carry one (`b5d3fea`). The report's "Ship nothing further today" was superseded inside the findings fence (`2024a17`).
- **Retro (`c377b03c`)** posted warm.

### The one I'd most want you to read

**I closed A-21 this morning — "a scan pointed at the wrong field" — and committed exactly that defect myself hours later**, checking for *any response-ish prose field* and reporting none missing while the typed check found seven. Closing a finding does not immunise you against it. When a contract names a **typed field**, check the typed field.

### State at close

Tree **clean**, HEAD `883e108` in sync, engineering zero **structural** (0 deps, 0 Dependabot alerts, no Sentry project — absence, not a swept zero). **Two PRs deliberately open**, held for the cross-family review lane; that is the correct posture, not a loose end. `docs/close-session-config.md` created today, because the primer gate approved this morning had **no caller** — a gate nothing invokes never fires.

### Owed tomorrow

- **`A-23`, due 2026-08-21:** the report carries one sentence classifying the window's findings as record-facing or instrument-facing — **never a counter** — plus the named shape of a **first record-facing catch** (what record, what class of drift, what would flag it). Four straight days of catches have all been instrument-facing.
- **Tomorrow's primer** before close. `npm run close:primer` fails until it exists.
