# Cold-start primer for 2026-08-11 (MT)

Multi-session primer — each /close-session run appends a new section below. Later sections
supersede earlier ones explicitly.

## Session close-out — 21:15Z (2026-08-10, ~15:15 MT)

You are the bounds-ledger agent. Read this, then `AGENTS.md` + `CLAUDE.md`, then
`continuity/items.json`. The repo is PUBLIC (since 2026-08-08); A-13 is closed.

### Tomorrow's first action

The steward cadence, unchanged, and it is still first:

1. `gh run list --limit 1`, then **read the LOG, not the badge**. Expect `No drift. 112 files match
   upstream dee1660…` — **112, not 111**: the upstream README joined the mirror today (`dee1660` is
   an UPSTREAM sha; do not `git show` it locally). Also `229 claim(s): 227 hold, 0
   broken/unreachable, 2 unverified (manual)` and **11** selftest PASS lines (was 10 — an eleventh
   shipped today). Green → **streak day 19 of 30**.
2. `npm run check` locally **with `CC_PROMPTS_PIN` set** — the only run that gives a real advisory
   read on the two manual claims.
3. **`npm run traffic`** — GitHub keeps only 14 days, so a skipped fortnight loses those days
   permanently; `continuity/traffic.json` is the only copy of anything older.

Then the increment. **The strongest remaining candidate is A-11 leg (b)** — pin a NEGATIVE for C-6
(Wikipedia) and C-8 (`teorth/erdosproblems` problems.yaml): assert no table row exists *below* the
cited one. It is the twin of what shipped today: `holds()` is a presence test, so a source that
keeps the pinned string and ADDS a better bound underneath stays green forever — and a new record
appearing is this lane's north-star event. Both sources are structured and CI-reachable. Doing it
closes A-11 outright (its `closeWhen` is exactly leg b for these two). Scope estimate 1–3 hr —
**and re-derive that estimate before deferring it, for the reason in the W-5 note below.**

The alternative is **G-2 round four**, the standing technical increment: rounds one through three
all built JOINs, and today's budget scan says why that family is exhausted — a join buys three
colours for `r·s` edges while the `6n−12` budget rises only 6 per vertex. Round four needs a family
that buys colour more cheaply per edge.

### Today's load-bearing ships

- **`575c01f` — G-2 round three.** Two results. (a) The record's own family (`K_r ∨ C_s`) is CLOSED
  by edge count: of 30 members, 15 reach χ≥10 and exactly one fits `m ≤ 6n−12` — and that survivor
  is `K7 ∨ C3`, which **is `K10`** (verified by execution: n=10, m=45=C(10,2)), thickness 3. Two
  near misses are over by exactly ONE edge. The `θ(K10)=3` step is **cited, not executed**, and the
  finding says so. (b) Seeded growth from Sulanke's verified layers over n=11..14, 40 seeds each:
  χ=9 on 40/40 at every n, never below and never above — round two's unseeded greedy reached only 8,
  so the design carries the whole ninth colour and generic growth adds none of it.
- **`3a698b1` — W-5 CLOSED, the lane's blindest class.** Upstream `README.md` joined the mirrored
  surface, so a record demoted from headline to **asterisked-unverified** now trips the alarm. On
  2026-08-02 exactly that happened to C_21 and C_71 and we said nothing, because a demotion leaves
  every bounds table byte-identical. File maps are now keyed by **path as upstream sees it**, so a
  report names the README at the root and each constants file under its own `constants/` prefix.
  Mirror 111 → 112 files.
- **`97ac73c` — the daily report**, 7/7 linters PASS, BLUF voice-clean.

### CI

**`reverify`: success @ `3a698b1`** — verified by conclusion AND read by log. Local `main` and
`origin/main` agree (`3a698b1`). Tree clean. 3 commits today.

### What changed about how you work here

- **The mirror is `constants/**` PLUS repo-root `README.md`.** `ROOT_FILES` in `reverify.mjs` is the
  list. A byte-mirror fires on *any* README edit, including a typo fix in the introduction — that is
  accepted deliberately: the file is small and curated, and the alternative (pinning only the table
  spans) reintroduces a blind class to avoid a minor annoyance.
- **A budget/elimination gate must be proven SILENT on the record itself.** Round three's edge-budget
  test would have closed a whole family by arithmetic error if it had flagged Sulanke's own graph.
  Now in `CLAUDE.md` beside the KP-78 rule.
- **`git status` still over-reports after a `--snapshot`** — `--numstat` settled it again today
  (zero content lines changed in any constants file). Fifth time; the habit held.

### The one method lesson worth carrying

**An item's estimate of its own cost is an unverified claim, and a wrong one is a silent veto.**
W-5's `closeWhen` described a "generated 421KB index". The file is **24KB** — the 421KB figure
belongs to a *different* repo's README (`teorth/erdosproblems`, from A-6's scoping) and was copied
across when the item was written. That number is a plausible reason it sat eight days: 421KB of
near-daily churn reads like a permanently-red alarm waiting to happen, which this lane refuses to
ship; 24KB of curated table does not. The value was never in dispute — **the estimate parked it, and
falsifying the estimate cost one `curl`.** When an item has been skipped repeatedly, re-derive its
cost before re-deferring it.

Smaller instance, same genre: a new assertion failed on first run because it read the report's
HEADER rather than its verdict lines. **A test that reads the wrong line of a correct report looks
exactly like a broken instrument.** Fixed by asserting on the verdict set, which is strictly
stronger than the original check.

### Nothing is blocked on David

Zero awaiting rows (checked, HTTP 200, 0 rows). Zero live board cards. Today's capacity-ask reply
was answered with a greenlight and the work is done. **Do not re-ask about the rewritten-history
card — it was answered on 8/08 and any board pass still listing it is stale.**

### Tier 0 / binding constraint

**North star: 0 externally-acknowledged corrections. UNCHANGED, and nothing today moved it.**
PR **#141** open, 0 comments, 0 reviews, untouched since 8/05 — **check, do not poll, do not nudge**
(his standing word). The July email is unanswered; W-3's two advisory legs both read unchanged from
this machine today (page still shows the old number, still dated 23 January).

**Evangelism read, honestly:** the ledger *is* the product and it still has no users. The
would-tell-a-friend moment would be a stranger finding a record we caught moving before their own
source did — nobody has arrived to have it. Retention/word-of-mouth instrument: the traffic sampler,
reading **1 unique viewer** on **day 2 of W-6's 90**. Advocacy signal this session: none. Today
widened *what we can catch*, which is upstream of the north star but is not the north star.

**Worth saying plainly:** two of the last three sessions have improved the instrument. That is the
right work — today's addition closes the class the lane most exists to catch — but the thing that
moves the north star is a stranger arriving, and the only lever we hold there is being findable. It
was candidate #3 on today's capacity ask and was not the one greenlit. It is still unbuilt.

### What NOT to touch

- Do not re-automate C-7/C-9 — the erdosproblems 403 is IP-dependent, a local 200 proves nothing
  about CI, and only a green CI run is evidence.
- Do not put `CC_PROMPTS_PIN` in repo secrets. Public repo; load-bearing, not a precaution.
- Do not re-propose the public comment on /36 — decided NO on 2026-08-02, not a deferral.
- Do not hand-edit the mirror (now including its `README.md`) or the generated pins; do not weaken
  the 27b verifier to make a candidate pass; do not claim any G-2 result without exact re-verify +
  literature check + adversarial review + David's gate.
- Do not allowlist `history-sweep.mjs`'s four hits or neuter its fixture — it exits 1 by design and
  shipped public that way, adjudicated in writing.

### State at close

`main` at `3a698b1`, tree clean, local and origin agree, PUBLIC. 3 commits today. 11/11 self-tests.
CI green. Daily report `docs/daily/2026-08-10-prelaunch.md`, 7/7 linters PASS. Open continuity
items: **9** (W-5 closed today). Zero cards waiting on David. Codex calls: 0.
