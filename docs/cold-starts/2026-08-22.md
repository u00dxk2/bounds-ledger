# Cold-start primer for 2026-08-22

Multi-session primer — each close-out appends a section below. Later sections supersede earlier ones.

## Session close-out — 2026-08-21

Paced rail, day 20. **Today the lane got a public surface.** The spot-check page is live, the review lane cleared it, and both held pull requests merged. Tomorrow is the day G-1 finishes.

### Tomorrow's first-action — VERIFIED, exit 0 in the turn that wrote this

```bash
cd C:/dev/skylark/bounds-ledger && git rev-parse HEAD origin/main && [ "$(git rev-parse HEAD)" = "$(git rev-parse origin/main)" ] && npm run verify && node ../skylark-site/scripts/check-ci-status.mjs --workflow reverify.yml
```

Written to a file and run verbatim at close, never retyped: printed `10e1db3` twice, `npm run verify` **exit 0** with the receipt at that sha, then CI **GREEN**. Whole chain **exit 0**.

**It is `git rev-parse HEAD origin/main`, NOT `--short`.** I reached for `--short` on two revisions this afternoon and it exits 128 (`fatal: Needed a single revision`) — the exact 2026-08-18 defect, reproduced by the person who had written the rule into `CLAUDE.md` that morning. The rule being written down does not stop you typing it; running the command does.

**Do not reach for `--no-verify-receipt` or `CC_VERIFY_RECEIPT_DISABLED=1`.** The refusal prints both overrides, which is the most tempting shape a guard can take. It refused me twice today and was right both times — once on prose that merely *described* a gates-green claim, once on a sentence of mine that was simply false because I had committed after the last gate run.

### TODAY — the named user-visible intent, before the ledger work

**Ship: move the "this number looks wrong — tell us" path out of the page footer and up beside the data. Item: `G-3`.**

One sentence of why, so you can disagree with it rather than inherit it: the live page links to `/issues` **only from the footer, below all 111 rows**, so a visitor who spots a bad number has to scroll the whole table to find the way to say so — and **G-3's own `onTrigger` says the read on 2026-09-22 is not "has an outsider used it yet" but "could we DETECT it if they did", naming the report-an-error channel first.** A detection path that exists but sits below the fold is the weakest link in the goal that becomes Tier-0 tomorrow. Small, content-and-markup only, no new dependency.

**This is the 80% ship; G-1's close below is the 20% substrate.** Do not let the goal transition eat the day — that is precisely the inversion David ruled against on 2026-08-20.

### THE OTHER THING TOMORROW: G-1 closes, and it is DAVID'S CALL

**G-1 reaches both close conditions on 2026-08-22 — that is tomorrow, i.e. today for you.** Acknowledgement met since 2026-08-11 (Tao merged `teorth/optimizationproblems#141`); the 30-day armed-and-functioning leg lands on the 22nd under the pre-registered `streakDayRule`.

**Closing it is David's decision, never a self-approval and never a self-rating.** He approved the close on 2026-08-20 with an explicit DO-NOT-RE-ASK, so his approval already exists — what remains is confirming the arithmetic, not asking him again. The orchestrator is carrying it.

**The close procedure is COMPLETE and sitting in `G-1.onTrigger`.** It was finished yesterday-for-today precisely so you execute a checklist instead of re-deriving what a close means. It has three parts and the last two are the ones that get forgotten:

1. Confirm day 30 under `streakDayRule` (a day counts only if its scheduled run reached a verdict; a run that could not fetch PAUSES the clock rather than counting or resetting). If the streak paused, say so plainly and close on the day it lands — his approval is of the outcome, not of a date.
2. **Re-point the daily report frontmatter IN THE SAME COMMIT.** `north_star_metric`, `_value`, `_status`, `_classification` all still describe G-1 at `1 / green`. **G-3 will legitimately read ZERO for months.** Left alone, tomorrow's daily reports a flattering number for a closed goal while the live Tier-0 goal reads zero.
3. **Sweep for text that cited G-1.** README, `docs/lane-brief.md`, `docs/key-user-flows.md`, this primer, and any open row quoting "the 30-day streak" or "the north star" as a LIVE gate. This repo's own rule says closing a gate must include a sweep for text that cited it, and **that rule has now failed four times** — most recently inside a closed item, because closed rows are not read. G-1 is the most-cited row here, so this is the largest such sweep the lane will ever do. Do not skip it as ceremonial.

**On close, G-3 becomes Tier-0 and G-2 is Tier-1.** G-3 is *"someone outside Skylark uses the ledger and acts on it"* — and it may read zero for months. Do not substitute an activity count, and never read the clone figure as arrivals.

### What a user can now see that they could not

**`https://u00dxk2.github.io/bounds-ledger/` is LIVE** (`10e1db3`, Pages enabled today). All 111 constants, filter as you type, both pinned rows each, **the date each row last moved**, and a primary source beside every number. No account, no clone, nothing fetched at runtime. **F-2 — the flow that IS this lane's core problem** (*"I cited a number and have no idea if it is still the current one"*) — went from no surface at all to a live one.

**Verified as SERVED, not as deployed:** byte-identical to the committed `index.html`, checked with positive controls (the H1, the upstream sha, a constant name, 222 last-changed spans, the favicon) and negative controls (no dead CSS, no review narrative). An HTTP 200 is not evidence you fetched the artifact — that is a standing rule here and it applies to our own page.

Also shipped: **`f421ea6`**, the README's first screen now shows a real drift verbatim instead of describing one, and **`10e1db3`** links the live page from the section that asks the same question.

### The review lane earned its keep — read this before the next in-class diff

It returned **needs-changes on one HIGH correctness defect that all our own gates were green through**: `lookup.mjs` handed `git log -S` the DECODED expect string while `claims.json` holds JSON-escaped bytes, so **159 of 222 pins could never be dated** — and the CLI then printed *"unknown (no history available in this checkout)"* on a full clone, a trusted-print instrument asserting a false reason for its own silence. Fixed with the JSON-encoded needle; **63 → 222 of 222**. The reviewer's independently measured 63 matched ours exactly.

**It also ran the browser render nobody here could get** — two viewports, filter behaviour, console, byte-comparison. Request the lane unprompted for the next in-class diff; that is now twice it has paid for itself in one posted line.

### Traps this session actually hit, in the order they will bite you

- **A stacked PR does not auto-retarget.** #27's base was `f2-lookup-surface`, which I did not delete, so after #26 merged GitHub left #27 pointing at a dead branch. Merging as-was would have landed the page nowhere — **a silent no-op that looks exactly like success.** Retarget with `gh pr edit <n> --base main` first.
- **FIVE silent escaping failures in one day, none of which errored.** A jq template that printed itself instead of values (so a *release command written to fix commands-that-do-not-run* did not run); a retyped README quotation that lost upstream's LaTeX spacing under a sentence claiming it was **unedited**; an inline example that arrived as an empty code span; a `CLAUDE.md` bullet about silent corruption that was itself silently corrupted; and a bus paragraph disclosing a flag that had the flag name eaten by backtick command substitution. **The rule, now in `CLAUDE.md`: do not retype it — EXTRACT it**, assemble in a quoted heredoc or a file, and grep the artifact for your own words before believing it says them.
- **"Verbatim" and "unedited" are METHOD claims.** They fall under the audit-the-method-sentence rule, and the audit is mechanical: grep the source for your own quotation and require a hit. Mine returned zero.
- **A CSS comment inside a template literal ships to every visitor.** My explanation of a CSS fix went into the generated page in full. Grep the generated artifact, not the source.

### What is owed

- **Nothing is held on you, and nothing is owed to David except G-1's close**, which is his call and already approved.
- **A-25 — deferrals carrying an expiry AND a `releaseTest`, with a check that reads both — is due 2026-08-25 and its single re-date is SPENT.** On the 25th it ships or it closes as declined with a reason; it does not roll again. Design is orchestrator-approved and recorded in the row; do not re-litigate it. **Both KP-78 answers are already reproducible from history:** the finished check must go RED against 2026-08-20 state (PRs #26/#27, no request ID, no `Release:` line) and stay silent after `b63391b`.
- **A-22 — the bus `sha-file-claim` linter — is due 2026-08-27**, diagnosis only.

### The honest label on today's catches

**All five instrument-facing — the fifth consecutive day.** The last record-facing catch was 2026-08-14. That classification is now a standing convention in `CLAUDE.md` (A-23, closed today), and it carries a **falsifiable prediction**: the next record-facing catch will be a witness-value mismatch on a constant upstream added within ~30 days, and **nothing we run automatically would flag it** — a witness mismatch is byte-stable, so the mirror diff sees nothing and the pins match. It takes a human recomputing a cited certificate. **If the next one arrives through an alarm instead, the prediction was wrong: say so and correct it.**

### State at close

Tree clean, HEAD `10e1db3` in sync, CI **GREEN**. Mirror 113 files @ upstream `e70b4a4` (an upstream sha; it does not resolve in this repo). 233 claims — 231 hold, 2 unverified (the two `manual: true` erdosproblems pins, correct by design). **Zero open PRs, zero open drift issues, zero overdue items, zero due tomorrow except G-1.** Traffic: 3 unique viewers against 164 unique cloners — **the clones are largely our own CI, and the viewer count is the only arrival figure that is not mostly us.**
