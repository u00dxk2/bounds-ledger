# Cold-start primer for 2026-07-29 (MT)

Written at the close of 2026-07-28. Multi-session primer — each `/close-session` appends below; later
sections supersede earlier ones explicitly.

You are the bounds-ledger agent. Read this, then `CLAUDE.md`, then `continuity/items.json`. This lane is a
**steward** of drifting mathematical records — a ledger that alarms when a cited record moves. Lite rail,
pre-launch, private repo. **No self-rating** (no `/daily-close`).

**This primer deliberately does not restate `CLAUDE.md`.** The conventions, the gotchas, and the
never-do-this list live there and are current. This file is state + first action + what changed.

## State this morning

**227 claims / 225 hold / 0 broken / 2 UNVERIFIED** across two stewarded surfaces, over a **110-file** mirror.
Clean at upstream **`c4f8386`** (that's the `teorth/optimizationproblems` sha in `ledger/.../manifest.json`,
not a local commit). **Both numbers moved on 7/28 evening — it was `a002311` / 225 claims / 109 files all
week.** Upstream pushed *twice* in ~20 minutes and the second push **added a constant**. See "What happened".
**G-1 green-streak: day 5 as of 7/28** (day 1 = 7/24) — a green run on 7/29 makes it **day 6 of 30**, ending
~2026-08-22. No open GitHub issues. Working tree clean at `main`.

**North star — externally-acknowledged corrections: 0.** The correction was sent 2026-07-24; sending is not
acknowledgement. As of the 7/28 local read, erdosproblems.com/36 still shows `0.380876` and still says
`last edited 23 January 2026` — **the maintainer has not touched the page.** Things requiring David: **1**
(forward a reply, if one arrives).

Both UNVERIFIED claims are `manual: true`, both the same blocked page, both by design: **C-7** pins the bound,
**C-9** pins the edit date. Two is correct — David ruled on 7/26 that it should be left as is.

## First action

**Read the overnight run's LOG, not the badge** (`gh run list --limit 3`, then `gh run view <id> --log`).
Expect:

- `No drift. 110 files match upstream c4f83863…`
- `227 claim(s): 225 hold, 0 broken/unreachable, 2 unverified (manual).`
- **TWO** `advisory fetch failed (HTTP 403) — expected from datacenter IPs (CI)` lines, one under C-7 and one
  under C-9.

Green → say so, note the streak, move on. Then optionally `npm run check` locally — on a residential
connection that does a **real** advisory fetch of the page for both C-7 and C-9 (CI structurally cannot).
Redirect to a file and check `$LASTEXITCODE`; never read the exit code through a pipe.

### If something is red, or a local advisory says NOT FOUND

- **C-9 NOT FOUND locally** → *the maintainer edited the page.* Highest-signal event short of a reply.
  Hand-verify, then follow **W-3's `onTrigger`** in `items.json` step by step. Do not re-pin the new date
  until the state is recorded as a finding — the old date is the evidence.
- **C-7 NOT FOUND** (`0.380876` gone) → check whether the bound is now `0.380868`. If so **that is the
  north-star event.** W-3 `onTrigger`, then tell David.
- **Red on C-8** → problem 36's entry moved in `teorth/erdosproblems`. Read the upstream commit, re-verify,
  re-pin deliberately.
- **Red on a generated pin** → a bounds table moved: primary sources → deliberate `--snapshot` → **re-run
  `extract-pins.mjs` + commit** (the post-snapshot ratchet).
- **`Check error:` title** → transient fetch flake. Confirm next run green.
- **`Instrument failure:` title** → a self-test failed before the checks ran. The alarm is telling you it
  broke, which is working as designed (A-4, shipped 7/25).

## Decisions David ALREADY MADE — do not re-raise

Unchanged from the 7/27 primer; he answered all three in-pane on 7/26.

1. **W-3's close date: he said WAIT.** The old `~2026-08-24` was my placeholder, not his. `signalDate` is
   **2026-09-24 and is a CHECK-IN, not a deadline**. **Closing the watch requires his decision, not a date.**
2. **The public comment on erdosproblems.com/36 — approved in principle but not yet.** Approved *in principle*, **not now**;
   needs his explicit go plus a pre-send read. It would be this lane's **first genuinely public act** — repo
   private, brief `noindex`/robots-disallowed/unlinked, the only outward trace is his one 24 July email.
3. **UNVERIFIED at 2: left as is, per David.**

## What happened 2026-07-28

**No session ran 7/27** — zero commits; the orchestrator's 7/27 kickoff sat queued ~24h and this pane drained
it on 7/28 morning. It was run as **today's** daily covering both days rather than backdated, and the
orchestrator confirmed that call. **The instrument ran fine unattended** — both days green in CI, and the
7/28 local read confirmed the page untouched. Nothing was lost; the gap cost a day of latency on noticing,
which for a watch whose next check-in is 2026-09-24 is immaterial.

| Commit | What |
|---|---|
| `b5733af` | The 7/28 daily (covers both days) + `key-user-flows.md`: F-3 was listed "never exercised" when it was **sent 7/24** |
| `34a37fc` | **The alarm fired for real — the first true drift since it was armed 7/24.** Verified, snapshotted, re-pinned + finding |
| `47ddf8d` | Housekeeping: `tmp/` cleared of 929 KB of session scratch; A-4's superseded `note2` marked; `MEMORY.md` index cut from ~450 words to a hook; this primer |

**THE FIRST REAL DRIFT (7/28 ~21:36Z) — already resolved, no action owed.** Upstream pushed `937f4e5`;
`1a.md` and `41b.md` drifted. **Neither was a record change**: a dead cross-link
(`teorth/optimizationproblems:constants/2.html`, a page that does not exist) repointed to
`teorth/optimizationproblems:constants/1b.html` (*Erdős minimum overlap constant* — this lane's founding
finding), and a missing `C` in `$C\_{41a} \ge \_{41b}$`. Both verified against the repo's own content, then
the full discipline ran: snapshot → re-run `extract-pins.mjs` → commit. Pins regenerated **byte-identical**,
independently confirming no bounds row moved. Full write-up:
`docs/findings/2026-07-28-first-real-drift-was-editorial.md`.

**THEN IT FIRED AGAIN AT 21:57Z** (`937f4e5` → `c4f8386`) — upstream pushed twice in ~20 minutes. `85a.md`
was another escape fix, but the second change was **`ADDED constants/86a.md`: a new constant entered the
stewarded surface** — the Schur–Siegel–Smyth trace constant $C_{86}$. Mirror 109 → **110 files**, pins
216 → **218**, ledger 225 → **227 claims**. Absorbed the same way; `extract-pins.mjs` handled the new file
unaided and its two pins were checked against the page's own stated range ($1.80203 \le C_{86} \le 1.8216$).

**Note for anyone citing `86a`:** that page's own "Contribution notes" say it was AI-assisted and that its
pre-2021 rows *"have not been checked against the primary sources themselves."* Our pins are still honest
because they assert **listing position, never correctness** — which is precisely why that rule exists. Do not
"upgrade" the `86a` pins to record claims.

**Take the lesson, not just the outcome:** a red `reverify` is not automatically a record event, and
**editorial drift is still real drift** — the mirror's contract is byte-fidelity, not just numeric fidelity.
Also logged there: `git status` showed a third changed file (`21a.md`) that `git diff --numstat` proved had
**zero** content change. The differ was right. `git status` is not a content diff.

**Flow-craft rotation was honestly skipped, and should be again until something changes.** F-2 (*spot-check a
claim* — where the core problem actually lives) has **zero surface**, and building one **is** the public-repo
flip, which is David-gated. F-3 is one sent email whose only open question is W-3, not flow design. F-1 was
rotated 7/24 and its remaining proposals P-2/P-3 stay deferred. Rotation resumes when F-2 has a surface —
do not invent a critique to fill the slot.

## Traps

All four are in `CLAUDE.md` § Gotchas and are current: **CRLF silently no-ops `\n` string-replaces** (`git
diff` to prove a test mutation landed before believing a result); **never read an exit code through a pipe**;
**test a capability from the environment that will exercise it** (a local 200 proves nothing about a runner);
**a gate you can only confirm green is UNKNOWN** — feed it the input that should trip it.

## Tier 0 / evangelism read (pre-launch)

- **Core problem, user's words:** *"I cited a number. I have no idea if it's still the current one."*
- **Would-tell-a-friend moment:** the alarm catches a stale citation before the citer is embarrassed by it.
- **North star — externally-acknowledged corrections: 0.** Not moved.
- **Retention/word-of-mouth:** no instrument by design (no users, private repo). Honest proxy = the
  external-ack metric, instrumented as W-3.
- The lane's only current "user" is David. His standing ask has been **1** since 7/26.

## State at a glance

| Item | What | Status |
|---|---|---|
| G-1 | GOAL: steward drifting record inventories | open — streak day 5/30 (ends ~8/22); external-ack **0** |
| A-2 | Re-verification CI | open — 225 claims, 2 surfaces; closes with the 30-day streak |
| W-3 | Ack watch for the /36 correction | open — reply leg David's; both page legs mechanical |
| A-1 / W-2 / A-3 / A-4 / A-5 / A-6 | reconciliation, Haugland, correction sent, alarm fixes, second surface | closed 7/22–7/25 |

**Active in-flight work:** none. No Codex jobs, no deploys, no open PRs, no open issues.
**Watch items maturing within 24h:** none.
**Not David-gated; no pane presence needed** for the first action.

---

## Session close-out — 21:54Z

Same 2026-07-28 session that wrote everything above; this closes it. **Everything above is current** — the
state block, the first action, and the drift write-up were all authored or corrected after the drift landed.

**Today's load-bearing ships** (3 commits, all pushed, tree clean, `main` up to date with origin):

| Commit | What |
|---|---|
| `b5733af` | The 7/28 daily, covering the 7/27 gap without backdating (orchestrator confirmed the call). Also fixed `key-user-flows.md` F-3: listed "never exercised" when it was **sent 7/24** |
| `34a37fc` | **First real drift response.** Verified → `--snapshot` (`937f4e5`) → `extract-pins.mjs` → commit, + `docs/findings/2026-07-28-first-real-drift-was-editorial.md` |
| `47ddf8d` | Housekeeping: 929 KB of `tmp/` scratch removed; A-4's superseded `note2` marked; the leaner primer |
| *(see below)* | **Second drift, 21 min later** — upstream ADDED constant `86a`. Mirror 110 files, 227 claims. Stale counts swept from `CLAUDE.md` + `README.md` |

**CI:** `reverify`: completed/**success** @ `57442ae` (the commit that absorbed the evening's second drift;
run `30403156190`; later same-day commits are docs-only and leave the mirror untouched) —
conclusion observed, then the **log** read: `No drift. 110 files match upstream c4f83863…` + `227 claim(s):
225 hold, 0 broken/unreachable, 2 unverified (manual).` + both self-tests PASS + the two expected
advisory-403 lines. Not a badge read. Both of the evening's drifts are absorbed and green in CI.

**One task available but NOT done, deliberately:** `docs/lane-brief.md` (the David-facing brief hosted at
`/t/lanes/bounds-ledger`) still says *"re-checked … on 24 July: no drift, all 109 files identical."* That
sentence is **date-anchored and still true**, so it is not a defect — but the brief now has genuinely
newsworthy material it lacks: the alarm caught its first real drifts today and the inventory grew. Editing
the brief requires a bus note so the orchestrator re-ports it, so it was left for a session with room.

**Pending decision-blocked items:** none new. David's three 7/26 decisions stand (see the section above —
**do not re-raise**). One optional call was surfaced to him this evening and needs no action if he passes on
it: whether to compress `items.json`'s closed-item notes (21.5 KB, ~60% of the file). **My recommendation was
LEAVE IT** — A-4's and A-6's notes carry reasoning that exists nowhere else — so **the default if he says
nothing is: do nothing.** Do not compress on your own initiative.

**Tomorrow's first-action:** unchanged from the top of this file — read the overnight run's **log**, expect
`225 claim(s): 223 hold, 0 broken/unreachable, 2 unverified (manual)` and **two** advisory-403 lines, now at
mirror `937f4e5`. Then optionally `npm run check` locally for a real read of both page legs. Not David-gated;
no pane presence needed.

**Active in-flight work:** none. No Codex jobs (`codexCalls: 0`), no deploys, no open PRs, no open issues.

**Watch items maturing within 24h:** none. W-3's next check-in is 2026-09-24 and is **not** a deadline.

**What NOT to touch tomorrow:** everything in "What NOT to touch" / the traps above, plus three from today —
(1) **do not re-raise David's three decisions**; (2) **do not compress `items.json`** absent his word;
(3) **do not treat a red `reverify` as automatically a record event** — today's was editorial and still
required the full verify → snapshot → re-pin cycle.

**Tier 0 / evangelism read:** core problem — *"I cited a number. I have no idea if it's still the current
one."* Would-tell-a-friend moment — the alarm catches a stale citation before the citer is embarrassed by it.
**North star: externally-acknowledged corrections = 0, unchanged** — today's drift was upstream's own
self-correction, not an acknowledgement of ours, and the /36 page is still untouched since 23 January.
Retention/word-of-mouth: no instrument by design (no users, private repo); honest proxy is the external-ack
metric, instrumented as W-3. **Advocacy signal this session: none** — correct for day 7 pre-launch.

**The one thing today actually bought:** the instrument stopped being theoretical. Days 1–5 of the streak
were genuine no-drift reads, so the alarm had only ever been exercised by its own synthetic self-test. Today
a real upstream push moved two files and the whole chain — detect, verify against primary content, refuse to
snapshot-to-silence, snapshot deliberately, re-pin, stay green — ran end to end. That is A-2's `closeWhen`
("drift detection demonstrated") satisfied in production; only G-1's 30-day streak remains.

**Supersedes:** nothing. This is the first close-out block for this date.
