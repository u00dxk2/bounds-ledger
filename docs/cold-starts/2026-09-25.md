<!-- GENERATED — regenerating REPLACES everything outside the hand: markers, so put prose inside them. Provenance stamp at the foot of this block. -->

<!-- hand:begin banner -->
Rewritten 2026-09-24 late evening MT, at the close of 2026-09-24's own rail (P1 to P5 ran in one evening after the two-day stall). **Tomorrow's first work is `A-54`'s census session, the Small Ramsey Numbers rebuild (the second watched area). It was dated 2026-09-24 and did not happen, because the depth audit took that evening.** Start by reading the rebuild's scope rather than recalling it:

```bash
node scripts/sky.mjs show-item.mjs A-54
```

Its `rebuildScope2026_09_21` field is the plan: census sessions alternate with depth-audit slices, and the rate checkpoint is 2026-09-28. `ramsey.html` and `copying.html` stay FROZEN until the rebuild lands. David lifted the freeze for exactly ONE sentence on 2026-09-22, which shipped in `aee164c`.

**What tonight shipped, so you do not redo it.** `A-47` (the depth audit: our own cited rows read against their sources) ran slice 7 a day early, in `c05232f`. The draw was pushed first as `0ed11f7`. `node scripts/depth-audit.mjs` now reads "34 row(s) audited — 19 sound, 0 defective, 7 unresolved, 8 unreachable". `A-47` is re-dated to 2026-09-27, so it is NOT tomorrow's work. `npm run served` at 2026-09-25T02:14Z read 23 of 23 changed files served as committed.

> **46a is the finding, and it is UNRESOLVED on the public page.** The row credits Bourgain's GAFA 1991 paper with 58/15. The preprint body and the published pp. 147–148 give 31/8, and his companion paper (IHES M/90/74, Proposition 2.15) proves 58/15. I stored it DEFECTIVE, and the adversarial review downgraded it because the published body, pp. 149–187, is paywalled and unread. **`A-56` settles it on 2026-09-26**: it names three routes, what each outcome does to the verdict, and a stop condition that forbids re-dating it. **Nothing goes upstream on it.** A correction to the mirrored entry is outward contact: adversarial review first, then David's gate.

> **The standing prediction is CONSISTENT, not tested.** 46a is exactly its shape: a citation defect found by reading, not by an alarm. It is not a confirmed catch until `A-56` reads the published body. Quote the prediction from CLAUDE.md, never from a report.

> **`W-13` (someone must call the served-bytes check) reads 2026-09-29, and its scheduled workflow is APPROVED.** The orchestrator approved a new scheduled workflow file named served.yml (not yet written) at P2: exit 1 files or updates ONE `Publish: stale page` issue, exit 2 files nothing, and it stays outside the reverify suite. Its closeWhen now requires both firing checks run on that workflow and quoted on the row (fires seconds after a page-changing push with `CC_SERVED_WINDOW_MS=0`; silent on the settled tip). A workflow file existing does not close it.

> **Nothing waits on David.** P4's probe read `"items":[]` of 18 open rows. `W-3` (the reply watch on the 2026-07-24 email) passed its two-month mark tonight with both page legs unchanged. It is re-dated to 2026-10-08, and closing it for no response is his call. Pull request #194 upstream is OURS and never counts as a `G-4` event; `W-12` reads it 2026-09-28.

> **On arrival, Glob `docs/cold-starts/` for the newest file, not only today's date** (P1 finding, held on discipline until skylark-site's kickoff looks one date ahead). Tonight's kickoff reported the primer ABSENT while this file, dated for the next day, held the brief.

> **Waker ladder: relaunch ranks 1, 2 and 3 at session start** with three separate background calls from `/listen` step 3b, never joined. Read each rung's output file: `armed` and `already live (pid N)` are both fine. A rank-1 line reading `WAKE-SHORTEN` or `actionable loop-death signal` is a real fire.
<!-- hand:end banner -->
_bare item ids in the banner region above resolve via `node scripts/sky.mjs show-item.mjs <ID> --repo .` — never a whole-ledger grep._

UNVERIFIED FIRST ACTION — matching execution evidence and current governing dependencies have not been established by these release commands.
Recovery read (PowerShell or bash, cwd: lane root): `git status --short` followed by `git log -5 --oneline`; reconcile the authored action in docs/daily-config.md with the current ruling and ledger before running it.

First command (also below, in place):

```bash
git pull --ff-only
# commits after this primer was written are invisible to it — read them before acting:
git log --oneline b4003fa473eaf8137b86792ef3e3feb584fa3ee5..HEAD
```

### Primer — bounds-ledger, 2026-09-25

What ONLY this primer holds comes first; the FULL runnable **First action** block
sits at the foot, below the reads. The single command above is the same block's
first line, repeated for reference — it replaces nothing. Establish the governing action and its preconditions before running release commands.

_**Composed against HEAD commit `b4003fa473eaf8137b86792ef3e3feb584fa3ee5` at 2026-09-25T02:31:36.513Z.** Nothing that landed after that instant is in this file. David's board answers especially: an answer he gives later cannot be here, and this primer will go on reading as though the work were not owed — that is exactly how two lanes concluded an OTA was not owed on 2026-09-04. Read them live, never off this page: `node scripts/sky.mjs answered-cards.mjs --project bounds-ledger --full`._

_Shared skylark-site scripts below are spelled `node scripts/sky.mjs …` — this lane vendors `scripts/sky.mjs` (S-20260906-75), so a shared script is named LANE-RELATIVE and resolves from a worktree too._

#### Owed gates — 0 due on/before 2026-09-25

_Ledger read at 2026-09-25T02:31:36.513Z by `cc-due-gates.collectDue` — the same resolver `check-due-gates-dispositioned` uses, so any difference is TIMING, not two answers. Rows filed or re-dated after that are NOT READ at generation — the close-out commit lands after this, so the COUNT above is stale by construction and this list cannot claim completeness. The list below, not the count, is the artifact; re-derive both before acting: `node scripts/sky.mjs check-due-gates-dispositioned.mjs --print --today=2026-09-25`._

_None due. This is a real zero: 1 clock store(s) were read and returned no row due on/before 2026-09-25._

#### What shipped since 24 hours ago

_Read at 2026-09-25T02:31:36.513Z; commits landing after that — the close-out especially — are not here. Re-derive: `git log --oneline --since='24 hours ago'`._

- commit `b4003fa` — A-56: 46a's UNRESOLVED badge gets a named way out, dated 2026-09-26
- commit `c05232f` — A-47 slice 7: five more cited rows read against their sources, and 46a's reference looks wrong
- commit `0ed11f7` — A-47 slice 7 drawn before any source is opened: 46a, 52a, 5b, 71a, 80a
- commit `ea25512` — P2 2026-09-24: W-13 closes only on a workflow seen to fire and seen to stay silent
- commit `5243c1e` — Hygiene 2026-09-24: the 2026-09-22 report lands two days late and says so, tomorrow's primer leads with the dark window, and the four gates it swallowed are read

**Yesterday's report:** `docs/daily/2026-09-24-prelaunch.md` (coverage 2026-09-24, the prior day). No findings fence declared in it.
Prior report: historical; re-read its findings and later changes before treating its opener as current.

**Today's report (owed):** write `docs/daily/2026-09-25-prelaunch.md` — OUTPUT obligation due at close, the artifact of record for service day 2026-09-25. Check it exists at close: `node scripts/sky.mjs check-daily-report-exists.mjs --date 2026-09-25` (it sweeps the fleet and applies each lane's coverageOffset; it takes no --project). <!-- owed-output path="docs/daily/2026-09-25-prelaunch.md" service-day="2026-09-25" due="close" -->

#### State — as of 2026-09-25T02:31:36.513Z (re-run the release command; never trust the value)

- **ledger**: 72 item(s): 53 closed · 19 open · as of 2026-09-25T02:31:36.513Z — release: `node scripts/sky.mjs gen-primer-first-action.mjs --repo . --target-date 2026-09-25 --json`

#### Id glossary — 20 id(s) this primer's day already has words for

_Each gloss is the ledger row's OWN title, not a re-derivation — paste it. Rows = every open ledger row plus every id named above. `(no ledger row)` means the id is named here and the ledger carries nothing under it: that is a finding, not a formatting gap._

- `A-2` — STANDING: drift-resolution log for the adopted surfaces…
- `A-9` — engineering-health P2 backlog (fix-on-touch, not scheduled…
- `A-45` — TIER-1 LEADING INDICATOR (orchestrator-approved…
- `A-47` — THE DEPTH AUDIT, David-ruled 2026-09-09: read our own…
- `A-49` — npm run close:primer passes when tomorrow's banner is…
- `A-50` — Importing one of our CLI scripts can run it — one selftest…
- `A-52` — Write down what an outside party acting on a record could…
- `A-53` — The audit store and the public page can disagree about how…
- `A-54` — Adopt Small Ramsey Numbers (Electronic Journal of…
- `A-56` — Settle 46a's depth-audit entry: read the published body of…
- `G-2` — GOAL: contribute a verified bound improvement to the…
- `G-4` — GOAL (Tier-0 from G-3 close): an outside party acts on a…
- `S-20260906-75` — (no ledger row)
- `W-3` — Watch for acknowledgement of the erdosproblems.com/36…
- `W-4` — KP-78 applies to every new detector: demonstrate BOTH…
- `W-6` — Read window for the README report-an-error channel — the…
- `W-8` — Does a materially amended outward artifact go back to…
- `W-11` — Before asserting a claim is decision-ready, search this…
- `W-12` — Watch: does upstream act on PR #194, the [YKLBMWKCZGS2026]…
- `W-13` — STANDING: someone must CALL the served-bytes check — A-55…

### First action — bounds-ledger, 2026-09-25

Release commands — subordinate to the governing action.
HANDOFF INCOMPLETE — governing action not authored.

UNVERIFIED FIRST ACTION — these commands have no matching execution evidence here. Command text alone establishes neither safety nor the promised evidence, shell compatibility, or tomorrow's priority.

```bash
git pull --ff-only
node scripts/sky.mjs update-david-board.mjs --list --json --full-ids --project bounds-ledger
git rev-parse HEAD origin/main
git rev-list --count origin/main...HEAD
npm run verify > tmp/verify-out.txt 2>&1
node scripts/sky.mjs check-ci-status.mjs --workflow reverify.yml
```

Morning release commands — deliberately NOT READ at generation (a value written
last night would already be stale); run each, never trust a remembered one:
- `git -C . log -1 --format=%h%x20%s` — HEAD
- `git -C . status --porcelain` — working tree
- `node scripts/sky.mjs check-ci-status.mjs --workflow reverify.yml` — CI
- `node scripts/sky.mjs check-deployed-sha-drift.mjs` — deploy
  - WHERE IT LOOKED: no read-only deploy-state script in this lane's package.json (looked for ops:deploy-state, deploy-state, check-deploy, render-deploys, deploy:status, daily-snapshot). The fleet's check-deploy-state.mjs is not a substitute — it requires --fix <sha> --deploy <sha> and exits 1 without them — so this is the FLEET read, which takes no required flags. service: NOT DECLARED — no row for "bounds-ledger" in skylark-site/src/data/cc-deploy-gates.json (generatedAt 2026-09-22T02:18:27.672Z, 17 repo row(s)) — nothing in this repo declares that lane's Render service. No `--service` flag is emitted: the lane SLUG is not the service name (antebellum-twitter's is disunion-feed), and a guessed flag exits 1.

<!-- hand:begin narrative -->
_(orchestrator narrative — this region survives regeneration)_
<!-- hand:end narrative -->

<!-- generated by gen-primer-first-action.mjs (skylark-site) @ commit a78edc48c · lane bounds-ledger · for 2026-09-25 · at 2026-09-25T02:31:36.513Z -->
<!-- writtenAtSha: `b4003fa473eaf8137b86792ef3e3feb584fa3ee5` — the lane's HEAD commit when this primer was written. Provenance anchor for check-primer-freshness leg ii; NOT a claim about HEAD now (the close-out commit that carries this file lands after generation). -->
<!-- reading this block: a generated line that is wrong in a way you can see FIXED in skylark-site's git log after the commit above is STALE, not a finding — regenerate before filing it. `git -C C:/dev/skylark/skylark-site log --oneline a78edc48c^{commit}..HEAD -- scripts/gen-primer-first-action.mjs src/lib/cc-primer-commands.mjs src/lib/cc-due-gates.mjs src/lib/cc-report-artifact.mjs src/lib/cc-hand-edit-merge.mjs src/lib/cc-cold-readability.mjs src/lib/cc-skeleton-gloss.mjs` -->
