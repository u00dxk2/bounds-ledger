<!-- GENERATED — regenerating REPLACES everything outside the hand: markers, so put prose inside them. Provenance stamp at the foot of this block. -->

<!-- hand:begin banner -->
Written 2026-09-24 evening MT, closing the 2026-09-22 session two days late. **READ THIS FIRST: this lane went DARK for two days.** The session working 2026-09-22 stopped after its P4 post at 17:17Z and resumed 2026-09-24 23:55Z. Between those instants nothing ran here — no session, no commit, no bus post. Your first command is not a formality:

```bash
npm run served
```

It asks whether GitHub Pages is serving the bytes HEAD holds, for every published file the last page-changing commit touched. Run in this close it printed `117 checked — 117 served, 0 in flight, 0 stale, 0 unreachable`, exit 0, stamped 2026-09-25T00:03:53Z. Exit 1 is STALE and means readers are not getting the tip; exit 2 is a refusal and is never a pass.

**What the dark window did and did not cost.** The scheduled drift alarm covered both mornings on GitHub's clock — 2026-09-23 09:28Z and 2026-09-24 09:29Z, both success, today's log reading `No drift. 116 files match upstream` and `244 claim(s): 242 hold, 0 broken/unreachable, 2 unverified (manual)`. So no record moved unseen. What lapsed was everything keyed to a session: four gates came due inside the window and were read late in this close (`A-49` the primer-banner check still unbuilt, `A-52` the arrival-shapes list still unwritten, `A-53` store and page AGREE at 20 rows drawn by position, `W-3` the erdosproblems page unchanged on both legs), and `A-54`'s rebuild census session, dated 2026-09-24, did not happen.

> **`W-13` is new and it is the lesson, not a chore.** `A-55` shipped `scripts/check-served.mjs` on 2026-09-22 and closed the same day, which left that read with NO recurring caller: a closed row's `readCommand` is never swept (`--run A-55` answers `status "closed" is TERMINAL`, and a sweep dated a week out does not list it), and CI runs only its selftest. For two days the only instrument watching the product was one a human had to type. `W-13` carries the read on 2026-09-29 and its closeWhen asks for a caller that does not depend on anyone remembering — a scheduled job that files or updates ONE issue on a STALE verdict, kept out of the reverify suite. That is the next substrate piece worth pricing.

> **Tomorrow's only due gate is `A-47`, the depth audit (slice 7), dated 2026-09-25** — `node scripts/depth-audit.mjs`, last run 2026-09-21T15:44:29Z reading 29 audited / 17 sound / 0 defective / 6 unresolved / 6 unreachable. It is also the lane's best source of record-facing findings: four of the last six came from it. **The standing prediction was REPLACED on 2026-09-22** at `W-9`'s close, and CLAUDE.md now carries the live one — quote it from there, never from an older report. The retired wording was falsified on 2026-08-27 and again on 2026-09-05 and three later reports re-quoted it anyway, because the clause they copy from had never been updated.

> **`A-54`'s Wikipedia comparator is on its THIRD slip, dated 2026-09-28, and its condition is pre-committed.** If 2026-09-28 arrives with no table for it to point at, it is NOT re-dated a fourth time: it is dropped with a written reason, or scoped against the survey's own table, and that close names which. The dark window took two days out of the rebuild that date assumed, which makes firing more likely, and `phase2Slip3_2026_09_22` records that explicitly. `ramsey.html` and `copying.html` otherwise remain FROZEN: David lifted the freeze for exactly ONE sentence on 2026-09-22 ("Yes, fix that one sentence now and leave the rest of the page as it is"), it shipped in `aee164c`, and the second recorded defect on that page — "values" meaning entries in one sentence and bound slots in the next — is deliberately still unfixed and rides the rebuild.

> **Nothing waits on David.** Both board cards read `[answered]` (`943ef827` 2026-09-22T13:58:51Z, `5910378d` 2026-09-20). Upstream pull request #194 is open and is OURS, so it can never count as a `G-4` event; `W-12` reads it 2026-09-28. `G-4` itself read 0 outside arrivals in this close, measured: 30 raw issues, 30 ours by author, parts reconciling to 30.

> **Waker ladder — relaunch ranks 1, 2 and 3 at session start**, three separate background calls from `/listen` step 3b, never joined. Read each rung's own output file: `armed` and `already live (pid N)` are both fine; a rank-1 line reading `WAKE-SHORTEN` or `actionable loop-death signal` is a real fire. A context compact does not re-run `/listen`, and neither does a two-day pause.
<!-- hand:end banner -->

UNVERIFIED FIRST ACTION — matching execution evidence and current governing dependencies have not been established by these release commands.
Recovery read (PowerShell or bash, cwd: lane root): `git status --short` followed by `git log -5 --oneline`; reconcile the authored action in docs/daily-config.md with the current ruling and ledger before running it.

First command (also below, in place):

```bash
git pull --ff-only
# commits after this primer was written are invisible to it — read them before acting:
git log --oneline e35918084bc10c3cf376ceebbc4784ffd67c9a57..HEAD
```

### Primer — bounds-ledger, 2026-09-25

What ONLY this primer holds comes first; the FULL runnable **First action** block
sits at the foot, below the reads. The single command above is the same block's
first line, repeated for reference — it replaces nothing. Establish the governing action and its preconditions before running release commands.

_**Composed against HEAD commit `e35918084bc10c3cf376ceebbc4784ffd67c9a57` at 2026-09-25T00:11:19.720Z.** Nothing that landed after that instant is in this file. David's board answers especially: an answer he gives later cannot be here, and this primer will go on reading as though the work were not owed — that is exactly how two lanes concluded an OTA was not owed on 2026-09-04. Read them live, never off this page: `node scripts/sky.mjs answered-cards.mjs --project bounds-ledger --full`._

_Shared skylark-site scripts below are spelled `node scripts/sky.mjs …` — this lane vendors `scripts/sky.mjs` (S-20260906-75), so a shared script is named LANE-RELATIVE and resolves from a worktree too._

#### Owed gates — 1 due on/before 2026-09-25

_Ledger read at 2026-09-25T00:11:19.720Z by `cc-due-gates.collectDue` — the same resolver `check-due-gates-dispositioned` uses, so any difference is TIMING, not two answers. Rows filed or re-dated after that are NOT READ at generation — the close-out commit lands after this, so the COUNT above is stale by construction and this list cannot claim completeness. The list below, not the count, is the artifact; re-derive both before acting: `node scripts/sky.mjs check-due-gates-dispositioned.mjs --print --today=2026-09-25`._

Unfiltered as of the read time above — nothing was curated out of what the read could
see; a row absent here may simply have been filed after it ran.

- `A-47` (nextCheckDate=2026-09-25, open) — THE DEPTH AUDIT, David-ruled 2026-09-09: read our own cited records against their sources 
  - read: `node scripts/depth-audit.mjs — prints the verdict counts with the sampling rule and the denominator beside them, refuses a bare zero (exit 2) and a count that does not sum to the rows (exit 3). node scripts/depth-audit.mjs --corpus answers the population half. Literal outputs, the red-arm record and the retired placeholder are on this row in readCommandHistory2026_09_13.` · last run: 2026-09-21T15:44:29.609Z

#### What shipped since 24 hours ago

_Read at 2026-09-25T00:11:19.720Z; commits landing after that — the close-out especially — are not here. Re-derive: `git log --oneline --since='24 hours ago'`._

_No non-merge commit in the window. git was readable; this is a real zero._

**Yesterday's report:** `docs/daily/2026-09-22-prelaunch.md` (coverage 2026-09-22, 3 days before this primer). No findings fence declared in it.
Prior report: historical; re-read its findings and later changes before treating its opener as current.

**Today's report (owed):** write `docs/daily/2026-09-25-prelaunch.md` — OUTPUT obligation due at close, the artifact of record for service day 2026-09-25. Check it exists at close: `node scripts/sky.mjs check-daily-report-exists.mjs --date 2026-09-25` (it sweeps the fleet and applies each lane's coverageOffset; it takes no --project). <!-- owed-output path="docs/daily/2026-09-25-prelaunch.md" service-day="2026-09-25" due="close" -->

#### State — as of 2026-09-25T00:11:19.720Z (re-run the release command; never trust the value)

- **ledger**: 71 item(s): 53 closed · 18 open · as of 2026-09-25T00:11:19.720Z — release: `node scripts/sky.mjs gen-primer-first-action.mjs --repo . --target-date 2026-09-25 --json`

#### Id glossary — 19 id(s) this primer's day already has words for

_Each gloss is the ledger row's OWN title, not a re-derivation — paste it. Rows = every open ledger row plus every id named above. `(no ledger row)` means the id is named here and the ledger carries nothing under it: that is a finding, not a formatting gap._

- `A-2` — STANDING: drift-resolution log for the adopted surfaces…
- `A-9` — engineering-health P2 backlog (fix-on-touch, not scheduled…
- `A-45` — TIER-1 LEADING INDICATOR (orchestrator-approved…
- `A-47` — THE DEPTH AUDIT, David-ruled 2026-09-09: read our own…
- `A-49` — npm run close:primer passes when tomorrow's banner is…
- `A-50` — Importing one of our CLI scripts can run it — one selftest…
- `A-52` — Write down what an outside party acting on a record could…
- `A-53` — The audit store and the public page can disagree about how…
- `A-54` — Adopt Small Ramsey Numbers (Electronic Journal of…
- `G-2` — GOAL: contribute a verified bound improvement to the…
- `G-4` — GOAL (Tier-0 from G-3 close): an outside party acts on a…
- `S-20260906-75` — (no ledger row)
- `W-3` — Watch for acknowledgement of the erdosproblems.com/36…
- `W-4` — KP-78 applies to every new detector: demonstrate BOTH…
- `W-6` — Read window for the README report-an-error channel — the…
- `W-8` — Does a materially amended outward artifact go back to…
- `W-11` — Before asserting a claim is decision-ready, search this…
- `W-12` — Watch: does upstream act on PR #194, the [YKLBMWKCZGS2026]…
- `W-13` — STANDING: someone must CALL the served-bytes check — A-55…

### First action — bounds-ledger, 2026-09-25

Release commands — subordinate to the governing action.
HANDOFF INCOMPLETE — governing action not authored.

UNVERIFIED FIRST ACTION — these commands have no matching execution evidence here. Command text alone establishes neither safety nor the promised evidence, shell compatibility, or tomorrow's priority.

```bash
git pull --ff-only
node scripts/sky.mjs update-david-board.mjs --list --json --full-ids --project bounds-ledger
git rev-parse HEAD origin/main
git rev-list --count origin/main...HEAD
npm run verify > tmp/verify-out.txt 2>&1
node scripts/sky.mjs check-ci-status.mjs --workflow reverify.yml
```

Morning release commands — deliberately NOT READ at generation (a value written
last night would already be stale); run each, never trust a remembered one:
- `git -C . log -1 --format=%h%x20%s` — HEAD
- `git -C . status --porcelain` — working tree
- `node scripts/sky.mjs check-ci-status.mjs --workflow reverify.yml` — CI
- `node scripts/sky.mjs check-deployed-sha-drift.mjs` — deploy
  - WHERE IT LOOKED: no read-only deploy-state script in this lane's package.json (looked for ops:deploy-state, deploy-state, check-deploy, render-deploys, deploy:status, daily-snapshot). The fleet's check-deploy-state.mjs is not a substitute — it requires --fix <sha> --deploy <sha> and exits 1 without them — so this is the FLEET read, which takes no required flags. service: NOT DECLARED — no row for "bounds-ledger" in skylark-site/src/data/cc-deploy-gates.json (generatedAt 2026-09-22T02:18:27.672Z, 17 repo row(s)) — nothing in this repo declares that lane's Render service. No `--service` flag is emitted: the lane SLUG is not the service name (antebellum-twitter's is disunion-feed), and a guessed flag exits 1.

<!-- hand:begin narrative -->
_(orchestrator narrative — this region survives regeneration)_
<!-- hand:end narrative -->

<!-- generated by gen-primer-first-action.mjs (skylark-site) @ commit a78edc48c · lane bounds-ledger · for 2026-09-25 · at 2026-09-25T00:11:19.720Z -->
<!-- writtenAtSha: `e35918084bc10c3cf376ceebbc4784ffd67c9a57` — the lane's HEAD commit when this primer was written. Provenance anchor for check-primer-freshness leg ii; NOT a claim about HEAD now (the close-out commit that carries this file lands after generation). -->
<!-- reading this block: a generated line that is wrong in a way you can see FIXED in skylark-site's git log after the commit above is STALE, not a finding — regenerate before filing it. `git -C C:/dev/skylark/skylark-site log --oneline a78edc48c^{commit}..HEAD -- scripts/gen-primer-first-action.mjs src/lib/cc-primer-commands.mjs src/lib/cc-due-gates.mjs src/lib/cc-report-artifact.mjs src/lib/cc-hand-edit-merge.mjs src/lib/cc-cold-readability.mjs src/lib/cc-skeleton-gloss.mjs` -->
