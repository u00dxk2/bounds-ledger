<!-- GENERATED — regenerating REPLACES everything outside the hand: markers, so put prose inside them. Provenance stamp at the foot of this block. -->

<!-- hand:begin banner -->
> **KEEP THIS BLOCK UNDER 2000 CHARS.** The kickoff quotes it whole below that and cuts the MIDDLE
> above it, so a long banner loses its own instructions while keeping its head and tail. Test every
> line by "does a cold agent act differently because it read this?"
>
> **`npm run verify` HAS TWO REDS AND ONLY ONE OF THEM IS PRE-EXCUSED. READ THE EXIT CODE.**
> **Exit 3 with `check-brief` as the only failing leg is `A-41` and is NOT yours** — `/t/*` went
> Google-session-only on 09-04 and `cc_pin` was retired, so nothing can sign in from here.
> **ANY other exit code, or exit 3 with a different failing leg, is a real failure and IS yours
> today.** On 09-05 the banner said only "will exit 3"; it exited **1**, on a live drift carrying two
> record movements, and one digit was all that separated a real alarm from being waved through.
> Clearing condition for the 3: if it ever exits 0, the auth model changed and `A-41` has moved.
>
> **START LINE 4 OF THE FIRST ACTION BEFORE READING THIS DOCUMENT** — three minutes, all network,
> and on 09-05 it was the command that found the day:
> ```
> npm run verify > tmp/verify-out.txt 2>&1
> ```
>
> **Nothing is dated due on 09-06.** `A-34` is 09-08 (the ranked proposal for David — third slip,
> needs a session, open it with 09-05's two record movements). `A-33` and `A-43` are 09-12.
>
> **A red drift run is the alarm working.** Verify against PRIMARY SOURCES before `--snapshot`, then
> `npm run resnap`, then a SECOND `render-site.mjs` AFTER the commit, or the rows that just moved
> publish "date unknown". Close the `Drift:` issue by hand naming the resolving shas.
>
> **`npm run catches` is a CEILING, never a count.** On 09-05 it read 7 and two were records; the
> other five were reorders, an asterisk, a row moved to prose and an escaping fix.
<!-- hand:end banner -->

First command (also below, in place):

```bash
node ../skylark-site/scripts/update-david-board.mjs --list --json --full-ids --project bounds-ledger
```

### Primer — bounds-ledger, 2026-09-06

What ONLY this primer holds comes first; the FULL runnable **First action** block
sits at the foot, below the reads. The single command above is the same block's
first line, repeated so it can be running while you read — it replaces nothing.

_**Composed against HEAD commit `915d5f2395b684f8c5cd773b4ff8af96c4957a54` at 2026-09-05T16:48:28.243Z.** Nothing that landed after that instant is in this file. David's board answers especially: an answer he gives later cannot be here, and this primer will go on reading as though the work were not owed — that is exactly how two lanes concluded an OTA was not owed on 2026-09-04. Read them live, never off this page: `node ../skylark-site/scripts/answered-cards.mjs --project bounds-ledger --full`._

#### Owed gates — 0 due on/before 2026-09-06

_Ledger read at 2026-09-05T16:48:28.243Z by `cc-due-gates.collectDue` — the same resolver `check-due-gates-dispositioned` uses, so any difference is TIMING, not two answers. Rows filed or re-dated after that are NOT READ at generation — the close-out commit lands after this, so the COUNT above is stale by construction and this list cannot claim completeness. The list below, not the count, is the artifact; re-derive both before acting: `node ../skylark-site/scripts/check-due-gates-dispositioned.mjs --print --today=2026-09-06`._

_None due. This is a real zero: 1 clock store(s) were read and returned no row due on/before 2026-09-06._

#### What shipped since 24 hours ago

_Read at 2026-09-05T16:48:28.243Z; commits landing after that — the close-out especially — are not here. Re-derive: `git log --oneline --since='24 hours ago'`._

- commit `915d5f2` — The day's report, and three gates dispositioned against a world the cycle changed
- commit `5147d39` — The fourth artifact in the ratchet, exercised for the first time by a real cycle
- commit `7090832` — C-2 broke because upstream moved to the digit this lane already ruled unsafe
- commit `b44e3c2` — The second render, which is the only one that can date the rows that just moved
- commit `d834f10` — Two records moved overnight; five more pins moved with no bound behind them
- commit `9b5d6aa` — The fourth artifact in the ratchet: a README table that resnap does not touch
- commit `c9b3599` — Drift resolved at close: upstream swapped a cross-link on 88a, and no bound moved
- commit `5126065` — Close-out: G-4's own row now says its read date cannot be interpreted
- commit `a8cd408` — Hygiene 2026-09-04: the banner goes under the cap, and CLAUDE.md sheds the one section a tree can answer
- commit `f6192e2` — The one-pass audit found two more, and both of them are on the page
- commit `7e741c8` — A-33 leg 1: the alarm that has to be able to go quiet
- commit `356987f` — The reader holding the stale number was the one reader the search turned away
- commit `a962921` — A verdict reached the bus and never reached the row it was a verdict about

**Yesterday's report:** `docs/daily/2026-09-05-prelaunch.md` (coverage 2026-09-05, the prior day). No findings fence declared in it.

**Today's report (owed):** write `docs/daily/2026-09-06-prelaunch.md` — the artifact of record for service day 2026-09-06. Check it exists at close: `node ../skylark-site/scripts/check-daily-report-exists.mjs --date 2026-09-06` (it sweeps the fleet and applies each lane's coverageOffset; it takes no --project).

#### State — as of 2026-09-05T16:48:28.243Z (re-run the release command; never trust the value)

- **ledger**: 56 item(s): 36 closed · 20 open · as of 2026-09-05T16:48:28.243Z — release: `node ../skylark-site/scripts/gen-primer-first-action.mjs --repo . --target-date 2026-09-06 --json`

#### Id glossary — 21 id(s) this primer's day already has words for

_Each gloss is the ledger row's OWN title, not a re-derivation — paste it. Rows = every open ledger row plus every id named above. `(no ledger row)` means the id is named here and the ledger carries nothing under it: that is a finding, not a formatting gap._

- `A-2` — STANDING: drift-resolution log for the adopted surfaces…
- `A-7` — engineering-health P1s — four of five shipped 2026-08-09;…
- `A-9` — engineering-health P2 backlog (fix-on-touch, not scheduled…
- `A-20` — Should the fetch layer retry with backoff on 429/502? —…
- `A-29` — secret_scanning_validity_checks will not enable via API —…
- `A-31` — Should the public page publish be CI-gated? It is not, and…
- `A-33` — A change to a file we have filed an upstream report…
- `A-34` — David asked where else this ledger can have the kind of…
- `A-39` — A mid-run 429 becomes up to 339 requests in two seconds at…
- `A-41` — The brief leg became permanently unverifiable on…
- `A-42` — Two user-facing readers still key off OUR pin history and…
- `A-43` — Upstream moved Haugland's minimum-overlap bound to its…
- `C-2` — (no ledger row)
- `G-2` — GOAL: contribute a verified bound improvement to the…
- `G-4` — GOAL (Tier-0 from G-3 close): an outside party acts on a…
- `W-3` — Watch for acknowledgement of the erdosproblems.com/36…
- `W-4` — KP-78 applies to every new detector: demonstrate BOTH…
- `W-6` — Read window for the README report-an-error channel — the…
- `W-8` — Does a materially amended outward artifact go back to…
- `W-9` — Does this lane's detection method find what the lane…
- `W-10` — reverify.test.mjs alarm-title leg failed once and would…

### First action — bounds-ledger, 2026-09-06

Every line in the block below is a COMMAND, not a claim. A command re-reads at the
moment you run it and cannot go stale; a state line written last night can.

```bash
node ../skylark-site/scripts/update-david-board.mjs --list --json --full-ids --project bounds-ledger
git rev-parse HEAD origin/main
git rev-list --count origin/main...HEAD
npm run verify > tmp/verify-out.txt 2>&1
node ../skylark-site/scripts/check-ci-status.mjs --workflow reverify.yml
```

Morning release commands — deliberately NOT READ at generation (a value written
last night would already be stale); run each, never trust a remembered one:
- `git -C . log -1 --format=%h%x20%s` — HEAD
- `git -C . status --porcelain` — working tree
- `node ../skylark-site/scripts/check-ci-status.mjs --workflow reverify.yml` — CI
- **deploy read: NOT AVAILABLE** — no read-only deploy-state script in this lane's package.json (looked for ops:deploy-state, deploy-state, check-deploy, render-deploys, deploy:status, daily-snapshot). The fleet's check-deploy-state.mjs is not a substitute — it requires --fix <sha> --deploy <sha> and exits 1 without them

<!-- hand:begin narrative -->
_(orchestrator narrative — this region survives regeneration)_

**The glossary line reading `C-2 — (no ledger row)` is a FALSE POSITIVE, not a finding.** C-2 is a
hand CLAIM in `ledger/claims.json`, not a continuity item; the two id spaces share a shape and the
glossary reads only one of them. It appears because `A-43` names C-2 in its notes. Do not mint a row
to silence it.
<!-- hand:end narrative -->

<!-- generated by gen-primer-first-action.mjs (skylark-site) @ commit b7905498 · lane bounds-ledger · for 2026-09-06 · at 2026-09-05T16:48:28.243Z -->
<!-- writtenAtSha: `915d5f2395b684f8c5cd773b4ff8af96c4957a54` — the lane's HEAD commit when this primer was written. Provenance anchor for check-primer-freshness leg ii; NOT a claim about HEAD now (the close-out commit that carries this file lands after generation). -->
<!-- reading this block: a generated line that is wrong in a way you can see FIXED in skylark-site's git log after the commit above is STALE, not a finding — regenerate before filing it. `git -C ../skylark-site log --oneline b7905498..HEAD -- scripts/gen-primer-first-action.mjs src/lib/cc-primer-commands.mjs src/lib/cc-due-gates.mjs src/lib/cc-report-artifact.mjs src/lib/cc-hand-edit-merge.mjs src/lib/cc-cold-readability.mjs src/lib/cc-skeleton-gloss.mjs` -->

<!-- hand:begin closeout -->
### Close-out append — 2026-09-05 15:35 MT

Added after the sections above; nothing above was overwritten. **The banner still governs** — it was written at P2 and nothing today contradicted it.

**Primer re-read: `af6a180..5cd1375` — APPENDED.** Ten commits landed after P5, and the generated sections above predate all of them. What they changed is below; nothing above is rewritten.

**Today's load-bearing ships.**

- `d834f10` + `b44e3c2` — the drift cycle. **Two records moved upstream overnight** and the page now serves them: the bounded prime gap constant `240 → 186`, and the Steiner ratio lower bound `0.8559 → 0.860*`. Both verified against artifacts (the Lean manifest, the Zenodo record) before the mirror moved, never against the preprints' summaries of themselves.
- `88fe58f` — **the search fix, and the day's user-visible ship.** A reader arriving with `0.380927` — the value upstream carried until that morning, still cited nine times in arXiv:2601.16175 — got the empty state, because the haystack only knows values from OUR pin history. Hand-curated `searchAliases` now catch them. The alias asserts nothing and never renders.
- `0901fb7` — **five entry guards on `main` that printed nothing and exited 0 through a junction.** `check-deferrals` and `render-site --check` both measured silent, and the second is what stops a stale public page shipping.
- `48b57a5` — the verify receipt now names the leg that failed (`check:brief`) instead of hanging a true exit 3 on a leg that passed.

**Tomorrow's first action — and it is NOT a ship.** The orchestrator ruled the 09-05 P4 skip **PARTIAL**: two of its three clocks were declared, the third was not. Before any ship work, run P4's three reads and post the result.

```bash
node ../skylark-site/scripts/check-due-gates-dispositioned.mjs --print --today=2026-09-06
```

That is clock 2. Clock 1 is **the ≥7d carried-row sweep across all 57 rows in `continuity/items.json`** — each row gets a decision, a dated deferral with a reason, or a strike; tier them, do not re-date them blind. Clock 3 is the report of record for the service day, named by path. **An explicit zero closes the skip retroactively; silence does not.**

**Watch items maturing tomorrow — DERIVED, not authored.** `check-due-gates-dispositioned --print --today=2026-09-06` → `0 gate(s) due on/before 2026-09-06`, `RESULT: PASS (exit 0)`, swept 57 rows. Nothing is owed on the clock tomorrow. Nearest: `A-34` and `A-20` on 09-08; `A-33` and `A-43` on 09-12.

**CI:** reverify: completed/success @ `5cd1375`; pages-build-deployment: completed/success @ `5cd1375`.

**What NOT to touch tomorrow.** The scheduled `reverify` alarm and its issue-filing step — it fired correctly at 09:23Z, titled a real record movement `Drift:` rather than `Check error:`, and filed issue #33. It works because it is allowed to go red for a reason nobody pre-excused.

**Evangelism read.**

1. **The one core problem, in the user's words:** *"Is the number I'm about to cite still what the source says?"*
2. **The would-tell-a-friend moment:** *"I was about to cite a stale bound and this caught it."* **Today closed real distance on it** — two records genuinely moved and the ledger had both inside six hours, and the reader arriving with the superseded figure is no longer turned away. The remaining distance is reach, not correctness: the instrument works and almost nobody is pointed at it.
3. **Retention / word-of-mouth trend:** `npm run traffic` reads **1 unique viewer in the latest 14-day window**, 6 viewer-days since the public flip. The return-rate proxy prints **INAPPLICABLE at n=1**, below its own N_min of 3, and prints its arithmetic so the figure stays auditable. **Page readership is not instrumented and deliberately will not be** — the published page carries no analytics; repo views are not a proxy for page readers.
4. **Real-user advocacy signal this session:** none. `npm run reports` → **0 outside arrivals, measured** (29 issues fetched, all accounted for as ours, parts reconcile to the raw total). A measured zero, not a dead probe.

**Tier-0 status.** `G-4` — an outside party acts on a watched record WITHOUT us filing the report — remains at **0**, expected. Its 2026-09-26 gate is known-uninterpretable in advance: the threshold is 30 unique viewers and the trajectory reaches roughly 8-10. That is routed into `A-34`, due 09-08, and re-pointing it is David's call, not this lane's.

**Nothing is blocked on David, and nothing was carded today.** `A-43`'s outward question — whether upstream's truncated Haugland bound is one wrong cell — became **fileable** this afternoon when the four primary-source lookups landed (`a3bd3fa`). It is a card for a session that has the adversarial review in hand, not a blocker.
<!-- hand:end closeout -->
