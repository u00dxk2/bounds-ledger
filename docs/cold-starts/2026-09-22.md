<!-- GENERATED — regenerating REPLACES everything outside the hand: markers, so put prose inside them. Provenance stamp at the foot of this block. -->

<!-- hand:begin banner -->
2026-09-21 close. **The day's ship is `A-55`, and its first command is the one that STARTS the work rather than the one that measures it** — an authoring rule this lane adopted today and is applying here for the first time. `A-55` is the row filed today because a GitHub Pages build failure is invisible to every gate this lane runs. Fetch a live page and read what a reader is actually served:

```bash
curl -s https://u00dxk2.github.io/bounds-ledger/c/35a.html
```

That page should contain the string `Reference column reads Folklore`, which the 2026-09-21 ship put there. If it does not, the publish path is broken again and that is the day's finding before anything else is planned. The check `A-55` owes is that read made mechanical: fetch a known page, assert a string the current commit put there, prove both polarities (stale page fires it, fresh page silent), and wire it as an INDICATOR, never a gate — a transient GitHub failure must not turn this suite permanently red, and today's two failures were transient.

**Why it is the ship and not substrate:** on 2026-09-21 the Pages builds for `874e411` and `07b8f0b` both ERRORED while CI was green, `npm run verify` was exit 0 and the drift alarm was silent. The live site served the previous tip and every instrument in this repo said the day had shipped. It surfaced only because the receipt's delivery leg was read by hand. For a lane whose whole product is a published page, that is the silent-ship failure mode, and nothing watches it.

> **Second: `A-54`'s catch-counter blind-spot statement, in every place a detection figure is printed** — the surfaces are listed on `A-54.phase2Slip2026_09_20` and are not re-derived here. The CHOICE was taken on 2026-09-20 and is not reopened: state the blind spot, do not make the second area countable. Two of those surfaces are public and one is David-facing, so it owes an adversarial review and the brief's whole-file voice lint. **The Wikipedia comparator is dated 2026-09-23, not tomorrow** — re-dated on 2026-09-21 before its date, with the capacity named, and `A-54.phase2Slip2_2026_09_21` carries the pre-committed condition: if 2026-09-22 closes with the comparator unstarted, that close says the third slip with a date, on the 22nd and not on the 23rd.

> **`ramsey.html` and `copying.html` remain FROZEN.** David ruled on 2026-09-20: rebuild the table from the underlying papers as our own arrangement, say so on the page, keep the survey credited, and "until that is done, leave the page as written". Two real defects in that page's own coverage prose were measured on 2026-09-21 and deliberately NOT fixed — the promised range holds 76 pairs while the table prints 72, with R(9, 14), R(9, 15), R(10, 13) and R(10, 14) absent and unnamed; and "values" means entries in one sentence and bound slots in the next (72, then 128). Both are recorded on `A-54.pageDefects2026_09_21` and carried into the rebuild's render. A ruling was requested at P4 on whether an accuracy clause is exempt from the freeze; **absent an answer, do not edit the page.** The rebuild's first census session is 2026-09-24, its rate checkpoint 2026-09-28.

> **EIGHT gates are due 2026-09-22 and most of them are READS, not builds** — `A-46`, `A-54`, `A-55`, `G-2`, `W-4`, `W-8`, `W-9`, `W-10`. Two share one command (`node scripts/reverify.test.mjs`). Run the steward cadence first, disposition the reads, and do not let eight rows displace the ship: `A-55` and `A-54`'s statement are the work, the other six are readings to record.

> **Upstream pull request #194 is OPEN, and it is ours.** The dead-link correction David approved on 2026-09-21 is live at https://github.com/teorth/optimizationproblems/pull/194. `W-12` reads its state on 2026-09-28. **It is not a G-4 event and must never be counted as one** — we authored and sent it. If it merges, that is an accepted correction and a dated public acknowledgement, which is `G-2`'s shape.

> **The depth audit reads 29 rows — 17 sound, 0 defective, 6 unresolved, 6 unreachable** (`node scripts/depth-audit.mjs`, 2026-09-21). Slice 7 is dated 2026-09-25 on the alternating cadence, so it is NOT tomorrow's work. Three citation discrepancies found in slice 6 are recorded as found-not-filed on `A-47.slice6Landed2026_09_21`; whether they are worth sending is a decision to take once #194 is answered, not three more drafts.

> **Waker ladder — relaunch ranks 1, 2 and 3 at session start.** The commands are in `/listen` step 3b, one per rank, as separate background calls, never joined. Read each rung's OUTPUT file, not its launch acknowledgement: `armed` and `already live (pid N)` are both fine, while a rank-1 line reading `WAKE-SHORTEN` or `actionable loop-death signal` is a real fire. A context compact does not re-run `/listen`.
<!-- hand:end banner -->

UNVERIFIED FIRST ACTION — matching execution evidence and current governing dependencies have not been established by these release commands.
Recovery read (PowerShell or bash, cwd: lane root): `git status --short` followed by `git log -5 --oneline`; reconcile the authored action in docs/daily-config.md with the current ruling and ledger before running it.

First command (also below, in place):

```bash
git pull --ff-only
# commits after this primer was written are invisible to it — read them before acting:
git log --oneline 90659096cac31ffc7cf77a1795073ab1a92a034e..HEAD
```

### Primer — bounds-ledger, 2026-09-22

What ONLY this primer holds comes first; the FULL runnable **First action** block
sits at the foot, below the reads. The single command above is the same block's
first line, repeated for reference — it replaces nothing. Establish the governing action and its preconditions before running release commands.

_**Composed against HEAD commit `90659096cac31ffc7cf77a1795073ab1a92a034e` at 2026-09-21T17:02:00.326Z.** Nothing that landed after that instant is in this file. David's board answers especially: an answer he gives later cannot be here, and this primer will go on reading as though the work were not owed — that is exactly how two lanes concluded an OTA was not owed on 2026-09-04. Read them live, never off this page: `node scripts/sky.mjs answered-cards.mjs --project bounds-ledger --full`._

_Shared skylark-site scripts below are spelled `node scripts/sky.mjs …` — this lane vendors `scripts/sky.mjs` (S-20260906-75), so a shared script is named LANE-RELATIVE and resolves from a worktree too._

#### Owed gates — 8 due on/before 2026-09-22

_Ledger read at 2026-09-21T17:02:00.326Z by `cc-due-gates.collectDue` — the same resolver `check-due-gates-dispositioned` uses, so any difference is TIMING, not two answers. Rows filed or re-dated after that are NOT READ at generation — the close-out commit lands after this, so the COUNT above is stale by construction and this list cannot claim completeness. The list below, not the count, is the artifact; re-derive both before acting: `node scripts/sky.mjs check-due-gates-dispositioned.mjs --print --today=2026-09-22`._

Unfiltered as of the read time above — nothing was curated out of what the read could
see; a row absent here may simply have been filed after it ran.

_Shared read #1 — `node scripts/reverify.test.mjs` — answers 2 gate(s) below; per-row trailing comments differ, see each ledger row._

- `A-46` (expectedSignalBy=2026-09-22, open) — A close-out post announcing a FIX is textually identical to one reporting the live defect 
  - read: `node ../skylark-site/scripts/read-msg.mjs 42cc5245 --project bounds-ledger # the close post whose sentence the lead re-read as a live defect. POSITIVE CONTROL that the pairing is real rather than remembered: `git -C . show --format=%aI -s 27d4bcd` prints 2026-09-07T15:47:16-06:00 = 21:47:16Z, and the msg above is stamped 21:48:39Z — the fix predates the post announcing it by 83 seconds, so a lead built from that post was reading a repaired record.` · last run: NEVER
- `A-54` (expectedSignalBy=2026-09-22, open) — Adopt Small Ramsey Numbers (Electronic Journal of Combinatorics dynamic survey DS1) as the
  - read: `node scripts/check-claims.mjs # exit 0 means every fetchable claim holds, and C-12, C-13 and C-14 are not manual, so all three are among them. POSITIVE CONTROL that the three are in the set it read: its summary line counts 244 claims as of 2026-09-17, against 241 before this row. Exit 1 prints a BROKEN or UNREACHABLE row naming the claim; for these three, a BROKEN row is a new survey revision to verify, and UNREACHABLE is a host refusing the fetch, which is NOT a revision.` · last run: NEVER
- `A-55` (expectedSignalBy=2026-09-22, open) — A GitHub Pages build failure is invisible to every gate this lane runs, and the page is th
  - read: `gh api repos/u00dxk2/bounds-ledger/pages/builds --jq '.[0:3] | .[] | {commit, status, error: .error.message}' — prints the three most recent Pages builds with their status. This is the DIAGNOSTIC, not the check this row owes: it reads GitHub's own record rather than what a reader is served.` · last run: NEVER
- `G-2` (expectedSignalBy=2026-09-22, open) — GOAL: contribute a verified bound improvement to the stewarded inventory (steward → stewar
  - read: `node -e "const fs=require('fs');import('./search/27b/verifier.mjs').then(v=>{const c=JSON.parse(fs.readFileSync('search/27b/sulanke-certificate.json','utf8'));const pc=v.verifyCertificate(c);console.log('positive control - verifier still scores the KNOWN 1980 record:',JSON.stringify(pc));if(!pc.ok)throw new Error('verifier cannot score the known record - the instrument is broken, not the search');for(const f of ['round1-results.json','round2-results.json','round3-results.json']){const j=JSON.parse(fs.readFileSync('search/27b/'+f,'utf8'));const rows=j.results||j.rows||[];const vals=rows.map(r=>typeof r.bestChi==='number'?r.bestChi:(typeof r.chi==='number'?r.chi:null)).filter(x=>x!==null);const best=vals.length?Math.max(...vals):null;console.log(f,'rows='+rows.length,vals.length?('best chi='+best+' over '+vals.length+' scored row(s) - beats the record only if >9'):'0 rows carry a chromatic value (keys checked: bestChi, chi) - eliminated before the chromatic gate; verdicts: '+rows.map(r=>r.verdict||'?').join('/'))}})"` · last run: NEVER
- `W-10` (expectedSignalBy=2026-09-22, open) — reverify.test.mjs alarm-title leg failed once and would not reproduce
  - read: shared read #1 (printed once above) · last run: NEVER
- `W-4` (nextCheckDate=2026-09-22, open) — KP-78 applies to every new detector: demonstrate BOTH sides at write time
  - read: shared read #1 (printed once above) · last run: NEVER
- `W-8` (expectedSignalBy=2026-09-22, open) — Does a materially amended outward artifact go back to David before it sends?
  - read: `gh issue list --repo teorth/optimizationproblems --author u00dxk2 --state all --limit 5 # SEPARATOR CONVERTED 2026-09-02 (bonus cycle), em dash to hash. W-8 asks whether a materially amended outward artifact returns to David before sending, and NO COMMAND CAN ANSWER THAT: the answer exists only in something David says, and note2 records the conservative default governing every send until he does. What IS checkable is whether the question has gone live again, i.e. whether we have sent anything since 2026-08-21. Literal output 2026-08-26: #150 (closed 2026-08-23), nothing newer. USE THE ISSUE LIST, NOT gh pr list: our most recent outward artifact is an ISSUE, so a PR-only query returns #141 from 2026-08-05 and is BLIND to the very send this row is about — that miswrite was caught here on 2026-08-26 by running the command before writing it down. POSITIVE CONTROL: the listing must contain #150; an empty list means the query is wrong, not that we have never sent.` · last run: NEVER
- `W-9` (expectedSignalBy=2026-09-22, open) — Does this lane's detection method find what the lane exists to find?
  - read: `npm run catches # SEPARATOR CONVERTED 2026-09-02 (bonus cycle), em dash to hash. this is note3 branch (c): before reading a quiet window as a DETECTION failure, establish whether upstream was moving at all. Literal output 2026-08-26: 15 movements on distinct pins across 6 weeks, current partial week 2 (pin:53a:L, pin:56a:U), 0 COMPLETED consecutive dry weeks — so the surface IS still moving and an empty window cannot be blamed on upstream silence. THE COUNTER CANNOT ANSWER W-9'S ACTUAL QUESTION and says so in its own stdout: it counts drift DETECTION only, so the instrument-vs-record ratio remains a human read of the daily classification sentences. POSITIVE CONTROL: the per-week table must show at least one non-zero week; an all-zero table means the git-history walk failed rather than that nothing moved.` · last run: NEVER

#### What shipped since 24 hours ago

_Read at 2026-09-21T17:02:00.326Z; commits landing after that — the close-out especially — are not here. Re-derive: `git log --oneline --since='24 hours ago'`._

- commit `9065909` — P4 2026-09-21: the 35a page names its own Reference column, and the publish blind spot gets a dated row
- commit `2efa648` — A-51 closes on the send: PR #194 is open upstream, and W-12 watches what happens to it
- commit `07b8f0b` — A-51: the draft reached David's board, and the row stays open so the queue counter stays true
- commit `874e411` — A-47 slice 6: five more cited rows read against their sources, and the page stops hiding the draws it could not read
- commit `d7f10db` — P2 2026-09-21: A-54's comparator slip is said a day early, and the rebuild is scoped on the reader it already has
- commit `62179bd` — Primer 2026-09-21: the first command was RUN, and its count has not moved in four days
- commit `059f3e7` — A-54: David says ship the partial table, and tomorrow's primer stops telling the next agent to wait
- commit `8702fc3` — Hygiene 2026-09-20: the day's report, tomorrow's primer, and a fleet-wide unauthored contract found by looking for one field
- commit `30d5e02` — A-54 P4: the slip fired, and it is said today with a date rather than on the 21st
- commit `988ca44` — A-54: David rules the table gets rebuilt from the underlying papers, and the page stays as written until it is

**Yesterday's report:** `docs/daily/2026-09-21-prelaunch.md` (coverage 2026-09-21, the prior day). No findings fence declared in it.
Prior report: historical; re-read its findings and later changes before treating its opener as current.

**Today's report (owed):** write `docs/daily/2026-09-22-prelaunch.md` — OUTPUT obligation due at close, the artifact of record for service day 2026-09-22. Check it exists at close: `node scripts/sky.mjs check-daily-report-exists.mjs --date 2026-09-22` (it sweeps the fleet and applies each lane's coverageOffset; it takes no --project). <!-- owed-output path="docs/daily/2026-09-22-prelaunch.md" service-day="2026-09-22" due="close" -->

#### State — as of 2026-09-21T17:02:00.326Z (re-run the release command; never trust the value)

- **ledger**: 70 item(s): 49 closed · 21 open · as of 2026-09-21T17:02:00.326Z — release: `node scripts/sky.mjs gen-primer-first-action.mjs --repo . --target-date 2026-09-22 --json`

#### Id glossary — 27 id(s) this primer's day already has words for

_Each gloss is the ledger row's OWN title, not a re-derivation — paste it. Rows = every open ledger row plus every id named above. `(no ledger row)` means the id is named here and the ledger carries nothing under it: that is a finding, not a formatting gap._

- `A-2` — STANDING: drift-resolution log for the adopted surfaces…
- `A-9` — engineering-health P2 backlog (fix-on-touch, not scheduled…
- `A-45` — TIER-1 LEADING INDICATOR (orchestrator-approved…
- `A-46` — A close-out post announcing a FIX is textually identical…
- `A-47` — THE DEPTH AUDIT, David-ruled 2026-09-09: read our own…
- `A-49` — npm run close:primer passes when tomorrow's banner is…
- `A-50` — Importing one of our CLI scripts can run it — one selftest…
- `A-51` — Upstream's [YKLBMWKCZGS2026] reference in the…
- `A-52` — Write down what an outside party acting on a record could…
- `A-53` — The audit store and the public page can disagree about how…
- `A-54` — Adopt Small Ramsey Numbers (Electronic Journal of…
- `A-55` — A GitHub Pages build failure is invisible to every gate…
- `C-12` — (no ledger row)
- `C-13` — (no ledger row)
- `C-14` — (no ledger row)
- `G-2` — GOAL: contribute a verified bound improvement to the…
- `G-4` — GOAL (Tier-0 from G-3 close): an outside party acts on a…
- `KP-78` — (no ledger row)
- `S-20260906-75` — (no ledger row)
- `W-3` — Watch for acknowledgement of the erdosproblems.com/36…
- `W-4` — KP-78 applies to every new detector: demonstrate BOTH…
- `W-6` — Read window for the README report-an-error channel — the…
- `W-8` — Does a materially amended outward artifact go back to…
- `W-9` — Does this lane's detection method find what the lane…
- `W-10` — reverify.test.mjs alarm-title leg failed once and would…
- `W-11` — Before asserting a claim is decision-ready, search this…
- `W-12` — Watch: does upstream act on PR #194, the [YKLBMWKCZGS2026]…

### First action — bounds-ledger, 2026-09-22

Release commands — subordinate to the governing action.
HANDOFF INCOMPLETE — governing action not authored.

UNVERIFIED FIRST ACTION — these commands have no matching execution evidence here. Command text alone establishes neither safety nor the promised evidence, shell compatibility, or tomorrow's priority.

```bash
git pull --ff-only
node scripts/sky.mjs update-david-board.mjs --list --json --full-ids --project bounds-ledger
git rev-parse HEAD origin/main
git rev-list --count origin/main...HEAD
npm run verify > tmp/verify-out.txt 2>&1
node scripts/sky.mjs check-ci-status.mjs --workflow reverify.yml
```

Morning release commands — deliberately NOT READ at generation (a value written
last night would already be stale); run each, never trust a remembered one:
- `git -C . log -1 --format=%h%x20%s` — HEAD
- `git -C . status --porcelain` — working tree
- `node scripts/sky.mjs check-ci-status.mjs --workflow reverify.yml` — CI
- `node scripts/sky.mjs check-deployed-sha-drift.mjs` — deploy
  - WHERE IT LOOKED: no read-only deploy-state script in this lane's package.json (looked for ops:deploy-state, deploy-state, check-deploy, render-deploys, deploy:status, daily-snapshot). The fleet's check-deploy-state.mjs is not a substitute — it requires --fix <sha> --deploy <sha> and exits 1 without them — so this is the FLEET read, which takes no required flags. service: NOT DECLARED — no row for "bounds-ledger" in skylark-site/src/data/cc-deploy-gates.json (generatedAt 2026-09-20T23:08:19.737Z, 17 repo row(s)) — nothing in this repo declares that lane's Render service. No `--service` flag is emitted: the lane SLUG is not the service name (antebellum-twitter's is disunion-feed), and a guessed flag exits 1.

<!-- hand:begin narrative -->
_(orchestrator narrative — this region survives regeneration)_
<!-- hand:end narrative -->

<!-- generated by gen-primer-first-action.mjs (skylark-site) @ commit 68c12f611 · lane bounds-ledger · for 2026-09-22 · at 2026-09-21T17:02:00.326Z -->
<!-- writtenAtSha: `90659096cac31ffc7cf77a1795073ab1a92a034e` — the lane's HEAD commit when this primer was written. Provenance anchor for check-primer-freshness leg ii; NOT a claim about HEAD now (the close-out commit that carries this file lands after generation). -->
<!-- reading this block: a generated line that is wrong in a way you can see FIXED in skylark-site's git log after the commit above is STALE, not a finding — regenerate before filing it. `git -C C:/dev/skylark/skylark-site log --oneline 68c12f611^{commit}..HEAD -- scripts/gen-primer-first-action.mjs src/lib/cc-primer-commands.mjs src/lib/cc-due-gates.mjs src/lib/cc-report-artifact.mjs src/lib/cc-hand-edit-merge.mjs src/lib/cc-cold-readability.mjs src/lib/cc-skeleton-gloss.mjs` -->
