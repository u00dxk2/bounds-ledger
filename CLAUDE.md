# CLAUDE.md — bounds-ledger

@AGENTS.md

Cross-vendor invariants (commands, what-never-to-hand-edit, the outward gate, definition of done) live in `AGENTS.md` above — this file carries only the Claude-specific behavior and the rationale/anchors behind those invariants. Portable facts go THERE, not here; move, never duplicate.

Project-specific guidance. The global `~/.claude/CLAUDE.md` still applies (MT dates, secrets, task-completion docs step). Start each session with the cold-start primer at `docs/cold-starts/<today-MT>.md`, then `continuity/items.json`.

## What this lane is

A **steward** of drifting mathematical records: a reproducible, continuously re-verified ledger that alarms when a cited record (bound / constant / certificate) drifts. Not a record-search engine. Full thesis in `README.md`. Pre-launch.

**Rail: PACED (David-ruled 2026-08-01, effective 2026-08-02; replaces the lite default).** Each session: the steward cadence FIRST (read the overnight run's LOG not the badge; drift cycle if red; local `npm run check` for the W-3 advisory legs), then **≥1 concrete launch increment from A-13's backlog** (first-PR prep, README-as-product, history sweep, adversarial review — the exit is the public flip, tracked as A-13 with the decision proposed to David at streak day 14 = 2026-08-06). **Weekly forced-decision pass** over `continuity/items.json` (stale items move or die). Still **no self-rating**.

## Commands

See `AGENTS.md` § Commands (imported above) — the command list is single-sourced there. What each self-test covers: reverify matcher, claim matcher, pin extractor, brief verdicts, sweep patterns.

## Conventions (the load-bearing ones)

- **Curated vs live records: track both, labeled, NEVER blend.** A live/leaderboard claim graduates to the curated column only via a checkable construction or acceptance into a curated source. (Reconciliation #1 finding 3.)
- **A `manual: true` claim reports UNVERIFIED, never green.** When a source blocks automated fetch *from CI* (erdosproblems.com 403s datacenter IPs), the checker cannot re-run it, so it stays UNVERIFIED until a human — or a local `npm run check` — re-verifies. A done-click or hand-verify does not flip it green. That is the "never silently trusted" rule made mechanical, and 2026-07-25 is the argument for keeping it: an attempt to automate C-7 away was caught by CI within minutes precisely because UNVERIFIED-vs-UNREACHABLE is tracked honestly. **A `manual: true` pin on a blocked source is still worth adding** (C-9, 2026-07-26): it reports UNVERIFIED in CI and never touches counts or the exit code, while a local run gets a real advisory read. What failed on 7/25 was making such a pin *fetchable*, not pinning the fact.
- **Verify an artifact written FOR an audience from that audience's environment, before review** (endorsed 2026-08-16). The README's 1-minute check passed only in a shell holding `CC_PROMPTS_PIN`; `npm run check` exited 3 for every visitor. Re-running in the author's shell cannot find an environment defect. Strip author state first: `env -u <VAR>`, fresh clone, no repo-local config. This generalizes the capability/environment rule written after the 2026-07-25 CI incident; it is not merely a CI lesson. **Degenerate case, 2026-08-18: verify a documented command by RUNNING it.** The primer used `git rev-parse --short HEAD origin/main`; `--short` accepts one revision, exited 128 (`fatal: Needed a single revision`), and its leading position in an `&&` chain skipped the cadence for anyone pasting it. The working form existed in prose but was never run. Treat each line as its own artifact: a KP-78 "both answers demonstrated" note certifies only the command it ran, not adjacent lines. Do not try `--short=7`; it also exits 128 because the flag's arity is fixed. Finding: `docs/findings/2026-08-18-the-guard-against-a-wrong-commit-read-never-ran.md`.
- **The visitor path is part of the steward cadence.** Run the README's credential-free check (`node scripts/reverify.mjs --check && node scripts/check-claims.mjs`, then the tamper/restore pair) as its own cadence step. Every other indicator here measures the ledger's internal correctness — drift caught, claims holding, streak days — and all of them were green on 2026-08-16 while the front door was broken for every visitor. G-3 would reflect that eventually, at a resolution of about one event a month; ninety seconds is better.
- **Steward cadence runs `npm run check` FIRST, then reads the overnight CI log.** The two answer different questions and only one is actionable at 07:45: the check says *is the mirror in sync now*, the log says *did the alarm fire overnight and go unnoticed*. Both are needed; the order is not arbitrary. On 2026-08-12 the overnight green log was four and a half hours stale and described a world that no longer existed — worse than no log, because it buys false confidence. Read the log for the **final pushed commit**, never a earlier one (2026-08-11: a close-out verified green *before* its own last push and a red run sat unnoticed for seven hours).
- **On an upstream editing burst, resolve once and stop.** Upstream pushed five times on 2026-08-12, three of them rewording one attribution paragraph, and we chased all of it through three resolution cycles in forty minutes. **A burst inflates the drift-cycle count in a way that looks like productivity and is really a measure of upstream's typing speed** — which corrupts the reading of the streak and of the catches-per-week indicator, not just the forty minutes. Rule: after a second consecutive red caused by an upstream push inside an hour, resolve to the latest sha, record the burst, and let the scheduled run be the next check. A red run during a burst is the alarm working; the mirror's contract is byte-fidelity at a **known sha**, not at every instant.
- **Positive control before any absence claim (drift-cycle rule, ruled in 2026-08-12).** Before concluding that a cited artifact lacks something, **name the token you found in the SAME fetch that proves you had the right document.** A positive match is self-validating; "the word is absent" cannot go red on the wrong document, so it checks nothing. See the HTTP-200 gotcha below for the incident this comes from — a prose rule already existed and did not fire, which is why this one is worded as a control with a named output.
- **Gloss the sha CLASS on first use in anything that leaves the repo.** Upstream `teorth/optimizationproblems` shas do not resolve locally: `git cat-file -t 603052d` returns *not a valid object*, and an unglossed arrow chain of them reads to an outside reader as local commits that do not exist — indistinguishable from fabrication until they go looking. Write "(an upstream sha; it does not exist in this repo)" the first time. This bit a retro on 2026-08-12 whose figures were all correct. **Same rule for upstream PATHS, found 2026-08-15 by `check-doc-references.mjs`:** a bare `constants/<name>.md` in our prose is upstream's path, not ours, and resolves to nothing at our HEAD — write the mirrored `ledger/teorth-optimizationproblems/…` form in prose, and keep the bare upstream form only inside file maps and drift reports, where "path as upstream sees it" is the documented key. Note that the first draft of this very rule quoted the bare path as its example and tripped the checker — the note explaining a fix becoming a fresh instance of it is a shape this repo has now hit five times.
- **Snapshot-on-drift discipline.** Treat red `reverify` as the alarm, not a bug: records are supposed to move. Verify the change against **primary sources** before `--snapshot`, then commit the refreshed mirror deliberately; never snapshot to silence red. **Red is not automatically a record event, and editorial drift is real drift** (2026-07-28: dead cross-link and missing `C` in a LaTeX inequality; no bound moved and pins were byte-identical). The mirror promises byte-fidelity, not only numeric fidelity, so never wave editorial changes through as cosmetic: verify against repo content—the link exists, the corrected inequality is right—and use the same verify → snapshot → re-pin cycle. **The ratchet is THREE steps** (corrected 2026-08-14): snapshot; `node scripts/extract-pins.mjs`, then commit regenerated pins (they never auto-follow, so moved rows remain BROKEN in `check-claims`); and `node scripts/render-state-block.mjs` (README file/claim counts otherwise go STALE). Omitting the third step made `npm run check` exit 1 on 2026-08-14. Use `npm run resnap` (added that day) to run all three in order.
- **Generated pins assert listing position, never "the record".** The generated claims pin the LAST-LISTED row of each bounds table verbatim. Do not "upgrade" them to record-row claims: numeric record-ranking is defeated by symbolic cells (`$K_{DR}+10^{-26}$`), negatives, and O(·) asymptotics (a min/max prototype mis-picked 10a/21a/41a on 2026-07-24), and auto-asserting "record" would put unverified mathematical statements in our own ledger. Hand claims (C-1…C-9) may say "record" because a human verified it.
- **Second surface (A-6, adopted 2026-07-25) is ENTRY-LEVEL claims, not a mirror.** `teorth/erdosproblems` (the community metadata DB behind erdosproblems.com, CI-reachable via raw.githubusercontent) is stewarded through pins on specific entries in `teorth/erdosproblems:data/problems.yaml` (C-8 = problem 36), NOT a byte-level mirror: the repo is pushed near-daily, so a whole-file drift alarm would be permanently red — and an always-red alarm carries no information. Pin stable field spans; stop before `last_update` (churns on any edit). The repo takes PRs on the metadata table — an accepted metadata-correction PR is a public dated external ack (G-3), but a PR is outward contact: adversarial review + David's gate, as always. Bounds do NOT live in that repo (verified 2026-07-25) — a bound correction can never be a PR there.
- **Prove the instrument can fail (KP-78).** Ship no detector—test, guard, lint, probe, `--check`, alarm—until it fires with the condition present and stays silent when absent; record both outputs in the finding/commit. An always-firing detector satisfies the first half while proving nothing. First prove the mutation landed with `git diff --numstat`: `\n` against CRLF silently no-opped and falsely condemned the pipefail guard on 2026-07-25. **Test recent commits, not only the test directory.** The 2026-07-31 audit found A-8's hand-added CI step had no detector preserving it: `docs/findings/2026-07-31-kp78-prove-the-instrument-can-fail.md`, watch W-4. **Extension, 2026-08-04: one proven probe does not validate its enumeration.** `gh secret list` was positively controlled on other repos, but "0 secrets, 0 deploy keys, 1 collaborator" still missed four GitHub Apps (`vercel`, `render`, `cursor`, `claude`, visible only in `/check-suites`; scope needs an app-authorized token). A probe licenses only its claim. For enumeration ask what it can never show (A-13 F-4). **Extension, 2026-08-17: prove non-firing means the CONDITION is absent, not the INSTRUMENT.** The alarm-title guard resolved `bash` from `PATH` on PowerShell, spawn failed, returned `""`, and `assert.doesNotMatch(title, /^Drift:/)` passed on empty output. Before asserting output lacks X, assert output exists; otherwise a broken instrument certifies absence.
- **The hosted brief drifts from its source, and hand-checking it under-reports.** `docs/lane-brief.md` is ported to `/t/lanes/bounds-ledger` by the orchestrator, so the page can silently fall behind. It did — and two consecutive hand-checks (7/30, 7/31) each reported ONE missing block because both searched for the block the primer named, instead of deriving the expected set from the source. `check-brief.mjs` (2026-07-31) derives it and found **two** missing, stale since 26 July. Recognition-over-recall applies to our own checks: a hand-check that inherits its search term from a doc can only ever confirm that doc. **Deliberately NOT in the CI drift job** — it would go red and stay red until an orchestrator re-port lands, and a permanently-red alarm carries as much information as a permanently-green one. It runs in `npm run check` and **stays out of CI permanently** — ~~joins CI the run after the page first reads green~~, a pre-commitment written 7/31 that was **retired 2026-08-03, the very day it first fired**. The page read green for the first time that morning, and the clause would have sent the next session to wire a check into CI that CI cannot run: the 8/02 sign-in wall means a runner with no pin exits 3 UNVERIFIABLE, so the only way to satisfy the clause is putting `CC_PROMPTS_PIN` in repo secrets — forbidden with a public flip pending. A threshold derived from the old regime kept reading as authoritative after the regime changed (the A-11 class, 2026-07-31). the lane's goal signal measures the LEDGER; a stale brief must not consume it.
- **David-facing prose is linted; which surface decides how.** `node ../skylark-site/scripts/check-david-voice.mjs <file>` (standard: `skylark-site/docs/skylark-voice-standard.md`, adopted 2026-08-03 after David said again that he cannot understand what we write). **`docs/lane-brief.md` is wholly David-facing, so lint the WHOLE file** — the fleet's lint-the-summary-line-never-the-file rule exists because daily reports are repo-facing, and it does not apply here. Daily reports: lint the BLUF line only. Two carve-outs found on 2026-08-03 in this repo, both reported upstream: `supersedes` is on the jargon list glossed as the bus flag ("replaces an earlier message") and mis-fires on ordinary English about a number; and the check does not exempt blockquotes, so **the verbatim quote of the email David sent on 24 July keeps its em dashes** — that block is a record of an outward artifact, and rewriting it to satisfy a style rule would make the brief misquote what went out. Same instinct as the mirror: byte-fidelity to what was actually sent.
- **Reviewing what we WROTE is not reviewing what the repo DISCLOSES.** Four artifact-level reviews plus a package-level review all cleared the flip package, and a whole-project pre-publication review (2026-08-05, David's instruction) then returned **eight blockers** and "I would not flip public with this tree and history". None of the earlier five could have caught them, because every one asked "is this artifact accurate?" and none asked "what does this repository reveal about us, our machine, and other people?" The eight were: no `LICENSE` while redistributing 111 mirrored files; an absolute local path in `continuity/health.json`; a doc publishing our own unprotected security posture (which platform-side controls are off); `items.json` publicly describing a portfolio-wide personal-data question belonging to other repos (details held privately — not ours to disclose); a personal author email on every commit; 15 commit bodies carrying private `claude.ai/code/session_…` URLs; and commit `293cec9`, which added a Playwright snapshot of erdosproblems.com containing **third-party forum user handles**. Before any publication, run the disclosure question as its own pass — and note that a decision to accept one disclosure (David's 8/05 accept the history, given about a maintainer's *published* email address) licenses that item and nothing wider.
- **A redaction is done when the STRING is absent from the repository, including from the record of the redaction — and a redaction scoped to one SURFACE leaves the same string on the others.** Four recurrences now, all the same shape: the fix cleans the flagged location and the note explaining the fix becomes a fresh copy. 8/06 the eight-blocker list spelled out the third-party handles and an address *while cataloguing them as things not to publish*; 8/07 the commit written straight after the rewrite re-contaminated from a stale git config; 8/08 the write-up of that put the address back into four files; **8/09 the session-URL sweep was scoped to COMMIT BODIES — which worked, zero — and nobody swept TRACKED FILES, where a complete artifact UUID was sitting in A-13's own note** (`2b751f5`). An identifier has at least three surfaces — author fields, tracked files, commit bodies — and a doc is a surface too. **The check that works is ENUMERATE what the repo reveals from a FRESH CLONE OF THE REMOTE; the check that keeps failing is confirm-the-named-defect-is-gone** (`git log --format=%ae` returning one noreply is TRUE and licenses only *no author field carries it*). Enumerate by shape — every email-shaped token, every URL-with-an-id — not by searching for the string you already know about. Never verify a redaction from the working tree.
- **Never hand David an artifact link.** `claude.ai/code/artifact/...` URLs are session-scoped and do not resolve in his browser — a brief delivered that way reads as page-not-found and the board card gets dismissed (happened 2026-07-24). David-facing briefs for lanes without their own site go on the Skylark site at `/t/lanes/<slug>`: keep the source at `docs/lane-brief.md`, post a bus note, the orchestrator ports it.
- **Audit the method sentence, not just the claim.** Any outward artifact that says "cross-checked against X" must have that sentence verified as its own review angle. On 2026-07-24 a draft claimed cross-checks against "the source-paper abstracts" when none of the three abstracts state their bounds — the phrase was inherited from an earlier, different claim and survived a 5-angle adversarial review because every angle attacked the *value*, not the *method*. A false claim about our own method is the one thing the recipient cannot check. **Twice now** — the second was the Mathstodon draft on 2026-08-13, whose method sentence advertised "231 claims across arXiv, Wikipedia and the erdosproblems metadata DB" when 220 of the 231 are generated pins against the ONE mirrored surface. Four of that review's six amendments landed on that single sentence and none on the rest of the post. Review it as its own angle, every time, and expect it to be where the artifact is wrong.

- **A decision that forbids a channel forbids the CONTENT, not just the form.** David decided NO to a public comment on erdosproblems.com/36 (2026-08-02). The most compelling thing this lane could tell an audience of mathematicians is that a widely-cited index page has shown a superseded bound since January — which is that same comment, delivered somewhere louder. Writing it into a Mathstodon post would have honoured the letter of his no and broken it. The pull toward that content is strong and it will recur, because it is genuinely our best material; the test is not "is this the surface he named" but "is this the thing he declined to say".
- **Outward gate.** Any upstream contact (PR, correction, email) or the public-repo flip goes through the **adversarial refute-it review first** (portfolio standing rule), and the actual send is **David-gated**. Never contact a maintainer or flip the repo public on your own. **Extended by David 2026-08-01, as a matter of practice: at least one adversarial review on EVERYTHING this project produces that might touch the public** — not just sends: the flip package, README-as-product, brief blocks, any artifact a stranger could read.
- **No self-rating.** The lane has no self-rating/audit contract (kept under the 2026-08-01 paced-rail ruling). Write the daily report + run the presence linters; skip `/rate-day` scoring (renamed from `/daily-close` on 2026-07-29; retired 2026-08-03).
- **Daily reports use the canonical H2 set** (adopted 2026-07-25, replacing the old numbered `## (1) …` format): BLUF / What changed / Inputs (controllable) / Outputs (lagging) / Recommendation / On hold pending data / State Appendix — BLUF first, State Appendix present. Lint with `node ../skylark-site/scripts/check-report-all.mjs docs/daily/<date>-prelaunch.md` (8 linters as of 2026-08-14; expect all clean — don't hard-code the count in your head, the runner prints `N/N`). Adopted rather than waived because a permanently-red linter gets ignored exactly like a permanently-green alarm carries no information — the lane's founding defect, applied to itself. Gloss every tracker ID on first mention per section (`W-3 — the watch on …`); `check-bare-itemids` is strict in the State Appendix.
- **Every daily report classifies the window's findings as record-facing or instrument-facing, in ONE sentence of human judgment — and NEVER as a counter.** Standing from 2026-08-21 (A-23, closed that day after the classification and the named first-catch shape both shipped in the report). A counter over `docs/findings/` is inflatable exactly like the self-rating this lane refused to build; the guard against inflation is that a human reads the window and nothing counts it. **Why it exists:** `npm run catches` counts drift DETECTION only, so it cannot tell a defect found in the mathematics from a defect found in our own machinery — and a lane that only ever finds faults in its own tooling is maintaining itself rather than stewarding records. Five consecutive instrument-facing days as of 2026-08-21; the last record-facing catch was 2026-08-14 (`87a.md`'s record row citing a witness that proves the previous record). **What it decides:** whether adopt-a-second-surface is the right answer to a quiet stretch — four instrument-facing findings beside zero movements is a different situation from four record-facing ones, and the dry-week rule treats them identically. **Carry the prediction forward too, because a prediction that is never checked is decoration:** the next record-facing catch is claimed to be a witness-value mismatch on a constant upstream added within ~30 days, flagged by a human recomputing a cited certificate and by no instrument we run. If one arrives via an alarm instead, say so and correct the claim.
- **MT for human labels** (commits, file slugs, doc headers, `docs/cold-starts/<MT-date>.md`); UTC only for cross-system timestamps (bus, git `%ai`, logs).

## Layout

- `continuity/items.json` — the item ledger (IDs: `G-` goal, `A-` active, `W-` watch). The source of truth for what's open.
- `continuity/traffic.json` — accumulated GitHub traffic samples (W-6). Append-only in effect; the API forgets days older than 14, so this file is the only copy of anything before the current window. Refresh with `npm run traffic` during the steward cadence.
- `docs/reconciliations/` — resolved record discrepancies (the lane's method template lives in #1).
- `docs/findings/` — smaller verified catches.
- `docs/decisions/` — filed decisions awaiting a gate (e.g. A-3's report-upstream draft).
- `docs/daily/<date>-prelaunch.md` — daily reports (needs §3a YAML frontmatter — the r8 linter FAILs without it).
- `docs/cold-starts/<MT-date>.md` — next-agent primer.
- `docs/lane-brief.md` — **source** for the David-facing brief hosted at `https://skylarkcreations.com/t/lanes/bounds-ledger`. Edit here, then post a bus note so the orchestrator re-ports it.
- `ledger/teorth-optimizationproblems/` — machine **mirror** of the adopted surface: every `constants/*.md` file **plus the repo-root `README.md`** (W-5, added 2026-08-10) + `manifest.json` pinning the upstream sha. The count grows when upstream adds a constant; `--check` prints the live figure, so don't write one here. **Why the README is in scope:** the constants files carry the bound tables, but the README is where upstream declares which of those rows it will STAND BEHIND — a demotion to asterisked-unverified leaves every table byte-identical, so mirroring `constants/**` alone was blind to it (upstream `dee1660`, 2026-08-02: our alarm named three constants files and was silent on the semantically largest change of the day). File maps are keyed by **path as upstream sees it**, so a report names the README at the root and each constants file under its own `constants/` prefix — never the README filed under the constants prefix, which is where the old string-concatenation would have put it. **Do not hand-edit**; refresh only via `--snapshot`.
- `ledger/claims.json` — claim-level value pins (source URL + exact expected string per claim). 11 hand claims (C-1…C-11; C-10 and C-11 are the `nothingAfter` negative pins added 2026-08-11 by A-11, on Wikipedia and `teorth/erdosproblems` respectively; C-8 pins problem 36's entry in `teorth/erdosproblems:data/problems.yaml` — the A-6 second surface; **C-7 + C-9 are the two `manual: true` claims, both on erdosproblems.com/36** — C-7 the bound, C-9 the `last edited 23 January 2026` string) + generated pins (`pin:<file>:U|L`, `generated: true`) covering every constant — 220 across 110 files as of 2026-08-01 (1b.md stays hand-pinned), and the count moves whenever upstream adds a constant. Hand-edit only the hand claims; generated pins are rewritten wholesale by `extract-pins.mjs`.
- `scripts/` — `reverify.mjs` (mirror diff), `check-claims.mjs` (cross-surface claim check), `extract-pins.mjs` (regenerates generated pins), `render-state-block.mjs` (**generates README's committed "Current state"; never hand-edit it**; `--check` is in `npm test` and `npm run check`; it records file count, upstream sha and claim inventory, never a stale live verdict), `check-brief.mjs` (hosted-brief drift; needs the pin), `history-sweep.mjs` (no-secrets gate), `gap-table.mjs` (G-2 ranker; writes `docs/gap-table.json`, asserts nothing mathematical), `traffic-snapshot.mjs` (W-6 14-day arrival sampler; renders no verdict, uses `gh` credentials, and only its merge is CI-guarded). `scripts/catch-rate.mjs`: a catch is a GENERATED pin whose `expect` changed; count DISTINCT records per week, never movements. Positive counts are an UPPER BOUND/activity ceiling; zero is exact. Quote PER-WEEK, never the running total. A month of zeros means adopt a second surface, but that is David's call, never self-approved; see the script header for the full 2026-08-14 derivation. `*.test.mjs` / `--selftest` are network-free and CI-wired; `reverify.test.mjs` prints the live count and fails if a new one lacks a workflow step.
- `search/27b/` — G-2 contribution harness for the Earth-Moon target: `verifier.mjs` (exact DMP planarity + exact chromatic number + certificate check — the DUMB half; model proposes, this code disposes), `selftest.mjs`, `find-sulanke-partition.mjs` + `sulanke-certificate.json` (the reproduced 1980 record — proof the harness can score the known answer), and the search rounds `propose-round.mjs` (r1, propose-then-test) / `round2-grow.mjs` (r2, biplanar by construction) / `round3-seeded.mjs` (r3, edge-budget family scan + growth from a held seed). Certificates are the product; never weaken the verifier to make a candidate pass. **A budget/elimination gate must be proven silent on the record itself** — a gate that flagged Sulanke's own graph would close a family by arithmetic error and every conclusion drawn from it would be worthless (round three).

## Gotchas

- **A feature branch gets ZERO CI runs from pushing — `reverify.yml` declares `push: branches: [main]`.** `gh run list --branch <b>` returning an empty list for a pushed branch is the workflow's configuration, not a missing run, and the branch page looks innocuously blank rather than red. `gh workflow run reverify.yml --ref <branch>` is the only route to a runner verdict. Consequence for holds: **a hold whose release condition has no stated command outlives its reason.** The 8/16 primer said "run it on a Linux runner first" and that read as actionable while the obvious action — push and look — could never have produced a run. When you hold work pending a check, write the command, not the check.
- **Check a held branch can still MERGE before spending a runner on it.** On 2026-08-17 a branch was validated on a runner and returned a clean verdict about code that could never land: `main` had moved and both sides had independently fixed the *same lines* differently, so `git merge-tree --write-tree main <branch>` reported a content conflict. **A runner verdict on an unmergeable sha is a verdict about nothing** — the thing that will actually merge is the conflict resolution, which no run has seen. One command, run first.
- **Do not dispatch `reverify.yml` repeatedly** — each run issues ~450 requests (113 individual raw fetches plus ~110 claim URLs), and the scheduled run is the lane's daily alarm. The first run generally already answered the question; validate a conflict resolution locally rather than with a second dispatch. Since b1 (2026-08-17) a PR gets a run automatically, so there is rarely a reason to dispatch at all. **This is practice guidance, not a diagnosis — read the next bullet before repeating the story that used to be attached to it.**
- **A cause you find flattering to admit still needs a positive control.** This bullet first read "two dispatches inside ten minutes tripped `raw.githubusercontent.com` secondary rate limiting on 2026-08-17". **That was wrong and is retracted.** GitHub was in a *critical outage* from 2026-08-17T13:40:03Z (githubstatus.com incident API, read directly); the first dispatch was 14:32Z, fifty-two minutes later, with Actions already degraded. Three pieces of disconfirming evidence were in hand and unassembled: a local ~450-fetch run was **fully green at 14:28Z**, 48 minutes into the outage; a later *local* check failed although the dispatches were runner-side and share no egress with this machine; and the errors included **502 and 503**, which request volume does not produce. The same self-blame went into a finding, a commit message, `A-5 note4` and two issue comments before any of it was checked. **Self-blame feels like rigour and is just another unverified attribution — and it is the more dangerous kind, because nobody challenges you for blaming yourself.** The repo's existing rule already covered it (positive control before any absence or cause claim); it did not fire because the claim was about our own infrastructure rather than someone else's record. `A-5 note5`.
- **`UNREACHABLE` is not `BROKEN`, and the alarm must never title it as one.** `BROKEN` = the cited source was read and the pinned string is gone (a record moved). `UNREACHABLE` = it was never read. Grouping them titled a pure HTTP 429 `Drift: claims C-1,pin:12a:U …` — six records asserted moved, zero actually — which is A-5's defect surviving where A-5's own fix could not reach it, because `UNREACHABLE` matched before the `elif` testing for `^error:`. **The red itself is correct and must stay red**: unreachable means unverified, and a green there would launder an unverifiable into a pass. Only the title was ever wrong. Do not "fix" a 429 by making it non-fatal. Finding: `docs/findings/2026-08-17-the-alarm-called-a-rate-limit-a-record-movement.md`.
- **`gh api .../issues` counts PULL REQUESTS as issues, and the count lags a close by ~10s.** Both bit on 2026-08-17. The first silently inflated an open-issue count the moment two PRs existed — filter `.pull_request == null`. The second returned "1 open" seconds after closing the last one, three times; two consistent reads settle it. Neither is a bug in the alarm, but both feed a figure this lane reports, and a stale read taken immediately after your own mutation is the shape that looks like a real finding.
- **Closing the issue is part of the drift cycle.** Five resolved drifts sat open as `Drift:` issues for up to six days (#10–#14, closed 2026-08-17 naming their resolving commits). Five standing reds teach the next reader that `Drift:` issues can be ignored, which is the precondition for missing a real one — the founding defect in yet another hat. Close on resolution, naming the sha that resolved it.
- **Prove a dangerous command was neutered BEFORE running an extracted body.** The mirror image of "prove the mutation landed": a throwaway one-liner meant to strip `gh issue create` out of a workflow step before executing it had its escaping silently fail, and it filed a real issue against the public repo. The in-repo harness asserts the strip changed the text (`assert.notEqual(harness, raw)`) and refuses to run otherwise; the debug script did not. Any script that executes a step body verbatim asserts the side effect is gone first.
- **A negative control's restore must not be `git checkout --` while the fix is uncommitted.** Restoring the mutated file that way discarded the uncommitted fix being tested, and `npm test` failing was the only reason it surfaced. Commit or stash, then negative-control.
- **`check-brief.mjs` needs `CC_PROMPTS_PIN` in env, or it can see nothing.** `/t/lanes/*` went behind a Supabase sign-in wall on 2026-08-02 (skylark-site `53c87f83`, intentional, site-wide). The route 307s to `/t/signin` and **the sign-in page returns HTTP 200**, so an anonymous fetch looks successful and every dated block reads "missing" — which is exactly how the checker spent a morning reporting `4 of 4 missing … the port is the orchestrator's` about a page it had never received. The script now proves the response IS the brief (tests for the source's own H1) before assessing staleness, and sends `x-cc-pin` from `process.env.CC_PROMPTS_PIN` when present. **Consequences: CI can never run this check** (no pin on a runner → exit 3 `BRIEF UNVERIFIABLE`, the brief job logs it and deliberately files nothing — a permanently-open issue no action can close is the founding defect again), and **an anonymous local run proves nothing either way**. Only a run with the pin set is evidence. Same shape as C-7. Do NOT "fix" this by putting the PIN in repo secrets — this repo is heading for a public flip. Finding: `docs/findings/2026-08-02-the-brief-check-blamed-the-porter-for-a-login-wall.md`.
- **An HTTP 200 is not evidence you fetched the artifact, and a NEGATIVE result from the wrong document is how you publish a false accusation.** Two cases in one day (2026-08-12): preprints.org returns 200 for a 2.6 KB bot-protection page (title `&nbsp;`), and **ar5iv silently falls back to the arXiv abstract page** — real, relevant, correctly-titled HTML about the right paper. On the strength of "Jin" being absent from that 38 KB fallback I was one step from writing that a mathematician's paper omits an acknowledgement upstream says it makes. **A positive match is self-validating; a negative match proves nothing until you have separately proven you had the right document.** Before concluding a cited source lacks something, name the token from the SAME fetch that proves it is the source — here `numerical range` and `spectral set` were also missing, which for a paper about the numerical range being a spectral set was the tell. The route that worked when the server is walled: **Crossref content negotiation** (`api.crossref.org/works/<doi>`) never touches it and returns title/author/date, enough to verify a citation including a priority claim. Same family as the sign-in wall below and the 403 below that. Finding: `docs/findings/2026-08-12-two-record-movements-in-one-cycle.md`.
- **erdosproblems.com 403s automated fetch — from DATACENTER IPs. The block is IP-dependent.** A plain Node fetch from a residential IP returns **HTTP 200** with the bound readable; the identical fetch from a **GitHub Actions runner returns 403**. So: **CI can never verify C-7** (it stays `manual: true` / UNVERIFIED by design), but **an agent running `npm run check` locally genuinely re-verifies it mechanically** — better than the screenshot round-trip, and available to any local session. **A local 200 proves nothing about CI.** This was established the hard way on 2026-07-25: I tested locally, concluded the constraint was stale, automated C-7, declared "zero UNVERIFIED claims", and CI went red four minutes later. Finding: `docs/findings/2026-07-25-the-403-that-wasnt.md`. **Do not re-automate C-7 on the strength of a local 200 — it has been tried; the only evidence that counts is a green CI run.**
- **Test a capability from the environment that will exercise it.** Generalising a laptop result to a runner is how the above happened, and it failed optimistically — the ledger briefly asserted "zero unverified claims" while the claim gating the lane's north-star watch was silently unverifiable in CI.
- **LF→CRLF git warnings on Windows are benign** — ignore them on commit. **But the files on disk really are CRLF**, which has teeth: a Node/regex string replacement written with `\n` (e.g. `s.replace("- name: X\n        shell: bash\n", …)`) **silently no-ops** — no error, no match, and your "edit" never happened. On 2026-07-25 this produced a false INERT verdict on the pipefail guard during the gate sweep: the guard was fine; the negative control hadn't applied. **Always `git diff` to prove a test mutation landed before believing the result.** Use `\r?\n` in patterns.
- **If a guard or shell refuses a documented command, write it VERBATIM to a file and run that file; never paraphrase inline.** On 2026-08-19 a refused README tamper, `node -e "require('node:fs').appendFileSync(…)"`, was retyped as `require('0node:fs')`; `MODULE_NOT_FOUND` left the mirror untouched, then `reverify.mjs --check` falsely printed `No drift.` at exit 0. This is KP-78's silent half: `git diff --numstat` was empty. Use a quoted heredoc (`cat > /tmp/x.sh <<'EOF'`) containing the exact command. After running it, require a non-empty `git diff --numstat` before reading the detector's verdict; an absent mutation makes either answer meaningless. Retyping tests the retyping, not the documented command—the 2026-08-18 lesson.
- **Never write backslashes through a JS string literal into a file; they can disappear SILENTLY** (2026-08-21: one false claim, dead command, and empty code span). The three non-erroring cases were jq `\(...)` in `releaseTest` printing a literal template; an "unedited" README quote retyped as `-C_{71} > 6.5143...` instead of `-C_{71}\ >\ 6.514326913930565372,`; and `` `\ ` `` becoming an empty span. **Do not retype quoted records—extract them.** Pull the commit line with parsed `git show …`, then require `grep -F -x` hits in source and copy plus a fabricated non-match. Reword decorative backslashes away. The first draft of this bullet itself turned escaped em dashes into mojibake: the SIXTH instance of a fix note reproducing its defect; keep the count because the document is inside this silent-corruption class. **"Verbatim" and "unedited" are method claims**: audit them by grepping the source for the quotation and requiring a hit.
- **Never read an exit code through a pipe — locally, not just in CI.** `node scripts/check-claims.mjs --selftest | tail -3` reports *`tail`'s* exit 0 even as the selftest fails. The CI form of this bug is the lane's founding defect (below); the local form bit again during the 2026-07-25 sweep. Redirect to a file and check `$?`, or use `${PIPESTATUS[0]}`.
- **Never pipe a check step in CI without `shell: bash`.** GitHub Actions' default runner shell is `bash -e {0}` — no `pipefail` — so `node scripts/reverify.mjs --check | tee ...` reports `tee`'s exit 0 and a real drift passes green. This made the alarm fake for the lane's first two days (`docs/findings/2026-07-24-drift-alarm-was-never-armed.md`). `reverify.test.mjs` now guards it; don't defeat the guard.
- **After a `--snapshot`, `git status` over-reports — it is not a content diff.** The snapshot rewrites every mirror file, so files whose *content* is unchanged can still show as modified (line-ending / stat-cache artifact). On 2026-07-28 this showed a third changed file the drift report hadn't named, which looks exactly like "our differ under-reports" — a serious charge in this lane. `git diff --numstat` settled it: zero lines changed, differ was right. **Check `--numstat` before believing `git status` disagrees with the drift report.**
- **The clone count in `npm run traffic` is mostly US, and it recurs every single day.** A sample on 2026-08-20 read **154 unique cloner-days against 3 unique viewers** — a ratio that is not an audience. `reverify.yml` checks out the repo on a daily schedule, on every push and on every PR, so the clone figure accrues from our own CI whether or not a human ever arrives. The sampler deliberately does not deduct our own clones, CI checkouts or crawlers and says so in its output; number-provenance rule 2 ("is this number MINE?") applies directly. **The only arrival figure here that is not mostly ours is the viewer count.** Recorded as a known pattern because it is a scheduled-event side-effect that regenerates daily, so every future reader meets it fresh and would otherwise re-investigate it.
- **The mirror diff only sees changes *inside* the adopted repo.** Cross-surface divergence (the lane's founding finding) is caught by `check-claims.mjs`, not `reverify.mjs`. Both run in CI.
