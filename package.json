{
  "name": "bounds-ledger",
  "version": "0.0.0",
  "private": true,
  "description": "Steward of drifting mathematical records: re-verification CI + drift alarm. No dependencies (Node stdlib + fetch).",
  "type": "module",
  "scripts": {
    "test": "node scripts/reverify.test.mjs && node scripts/check-claims.mjs --selftest && node scripts/extract-pins.mjs --selftest",
    "check": "node scripts/reverify.mjs --check && node scripts/check-claims.mjs && node scripts/check-brief.mjs"
  }
}
